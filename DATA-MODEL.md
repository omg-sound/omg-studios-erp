# OMG Studios ERP — 데이터 모델 (테이블·컬럼 정의)

> **이 파일은 CLAUDE.md에서 분리된 스키마 참조다**(2026-07-31 — CLAUDE.md가 142.7k자로 불어 매 세션 비용이 커져서, **스키마를 만질 때만 필요한 컬럼 서술**을 옮겼다. `SCREENS.md`(화면 배치)·`HISTORY.md`(세션 이력)와 같은 성격).
>
> ⚠️ **옮기는 규칙은 SCREENS.md와 같다**: 🔒불변식·🚫폐기 결정·데이터 계약은 **CLAUDE.md에 남기고**, 여기엔 '무엇이 어떤 컬럼을 갖나'를 둔다. 아래 본문은 분리 시점의 **원문 그대로**이므로 불변식 서술이 함께 들어 있다 — 그건 중복이지 이전이 아니다. **스키마를 손보다 새 불변식을 찾으면 이 파일이 아니라 CLAUDE.md에 올린다.**

- `rooms(name, bookable, is_external, sort_order, active)` — **스튜디오 룸 마스터**. `bookable`=예약 대상(Lounge 등은 0). 치프가 `/settings` 환경설정 탭에서 CRUD(**이름 수정 포함** — id 보존이 곧 기존 예약 보존). 삭제 시 `sessions.room_id` SET NULL. `parent_id`=**레거시**(2026-07-28 계층 폐지, 읽는 코드 0). 명칭 체계는 위 '세션 일정' 섹션 참조.
- `session_segments(session_id→sessions CASCADE, kind[setup|shoot|teardown], start_time, end_time, sort_order)` — **촬영 구간**(반입·설치/촬영/철수, 2026-07-26). ⚠️구간 이름은 세션 종류 개명(촬영→대관)과 **별개로 '촬영'을 유지**한다 — 대관 안에서 실제 촬영이 이뤄지는 단계를 가리키는 도메인 어휘이고, 노출 조건은 이름이 아니라 `SESSION_TYPE_RATE_KIND[t]==="filming"`으로 파생돼 개명에 자동으로 따라온다. 🔒**요금 시간 = 구간 합산 / 룸 점유·캘린더 = 첫 구간 시작 ~ 마지막 구간 종료 span**(사용자 확정). 구간 사이에 빈 시간이 있어도 그 방은 계속 잡혀 있으므로 점유는 아우르는 한 덩어리이고, 점유를 `sessions.start_time~end_time`에 그대로 두면 **겹침 판정·캘린더 동기화(gcal_event_id 1개)·청구 후보 쿼리가 종전 구조 그대로** 동작한다. `sessionFields`가 구간이 오면 start/end를 span으로 파생(같은 시간을 두 번 입력받지 않는다), `sessionBillableMinutes`가 합산. 종일은 구간을 지우고, **구간 칸이 없는 요청은 기존 구간을 건드리지 않는다**(구간을 모르는 폼·API의 저장이 조용히 지우지 않게 — `segmentsFromInput`이 null 반환). 헬퍼=`listSessionSegments`/`setSessionSegments`(트랜잭션 안 염 — 호출부가 감쌈, setSessionDirectors와 같은 규약)/`segmentsSpan`.
- 🚫 **할증 개념 없음**(2026-07-27 사용자 결정 — 도입 이틀째 폐기): 미장센처럼 가산이 필요한 요금은 **단가 항목 자체로** 표현한다(운영은 `촬영 (미장센 및 다수 카메라)` 600분/150만). 같은 요금을 '할증'과 '단가 항목' 두 경로로 넣을 수 있으면 어느 쪽으로 잡았는지에 따라 금액이 갈리기 때문. `surcharges` 마스터 테이블은 `surcharges_drop_v1` 게이트로 제거됐고 `computeCharge`도 surcharge 인자가 없다. `sessions.surcharge_key`/`surcharge_memo`는 **레거시 컬럼으로만 잔존**(읽는 코드 0, 참조 세션 0 — sessions는 큰 테이블이라 DROP COLUMN은 이득 없이 위험만 있다: `director_contact_id`와 같은 취급). 회귀 `pricing-rooms-migration.test.js`가 '테이블 없음'을 잠금.
- `users(email, role[owner|chief|staff], name, google_sub?, active, client_id?[레거시], password_hash?[레거시])` —
  `active=0`이면 로그인 차단(화이트리스트 제거). 마이그레이션에서 기존 `admin`→`chief` 자동 승계.
  `password_hash`/`client_id`는 구 모델 잔재 컬럼(미사용). **`name`은 기존값 보존**(치프가 `/settings`에서 수정한 한글 이름이 Google 프로필 영문 이름에 매 로그인 덮어쓰이지 않게; `users.name` 빈 경우만 Google 이름). 이름 수정 시 `project_managers.name`·`track_tasks.engineer_name`(작업 스냅샷)도 동기화.
- 🏗 **당사자(Party) 모델 — 정체성 통합(완결·배포)**. 옛 clients/contacts 이중화를 **`parties` 한 테이블**로 통합: kind=person|company|group·is_artist 플래그, 역할(아티스트·청구처·담당자·디렉터·엔지니어)은 전부 `parties.id` 참조. 데이터 헬퍼=`src/data/parties.js`. 이관은 `migrateToPartyModel`(party_model_v1)로 parties·affiliations·역할 컬럼 생성 후 `dropLegacyIdentity`(legacy_drop_v1)가 레거시 테이블(clients·contacts·contact_affiliations)·FK 컬럼을 제거 — **둘 다 완료·배포됨**(신선 DB는 CREATE에서 레거시 없음, 이관 게이트는 `hasLegacy` 가드·멱등). 임시 어댑터 compat.js도 제거됨. **⚠️ `projects.artist/artist_company/production_company` TEXT는 표시용 denormalization으로 의도적 유지**(파생·표시 편의). 완결 이력 상세는 git.
- **`parties(id, kind[person|company|group], name, activity_name?[활동명], is_artist, phone?, email?, memo?, / person: family_name·given_name·honorific·department·job_title·user_id·google_resource_name·google_etag·cash_receipt_no / company: biz_no·owner_name·owner_party_id·address·roles[겸업 CSV] / person(아티스트): group_id[소속 그룹→parties.id kind='group']·activity_form[활동 형태 solo|group|both — 수동 3택, 2026-07-16, **2026-07-17부터 UI 레거시**(유일한 표시처였던 업체·그룹 아티스트 탭이 사람/조직 축 정리로 폐기). **컬럼·백필만 보존하고 config 상수(`ARTIST_ACTIVITY_FORMS`·`..._LABELS`)는 2026-07-27 제거** — 데이터 호환은 컬럼이 지키고, 읽는 코드가 없는 상수는 '아직 쓰는 화면이 있다'는 오해만 남긴다] / group: **name=activity_name 동기화**[그룹명=단일 정체성 — 생성 시 activity_name=name, 수정 라우트도 `activity_name=새 name`으로 동기화(안 그러면 옛 activity_name이 남아 '옛이름 (새이름)'으로 표시되던 버그, 2026-07-16 사용자 리포트·1회 백필 `group_activity_name_sync_v1`. **솔로 아티스트도 같은 규칙**: 수정 시 `(그룹 || 옛 name==activity_name) ? 새 name : 옛 activity_name` — 이름==활동명이던 흔한 케이스는 동기화, 본명≠활동명(모달 등록)은 activity_name 보존[이름 필드=본명 편집]. 솔로는 본명≠활동명이 정상이라 백필 없음)]·contact_party_id[그룹 담당자→parties.id 사람·멤버 또는 관계자])`** — **정체성 통합 마스터**(clients+contacts 대체). 사람(아티스트 포함)=연락처 뷰(`listContacts`·`/contacts`), 조직(업체·그룹)=업체·그룹 뷰(`listClients`·`/clients`, 2026-07-17부터 사람은 여기서 완전히 빠짐). `is_artist`=아티스트 역할(person solo·group). `user_id`=로그인 계정(스태프·owner) 연결(`ensurePartyForUser`). 활동명(솔로 아티스트)=`activity_name`=Google nickname 동기화. 청구처(payer)·프로젝트 아티스트/소속사/제작사/담당자·세션 디렉터·담당자 연동이 전부 `parties.id` 참조. 데이터 헬퍼=`src/data/parties.js`(CRUD·`classifyParty`·`resolvePersonByName`·`partyOptions`·`ensurePartyForManager/User` 등). **`updateParty`=부분 갱신 계약(2026-07-04)**: 호출부가 보낸 필드(`undefined` 아님)만 갱신, 미전송 필드는 보존, 빈 문자열=의도적 비움(null) — Google 동기화(`personToContactFields`가 Google에 있는 필드만 전송)·유형별 폼이 나머지 필드(메모·전화·현금영수증 등)를 지우던 데이터 손실 클래스 근본 차단(person·company·group 3분기 모두, 가드레일 ⑦로 보증). 삭제(`deleteParty`)는 역할 참조 SET NULL·첨부 CASCADE. 🔒 **발행 청구의 청구처(payer)는 삭제 거부 — 세 문 공용 가드**(2026-07-23 기능성 평가): `deleteParty`가 `invoices.payer_id`를 NULL로 만들어 그 사람 기준 미수 추적(`listInvoicesForParty`)이 끊기고 `payer_kind` 소실로 현금영수증이 '계산서'로 오표시되므로, **`partyHasIssuedInvoice(id)`**(`status='발행' OR tax_status IN ('계산서 발행','입금완료')`)면 삭제를 막는다. ⚠️예전엔 이 판정이 **`/clients` 삭제 라우트에만 인라인**으로 있었고 **`/contacts` 삭제와 구글→앱 pull(`people.js` meta.deleted)은 무방비**라, 같은 사람이 **어느 문으로 들어오느냐에 따라** 지워지거나 보존되는 비대칭이었다(연락처에서 개인 청구처를 지우면 재무 링크가 끊겼다). → 판정을 헬퍼로 올려 **세 곳이 공유**(clients·contacts=409 거부, 구글 pull=삭제 건너뛰고 `keptWithInvoice` 집계 — 구글 주소록 정리가 우리 매출 기록을 소급 삭제하지 못하게). 인라인 SQL 판정 재등장·헬퍼 호출 누락은 `test/party-delete-guard.test.js` 계약 테스트가 차단. **직책(job_title) 입력 시 호칭(honorific) 자동 파생**(2026-07-10 사용자 결정 — '대표 직책→대표님 호칭' 흐름을 전 직책으로 일반화: `honorificFromTitle`[lib/korean-name] '실장'→'실장님'·이미 '님'으로 끝나면 그대로, `createPerson`/`updateParty`가 **호칭이 비어 있을 때만** 파생[명시 호칭 존중], 1회 백필 `person_honorific_from_title_v1`. → 호칭 컬럼 하나만 보면 전 화면[연락처 목록·콤보 칩·청구처 카드]이 '엄유미 실장님'으로 일관 표시). **대표자(회사 owner) 등록 시 그 사람 party 호칭='대표님' 자동 세팅**(`resolveOwnerParty`, 2026-07-03 — 이름 기반 재사용/생성·호칭 비어 있을 때만·기존 호칭 존중·이름 유지로 중복 방지) **+ 그 대표자의 직장(소속)을 그 회사로 연결**(`ensureOwnerAffiliation` — 현재 소속이 이 회사가 아니면 이직 등록[직함 '대표'], 이미면 no-op·중복 없음; 업체 생성·수정 양쪽). **대표자 입력=칩 콤보(공동대표 여러 명, 2026-07-10)**: 담당자 연락처와 동일한 `personCombo(multi, selected=listCompanyOwners)` — 제출은 칩마다 `owner_id`+`owner_name` 쌍, 라우트 `resolveOwnerIds`가 인덱스 페어링으로 해석해 `setCompanyOwners`에 넘긴다(호칭·소속·레거시 컬럼 동기화는 그쪽 책임). 상세 크로스링크 '대표자 연락처'도 전원 나열. **업체 생성 모달(companyCombo '＋새 업체 등록') 안 대표자 미니콤보도 칩**(2026-07-10 — 신규 회사를 만들 때부터 공동대표 지정 가능): 저장 시 fetch body에 `owner_id`/`owner_name` 쌍을 칩 수만큼 append(서버 `resolveOwnerIds` 인덱스 페어링 재사용, 라우트 무변경). 옛 단일 hidden `data-cc-owner-id` 폐기.
- `company_owners(company_id→parties CASCADE, party_id→parties CASCADE, sort_order, PRIMARY KEY(company_id, party_id))` — **회사 대표자 다대다(공동대표, 2026-07-10 사용자 요청 '대표자가 2명인 경우도 있다')**. 진실원천은 이 테이블, `parties.owner_party_id`=첫 대표(레거시 참조)·`owner_name`=콤마 목록(청구처 카드 '성명(대표자)'·거래명세서 스냅샷 표시 텍스트)을 `syncCompanyOwnerColumns`가 동기화. `setCompanyOwners(companyId, ids)`=통째 교체(각 대표에 '대표님' 호칭[비었을 때만]·이 회사 소속[직함 '대표'] 부여, **빠진 대표는 대표 역할만 해제 — 연락처·재직 유지**), `listCompanyOwners`. `deleteParty`가 대표 삭제 시 남은 대표로 레거시 컬럼 재동기화(승계). `orgsWithOwnerParty`·관계자 탭 `ASSOCIATE_ROLE_SUBQUERY`도 company_owners 기준(둘째 대표 누락 방지). 1회 백필 `company_owners_backfill_v1`(기존 owner_party_id 복사).
- `affiliations(person_id→parties CASCADE, org_id→parties SET NULL, title?, started_on?, ended_on?, memo?, is_contact, created_at)` — **소속 이력(이직 히스토리)**. **`is_contact`=이 조직의 '담당자'로 지정됨**(2026-07-10 사용자 결정 — 재직과 별개 역할: 담당자 해제≠퇴사). 담당자 조회=`listOrgContacts`(재직 + is_contact=1), 재직 전원=`listPersonsForOrg`. 1회 백필 `affiliation_is_contact_v1`(기존 재직자 전원 담당자 승계 — 화면·청구서 표시 불변). `ended_on IS NULL`=현재 소속, `org_id` NULL=무소속. 이직 시 기존 현재 소속 종료 후 새 행(`addAffiliation(closeCurrent)`). 헬퍼: `currentAffiliation`·`listAffiliations`·`syncCompanyAffiliation`·`listPersonsForOrg`·`listArtistsForAgency`. 뷰 호환 별칭 `client_*`(=`org_*`) 제공.
- `client_files(client_id→parties CASCADE[조직 party], kind['biz_license'], storage_backend, file_id, file_name, mime_type, file_size, created_at)` — **조직 첨부 서류**(사업자등록증). 컬럼명은 `client_id`(레거시) 유지하되 **parties(조직) 참조**. party당 kind별 1개(UNIQUE, 교체식). 매직바이트(PNG/JPEG/PDF)·치프 전용 인증 다운로드(공개 링크 없음).
- `projects(title, project_type[레거시·미사용], artist?, artist_company?, production_company?,
  client_id?→clients ON DELETE SET NULL, manager_id?→project_managers ON DELETE SET NULL, contact_id?→contacts ON DELETE SET NULL, services JSON, due_date?, rate, memo)` —
  `project_type`은 **레거시**(유형 구분 폐기 — 신규=session, UI 미노출). `contact_id`=고객측 담당자 연락처(내부 `manager_id`와 별개). `services`는 레거시 배열(편집 UI 제거). `status`·`kind`·**`due_date`는 레거시**(프로젝트 마감일 UI 폐기 — 청구 입금마감일과 무관한 잔재 컬럼).
- `project_managers(name, email?, phone?, active, user_id?→users, contact_id?→contacts, id_number?, bank_name?, account_number?, account_holder?, created_at)` — 작업 담당자 마스터.
  `user_id` 있으면 **하우스 엔지니어**(로그인 사용자와 링크, `auth.syncUserToManager`가 자동 생성·동기화),
  null이면 **외주 작업자**(로그인 없이 관리에서 직접 추가). 둘 다 세션·작업 담당 드롭다운에 노출. **`contact_id`=연동 연락처**(서버 기동 시 백필로 전 담당자 자동 연결): 전화는 연락처↔담당자 **양방향**, 이메일은 외주만 양방향·**하우스는 잠금**(`users.email` 보호). `ensureContactForManager`/`syncContactToManager`/`syncManagerToContact`(상호 재호출 없음=루프 방지). **`id_number`(주민등록번호/사업자등록번호)·`account_number`(계좌번호)는 `db.encrypt`로 암호화 저장**(2026-07-06 — 외주 정산 정보, `/workers/:id/edit`에서만 치프가 입력·복호화 표시). `bank_name`·`account_holder`는 평문.
- `worker_files(worker_id→project_managers CASCADE, kind['id_card'|'bankbook'], storage_backend, file_id, file_name, mime_type, file_size, created_at)` — **외주 작업자 첨부 서류**(주민등록증 사본·통장사본, 2026-07-06 사용자 요청). `client_files`와 동일 구조(kind별 1개 UNIQUE 교체식). 매직바이트(PNG/JPEG/PDF)·치프 전용 인증 다운로드(공개 링크 없음). `storage-migrate.js` 로컬→Drive 이관 대상 포함.
- `task_types(key UNIQUE, label, task_group[레거시·분류 폐기], billing_type, unit_price, is_quick, sort_order, active)` — **작업 종류 카탈로그**
  (곡·콘텐츠 후반작업). config `TASK_TYPES`를 `task_types_seed_v1` 게이트로 1회 시드 후 DB가 단일 진실원천(기존 9 key 보존, 신규=`tt_<hex>`).
  `track_tasks.task_type`이 이 key를 문자열로 보관(FK 아님). 라벨 해석은 `data.js` 모듈 캐시(`taskTypeLabel`, 쓰기 시 무효화). **분류(`task_group`) 폐기**(2026-07-01 — 곡·콘텐츠 작업은 모두 후반작업이라 분류 무의미: 추가/수정 폼 select·행 배지·빠른추가 optgroup·진행요약 그룹묶음 제거[진행요약은 종류별로], 신규=`Post_Production` 고정, `taskTypeGroup` 헬퍼 제거. `TASK_GROUPS`/`TASK_GROUP_LABELS` config·컬럼만 레거시 보존).
  `is_quick`=곡·콘텐츠 빠른추가 버튼 노출, `unit_price`=빠른추가 기본 단가. 삭제-only(강제), 치프가 `/settings?s=tasks`에서 CRUD.
- `project_service_items(key UNIQUE, label, active, created_at)` — 레거시(구 services JSON 라벨 호환). **관리 UI·시드 폐기**(작업 종류 카탈로그가 대체), 테이블만 잔존.
- `rate_items(name, category[스튜디오 녹음|로케이션 녹음|스튜디오 촬영|공연], base_minutes, base_price, extra_minutes, extra_price, price_type[레거시], sort_order, active)` —
  🏷 **과금 체계**(2026-07-26 사용자 지시): **과금은 룸이 아니라 작업 항목 단위**다(어느 룸을 썼는지는 요금에 영향 없음 — 룸은 일정 관리용). 1프로=210분, 초과 요율은 전 서비스 공통 시간당 10만. 마이그레이션 시드(**신선 DB 한정**) = 녹음 `솔로 녹음`(210분/30만)·`드럼 · 합주 녹음`(210분/40만). **촬영 항목은 시드하지 않는다**(2026-07-27 — `기본 패키지`를 넣었다가 운영의 기존 `촬영`·`촬영 (미장센 및 다수 카메라)`와 역할이 겹쳐 폐기). ⚠️**운영(프로덕션)에는 녹음 2건이 없다**(2026-07-26 배포 후 사용자 결정): 운영의 실제 항목명은 `보컬녹음`·`보컬녹음 (그룹)`·`성우녹음`·`악기 녹음`·`악기 녹음 (드럼)`이라 마이그레이션의 rename 별칭(dev 시드 이름 `보컬 녹음`·`악기+보컬 녹음(그룹)`)과 정확 일치하지 않아 **신규 삽입 경로를 탔고**, 사용자가 중복을 원치 않아 그 2건(id 12·13)을 삭제했다. 기존 항목은 손대지 않았으므로 과거 세션·청구 근거는 그대로다. **운영 녹음 단가는 여전히 기존 이름으로 청구한다** — 문서의 시드 목록을 운영 현황으로 읽지 말 것. 운영 촬영 항목은 기존 `촬영`(240분/40만)·**`촬영 (미장센 및 다수 카메라)`(600분/150만)** 둘이다 — **가산은 단가 항목으로 표현하는 것이 정본**(할증 메커니즘은 폐기). **포스트(믹싱 100만 `base`·보컬튠 20만 `minimum`)는 rate_items가 아니라 `task_types`에 있다** — 이 ERP는 세션(대관)=rate_items / 곡·콘텐츠 후반작업=task_types로 청구 경로가 갈려 있어서, 포스트를 rate_items에 넣으면 세션 폼에서 도달 불가한 죽은 데이터가 되고 '믹싱'이 두 카탈로그에 동시 존재한다.
  🚫 **가격 유형(`price_type`) 폐지**(2026-07-29 사용자 지시): 옛 fixed(잠금)/base/minimum(상향만) 3택을 없애고 **청구 화면 금액칸은 언제나 수정 가능**하다. 잠긴 칸은 협의 감액이 생길 때마다 카탈로그를 고치러 가게 만들었고, 같은 감액을 '금액 수정'과 '청구서 할인' 두 경로로 할 수 있어 어느 쪽으로 잡았는지에 따라 기록이 갈렸다. **조정 사유 메모는 이제 모든 행에 상시**(`session_billing_memo_<id>`/`task_billing_memo_<id>` → 청구 라인 근거로 스냅샷 + 원본 행 `billing_memo`에도 저장) — 산정치와 다른 금액을 넣었으면 그 이유가 유일한 근거다. `rate_items.price_type`·`task_types.price_type` 컬럼과 설정 화면의 select, `data-line-min` 하한, config `PRICE_TYPES`/`PRICE_TYPE_LABELS`, `taskTypePriceType`는 **전부 제거**(컬럼만 레거시로 잔존, 읽는 코드 0). 회귀 `test/invoice-amount-editable.test.js`(옛 `invoice-price-type.test.js` 대체).
  `sort_order` + ↑↓ 이동(`moveRateItem` — **분류 경계는 넘지 않는다**, 관리 화면이 분류별 접이식이라 넘으면 사라진 것처럼 보인다), 활성/비활성 토글(`setRateItemActive` — `active` 컬럼은 처음부터 있었는데 라우트·UI가 없어 한 번 비활성이 되면 되살릴 수 없었다).
  **단가표 · 녹음 종류**. `category`는 이제 **DB `rate_categories` 테이블이 진실원천**(2026-07-05 사용자 요청으로 config 하드코딩에서 전환 — `RECORDING_CATEGORIES`[스튜디오/로케이션 녹음]·`FILMING_CATEGORIES`[스튜디오 촬영]·`PERFORMANCE_CATEGORIES`[공연]는 db.js `seedDefaultCatalogs`의 **1회 시드 데이터**로만 남음, `rate_categories_seed_v1` 게이트). **녹음/촬영/공연 kind는 category로 구분**(별도 컬럼 없음): `rateCategoryKind(name)`(`src/data/rate-categories.js`)이 DB 조회로 kind 반환(등록 안 된 이름은 recording 폴백); `SESSION_TYPE_RATE_KIND`={녹음:recording,촬영:filming,공연:performance}는 여전히 config. **세션 폼 단가 항목은 세션 종류에 맞춰 녹음/촬영만 노출** — 서버가 현재 종류 kind로 렌더 + kind별 `<template data-rate-opts-{kind}>` 임베드(recording·filming·performance — SESSION_TYPE_RATE_KIND에서 파생, `views.sessions.js`가 `listRateCategories()`로 optgroup 순서 결정), app.js `swapRateOptions`가 세션 종류 변경 시 옵션 교체(`data-rate-kinds` 맵). 관리>콘텐츠 단가표 폼 category select는 kind별(녹음/촬영/공연) optgroup(`rateCategoryOptions`, DB 조회). **단가표 목록은 분류별 접이식(`<details>`, 기본 접힘)으로 묶임**(2026-07-05 사용자 요청 — `ratesGroupedByCategory`, 분류당 항목 수 배지). **기준시간(1Pro)마다 묶어서 과금** — 완전한 Pro 블록은 각각 `base_price`, 마지막 1Pro 미만
  자투리만 `extra_minutes` 단위 올림으로 `extra_price`(`base_minutes=0`이면 정액). 예) 1Pro=210분·30만/초과 60분·10만
  → 630분(3Pro)=90만·240분(1Pro+30분)=40만. `computeRatePrice(item, minutes)`가 산정.
  🔒 **초과 규칙 = 1프로 블록 반복(2026-07-26 사용자 재확정)** — 홈페이지 `pricing.ts computeCharge`는 '기본가 + 초과 시간당'이라 **7시간부터 갈린다**(630분: 블록 반복 90만 vs 시간당 100만, 420분: 60만 vs 70만. **2프로 미만은 두 방식이 완전히 동일**). 후자는 2026-07-01에 사용자가 '10.5시간 → 90만'으로 리포트해 고친 것을 되돌리는 셈이라 **블록 반복을 지킨다**. 홈페이지의 '초과 시간당 10만' 문구와는 7시간부터 어긋나며, 이는 **의도된 차이**다(공개 안내 ≠ 실청구, 두 앱은 동기화하지 않는다). 회귀 `test/charge-lines.test.js`가 경계값을 잠금.
  **`computeCharge(item, minutes, {surcharge})` → `{lines, total}`**(2026-07-26, 홈페이지에서 이식): 근거를 한 줄로 뭉치지 않고 **기본가 / 초과 N시간 / 할증**을 라인으로 쪼개 돌려준다(각 라인 `{label, amount, detail, quantity, unit_price}`). `computeRatePrice`는 `computeCharge().total` 래퍼로 남아 기존 호출부 6곳 무변경(동일성을 전 구간 파라미터화 테스트로 잠금). **정액(회당) = 기준 시간 비움(base_minutes=0)** — 시간 무관 1회=기준가; **가격까지 비우면 '금액 미정'**(2026-07-04 사용자 결정 — 플레이백 세션처럼 회당 가격이 매번 다른 항목): `RATE_PRICE_REQUIRED` 가드는 시간제만 강제, 산정 0원 세션도 청구 후보 유지(세션 행 '청구액 미정' 표시·`listBillableSessionsForProject`/`computeInvoiceDraft`의 amount>0 필터 제거). **0원 항목은 확인 후 청구 가능(2026-07-05 사용자 결정 — 이전 '발행 시 0원 라인 차단' 정책 완화, 무료로 해준 작업도 기록·청구서에 남길 수 있게)**: 체크한 항목 중 0원이 있으면 제출 시 `window.confirm("금액이 0원인 청구 항목이 있습니다. 0원으로 청구할까요?")` → 확인하면 hidden `confirm_zero_amount=1` 전송(app.js), 서버 `createInvoiceFromTasks`는 이 플래그 없이는 여전히 `TASK_AMOUNT_REQUIRED` throw(JS 미동작·경합 대비 안전망 — 세션 겹침 `override_conflict`와 동일 패턴). **버그수정**: 애초 확인창은 있었으나 서버에 알리는 수단이 없어 '확인'을 눌러도 서버가 그대로 되돌리던 잠복 결함이었음(사용자 리포트로 발견) — 이번에 hidden 플래그로 실제 연결. 미리보기 PDF는 참고용이라 원래도 0원 허용. 청구 폼에서 금액 입력=확정(2026-07-01 수정: 이전엔 1Pro
  초과분 전부를 시간당 추가요금으로 매겨 3Pro가 100만이 되던 버그). 관리 메뉴에서 치프가 CRUD.
- **`rate_categories(name UNIQUE, kind[recording|filming|performance], locked, sort_order)`**(2026-07-05 신설) — 단가표 분류 마스터. 🌳**요금표 화면은 '분류(어미) → 단가 항목(하위)' 2단 트리**(2026-07-29 사용자 결정, 지메일 라벨식) — 분류 행이 곧 편집 행(이름·세션 종류·↑↓·분류 삭제)이고 그 아래에 항목 행 + '+ 항목 추가'(분류 고정)가 붙는다. **항목 행은 2줄 + 인라인 단위**(`chargeLine`): 1줄=이름·분류 이동·행 액션, 2줄=`기준 [3.5]시간 [300,000]원 · 초과 [1]시간마다 [100,000]원`. ⚠️**열 제목 줄은 두지 않는다** — 옛 7열 표는 제목이 맨 위 한 번뿐이라 아래쪽 분류를 입력할 땐 위를 올려다봐야 했고, 쌍인 기준(h)/기준가·초과(h)/초과가가 네 열로 흩어져 매번 관계를 되짚어야 했으며, 헤더가 트리 밖이라 들여쓰기된 행과 17px 어긋나 있었다(실측). 단위·접속사를 칸 사이에 넣어 **행 자체가 설명하게** 했고, 입력 폭은 고정이라 행끼리 세로 정렬은 유지된다. 옛 접이식 '분류 관리' 섹션은 폐지(같은 대상을 두 곳에서 고치게 만들던 구조). 저장은 트리 전체에 하나(`POST /settings/rates/bulk` — `bulkUpdateRateCategories`+`bulkUpdateRateItems`, 필드 접두가 달라 서로 간섭 없음). ⚠️**트리 전체가 통합 저장 폼 안**이라 '+ 항목 추가'는 `form="rate-add-<분류id>"`로 폼 밖 형제를 가리킨다(중첩 폼 금지 — 안 그러면 '추가'가 통합 저장을 제출한다). 🔓**순서는 한 축의 자유 배치**(`ORDER BY sort_order, name`): 종류가 달라도 섞어 놓을 수 있고 `moveRateCategory`가 종류 경계를 넘는다. 예전엔 `ORDER BY kind`가 먼저라 촬영 분류를 녹음 사이에 둘 수 없었고 그 종류 순서마저 영어 알파벳순(filming<performance<recording)이라 녹음이 맨 아래였다. 1회 마이그레이션 `rate_categories_flat_order_v1`이 옛 표시 순서(녹음→촬영→공연)를 물질화해 sort_order를 10 간격으로 재부여한다(**옛 값은 종류마다 겹쳐 있어** 그대로 두면 순서가 뒤섞인다). **종류(kind)는 이제 분류의 속성일 뿐**이고 세션 종류에 맞는 항목을 거르는 데만 쓴다 — 세션 종류와 묶인 값이라 이름 변경·추가는 불가(`SESSION_TYPE_RATE_KIND`). 🔓**기본 분류도 수정·삭제 가능**(2026-07-29 사용자 요청 — 옛 `locked=1` 잠금 해제): 잠금 근거였던 '세션 종류↔kind 매핑 등 코드가 이름에 의존'을 전수 확인하니 **이름에 의존하는 코드가 없었다** — kind는 이 테이블 조회로 얻고(`rateCategoryKind`), 이름을 바꾸면 참조 중인 `rate_items.category`가 함께 갱신되며, 시드는 마이그레이션 게이트(`rate_categories_seed_v1`)라 지워도 되살아나지 않는다(config 상수는 시드 값일 뿐). **`locked` 컬럼은 레거시**(읽는 코드 0)이고 `CATEGORY_LOCKED` 에러도 제거됐다. ⚠️**남은 안전망은 `CATEGORY_IN_USE` 하나** — 그 분류를 쓰는 rate_items가 있으면 기본이든 아니든 삭제 거부(먼저 항목을 옮기거나 지워야 한다). ⚠️분류를 다 지워 특정 kind가 0개가 되면 그 세션 종류의 단가 select이 빈다(막지 않는다 — 다시 추가하면 복구). `src/data/rate-categories.js`(listRateCategories·rateCategoryKind·createRateCategory·updateRateCategory·moveRateCategory·deleteRateCategory), 라우트=`POST /settings/rate-categories`(+`/:id`·`/:id/move`·`/:id/delete`). rate-categories.test.js(시드·kind 매핑 / 기본 분류 수정·삭제 + 사용 중 거부 / 커스텀 이름변경 cascade / 이름누락).
- `project_contacts(project_id→projects CASCADE, party_id→parties CASCADE, created_at, PRIMARY KEY(project_id, party_id))` — **프로젝트 고객측 담당자 다대다**(2026-07-11, 여러 명 칩·project_artists 패턴). `projects.contact_party_id`=첫(대표, 청구처 파생·관계자 탭·표시 호환), 전체=이 테이블. `setProjectContacts`/`listProjectContacts`(src/data/projects.js), 1회 백필 `project_contacts_backfill_v1`(기존 contact_party_id 복사). `listProjectsForParty`·`ASSOCIATE_ROLE_SUBQUERY`가 UNION해 각 담당자 상세·관계자 탭에 전원 반영. 인덱스 `idx_project_contacts_party`.
- `project_artists(project_id→projects CASCADE, party_id→parties CASCADE, PRIMARY KEY(project_id, party_id))` — **프로젝트 아티스트 다대다**(2026-07-05, 콤마로 여러 명). `projects.artist` TEXT=콤마 표시 목록, `projects.artist_id`=첫(대표) 아티스트(청구처 파생·호환), 전체 목록은 이 테이블(각 아티스트 상세 '연결 프로젝트' 매칭 `listProjectsForParty`). `setProjectArtists`/`listProjectArtists`(src/data/projects.js), 1회 백필 `project_artists_backfill_v1`(기존 artist_id 복사).
- `project_tracks(project_id→projects CASCADE, title, artist?, content_type[Music|Video_Post], created_at)` —
  프로젝트 하위 곡·콘텐츠. **`artist`=곡별 아티스트**(한 프로젝트에 여러 아티스트 가능 — 곡·콘텐츠 추가/수정 폼에서 입력, 미입력 시 프로젝트 아티스트; 트랙 헤더에 표시). **콤마로 여러 명 가능(2026-07-05)**: 콤보는 공용 artistCombo라 조각 검색·이어붙임 자동, 서버 `normalizeArtistList`가 표시 정규화("A,B "→"A, B") — 트랙 artist는 표시 TEXT라 party 해석 없음(프로젝트 단과 달리). `content_type` 상수·정규화(`config.js`)는 있으나 **현재 UI 미노출 → 전부 Music**, 영상 구분은 향후 확장용.
- `track_tasks(track_id→project_tracks CASCADE, task_type, billing_type[Time_Charge|Fixed_Per_Track],
  quantity, unit_price, total_price, engineer_name?, engineer_id?→project_managers SET NULL,
  worker_rate?, status[Pending|Completed], waived[0|1],
  is_invoiced, invoice_id?, session_id?→sessions SET NULL, worker_paid, worker_paid_date?)` — 실제 청구 가능한 모듈형 작업 단위.
  `engineer_id`: 담당 엔지니어 FK(저장 시 `engineer_name` 동기 기록). `worker_rate`: 외주 **지급단가**(고객청구 `total_price`와 별개 — 마진 산정용). `worker_paid`=외주 정산(지급) 여부(`/workers` 정산 탭). 정산 합계=Σ`worker_rate`.
  `session_id`는 녹음 세션에서 자동 생성된 작업 추적(부분 유니크: 세션당 1건). **`waived`=청구 안 함(무료 처리)**(2026-07-06 — `setTaskWaived` 순수 토글, `total_price` 미변경). `is_invoiced=1`이면 waive 불가(`TASK_LOCKED`).
- `deliverables(project_id→projects ON DELETE CASCADE, title, version, kind, storage_backend[drive|local],
  file_id, file_name, file_size, mime_type, access_token?, expires_at?, download_count, revoked, note)`
- `invoices(project_id?→projects SET NULL, payer_id?→parties SET NULL, payer_snapshot?[발행 시점 청구처 JSON], title, amount, paid_amount,
  invoice_number?, tax_amount, discount_amount, status[미발행|발행], tax_status[계산서 미발행|계산서 발행|입금완료], issued_date?, due_date?, memo)` —
  돈=정수(원), 부분납은 코드 파생(연체는 2026-07-27 폐지). `amount`는 VAT 포함 총액. **청구처(payer)=`parties.id`**(당사자 모델). **`payer_snapshot`=발행 시점 청구처 정보 스냅샷 JSON**(상호·사업자번호·대표·주소·이메일·담당자) — 이후 클라이언트 정보가 바뀌어도 과거 청구서 표시·거래명세서 PDF가 발행 당시 정보로 고정(회계 정확성). 표시·PDF는 스냅샷 우선(`payerView`/`payerName`), NULL(레거시)이면 실시간 party 폴백. 발행 시 `snapshotPayer(payerId)`로 저장(from-tasks·수동 양 경로). **`email_overridden`**(2026-07-27)=청구서 건별로 발행 이메일을 party의 실제 이메일과 다르게 지정했다는 플래그 — 참이면 `payerSnapshotChanged`가 이메일 필드를 비교 대상에서 빼고 `refreshedPayerSnapshot`(새로고침)도 이 이메일을 보존한다. **상태 2축 분리**: `status`=청구서 발행 축(미발행/발행), `tax_status`=계산서·입금 축(계산서 미발행/계산서 발행/입금완료). 두 축 독립(자유 선택). `입금완료` 선택=완납(입금액=총액). 채번은 어느 축이든 발행되면. `discount_amount`=청구서 할인(공급가 차감, `invoiceAmountsFromSupply`로 과세표준·VAT 재계산).
- `invoice_items(invoice_id→invoices CASCADE, task_id?→track_tasks SET NULL, session_id?→sessions SET NULL, track_title, task_type,
  description, quantity, unit_price, amount, item_date?)` — 청구서 라인아이템 스냅샷.
  🔒 **세션 라인은 근거별로 쪼갠다**(2026-07-26 사용자 지시 — '청구서에 근거를 항목별로 분리 표시'): 한 세션이 **기본가 / 초과 N시간 / 할증** 최대 3라인. 첫 라인만 기존 형식(`날짜 · 아티스트 · 항목명`)을 유지해 목록·PDF·매출 집계가 종전대로 읽히고, 뒤 라인은 `초과 시간 · 1시간 30분 초과 → 2시간 × ₩100,000`처럼 근거를 그대로 쓴다. **금액을 조정했으면(입력값 ≠ 산정치, 또는 확정 청구액) 쪼개지 않는다** — 조정된 총액을 기본가·초과로 되쪼개면 거짓 근거가 되므로 한 줄 + 조정 사유가 근거로 들어간다.
  ⚠️**라인이 늘어난 다운스트림 정합**(라인 수가 곧 항목 수가 아니게 됐다): 한 세션의 모든 라인이 **같은 `session_id`·`item_date`**를 가져야 청구 잠금(`isSessionInvoiced` EXISTS)·삭제 시 미청구 복원·날짜 정렬이 성립한다. 라인 합 = 청구서 공급가는 그대로. `revenue.attachWorkSummary`의 `item_count`는 **라인 수가 아니라 일(작업·세션) 수**로 센다(안 고치면 세션 하나가 매출 상세에서 '3개 항목'으로 접혀 아티스트·세부가 사라진다). 회귀 `test/invoice-charge-breakdown.test.js` 7. **`item_date`**(2026-07-05 사용자 요청 — '청구 항목도 세션 탭처럼 항상 날짜순')=정렬용 날짜 스냅샷: 세션 라인=`session_date`(실제 일정), 작업 라인=작업 생성일(작업 자체엔 일정 개념이 없어 `created_at` 앞 10자로 근사). `computeInvoiceDraft`가 라인 생성 시 채우고 `items.sort()`로 미리 정렬(초안·미리보기 PDF도 반영), `listInvoiceItemsForInvoice`가 `ORDER BY (item_date IS NULL), item_date ASC, id ASC`(날짜순, 값 없는 레거시/원본삭제 라인은 뒤로). 1회 백필 `invoice_item_date_backfill_v1`(기존 라인을 task_id/session_id로 역참조해 채움 — 참조가 SET NULL된[원본 삭제] 라인은 복원 불가해 NULL 유지).
- `sessions(project_id→projects CASCADE, session_type[녹음|믹싱|마스터링|기타], session_date,
  all_day[0|1·종일=하루 종일·시간 없음], start_time?, end_time? "HH:MM"(종일이면 둘 다 NULL), booker_name?, engineer_name?(**첫 엔지니어 이름 — 다대다는 `session_engineers`**), status[예정|완료|취소], waived[0|1], memo,
  rate_item_id?→rate_items SET NULL, billing_amount?[확정 청구액 — NULL이면 단가표 자동 산정], room_id?→rooms SET NULL, director_contact_id?(→contacts, 담당 디렉터 **레거시**·다대다는 `session_directors`), gcal_event_id?)` — 스튜디오 일정. **`waived`=청구 안 함(무료 처리)**(2026-07-06 — `setSessionWaived` 순수 토글). 청구된(`invoice_items` 참조) 세션은 waive 불가(`SESSION_INVOICED`).
- `session_directors(session_id→sessions CASCADE, contact_id→contacts CASCADE, PRIMARY KEY(session_id, contact_id))` — **세션 담당 디렉터 다대다**(한 세션에 고객측 디렉터 여러 명). `sessions.director_contact_id`(첫 디렉터)는 레거시 동기화 보존. `setSessionDirectors`/`listSessionDirectors`/`resolveDirectorIds`, 기존 단일 디렉터는 `session_directors_backfill_v1`로 1회 복사.
- `session_engineers(session_id→sessions CASCADE, manager_id→project_managers CASCADE, worker_rate, worker_paid, worker_paid_date?, PRIMARY KEY(session_id, manager_id))` — **세션 담당 엔지니어 다대다**(2026-07-05, 한 세션에 스튜디오측 엔지니어 여러 명). 디렉터와 달리 자유 텍스트 등록이 없어(담당자 마스터에서만 선택) 세션 폼은 select 반복 행 + `+ 담당 엔지니어 추가하기`(template 복제·✕ 제거, app.js 위임). `sessions.engineer_name`(첫 엔지니어)은 레거시 동기화 보존. `setSessionEngineers`/`listSessionEngineers`/`resolveEngineerAssignments`(존재하지 않는 id만 제외·비활성 담당자는 유지·dedup. 옛 id-only 래퍼 `resolveEngineerIds`는 소비처 0으로 2026-07-27 제거), 기존 단일 엔지니어는 `session_engineers_backfill_v1`로 1회 복사(이름이 담당자 마스터와 유일 일치할 때만). **세션 외주 정산(`worker_rate`/`worker_paid`/`worker_paid_date`, 2026-07-06 사용자 상담)**: track_tasks와 동일 구조로 세션도 외주 지급 관리 — 세션 폼의 엔지니어 행마다 **외주(data-external=1)일 때만 지급단가 칸 노출**(`engineerRow`, app.js 위임 토글, 작업 폼 `engineerSelect`와 동일 규칙), 제출은 `engineer_ids[]`/`engineer_rates[]` 인덱스 페어링(`resolveEngineerAssignments`). **`setSessionEngineers`는 더 이상 DELETE+재삽입이 아니라 diff 갱신**(2026-07-06 수정 — 이전 방식은 매 저장마다 `worker_paid`/`worker_paid_date`가 초기화되던 버그: 계속 배정된 엔지니어는 단가만 갱신하고 지급상태 보존, 제거된 엔지니어만 삭제, 새 엔지니어만 `worker_paid=0`으로 삽입). 정산 조회·처리는 `listSessionPayoutsForWorker`(session_engineers에 실제 배정된 것만 — 레거시 `engineer_name` 폴백 매칭은 지급단가가 없어 정산 대상 제외)·`setSessionEngineerPayout`, `/workers/:id?tab=payout`에 작업과 함께 노출(`POST /workers/:id/session-payout/:sessionId`), 목록 카드 미지급 요약·`payout-all` 일괄 처리도 세션 포함.
  `booker_name`(예약 담당자)·`engineer_name`(담당 엔지니어)은 둘 다 담당자 마스터에서 선택(별개 역할).
  `rate_item_id`는 녹음 세션 시간제 자동 산정용 단가표 연결. `room_id`는 룸별 겹침 검사 단위(IFNULL 0으로 가상룸 처리). `gcal_event_id`는 자동 생성한 구글 캘린더 일정 id(수정·삭제 추적).
  **`surcharge_key`/`surcharge_memo`**=레거시(2026-07-27 할증 폐기 — 읽는 코드 없음). **`billing_memo`**=금액을 산정치와 다르게 조정한 사유(청구 화면 입력 → 청구 라인 근거로 스냅샷). 촬영 3구간은 `session_segments`(위 참조).
- `admin_state(key, value)` — drive folder_id·refresh token(암호화)·테마 캐시·`studio_calendar_id`(스튜디오 캘린더)·`studio_location`(기본 장소)·~~`studio_hours`~~(운영시간 — **2026-07-27 설정 자체 제거**[사용자 결정]. 예약 시작 그리드가 날짜·시간 콤보로 바뀐 뒤 저장값을 읽는 화면·검증이 하나도 없어 **바꿔도 아무 일이 없는 칸**이었다. 세션 시간은 자유 입력이 맞고[야간·초과 세션이 실제로 있다] 검증을 붙이면 정상 예약을 막는다 → 필요해지면 그때 만든다. `admin_state`의 옛 행[`studio_hours_start/end`]은 지우지 않았다)·`studio_biz_*`(공급자 세금정보, 거래명세서 PDF용, 평문)·`studio_logo`(거래명세서 로고, base64 data URI)·`alert_webhook_url`(알림 웹훅, 암호화)·**`default_booker`**(기본 예약 담당자 이름, 세션 폼 기본 선택).
- `payments(invoice_id→invoices CASCADE, amount, paid_on 'YYYY-MM-DD', memo?, created_at)` — **입금 이력**(부분납 여러 건). `invoices.paid_amount`는 **`SUM(payments.amount)` 파생 캐시**(add/deletePayment·recomputePaid가 유지; 기존 조회부·잔금·납입상태 파생 무변경). 입금 처리=이력 **건별 추가**(누적), 완납=잔금 전액 1건, '입금완료' 선택=잔금 자동 1건. 이력 행 **삭제** 시 재계산 + 잔금 생기면 입금완료→계산서 발행 강등(삭제 중심 관리). 마이그레이션 `payments_backfill_v1`이 기존 `paid_amount`>0을 이력 1건으로 백필(이력 없는 인보이스만). 헬퍼=`listPayments`/`addPayment`/`deletePayment`/`recomputePaid`(`src/data/invoices.js`), UI=`paymentHistory`(views.invoices.js·전체화면 전용). **입금 이력에 날짜 개념 없음(2026-07-05 사용자 결정)**: 입금일 입력·이력 행 날짜 표시 제거 — 행 라벨=메모(없으면 '입금'), `paid_on` 컬럼은 레거시 잔존(서버 addPayment가 기본 오늘 기록·표시 안 함). **전체화면(`/invoices/:id`) 레이아웃(2026-07-05 사용자 요청, 2026-07-08 순서 재배치 — 사용자 '헷갈린다')**: **청구처 정보 최상단**(분류 행 삭제 — 세금계산서 기입에 안 쓰는 부가 메타) → **통합 카드 하나**(**청구번호 → 발행일 → 프로젝트 → 청구 항목 → 소계·할인·VAT·총액** 순 — 영수증식으로 합산이 항목 아래. '공급가'(항목 헤더 우측)는 삭제하고 항목 아래 **'소계'** 행으로 이동, 총액은 `text-base font-semibold` 강조, 금액 블록 래퍼는 항목 있을 때만 `border-t`[중복 선 방지]. **행 사이 줄 제거(2026-07-08 후속)**: row() 자체의 border-b 삭제 — 청구번호·발행일·프로젝트 사이, 소계·할인·VAT·총액 사이엔 줄 없음, **청구 항목 목록 사이 줄도 제거**(invoiceItemsSection 항목 행 border-b 삭제), 구분선은 그룹 경계(메타 그룹 border-b / 금액 그룹 border-t)만. **상세 헤더 정리 + 클릭 복사(2026-07-09 사용자 요청)**: pageHeader의 상태 배지(우상단 라벨)·제목 밑 클라이언트명(desc) 제거(상태는 목록 배지·처리 토글로 충분, 청구처는 아래 카드), **청구 항목 설명·항목 금액·소계·할인·VAT·총액 전부 클릭 복사** — 금액은 `copyable(String(원값), {display: formatKRW})`로 **숫자만 복사**(홈택스 붙여넣기용, ₩·콤마 없음)) **세션 청구 라인 형식 = "7월 25일 · 차가을 · 보컬녹음"(2026-07-08 사용자 요청, · 구분)**: 이전 `{session_type} 세션 {날짜} · {단가항목} ({소요})`에서 **'세션' 접두·소요시간 제거** → `날짜 · 프로젝트아티스트 · 단가항목명`(`computeInvoiceDraft` — 거래명세서 품목명 동일, 기존 발행분 스냅샷은 불변). 청구 생성 폼 세션 체크박스 라벨(`unbilledInvoiceForm`)도 동일 형식. (`invoiceItemsSection` — 카드 래퍼 없는 내부 섹션, border 없음[2026-07-06 사용자 리포트 '프로젝트랑 청구 항목 사이 줄이 2개' — 바로 위 항목의 `row()` border-b와 겹쳐 보이던 것, 자체 border-t 제거로 1선 통일] → **PDF 발행 + 계산서·입금 처리를 한 줄에**(2026-07-06 사용자 요청 — 이전엔 각각 별도 줄·border-t로 분리: 왼쪽=PDF 발행 3종 항상, 오른쪽=**계산서·입금 처리 토글 버튼 2개**(canInvoice — [(계산서|현금영수증) 발행 완료]·[입금완료], 청구 목록 카드와 동일 `taxToggleButtons` 공용 헬퍼·select 폐기, 2026-07-05)) → 메모 → 삭제.
