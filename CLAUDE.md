# OMG Studios ERP — 설계 일지 (CLAUDE.md)

> 녹음/믹싱 스튜디오 **운영–정산 통합 ERP**(예약 → 작업 → 청구 → 정산). 사내 시스템(웹 기반).
>
> ## 📛 표준 호칭 = "ERP" (2026-07-17 사용자 지시 — 항상 적용)
> **이 제품을 부르는 표준 이름은 'ERP'다.** 이전에 '앱·페이지·스튜디오 매니저·(사내) 웹앱·관리 시스템' 등으로 부르던 것을 모두 **'ERP'로 통일**한다. 사용자가 "ERP"라고 하면 곧 **이 제품(OMG Studios ERP)**을 가리키는 것으로 알아듣는다. 문서·사용자 노출 UI 서술어(제품명·메일·로그인 부제 등)는 'ERP'로 칭한다. **단 브랜드 워드마크 "OMG Studios"는 유지**(사이드바 로고·페이지 타이틀 `· OMG Studios`·메일 발신명). **개발/인프라 식별자는 계층별로 갈린다**(2026-07-23 잔재 정리): **로컬·개발 정체성**(프로젝트 폴더명·GitHub 저장소 `omg-sound/omg-studios-erp`·`package.json`/`package-lock.json` `name`)은 옛 초기 잔재 `omg-studios-manager`를 버리고 **`omg-studios-erp`로 통일**(프로덕션·데이터와 무관해 무중단·무위험). 반면 **배포·데이터 인프라 식별자**(`render.yaml` 서비스명 `omg-studios-manager` + 거기서 파생되는 `omg-studios-manager.onrender.com`·Drive 루트 폴더 `omg-studios-manager`·GCP 프로젝트 `omg-studio-manager`)는 **동결**(불변) — 바꾸면 커스텀 도메인 CNAME·OAuth·Drive 데이터가 깨진다. 내부 코드 주석에서 실행체를 가리키는 일반적 "앱"은 그대로 둔다(제품 호칭이 아니라 러닝 인스턴스 지칭).
>
> ## 🎯 제품 정체성 (2026-07-10 확정 — 운영 원칙)
> **녹음 스튜디오 전용 버티컬(도메인 특화) 미니 ERP.** '웹앱'은 만든 형태일 뿐이고, 정체성은 **예약(수주)부터 작업·청구·정산까지 order-to-cash 흐름을 단일 데이터 모델(당사자 `parties`)로 통합한 사내 시스템**이다. 통합 모듈 = **운영**(세션·룸·프로젝트·작업)·**회계/재무**(청구·VAT·거래명세서·매출·미수)·**CRM**(클라이언트·연락처·소속 이력·구글 동기화)·**인사/정산 일부**(외주 정산·원천징수·하우스 계정). 스케줄러 하나·인보이스 툴 하나가 아니라 **이 흐름을 하나로 잇는 것**이 정체성이다.
> - **의도적으로 안 지는 무게(경계)**: 총계정원장(GL)·**판매 재고/구매발주(입출고·재주문·매출원가)**·정식 급여·근태·멀티 조직/테넌트 = **범위 밖**. '가장 가벼운 경로'·삭제 중심 관리 철학과 일치하는 **경량 버티컬 ERP**를 유지한다(대형 ERP를 흉내 내지 않는다). ⚠️**단 보유 장비 대장(자산 참조 목록)은 포함**(2026-07-23 사용자 요청·정체성 갱신 합의) — stock-flow(팔 물건의 입출고)가 아니라 **참조용 자산 대장**(마이크·프리앰프 등 보유 장비의 매입가·시리얼·구매일·현재 장소)이라 연락처·업체처럼 마스터데이터 목록에 가깝고 경량 정체성과 충돌 안 함. 우리는 여전히 물건을 팔거나 입출고를 관리하지 않는다(장비 대장 = `/equipment`, 아래 참조).
> - **세무 심화는 범위 밖**(2026-07-23 사용자 확정 — 기능성 평가가 '치명'으로 올린 항목들을 검토한 뒤 그은 선): **세금계산서 발행기한(익월 10일) 추적·가산세 관리 / 미수 에이징(30·60·90일)·독촉 자동화 / 기간 마감(close)과 마감 후 수정 잠금 / 대변전표(수정세금계산서·환불) · 음수 청구** = **안 만든다**. 우리가 지는 회계는 **청구서를 정확히 만들어 내보내고, 매출·미수·외주 지급을 기록으로 남기는 데까지**다 — 그 뒤의 신고·기한·정정 절차는 **세무사와 홈택스의 일**이고, 우리는 그쪽으로 **자료(거래명세서·매출 CSV·정산 CSV)를 잘 넘기는 것**으로 역할을 끝낸다. ⚠️**평가 점수에 끌려가지 말 것**: 이 항목들이 '치명'으로 나온 건 범용 ERP 기준으로 재면 그렇다는 뜻이고, 이 정체성에서는 **결함이 아니라 의도적 경계**다(severity=by-design로 읽는다). 반대로 **주문–청구 흐름 안에서 돈이 새거나 기록이 사라지는 것**(청구 빠뜨림·매출 귀속 오류·확정된 돈 기록의 소실)은 여전히 최우선으로 고친다 — 세무가 아니라 **운영**이기 때문.
> - **스코프 판단 기준(신규 기능 검토 시 항상)**: *"이게 이 스튜디오의 운영–정산 통합 흐름에 속하나?"* — 속하면 넣고(그 흐름의 한 단계이거나 단일 진실원천을 강화하면 ○), 범용 ERP 기능을 흉내 내는 것이면 뺀다. 정체성 확장이 필요하면 코드보다 이 정의를 먼저 갱신·합의한다.
>
> 녹음실 내부 전용 도구. 역할 3단계: **대표(owner)** = 청구 + 편집 전반(**자료 전달·관리 메뉴만 숨김**, 2026-07-07 전면 개방),
> **치프 엔지니어(chief)** = 운영 전반(스태프·담당자·클라이언트·설정 관리 + 프로젝트 편집 + 청구),
> **스태프(staff, 엔지니어·매니저)** = 프로젝트·곡·작업·자료 편집까지. 전원 Google 화이트리스트
> 로그인(치프가 허용한 계정만). 클라이언트(아티스트/소속사/제작사)는 프로젝트 데이터로 존속하고 로그인하지 않는다.
> 선행 플레이북 2종(`webapp-build-playbook.md`, `녹음실-앱-개발-경험-플레이북.md`)의 검증된 패턴·함정 반영.
> 이 파일은 **살아있는 설계 일지**다(현재 상태·아키텍처·데이터 모델·env·함정·TODO).
> **[현행화 규율 — 선별적으로](2026-07-21 사용자 지시로 완화)**: CLAUDE.md는 **매 세션 로드되는 통치 문서**라 커질수록 매 세션 토큰을 먹는다. 그래서 **아키텍처·현재상태·데이터모델·env·함정·원칙·설계 결정이 바뀌는 변경만** 반영한다(사용자 요청 없어도). 세부 UI 조정·문구·스타일·값 튜닝 같은 건 **git 커밋 메시지로 충분**하니 CLAUDE.md에 안 적는다("이 변경이 다음 세션의 판단을 바꾸나?"가 기준 — 아니면 커밋만). 새 함정·가드레일·불변식은 반드시 적는다(그게 이 파일의 핵심 값어치). 이력은 이 파일이 아니라 git·`HISTORY.md`, **화면 배치·컴포넌트 열거·반응형 단계는 `SCREENS.md`**(2026-07-27 분리 — 164.5k자까지 불어 한도를 넘겨서, 화면 상세 **7블록**[프로젝트 메타 폼·프로젝트 목록·세션 예약 폼·공통 뷰 헬퍼·매출 현황·일정 메뉴(캘린더)·청구 목록·업체그룹 상세]을 **원문 그대로** 옮기고 여기엔 불변식·계약만 남겼다(164.5k → 127.3k). ⚠️**옮길 때 규칙**: 🔒불변식·⚠️함정·권한·데이터 계약은 반드시 CLAUDE.md에 남기고, SCREENS.md에는 '어떻게 생겼나'만 둔다. 화면을 손보다 새 함정을 찾으면 SCREENS.md가 아니라 **이 파일에** 올린다). (이전 규율 '모든 커밋마다 반드시 현행화'가 파일을 173k→252k로 불렸다.)
> **세션 완료 이력은 이 파일이 아니라 `HISTORY.md`에 추가한다**(2026-07-15 한도 관리로 분리 — CLAUDE.md는 상시 참조 지침만 유지해 150k자 한도 아래로 관리, 세션 요약 로그는 HISTORY.md 상단에 누적). 상세 변경 근거는 git 커밋 메시지에 있다. 배포 런북=`DEPLOY.md`, 작업 이어가기=`WORKFLOW.md`, 디자인 시스템(색·컴포넌트·가드레일 1장 요약)=`DESIGN.md`, **화면 구현 상세(배치·컴포넌트·반응형)=`SCREENS.md`**, 세션 완료 이력=`HISTORY.md`.

## 작업 원칙 — 카파시 가이드라인 (2026-07-10 사용자 지시, 항상 적용)

> 이 프로젝트의 모든 작업(기능·수정·점검)에 다음 4원칙을 적용한다. 신중 > 속도 편향(사소한 작업은 재량).
1. **코딩 전 생각**: 가정을 명시하고, 해석이 여러 개면 조용히 하나를 고르지 말고 제시·확인. 더 단순한 접근이 있으면 구현 전에 말한다. 불명확하면 멈추고 묻는다.
2. **단순함 우선**: 요청을 푸는 최소 코드만. 요청 밖 기능·1회용 코드의 추상화·요청 없는 유연성/설정·불가능한 시나리오의 에러 처리 금지. "시니어가 과하다고 할까?" 싶으면 단순화.
3. **수술적 변경**: 요청과 무관한 인접 코드·주석·포맷을 '개선'하지 않는다. 기존 스타일을 따른다. 무관한 죽은 코드는 지우지 말고 보고만. 내 변경이 만든 고아(import·변수)는 정리. **모든 변경 라인이 요청으로 소급돼야 한다.**
4. **목표 주도 실행**: 작업을 검증 가능한 기준으로 바꿔 완료를 증명한다(버그=재현 테스트 먼저→통과, 리팩터=전후 테스트 통과). 다단계 작업은 단계별 검증 계획을 먼저 세운다. — 기존 관행(가드레일 승격 #21·회귀 테스트·E2E 검증)과 같은 방향.

> **상황별 규율(2026-07-10 사용자 승인 — superpowers 스킬 선별 도입)**: **버그 수정** = `systematic-debugging`(수정안보다 근본 원인 먼저) + 재현 테스트 먼저 / **완료 주장 전** = `verification-before-completion`(검증 명령 실행·증거 확인 후에만 "완료") / **대규모 컷오버** = git worktree 격리(`using-git-worktrees` — main 커밋=자동배포라 미완 상태 커밋 금지). 그 외 superpowers 스킬(brainstorming·plans류·review류)은 OMC/내장 기능과 중복이라 미사용.

## 현재 상태 — v1.0 (2026-07-01)

**프로덕션 라이브**: `https://omg-studios-manager.onrender.com` (Render web + 일일 백업 cron). 기능별 현재 동작:

### 인증 · 권한
- 전원 Google OAuth + 화이트리스트(`users`) 로그인 → httpOnly 서명 JWT(30일). 비밀번호 로그인 폐기.
- 3단계 역할: **대표(owner)**=청구 + **편집 전반**(프로젝트·클라이언트·연락처 — 2026-07-07 사용자 결정 '대표는 권한 전부 열고 관리·자료 전달만 숨김'. 배경: 대표 계정에서 사업자등록증이 안 보이던 문제), **치프(chief)**=운영 전반, **스태프(staff)**=프로젝트·작업·자료 편집 **+ 청구서 발행**(매출·외주 정산 제외). 대표가 못 보는 표면 = **자료 전달**(`/deliverables`·프로젝트 '자료 전달' 탭)과 **관리**(`/settings`) 두 곳뿐.
- **보기 모드(2026-07-09 사용자 요청 — 치프=시스템 관리자 역할)**: 치프가 **실제 권한 변경 없이** 대표/스태프의 화면·권한으로 전환해 점검하는 모드. 사이드바 하단(이름·역할 아래, 테마 위) 셀렉트로 전환(`POST /viewas`) → `omg_viewas` 쿠키(12h·httpOnly) → **attachUser가 실제 role=chief일 때만** `req.user.role`을 축소 적용(`real_role="chief"`·`view_as` 보존, DB 불변) — 전 화면·NAV·게이트가 그 역할로 동작. **상승 불가 설계**(비치프 쿠키 조작 무시 — auth.test.js 회귀), 활성 중 경고 배지 '보기 모드 — 실제 권한은 치프', 전환 후 대시보드 복귀(이전 페이지가 새 모드에서 403일 수 있음), 로그아웃 시 해제.
- 미들웨어: `requireAuth`(보기) / `requireEditor`(편집=치프·스태프·**대표**) / `requireStaff`(**치프·스태프 전용 — 자료 전달·관리(/settings)**, 대표 차단, 2026-07-07 신설) / `requireChief`(계정·웹훅 등 민감 관리) / `requireBilling`(청구 생성·발행=전원) / `requireInvoice`(**계산서·현금영수증 발행·입금 처리** + 매출·외주 정산=치프·대표). **청구 워크플로 역할 분리(2026-07-04)**: 스태프(엔지니어)=**청구 생성·보기·삭제**까지(금액 확정→선택 청구 생성=곧 청구서 발행), 대표·치프=**계산서/현금영수증 발행·입금 처리**(`tax-status`·`pay`·payment 삭제 라우트=`requireInvoice`, 목록 상태처리 토글·전체화면 계산서·입금 블록도 `canInvoice`만 노출).
  내부 도구라 로그인 직원은 전 프로젝트 열람. 치프가 `/settings`에서 화이트리스트(이메일+역할) 운영.

### 프로젝트
- **유형 구분 없음**: 세션 일정 자체가 클라이언트 방문·예약이고 녹음/믹스는 그 안에서 일어나므로 프로젝트 단에서 세션/작업을 나누지 않는다. **모든 프로젝트가 세션 일정 + 곡·콘텐츠를 동일하게** 갖는다. "+ 새 프로젝트"는 단일 진입. `project_type`/`PROJECT_TYPES`는 레거시 컬럼·상수로만 보존(신규=`session` 고정, 편집 시 기존값 유지, UI 미노출·드롭다운/배지 제거).
- 상세는 **탭**: `정보(메타) / 세션 일정 / 곡·콘텐츠 / 자료 전달 / 청구`(청구 탭=치프·대표·스태프. 첫 탭 라벨='정보', URL 키는 `?tab=project`[하위 호환]). 세션 일정 탭은 **전 프로젝트 노출**. 메타 편집은 **'정보' 탭**(첫 탭·기본), URL `?tab=`. **메타 폼 하단에 '프로젝트 삭제' 옆 '취소' 버튼**(저장하지 않고 프로젝트 목록으로, `dirtyActionRow({deleteFormId, cancelHref:"/projects"})` — 삭제·취소 나란히). `data-no-guard`로 dirty 네비게이션 가드의 커스텀 모달을 우회하되, 그것만으로는 브라우저 네이티브 beforeunload "나가시겠습니까?" 프롬프트를 못 막으므로 app.js 네비게이션 가드 클릭 핸들러가 `data-no-guard` 링크에서 **`bypass=true`(1초 타이머로 자동 해제)도 함께 세팅**해 beforeunload까지 통과한다(함정 #24).
- 메타('정보' 탭) — 배치·콤보 동작 상세는 **[SCREENS.md §프로젝트 상세 — 정보(메타) 탭 폼](SCREENS.md)**. 여기 남기는 계약·불변식:
  - **소속/레이블 입력 필드 없음** — 저장 시 `resolveProjectParties`가 **첫(대표) 아티스트의 현재 소속**(`currentAffiliation(artistId).org_id`)에서 `agency_id`·표시 `artist_company`를 파생한다(다중=첫 기준·미설정=null·기존 프로젝트는 편집-저장 시 재파생). 소속 지정은 아티스트/연락처 쪽에서 한다. **제작/운영은 프로젝트 속성이라 명시 입력 유지**.
  - **다대다 3종**: 아티스트(`project_artists`)·고객측 담당자(`project_contacts`)·(세션쪽) 디렉터. 제출은 **칩마다 `<id>`+`<name>` 쌍**(인덱스 페어링), 저장은 통째 교체. `projects.artist`(콤마 TEXT)·`artist_id`/`contact_party_id`(첫 명)는 표시·레거시 호환용.
  - ⚠️ **id 없이 텍스트만 오면 서버 안전망이 기존 party를 찾는다** — `resolvePartyByDisplay`(회사 상호 정확 → 사람 본명/`본명 호칭`/`본명 (활동명)`/활동명 **유일** 매칭, 생성 없음) 후 그래도 없을 때만 새 회사 생성. 이게 없으면 사람 라벨 텍스트가 **유령 회사**로 저장된다. `resolvePersonByName`도 같은 라벨 안전망(동명이인은 보수적으로 새로 만든다).
  - **표시명 규칙**: 공용 `personName`/`personLabel` — `본명 + 호칭(컬럼) + (활동명)`, name이 호칭으로 끝나면 중복 안 붙임. **name에는 호칭을 넣지 않는다**(호칭은 전용 컬럼). 제출 hidden 이름은 **순수 본명**. 거래명세서 PDF 상호는 **본명만**(현금영수증 법적 명의).
  - **제작/운영에 개인(관계자) 선택 가능** — `production_party_id`로 사람 party 직접 지정. 다운스트림(`client_id`/payer 파생)은 `COALESCE(production_id, agency_id, artist_id) JOIN parties`라 party kind 무관.
  - 소속/레이블·제작/운영에 **회사**가 지정되면 그 회사 클라이언트 역할에도 반영(`addCompanyRole`, 멱등·사람이면 no-op) → 겸업 시 배지 2개.
  - **아티스트↔사람 자동 연결로 중복 방지**(`applyArtistLink`→`linkArtistToContact`): 개인 아티스트는 명시 선택 또는 **정확 일치가 유일할 때만** 기존 연락처에 통합(`resolveContactForArtist` — 동명이인 2+면 새로 만든다). 그룹은 `is_group=1`로 연락처 미연결. 1회 백필 `artist_contact_link_backfill_v1`.
  - **마감일 없음**(`due_date`=레거시). 청구처(실결제자)는 이 폼이 아니라 **청구 섹션**에서 결정. `projects.client_id`는 제작사›소속사›아티스트 우선순위로 자동 파생(표시·대시보드용).
  - **필드명 표시/저장 분리**: 표시 `소속/레이블`·`제작/운영` ↔ 저장 role 값 `소속사/레이블`·`제작사`(`companyRoleLabel`이 변환 — 파생·마이그레이션 호환).
- 목록 — 컬럼 표·정렬·반응형·펼침 상세는 **[SCREENS.md §프로젝트 목록](SCREENS.md)**. 여기 남기는 계약·불변식:
  - **3탭 분리**(상호배타, 순수 함수 `splitProjectTabs`): 진행 중(`active`)/청구 필요(`billing`)/완료(`done`). `?tab=`(기본 active), 활성 탭만 렌더.
  - **완료 판정 `is_completed`** = **예정 세션 없음**(`sess_scheduled=0` — 완료를 안 누른 세션이 미래든 **지난 것이든** 있으면 진행 중. 작업 `open_tasks`와 대칭) AND 미완료 작업 없음 AND 실제 활동 있었음(`content_cnt>0`). ⚠️2026-07-25 교정 — 이전엔 '미래 예정만' 봐서 **지난 예정 세션이 날짜만 지나면 완료로 샜다**. 세션 상태는 자동으로 안 바뀐다(완료 버튼 기준).
  - 🔒 **미청구 신호(`unbilled_cnt`)는 완료 여부와 무관** — 행 배지는 어느 탭에서든 `>0`이면 뜬다. 산정 조건: 미청구 작업 + `listBillableSessionsForProject`와 **동일 조건** 세션(두 곳이 갈리면 '청구 필요 N'과 실제 후보 목록이 어긋난다) + **단가 미선택(`rate_item_id` NULL)도 센다**(2026-07-23 — 걸러내면 완료된 대관 세션이 후보와 집계에서 동시에 사라져 매출이 조용히 샌다) + **후반작업 세션 '청구 미착수' 플래그**(완료 믹싱/마스터링 세션이 있고 작업 0 AND 청구서 0이면 +1, `hasPostprodSessionNeedingBilling`과 **술어 정합**). 회귀 `billing-radar.test.js`.
  - **금액 = 전체 버짓 = 작업액 + 세션액**(청구 여부 무관). 확정 진행 시 확정액 우선(청구된 작업=`total_price`, 청구된 세션=`invoice_items` 합계), 미청구만 예상액. **청구서 할인 차감**(`invoice_discount_total`, 0 하한). 목록·상세 동일 규칙.
  - **'내 프로젝트만'**(`?mine=1`, 기억 안 함 — [[no-list-filter-memory]]): 내 것 = PM OR 담당 세션 OR 담당 작업(`listProjectIdsForManager` UNION 1쿼리). 담당자 행 없는 계정(대표 등)은 토글 숨김 + 강제해도 무효.
  - **백링크·카드 목적지**: 행 링크가 현재 목록 주소를 `?return=`으로 실어 보내고 상세가 `safePath`로 복귀(→ [돌아가기 규약](#레이아웃-통일) 인접 항목). 카드 클릭은 `projectRowHref(p, tab)`로 **탭 맥락 목적지 직행**(청구 필요→청구 탭 / 진행 중→세션, 세션 없고 곡만 있으면 곡·콘텐츠 / 완료→정보).
  - 요약은 `listProjectSummaries(ids)` **배치 2쿼리**(N+1 회피). 펼침 안 세션·작업 완료 토글은 `?open=projectId`로 그 카드를 재펼침.
- 삭제=치프·스태프(`requireEditor`; 상세 메타 하단, CASCADE: 트랙·세션·자료 / 인보이스는 `project_id=NULL` 보존). **청구된 작업·세션이 있으면 삭제 거부**(`PROJECT_HAS_INVOICED`).
- **곡 일괄 추가**: 줄바꿈으로 여러 곡명 한 번에 추가.

### 세션 일정(예약)
- **프로젝트 상세 세션 탭은 항상 날짜순**(`listSessionsForProject` `ORDER BY session_date ASC, start_time ASC, id ASC`). 전역 `/sessions`(다가오는·지난)·캘린더도 날짜순. **청구 탭의 '청구 가능 세션' 목록**(`listBillableSessionsForProject`)도 `session_date ASC, start_time ASC, id ASC`.
- 프로젝트 하위 세션 CRUD + 사이드바 `/sessions`(**캘린더 기본**·`?view=list`로 목록) — 캘린더 그리드·칩 팝오버 좌표·행 구성 상세는 **[SCREENS.md §일정 메뉴](SCREENS.md)**. 여기 남기는 계약·불변식:
  - **역할 분리**: 일정 메뉴 = 서베이(캘린더) / 프로젝트 세션 탭 = 관리 타임라인. 정렬은 **어디서나 날짜순**(`session_date, start_time, id`) — 프로젝트 세션 탭·전역 목록·캘린더·청구 후보(`listBillableSessionsForProject`)가 같은 기준.
  - 🔒 **캘린더 격자와 데이터가 같은 함수를 공유한다**: `calendarMonthCells(ym)`(lib/date, 완전한 주 7칸 배수 + `inMonth`)를 뷰(`monthCalendar`)와 조회(`sessionsForCalendar` — `LIKE` 대신 격자 범위 `BETWEEN`)가 함께 쓴다. 갈리면 앞뒤 달 넘침 칸의 일정이 안 보인다. 회귀 `calendar-cells.test.js`.
  - ⚠️ **`sessionsForMonth`는 취소 세션을 포함한다** — 예전엔 `status<>'취소'`로 걸러 캘린더에서 안 보였고 그래서 '취소는 흐리게' 코드가 죽어 있었다. `pastSessions`·`listSessionsForProject`도 포함, **`upcomingSessions`만 제외**(다가오는 목록·대시보드 노이즈 방지).
  - ⚠️ **칩 팝오버 좌표는 app.js가 잰다**(서버 인라인 style은 CSP에 막힌다 — 함정 #27). **`data-modal`을 일부러 안 붙인다** — 붙으면 공용 스크롤 잠금이 배경을 얼려 '칩을 연달아 눌러 훑기'가 죽는다. 바깥 클릭 판정은 **mousedown이 팝오버 안에서 시작했으면 무시**(함정 #25). 회귀 `session-cancel.test.js`가 '딤 없음 + `data-session-pop` 훅'을 잠금.
  - **완료 토글은 네 표면(캘린더 팝오버·목록 펼침·세션 탭·작업) 전부 `− 완료`/`✓ 완료` glyph 통일**, 고정폭 span으로 버튼 크기 불변. 청구된 세션 되돌리기는 거부(`SESSION_INVOICED`).
  - ⚠️ **취소 라벨은 '일정 취소'**(목적어 필수) — 그냥 '취소'면 팝오버 안에서 닫기(✕)와 헷갈린다. 취소는 **삭제가 아니라** 제목 '(취소)' prefix + 흐리게, 캘린더 일정도 남는다. `session-cancel.test.js`가 라벨 잠금.
  - **캘린더 동기화 가드**: `gcal_event_id` 없는 세션을 취소하면 `updateEvent(null)→createEvent` 폴백으로 **없던 '(취소)' 일정이 생기던 것** 차단(`syncSessionEvent`가 `취소 && !gcal_event_id`면 no-op).
- 폼 레이아웃(추가·편집 통일, 구글 캘린더식) — 3구역 배치·날짜/시간 콤보·소요 슬라이더 상세는 **[SCREENS.md §세션 예약 폼](SCREENS.md)**. 여기 남기는 계약·불변식:
  - ⚠️ **보이는 입력은 name 없음 + hidden으로 제출**(날짜·시간 콤보 전부, 함정 #19). 마커(`data-session-date`/`data-end-date`)도 **hidden에** 붙는다(소요 역산·겹침 조회·서버 계약이 그 값을 쓴다). **구간 입력에도 `attachTimeFormat` 필수** — 없으면 hidden이 비어 구간이 조용히 저장되지 않는다.
  - ⚠️ 서버 `formatYmdCombo`와 app.js `fmtDateCombo`는 **같은 표시 형식**이어야 한다(다르면 로드 직후 값이 튄다). 날짜 +1일 계산은 **로컬 파트 조립** — `toISOString()`은 UTC라 KST에서 하루 밀린다.
  - IME 조합 중 Enter 무시(함정 #18), 팝오버·시간 옵션 버튼은 `tabindex="-1"`(Tab이 다음 필드로).
  - **종일 = 구글/애플 개념**(하루 종일·시간 없음): 체크박스가 제출 소스이고 **서버가 권위**(`all_day=1`이면 시간값 무시하고 start/end NULL). JS는 UI만 숨기므로 해제 시 시간이 보존된다. 겹침 없음·캘린더는 date-only·청구는 1 기준 블록. 다일은 `end_date`로.
  - **소요 단일 진실원천 = app.js `curDur`**(`custom_hours`는 hidden 제출값, ≤960분일 때만 미러 — 초과분을 채우면 서버 `resolveEndTime`이 960으로 클램프해 종료가 잘린다). JS-off 폴백 = `start_time`+`end_time`.
  - **상태 필드 없음** — 대기/완료는 완료 토글, 취소는 행/팝오버의 '일정 취소'. 편집 저장 시 초기화되지 않게 **현재 상태를 hidden으로 보존**.
  - `app.js`가 `[data-session-form]`을 **폼별로 초기화**(한 페이지에 폼 여럿). ⚠️**초기화는 그 폼의 `<details>`가 열릴 때 일어난다**(app.js:586 — 닫힌 폼은 `toggle`에 1회 리스너만 걸어 두고 `initSessionForm`을 미룬다. 목록에 폼이 수십 개라 의도적 지연). 그래서 **닫힌 폼의 select에 change를 쏘면 아무도 듣지 않는다** — 자동화로 검증할 때 `<details>`를 먼저 열지 않으면 '구간이 안 나온다'로 보여 앱 결함으로 오진한다(2026-07-27 실측, 세 번 오진함). 사람이 행을 펼쳐 쓰는 실제 경로에는 영향 없음.
- **용어 통일**: `녹음 종류` = **단가표 항목**(`rate_item_id`, 스튜디오/로케이션 분류 optgroup, `rateOptionsHtml` — 옛 `rateSelectGrouped` 래퍼는 소비처 0으로 2026-07-27 제거).
  `세션 종류` = `session_type`(녹음/**촬영**/**공연**/믹싱/마스터링/기타). **대관 매출 세션 = 녹음·촬영·공연**(`RENTAL_SESSION_TYPES` config): 세션 자체가 단가표 시간제 청구 대상이라 완료 시 청구로 넘어간다(믹싱·마스터링은 곡·콘텐츠 후반작업으로 청구). `RENTAL_SESSION_TYPES.includes()`/SQL `IN (${RENTAL_IN})`(config 배열에서 생성 — 종류 추가 시 SQL 자동 반영)로 일반화(sessionRateAmount·listBillableSessionsForProject·computeInvoiceDraft·sessionAmountsByProject·청구사유·캘린더 라벨[촬영·{단가항목}]·세션폼 rate 필드 노출[app.js data-rec-types]·**청구 폼 라벨·invoice_items 스냅샷 description**[`{session_type} 세션 …` — 거래명세서 품목명도 촬영 표기]). 세션 종류는 관리에서 편집 불가(정적 config 유지). 추가·편집 폼에서 동일 의미.
- 소요시간 **슬라이더**(30분 단위·**최대 16시간**=`DURATION_SLIDER_MAX=960`·서버 `resolveEndTime` 960분 클램프) + 아래 `[1Pro][2Pro][3Pro][4Pro]` 프리셋(슬라이더와 양방향 동기화). **프리셋 배치**: 데스크톱(sm+)=슬라이더 위치에 절대배치(`positionTicks`가 기준시간으로 재배치·4Pro=base×4가 16h 트랙의 해당 %), **모바일(<640px)=위치정렬 없이 앞에서부터 흐름 정렬**(`sm:absolute`/`sm:left-*`·모바일은 `static`+flex, `positionTicks`가 모바일이면 인라인 위치 제거·resize 재계산). 종료는 서버가 시작+길이로 계산(`custom_hours`+`duration_mode=custom`, 1Pro=녹음 종류 기준시간). **프리셋 항상 활성**: `updateProAvailability`·클릭 핸들러가 `positionTicks`와 동일하게 `baseMinutes() || proDefaultMinutes()` 폴백을 써 **녹음 단가 없거나 믹싱 등 세션에서도 스튜디오 기본 블록(210분) 기준으로 1Pro×N 활성**. 프리셋 비활성/클릭은 **룸 예약·겹침과 무관**(겹침은 확인 후 강행 경로가 별도 처리).
  폼 인터랙션은 `public/js/app.js`(CSP: 인라인 0).
- **다중 룸**: `rooms` 테이블 + `sessions.room_id`. 세션 폼에 룸 select(**첫 룸=Studio A 기본 선택, '룸 미지정'은 맨 아래 옵션**). 겹침 검사를 `IFNULL(room_id,0)`으로 룸별 판정 — **같은 룸만 충돌, 다른 룸은 동시간 병렬 허용**(레거시 NULL끼리는 가상 룸 0으로 처리). 룸 CRUD는 `/settings` 환경설정 탭(`POST /settings/rooms`·`/:id`[수정]·`/:id/move`·`/:id/delete`).
- 🏷 **룸 명칭 체계**(2026-07-26 사용자 지시 — 홈페이지와 동일 스펙, 다만 **앱 간 동기화는 하지 않는다**[우산 CLAUDE.md 결정]): `Studio A` / `Studio B`(편집 전용) / `Studio C`(믹싱 전용) / `Lounge`(대기·접객 — 작업 공간 아님). 작업 공간이 아닌 장소는 `bookable=0`이라 세션 폼 장소 목록에서 빠진다(`listRooms({bookableOnly:true})`). 금지 표기 = Hall·Live Booth·Lobby·Room A/B/C·A룸/B룸/C룸.
  - 🚫 **룸 계층(상위/하위) 폐지**(2026-07-28 사용자 지시): 2026-07-26에 `Studio A`를 `Control Room A`+`Booth A`의 상위 단위로 두는 2단 계층을 넣었으나, **예약은 어차피 최상위 단위로만 잡아 하위 행이 아무 판단에도 쓰이지 않고 관리 화면에서 자리만 차지했다**. 계층 코드(상위 select·들여쓰기·2단 제한·`bookable` 강제 0·'같은 상위 안에서만' 순서 이동)를 전부 걷어내고 하위로 만들어 뒀던 두 행도 삭제했다(`rooms_drop_hierarchy_v1`). `rooms.parent_id`는 **레거시 컬럼으로만 잔존**(읽는 코드 0 — `sessions.director_contact_id`와 같은 취급). ⚠️마이그레이션은 **참조 세션이 없을 때만 삭제**하고, 있으면 지우지 않고 최상위로 전환한 뒤 로그를 남긴다(조용한 데이터 변경 방지 — `room_id`를 NULL로 미는 건 사람이 보고 판단할 일).
  - 🔒 **이름 수정 경로가 있다**(`updateRoom`, 2026-07-26): 이전엔 추가·삭제만 있어 오타를 고치려면 지웠다 만들어야 했고 `deleteRoom`이 `room_id`를 NULL로 밀어 **그 장소로 잡힌 세션이 전부 '장소 미지정'**이 됐다. 마이그레이션도 같은 이유로 **rename만**(id 보존) 한다.
  - 순서 이동(`moveRoom`)은 전체 목록 한 축에서 이웃과 교환하고 `sort_order`를 10 간격으로 재부여한다.
  - 📌**운영 실측(2026-07-26 배포)**: 운영 룸은 이미 `Studio A`·`Studio B`·`Studio C`·`Lounge`로 개명돼 있어(id 2·3·4·5) rename 별칭은 발동하지 않았다. 별도 `외부일정`(id 6, 외부·예약 대상)도 있다. (그때 추가됐던 `Control Room A`·`Booth A`[id 7·8]는 2026-07-28 계층 폐지로 삭제.)
  - ⚠️**sort_order가 0인 룸은 목록 맨 위로 와 세션 폼의 기본 장소가 된다**(`roomSelect`가 현재값 없으면 첫 장소를 자동 선택). 마이그레이션이 새 명칭에만 10·20·30·40을 부여해 손대지 않은 `외부일정`(0)이 맨 위로 올라가 **새 세션의 기본 장소가 외부일정이 되고 주소칸이 열리던 것**을 배포 후 발견해 ↓ 4회로 맨 아래로 옮겼다. 룸을 추가·개명할 때 순서값을 함께 보라.
  - ⚠️**세션 폼 장소 select은 예약 대상만 보여주지만, 이미 그 세션에 잡혀 있는 장소는 예약 대상이 아니어도 옵션에 살려 둔다**(`roomSelect`가 `getRoom`으로 되살려 '(예약 대상 아님)' 표기) — 안 그러면 그 세션을 편집·저장하는 순간 장소가 조용히 '미지정'이 된다(담당자 select `(목록에 없음)`·`validRoomId`의 비활성 룸 보존과 같은 철학). 서버 `validRoomId`는 종전대로 '존재하는 룸'까지만 검증한다(bookable까지 강제하면 위 케이스가 데이터 손실이 된다 — 예약 대상 제한은 폼이 제공하는 선택지로 지킨다).
- 🔒 **겹침 경고 + 확인 후 강행 — 경고 조건 ≡ 차단 조건**(2026-07-23 기능성 평가로 재설계): 같은 룸·같은 시간 겹침을 **막지 않고 확인 후 등록 허용**. **모든 세션 종류가 룸을 점유**(session_type 무관). ①선택한 시작+소요가 겹치면 **시작 시간 아래 경고**(`[data-conflict-warn]`, 어느 세션인지 라벨 명시 — `A룸 14:00–18:00 · 루나 1집`), ②**제출 시 confirm** → 승인하면 hidden `override_conflict=1` → 서버 `assertNoSessionConflict(f, excludeId, allowConflict)`가 겹침 검사를 **통째로 건너뛴다**(create·update 공통). override 없이 겹치면 서버가 409(`sessionConflictMessage`) — JS 미동작·경합 대비 안전망(409 화면에 강행 버튼은 **없음** — 사용자 결정, 폼으로 돌아가 저장하면 확인창이 뜬다).
  ⚠️**핵심 불변식**: override가 서버 검사를 통째로 끄므로 **확인창이 뜨는 조건은 서버가 실제로 막는 조건과 정확히 같아야 한다**. 달랐던 게 결함의 본체였다 — 옛 구조는 경고가 30분 슬롯 근사(`busySessionSlots`) + **구글 FreeBusy 합산**이었는데, FreeBusy는 단일 캘린더라 **룸을 구분 못 하고 이벤트 id가 없어 자기 일정도 못 뺐다** → 동기화된 세션을 편집할 때마다·다른 룸을 예약할 때마다 **거짓 경고**가 떴고, 거기 길들여진 사용자가 승인을 누르는 순간 **진짜 이중 예약이 통과**했다. 게다가 슬롯 창이 `12:00~23:30` 하드코딩이라 **오전 세션은 경고가 아예 안 떴다**(서버는 막으므로 사용자는 이유 없이 409를 맞음).
  → **판정을 한 구현으로 합쳤다**: `busySessionRanges(date, {excludeId, room})`(src/data/sessions.js)가 기준일 자정 절대 분축 점유 구간(`{start,end}` + 세션 행)을 반환하고, **`findSessionConflict`가 이 함수를 호출**하며 **`GET /sessions/availability`가 같은 함수 결과를 그대로 내려준다**(`{date, busy:[{start,end,label}]}`). app.js는 같은 반열린 비교(`s < r.end && r.start < e`)만 한다. 시간대 창이 없어 **오전 사각지대 소멸**, 전날 야간분은 start 음수로 들어오고 **자정 전에 끝난 전날 주간분은 제외**(`end<=0` — 겹칠 수 없는 노이즈). 룸은 클라이언트가 **항상** 보낸다(빈 값='룸 미지정'=`IFNULL(room_id,0)` → 서버와 같은 축). **구글 FreeBusy는 경고에서 제거**(사용자 결정 — 앱→구글 단방향이라 캘린더 내용은 사실상 DB 복제이고 룸 점유의 진실원천은 앱 DB. 일정 자동 생성/수정/삭제 동기화는 그대로). 고아가 된 `busySessionSlots`·`calendar.busySlotsForDate`·`config.SESSION_TIME_SLOTS` 제거. 회귀 `test/session-conflict-warn.test.js` 8(경고≡차단 매트릭스 포함) + `ui-interactions` 7(클라이언트 구간 비교·override 배선).
- **구글 캘린더 자동 연동**: 예약 시 스튜디오 캘린더에 일정 자동 생성/수정/삭제(제목=**아티스트·제작사**, 장소=기본 장소, `gcal_event_id` 추적). **이벤트 설명**=**프로젝트명**(맨 앞)·종류·예약·엔지니어·**디렉터**(다대다 `listSessionDirectors`, `본명 (활동명)` 병기)·메모·프로젝트 링크(`eventInputForSession`). **종류 라벨**(`sessionTypeLabel`): 녹음이면 **녹음 단가 항목명**(`녹음 · {rate_item.name}`, `getRateItem`, 단가 없으면 `녹음 세션`), 그 외 세션 종류는 **`{종류} 세션`**(믹싱=믹스 세션·마스터링 세션·기타 세션). 미연동/오류는 fail-safe(예약은 정상) — **실패 사유는 로그로 남긴다**(`[calendar] createEvent 스킵/실패`, 게이트별 사유). **스튜디오 캘린더 미선택(`studio_calendar_id`=사용 안 함)이면 자동 연동이 꺼진다**(가장 흔한 미연동 원인) — `/settings` 캘린더 섹션에 자동연동 켜짐/꺼짐 상태 배지 표시. **세션 '취소' 시에도 일정을 삭제하지 않고 제목에 '(취소)' prefix로 업데이트**해 기록으로 남긴다(2026-07-15 사용자 요청 — `eventInputForSession`이 `status==='취소'`면 `(취소) ` 접두, `syncSessionEvent`는 취소 삭제 분기 없이 `updateEvent`로 통일. 예: `(취소) 루나 · 뮤직팜`). **실제 세션 삭제(`/delete`)만 캘린더 일정도 삭제**(취소[기록 유지] ≠ 삭제[제거]). **역방향(캘린더→앱) 동기화는 없음 — 앱→구글 단방향 자동 연동(생성/수정/삭제)만 유지**.
- **예정 세션 1클릭 완료**: 세션 목록/상세에서 '예정' 상태 행에 완료 버튼 인라인 제공(별도 편집 폼 없이). **`/sessions` 목록 검색**(`?q=`) 지원. 청구 결핍 사유(미완료·단가 미선택·시간 없음)는 청구 생성 폼에 인라인 표시.

### 곡 · 콘텐츠 (녹음과 별개의 후반작업)
- 프로젝트 하위 곡/콘텐츠(`project_tracks`) + 모듈형 작업(`track_tasks`). **진행 단계 빠른 버튼**(보컬튠·오디오편집·믹싱·
  마스터링, +기타는 전체 종류 그룹 드롭다운), 곡별 **진행 요약** 한 줄, 작업 행 = **헤더 접기 토글**(`<details>`, 우측 chevron). **작업 상태=대기/완료 2단계**('진행중'[In_Progress] 폐기 2026-07-03 — `TASK_STATUSES`에서 제거, 레거시는 `task_status_drop_inprogress_v1` 마이그레이션+`normalizeTaskStatus`로 대기 승계). **완료 버튼 = 세션과 동일한 토글**(2026-07-05 사용자 요청 — 이전엔 헤더의 상태 select가 본문 저장 폼에 `form=`로 연결돼 있어 접힌 채 상태를 바꿔도 '작업 저장'을 눌러야 반영됐음): select 제거 → 독립 버튼(`completeToggle`) — 대기=비활성 `− 완료`(누르면 완료로 즉시 전환), 완료=활성 `✓ 완료`(`border-success/40 bg-success/10 text-success`·`aria-pressed`, 다시 누르면 대기로). 별도 라우트 `POST /projects/tasks/:id/status`→`setTaskStatus`(상태만 변경, 종류·담당·단가는 안 건드림 — 세션의 `/sessions/:id/status`와 같은 이유로 일반 수정 라우트와 분리: 그 라우트는 종류·담당 필드를 함께 요구해 상태만 보내면 리셋 위험). **버그수정**: `updateTask`(작업 저장 폼)가 `taskEditForm`에서 상태 필드를 뺀 뒤에도 `normalizeTaskStatus(undefined)`를 그대로 써 fallback(대기)으로 완료 상태를 되돌리고 있었음 — `input.status`가 없으면 기존 상태를 보존하도록 수정(명시 전송 시엔 여전히 검증·반영, 레거시 호출부 호환). task-status.test.js 회귀 3개. **후반작업은 전부 트랙/콘텐츠 고정·수량 1**(과금 유형·수량 선택 UI 폐기). **금액은 곡·콘텐츠 탭이 아니라 청구 탭에서 확정**(2026-07-01 — 세션 일정과 동일 철학: 작업 폼은 **종류·담당·상태만** 기록, 금액 칸 없음. 작업 생성·수정 시 종류 기본단가[`task_types.unit_price`]를 `total_price`에 **자동 적용**[`taskTypeUnitPrice`], 청구 생성 폼에서 작업별 금액 입력·조정해 최종 확정). 작업 행 헤더는 **청구된 작업의 확정액만** 표시(미청구는 숨김 — '기록만' 일관).
- 작업 폼에 **담당 엔지니어 select**(value=`project_managers.id`, 외주 포함) + **외주 지급단가**(`worker_rate`) 입력 — **담당 엔지니어가 외주 작업자일 때만 노출**(하우스 엔지니어=`user_id` 있으면 숨김·저장 시 0; engineerSelect `data-external`·app.js 토글). 저장 시 `engineer_id` 기록(→ `engineer_name` 동기 기록). 외주 정산 합계 = Σ`worker_rate`(고객 청구 `total_price`는 마진 참고 표기). **새 작업의 담당 엔지니어는 로그인 계정 기본 선택**(`createTask`가 `getManagerByUserId(user.id)`로 본인 담당자를 기본값 — 빠른 추가 시 본인 자동 배정; 명시 선택 시 우선).
- **작업 폼은 명시 저장**(`taskEditForm` = `data-dirty-form` + `[data-dirty-save]` '작업 저장' — 변경 없으면 버튼 흐림, 변경 시 강조. 저장 후 서버 재렌더로 행 헤더[종류·담당·금액]가 갱신된다). ⚠️**옛 '자동저장'(변경 즉시 저장 + JSON 응답으로 헤더 즉시 갱신)은 2026-07-01 `07bf6f6`에서 폐지**됐다 — 앱 전반의 명시적 저장 패턴으로 통일. 그때 클라이언트 IIFE만 지워 **서버의 `X-Requested-With:fetch` JSON 분기와 `data-row-*` 헤더 마커가 부르는 사람 없이 남아 있었고**(2026-07-27 전수 조사에서 발견·제거), 이 문서도 자동저장을 현재 동작으로 서술하고 있었다. 남아 있는 **즉시 저장은 청구 폼의 금액칸뿐**(`task_amount_<id>`·`session_amount_<id>` → 아래 항목).
- **청구 폼 세션 금액도 즉시 저장(2026-07-14 사용자 요청 — '수정해 놓은 금액이 계속 반영되게')**: 이전엔 세션 금액칸이 **폼 안에서만** 살아 있다가 새로고침·다른 기기에서 단가표 산정치로 되돌아갔다(작업 금액은 이미 즉시 저장이라 비대칭). → **`sessions.billing_amount`(확정 청구액, NULL=단가표 자동 산정)** 신설 + `POST /sessions/:id/amount`(requireEditor·JSON, 청구된 세션은 409 `SESSION_INVOICED`) + app.js 즉시 저장 핸들러를 `session_amount_<id>`까지 확장(작업 `task_amount_<id>`와 동일 경로). `sessionRateAmount`가 확정액 우선(`fixed:true` 플래그)·`sessionAmountsByProject`(프로젝트 목록 금액)도 확정액 반영. **0원도 유효한 확정액**(무료 협의), **빈 칸으로 지우면 NULL → 단가표 산정으로 복귀**. 청구 생성 시 폼 제출값이 최종(스냅샷)인 것은 무변경. session-amount.test.js 회귀 4 + E2E(수정→새로고침 유지→목록 금액→되돌리기). **청구 생성 폼 임시저장(초안, 2026-07-15 사용자 요청)**: 청구 생성 버튼 **왼쪽 '임시저장' 버튼**(`data-invoice-draft-save`·`type=button`)을 **누를 때만** 폼 필드(**청구처·할인(정액/정률)·VAT 포함·제목**)를 localStorage(`invdraft:<projectId>`)에 저장(저장 후 1.5초 '임시저장됨 ✓' 피드백) → 로드 시 복원, 발행(`data-invoice-submit`) 제출 시 삭제(막힌 제출[청구처 미선택·발행정보 없음·0원 취소]은 `e.defaultPrevented`로 유지, PDF 프리뷰는 대상 아님). **자동저장은 폐지**(2026-07-15 사용자 결정 — 값이 조용히 저장돼 나중에 되살아나는 놀람 클래스 제거, 앱 전반의 명시적 저장 패턴[작성일 등]과 통일). **금액(`task_amount`/`session_amount`)·항목 체크박스·발행일(`issued_date`)은 초안 제외** — 금액은 즉시 DB 저장(진실원천)이라 옛 초안이 확정 금액을 덮던 버그(2026-07-14) 회피, **발행일은 '발행하는 날'이라 항상 서버 기본값(오늘)** — 며칠 전 초안의 옛 '오늘'이 되살아나 과거 일자로 발행되던 위험 원천 제거(3차 전수 점검에서 발견, 사용자 결정으로 발행일 초안 제외). 콤보 값 복원은 payerCombo `__pkSet`(초기화 IIFE가 노출). '생성=발행 확정' 원칙은 유지(초안=폼 편의일 뿐 청구서 레코드 아님). ui-interactions.test.js 회귀(버튼 저장·자동저장 폐지·복원·삭제·금액/발행일 제외).
- **청구 폼 작업 금액 즉시 저장**(2026-07-01): 청구 생성 폼의 작업별 금액칸(`task_amount_<id>`)을 수정하고 포커스가 떠나면(change) **그 작업 `total_price`에 바로 저장**(`POST /projects/tasks/:id/amount`→`setTaskAmount`, 청구된 작업은 거부) — 초안이 아니라 즉시 기록되어 **프로젝트 목록 결제 예정 금액·청구 폼 기본값에 바로 반영**(청구서 생성 전에도). app.js가 `task_amount_<id>` change를 위임 처리(세션 금액칸은 제외 — 세션은 단가표 산정).
- **곡·콘텐츠 청구 완료요건**: 청구 생성 폼에서 **완료(Completed) 작업만 기본 체크**, 미완료 항목은 흐리게 표시·체크 해제 + 안내 메시지(녹음 세션 완료 강제와 동일 규칙 통일). **각 작업에 금액 입력칸**(`task_amount_<id>`, 기본값=종류 기본단가; 체크·금액 변경 시 공급가·VAT·총액 실시간 갱신 `data-line-row`/`data-line-input`, app.js `lineAmount`; **금액칸 Tab/Shift+Tab=다음/이전 금액칸으로 이동**[app.js keydown — 행 DOM 순서상 기본 Tab이 다음 행 체크박스로 가던 것, 처음/마지막은 기본 동작·IME 무시]; **포커스 시 전체선택**[Tab·포커스용 클릭이면 전체선택→타이핑=새 금액, 단 이미 포커스된 칸의 특정 숫자 클릭·드래그 범위 선택은 존중, app.js money IIFE mousedown/focusin/mouseup]). 청구 생성 시 입력 금액으로 작업 `total_price` 확정(`createInvoiceFromTasks` `taskAmounts`), **0원 항목은 확인 후 청구 가능**(2026-07-05 사용자 결정 — 아래 rate_items 섹션 참조). 미청구 후보는 `total_price>0` 조건 없이 노출(금액은 청구 시 정함). **'청구 안 함'(무료 처리) 버튼**(2026-07-06 사용자 요청 — 리허설 등 **의도적으로 무료**인 항목: 0원으로 청구하면 거래명세서에 0원 라인이 남고, 체크 안 하면 '청구 필요'에 계속 남는 딜레마 해소): 청구 생성 폼의 각 작업·세션 행에 **[청구 안 함]** 버튼(`data-waive-btn`, `formaction`으로 같은 폼에서 `/projects/tasks/:id/waive`·`/sessions/:id/waive` 별도 라우트 제출 — 중첩 폼 없이 처리) — 누르면 `waived=1`(순수 토글, `total_price`는 안 건드림)로 표시만 하고 **청구 생성 폼에는 계속 남아** '무료 처리됨' 배지 + **[되돌리기]** 버튼으로 언제든 취소 가능(청구 생성 폼 밖에는 노출 안 함 — 사용자 결정). `waived=1` 항목은 **청구 후보 체크박스·금액칸 자체가 사라지고**(선택 불가) **프로젝트 예산(`task_total`/`session_amount_total`)·'청구 필요'(`unbilled_cnt`) 집계에서 제외**(더 이상 청구가 '필요'하지 않으므로) — 실제로 청구서 발행은 절대 되지 않는다(체크 자체가 없음). 이미 청구된 항목은 `waived` 불가(`TASK_LOCKED`/`SESSION_INVOICED`).
- 청구된 작업(`is_invoiced=1`)은 수정·삭제 거부(invoice_items 스냅샷 보존). 트랙 삭제는 작업 CASCADE.

### 청구
- **청구 생성 경로 = 프로젝트 청구 탭 from-tasks 하나**. 기존 수동 발행분 데이터·PDF 폴백(라인 없는 인보이스 역산)은 잔존(과거 기록 표시용).
- 인보이스 생성/입금(부분→발행 유지·전액→입금완료). **청구 마감일 없음**: 목록=**발행일 최신순**(issued_date DESC·동일 발행일은 생성순), `due_date`는 **레거시 컬럼**(마이그레이션이 기존 값을 비웠고 신규는 항상 NULL). 🚫**연체 개념 없음** — 파생(`isOverdue`)·(연체) 배지·대시보드 배너·cron 스캔·웹훅 알림을 **2026-07-27 전부 제거**했다(미수 에이징·독촉이 2026-07-23에 범위 밖으로 확정돼 되살릴 후보가 아니고, 남겨두면 '연체 기능이 있나'를 매번 재확인하게 만든다). 미수는 **합계로만** 본다(청구 목록 미수금 카드·대시보드 지표). **프로젝트 청구 탭 펼침 순서**: 청구처 정보 → 발행일 → 청구 항목 → 금액 블록 → PDF → 삭제(`invoiceExpandBody`). **금액 블록 = 영수증식 우측 정렬 스택**: (할인) → VAT → **총액**(다음 줄·`text-base font-semibold` 강조). **입금 처리 = [입금완료] 토글 하나**(tax-status select — 선택 시 서버 자동 완납 입금 1건, 되돌리면 자동 입금만 제거·`addPayment`/`deletePayment`/`listPayments` 인프라 잔존). **미수 '합계'는 유지**(청구 목록 상단 미수금 합계 카드·프로젝트 청구 탭 섹션 미수 힌트·대시보드 지표·클라이언트 상세 합계). payments 테이블·paid_amount 파생 무변경. **단일 진행 흐름**: ①**청구 생성**(엔지니어 금액 확정=곧 **청구서 발행**, `status='발행'` 고정) → ②**계산서/현금영수증 발행**(대표·치프) → ③**입금완료**(대표·치프). from-tasks 항상 `발행`, status select 없음(`status` 컬럼은 항상 '발행'으로 잔존 → 미수·연체·채번·발행알림 로직 무변경). **배지**: `invoiceBadge=taxBadge`만 — 계산서/현금영수증 미발행·발행·입금완료(+연체·부분납)만 표시.
  채번 `OMG-YYYYMM-###`(원자화 BEGIN/COMMIT/ROLLBACK·중복 방지). **문서 유형별 번호**: 견적서=`OMG-EST-…`·내역서=`OMG-L-…`·거래명세서=`OMG-…`(`docNumberWithType`). **청구 생성 전 미리보기 PDF도 다음 번호 표기**(`invoiceDraftForPdf`가 peek). **PDF 다운로드 파일명=문서번호** — Chrome는 inline PDF에서 URL 마지막 경로를 파일명으로 쓰므로 미리보기는 **GET** `/invoices/preview/:type/:name`(폼 `formmethod=get`·선택은 쿼리) — Chrome '다운로드'가 같은 URL을 GET 재요청하므로 POST면 404, GET이라야 뷰·다운로드 모두 성공. 발행 청구서는 `GET /invoices/:id/statement/:name`. 경로 마지막이 문서번호(=다운로드 파일명), Content-Disposition도 동일, VAT=공급가 10%, 돈=정수(원).
- 청구 탭 **청구 생성 폼**: **청구처 = 항상 직접 지정** — 미선택=`PAYER_REQUIRED` 차단·클라이언트 제출 가드[토스트]. 프로젝트 당사자[제작/운영·소속·아티스트]는 **추천 칩** `payerSuggestChips`로 1클릭 입력, 콤보 옵션 밖 당사자는 hidden change로 id 유지. + **완료** 작업 + **청구 가능 녹음 세션**(녹음+단가+시간, 취소 제외)을 함께 체크박스로 노출 → 선택해 청구서로. **생성 즉시 발행**; 발행/입금완료 인보이스의 **청구처 변경은 409 잠금**(미발행만 변경, `resolveInvoiceRefs`는 폼 선택 우선·미제공 시에만 프로젝트 폴백). **할인**(선택): 청구서 단위(정액 원·정률 %), **공급가에서 차감 후 VAT 재계산**(과세표준=공급가−할인, VAT=과세표준×10%, `invoiceAmountsFromSupply`); 공급가는 **체크한 항목 합으로 동적**(app.js `[data-discount-form]`·`data-line-amount`), 거래명세서는 소계→할인→과세표준→VAT→합계. `invoices.discount_amount`. **정액 할인칸은 미리 채운 값 없이 placeholder만**(빈칸=0, 타이핑 시 바로 입력 — 서버 `parseMoney`·미리보기 모두 빈값=0). **공급가·VAT·총금액 카드를 발행일 위에 강조**(app.js 줄별 갱신). **폼 필드 엔터=무동작**: 폼 맨 앞 **비활성 sentinel submit**(`<button type="submit" disabled hidden>`) — 기본 버튼이 disabled면 암묵 제출 무동작(함정 #22). **부가세 포함 토글**(기본 체크, 해제 시 VAT 0=현금 거래, `vatIncluded`→`invoiceAmountsFromSupply`/`createInvoiceFromTasks`). **담당자(연락처)도 청구처 선택 가능**(`clientCombo`에 담당자 옵션·`payer_contact_id`→`ensureClientFromContact`로 `source_contact_id` contact별 '기타' 클라이언트 매핑[동명이인 병합 방지]·콤보 라벨에 소속·전화·#id 식별자). **청구처 정합**: 담당자로 선택한 연락처가 개인 아티스트(source_contact_id로 연결된 '아티스트' 클라이언트)면 '기타' 셸 대신 그 아티스트 클라이언트를 청구처로 재사용(`ensureClientFromContact` 선분기). **발행 이메일(청구서 건별, 2026-07-27)**: 청구처 선택 시 그 party의 세금계산서 발행 이메일을 프리필 표시(`payerCombo` 옵션에 `email` 탑재) — **제출 시 보이는 값이 그대로 이번 청구서의 발행 이메일**(WYSIWYG, 빈칸=이메일 없음으로 기록). party의 실제 이메일은 불변, **스냅샷에만** 기록되고(`payer_snapshot.email` + `email_overridden` 플래그) 프리필과 다르면 override로 표시. `email_overridden`이 참이면 `payerSnapshotChanged`의 이메일 비교를 제외(party 이메일과 달라도 거짓 '정보 업데이트' 경고 없음)하고 **[새로고침](`refreshedPayerSnapshot`)도 이 값을 보존**(다른 필드만 party 현재값으로 갱신). 마커 `payer_email_set`(hidden, app.js가 프리필·직접입력 시 1로 세팅)이 없으면(JS-off 제출) 기존 동작(청구처 실제 이메일 스냅샷) — 빈 필드가 이메일을 조용히 지우는 사고 방지. 옵션 밖 당사자(추천 칩 관계자 등, 이메일 미상)는 마커 0으로 시작해 직접 입력해야 override 적용. 형식 불량은 `PAYER_EMAIL_INVALID`로 생성 차단. 청구 생성 폼 임시저장(초안) 키에 `pe`(이메일)·`pes`(마커)도 포함(복원이 프리필보다 우선).
  미완료 작업은 흐리게·체크 해제(세션 완료 강제와 동일 규칙). 청구 목록(`/invoices`) 화면 구성(9열 표·반응형 3단계·모바일 카드·long-press 선택·플로팅 일괄 바·열 폭)은 **[SCREENS.md §청구 목록](SCREENS.md)**. 여기 남기는 계약·불변식:
  - **권한**: 청구권자(`isInvoicer`=대표·치프)만 체크박스·처리 열·일괄 처리 대상, 스태프는 상태 배지만(읽기).
  - **일괄 처리는 단건과 같은 함수를 쓴다** — `POST /invoices/bulk-tax-status`(`requireInvoice`)가 id마다 `applyTaxStatusTx`(단건 `/tax-status`와 공용)를 **한 트랜잭션**으로 적용(입금완료=완납 자동 입금·되돌리기=자동 완납 입금만 제거·채번 보장). 갈라 두면 일괄 경로만 상태가 어긋난다.
  - ⚠️ **표 폭은 `table-layout: fixed`** — auto면 셀 min-content가 컨테이너를 넘겨도 안 줄어 오른쪽 처리 열이 클립된다. 고정 열은 rem 폭(CSS 클래스), 이름 열은 폭 미지정으로 남은 폭 균등 분할 + `…`. 치수를 **서버 인라인 style로 주면 CSP에 막혀 조용히 무시된다**(함정 #27).
  - ⚠️ **정렬 셀에 `data-sort-value` 항상 명시** — 금액 `₩1,200,000`은 문자열 정렬 불가(정수 필요), 발행일 표시 '7월 14일'도 ISO 별도, 상태는 순위(`statusRank`). 빈 값은 방향 무관 뒤로. 서버 기본 정렬(발행 최신순)은 로드 시 유지되고 정렬은 클릭할 때만(리로드 시 초기화). 프로젝트 목록과 **정렬 코어 공용**(app.js `wireSortHeaders`+`sortCellValue`, 구조 차이만 `io`로 메움).
  - ⚠️ 실시간 필터로 숨기는 행은 **인라인 `style.display`**로 숨긴다(`hidden` 속성은 Tailwind display 유틸에 밀린다 — 함정 #26). 정렬은 숨은 행의 순서만 바꾸고 `display`는 유지. 필터·정렬 모두 **캡 안 로드분** 대상. **remote 보강은 미적용**(행에 벌크 체크박스·롱프레스가 배선돼 innerHTML 교체가 안전하지 않다 — Enter 서버 검색으로 대체).
  - **상태 필터칩**(`?filter=all|todo|done|paid`)은 탭이 아니라 칩이고, 미수금 합계는 **필터 무관 전체 기준**. 행 복귀 경로(`ret`)에 limit(더 보기 상태)까지 보존. 상세 백링크는 `?return=`(→ 돌아가기 규약).
  - **프로젝트 청구 탭·클라이언트 상세는 `invoiceRow`(compact 펼침형)** — 넓은 표는 `/invoices` 전용.
  **녹음 세션은 곡·콘텐츠/버튼 없이 직접 청구**: **녹음 세션은 완료 안 됐어도(예정) 청구 후보에 노출**(작업과 동일 규칙: 완료=기본 체크, 예정=흐리게·미체크·'예정' 태그, `listBillableSessionsForProject`/`computeInvoiceDraft` 조건 `status<>'취소'`). **예정 세션 체크 시 '완료로 바꿀까?' 확인**(data-confirm-pending) 후 **청구=완료 전환**(`createInvoiceFromTasks`가 `markSession`으로 status→'완료'), 선택 시 `invoice_items.session_id` 스냅샷으로 청구(곡·콘텐츠 안 거침). **세션 금액도 작업처럼 청구 폼에서 수정 가능**(`session_amount_<id>` 입력칸, 기본값=단가표 산정액 `s.billing.amount`; `data-line-input`로 공급가·VAT 실시간 갱신, 청구 시 입력값으로 `invoice_items` 스냅샷 확정[`createInvoiceFromTasks` `sessionAmounts`], **0원 항목은 확인 후 청구 가능**). 청구되면 세션 수정·삭제 잠금(`SESSION_INVOICED`), 인보이스 삭제 시 자동 미청구 복원. 🔒 **단가 항목 미선택 세션도 후보에 흐리게 노출**(2026-07-23 기능성 평가): `listBillableSessionsForProject`가 `rate_item_id IS NOT NULL`을 **안 건다** — 산정 불가면 `billing: null`로 함께 반환하고, `unbilledInvoiceForm`이 **체크박스·금액칸 없이** 사유('단가 항목을 선택해야 청구할 수 있습니다')와 **[청구 안 함] 탈출구**만 있는 흐린 행으로 그린다. ⚠️예전엔 걸러내서 **청구 후보와 미청구 집계 양쪽에서 동시에 사라졌고**, 프로젝트가 조용히 '완료' 탭으로 넘어가 매출이 소리 없이 샜다(세션 탭 행에 사유는 떴지만 프로젝트·대시보드 레벨에선 안 보였다). **발행은 여전히 막힌다** — 체크박스 미렌더 + `computeInvoiceDraft`가 `rate_item_id IS NOT NULL`을 유지(서버 재검증 안전망, 직접 제출 시 400). 관련: `listBillableSessionsForProject`(**날짜순 정렬** `session_date ASC, start_time ASC, id ASC`)·`unbilledInvoiceForm`·`createInvoiceFromTasks`(task+session 혼합)·`isSessionInvoiced`. 회귀 `test/billing-radar.test.js` 9.
- **청구서 수정 없음 — 발행=확정, 변경은 삭제 후 재발행**: 항목(invoice_items)은 스냅샷. 내용을 바꾸려면 **삭제(`/delete`) 후 다시 발행**(삭제 버튼 옆 안내문 '수정이 필요하면 삭제 후 다시 발행하세요').
- **확정 진행된 청구서 삭제 = 치프·대표만**(2026-07-23 기능성 평가 — 이전엔 `deleteInvoice`가 `canBill`만 보고 `tax_status`·`paid_amount`를 전혀 검사하지 않아 **홈택스에 계산서가 나갔거나 입금까지 끝난 건을 스태프도 지울 수 있었다**. `payments`가 FK CASCADE라 입금 이력까지 함께 소멸해 매출·미수가 소급 변하고 세금계산서 대사가 깨진다). **`invoiceIsSettled(inv)`**(src/data/invoices.js — `paid_amount>0 || tax_status!=='계산서 미발행'`)가 참이면 `canInvoice` 필요, 아니면 `INVOICE_SETTLED_FORBIDDEN` → 라우트 403. ⚠️**'정정=삭제 후 재발행' 경로는 그대로다** — 막는 게 아니라 **권한을 올린 것**(발행 직후 미발행·미입금 건은 종전대로 스태프가 정정). 삭제 확인 문구도 확정 진행 건이면 `총액 · 입금 · 세금상태 … 입금 이력도 함께 삭제되며 되돌릴 수 없습니다`로 바뀌고(`invoiceExpandBody`), **감사 로그에 금액·입금액·세금상태를 남긴다**(`invoice.delete` — 삭제하면 청구서와 입금 이력이 같이 사라져 사후 추적 수단이 이 줄뿐인데 이전엔 번호·제목만 남겼다). 회귀 `test/invoice-delete-guard.test.js` 7.
- **청구 탭 인보이스 인라인 펼침**: 청구 탭의 각 인보이스 행을 클릭하면 **그 자리에서 펼쳐**(`<details>`, `invoiceRow` compact=펼침형·`invoiceExpandBody`[views.invoices.js]) 금액 내역(총액·할인·VAT·입금·미수·납입상태·발행일·마감일)·**청구 항목**·**삭제**·**PDF**(거래명세서/내역서/견적서, 미발행은 견적서만)·'전체 화면으로 ↗'(`/invoices/:id`) 링크를 표시. **청구 처리 컨트롤(청구서 발행·계산서·현금영수증 발행·입금)은 프로젝트 청구 탭에 없음**: `invoiceExpandBody`에 status select·tax_status select·`paymentHistory` 없음, 탭에는 '청구서 발행·계산서·입금 처리는 청구 메뉴에서 합니다' 안내 + `/invoices?tab=todo` 링크. **청구서 발행·계산서/현금영수증/입금 처리는 오직 청구 메뉴**(`/invoices` 목록 카드 토글 + `/invoices/:id` 전체화면 '상태·입금 처리'). 프로젝트 탭은 **청구 생성·보기·삭제**까지만. 삭제(`/delete`) 폼은 `return` hidden(프로젝트 청구 탭, open-redirect 차단=내부 절대경로만·`returnTo` 헬퍼)으로 복귀하고 `?open=ID`로 **처리한 인보이스가 펼쳐진 채** 유지(app.js `?open=` 스크롤). 라우트는 각 인보이스에 `listInvoiceItemsForInvoice` 항목을 첨부. **청구 생성(from-tasks '선택 항목으로 청구 생성')도 프로젝트 청구 탭에 복귀**(`?open=새 id`로 새 청구를 펼친 채 노출). **청구 메뉴(`/invoices`)는 만들어진 청구를 모아 보는 곳**으로 역할 분리.
- **견적서 발행 허용**: 미발행 초안도 3종(견적서·내역서·거래명세서) PDF 발행 허용(참고용 문서라 상태 무관). 발행알림은 미발행→발행/입금완료 **첫 전이에만** 발송. 모든 발행이 `notify.js` 공용 `notifyInvoiceIssued(inv)`로 통지.
- 프로젝트 삭제 시 청구된 작업·세션이 있으면 거부(`PROJECT_HAS_INVOICED`).
- 🔒 **지급 축 삭제 잠금 `PAYOUT_LOCKED`**(2026-07-23 기능성 평가 — **이 라운드의 핵심 불변식**): 이 ERP는 **청구 축(`is_invoiced`)에만** 잠금 그물을 쳐뒀고 **지급 축(`worker_paid`)에는 아무것도 없었다** — 이미 외주에게 이체한 작업·세션·프로젝트·작업자를 그대로 지울 수 있었고(임시 DB로 재현), 정산 CSV·지급월 이력이 **라이브 행 파생**이라 원천세 신고 근거가 소급 소멸했다. → **`worker_paid=1` 기록이 걸린 삭제는 전 경로에서 거부**하고 **지급 취소가 유일한 해제 경로**(사용자 결정 — 확인 후 강행 override 없음. 돈이 이미 나간 축이라 세션 겹침식 override와 성격이 다르다). 적용: `deleteTask`(작업 자체) · `deleteTrack`(하위 작업 CASCADE 우회) · `deleteSession`(`session_engineers` CASCADE 우회) · `setSessionEngineers`(**배정에서 빼는 것도 행 DELETE라 삭제와 동일** — 지급완료 엔지니어 제거 시 throw, 전수 확인 후 삭제해 반쪽 상태 방지) · `deleteProject`(하위 작업·세션 전부) · `/workers/:id/delete`(지급 이력 있으면 409 — 작업자 행이 사라지면 `payoutExportRows`가 작업자를 순회하는 구조라 작업 지급 이력도 CSV에서 함께 빠진다). ⚠️**세션 편집 자체는 막지 않는다** — 지급완료 엔지니어를 **유지한 채** 시간·룸을 바꾸는 건 정상 통과(잠금 단위는 '그 배정 행'이지 세션이 아니다). 라우트 메시지=`payoutLockedPage`(projects.routes.js)·세션은 `sessionInputError` 공용 분기, 전부 409. 회귀 `test/payout-lock.test.js` 7(과잉 차단 방지 케이스 2건 포함 — 미지급 삭제·지급 유지 편집).
- 대시보드: 미수금(**클릭 시 청구 '발행 완료' 탭**(`/invoices?tab=done` — 계산서 발행됨·미입금 잔금)으로 이동)·이번 달 발행(치프/대표만) + **오늘·이번 주 세션 카드**(`upcomingSessions` — 각 행 우측에 **디데이 pill**(공용 `ddayPill(date)` in views.js — 프로젝트 목록과 동일 표기: 오늘=오늘·미래=D-N·과거=N일 지남, 3일 이내=빨강·2주 이내=주황·그 외=흐린 회색 옅은 보더 pill). `nextSessionLine`(프로젝트 목록)도 같은 헬퍼 사용해 일원화) + **'청구 필요' 카드**: 미청구 항목이 남은 프로젝트 **전부**(2026-07-23 — `unbilled_cnt>0`만 보고 **완료 여부는 안 본다**) 상위 5개, **완료분을 위로**(끝난 프로젝트가 더 급함·안정 정렬). 행 클릭=그 프로젝트 청구 탭 직행, '청구 필요 N' 배지, 진행 중이면 제목 옆 '진행 중' 표기. ⚠️예전엔 목록 '청구 필요' 탭과 **같은 정의**(`is_completed && unbilled_cnt>0`)를 썼는데, 그러면 다가오는 세션이 1건만 있어도 이미 끝난 세션의 미청구가 **어디에도 안 보여** 장기 앨범일수록 청구가 늦어졌다. **탭 3분류(상호배타)는 그대로 두고 대시보드만 넓혔다** — 여긴 분류가 아니라 '할 일' 표면이라서(사용자 결정). 헤더 링크는 **탭이 실제로 보여주는 완료분 건수**로 표기(`완료된 프로젝트 N건 ↗`, 0이면 링크 자체를 숨김 — 전체 건수를 걸면 눌렀을 때 수가 안 맞는 거짓말이 된다). 노출=canBill(치프·대표·스태프), 0건이면 카드 미노출. **빠른 작업**(2026-07-22): 대시보드 pageHeader에 **`+ 새 프로젝트`** primary 버튼(`canEdit`만). order-to-cash의 진짜 시작점은 프로젝트뿐이라 이것만 둔다 — 세션·청구는 프로젝트 종속(독립 생성 라우트 없음)이라 "+ 세션/+ 청구"는 넣지 않음(프로젝트 선택 모달=새 기능·경량 정체성 위반). **내 할 일**(2026-07-22): 로그인한 사람의 **개인 렌즈** 카드('오늘·이번 주 세션'[전체] 위) — 담당 다가오는 세션(`session_engineers.manager_id`=나 OR 레거시 `engineer_name`) + 미완료 작업(`engineer_id`=나·완료/waived 제외). `myTodo(user)`(src/data/dashboard.js) = `getManagerByUserId`로 담당자 행 조회, **없으면 null → 섹션 통째 숨김**(작업에 안 붙는 대표 등). 세션·작업 모두 0이면도 숨김. 회귀 `test/my-todo.test.js`(완료·과거·waived·담당자없음·engineer_name 폴백).
- **전역 검색 최근 방문**(2026-07-22): ⌘K로 사이드바 검색창 포커스 시(타이핑 전) **최근 본 상세를 "최근" 그룹으로** 드롭다운에 띄운다(기존 typeahead 재사용·↑↓·Enter 동일). 기록=상세 페이지가 `layout({recent:{cat,label,href}})`로 렌더한 hidden `[data-recent-item]` 마커를 app.js 레코더 IIFE가 `localStorage["omg:recent"]`에 쌓음(중복 제거=최신 승격·최대 8건). 상세 4곳(프로젝트·청구·연락처`sel`·업체그룹`sel`)만 recent 전달. 빈 상태 렌더는 `[data-global-search]` 입력에만(다른 suggest 박스는 기존대로 빈 입력=미표시). 회귀=`test/recent-search.test.js`(jsdom — 레코더 dedup·cap·빈포커스 렌더·비전역 제외).
- **네비게이션 단축키**(2026-07-22): **`g` 다음 니모닉 문자**로 메뉴 이동(Linear·GitHub식 순차 — `h`대시보드·`p`프로젝트·`s`일정·`d`자료전달·`i`청구·`c`연락처·`o`업체그룹·`r`매출·`w`외주·`e`환경설정). NAV 항목에 `navKey`, 사이드바 링크에 `data-nav-key`+`title`("프로젝트 · g p" hover 발견성). **목적지는 서버가 렌더한 `[data-nav-key]` 링크에서만 찾음** → 권한 없는 메뉴는 링크 자체가 없어 점프 안 함(403 방지·권한 반영 자동). ⚠️**입력 중엔 절대 미발동**(사용자 요청): app.js 핸들러가 `editable`(input·textarea·select·contenteditable) 포커스·IME 조합(#18)·수식키(⌘/Ctrl/Alt) 조합을 전부 무시, `g` 후 1.5초 안 둘째 키만. 회귀=`test/nav-shortcut.test.js`(jsdom — g→링크 클릭·입력/IME/수식키 차단·단일키·미존재키). ⌘K 검색과 별개(겹침 없음).
- **매출 현황**(`/revenue`, 사이드바 청구 그룹, 대표·치프 `requireInvoice`) — 화면 구성(탭 3종·마스터-디테일·차트·4칸 항목 행·패널 높이·목록 폭·스크롤 복원)은 **[SCREENS.md §매출 현황](SCREENS.md)**. 여기 남기는 회계 불변식:
  - **정의**: 매출 = **공급가**(VAT 제외 `amount−tax_amount`) · **발생 기준**(발행일 `issued_date`, `status<>'미발행'`). 순이익 = 매출 − 외주지급(`track_tasks.worker_rate` + `session_engineers.worker_rate`, 같은 청구서 발행일 기간). ⚠️'매출'이라는 단어는 **이 화면 전용** — 청구 화면의 '발행액'(VAT 포함)·입금·미수는 거래액이고 의도적으로 분리돼 있다.
  - **다인 세션 귀속 = 모델 A(리드 귀속, 사용자 확정)**: 세션 매출과 **전체** 외주지급을 리드(`engineer_name`=첫 엔지니어)가 흡수하고 공동 엔지니어 행은 만들지 않는다 → 총 순이익은 정확(세션 외주를 한 번만 합산).
  - 🔒 **할인은 라인에 금액 비례 배분**(2026-07-23): `ATTR_SUPPLY = ROUND(ii.amount × (i.amount−i.tax_amount) ÷ 라인총합)`(`LINE_TOTAL_JOIN`). `i.amount−i.tax_amount = 라인총합−할인`이라 배분 후 **스탭 합 = 청구처 축 = 개요 = 종류별 축**(네 축 일치). 옛 `SUM(ii.amount)`(라인 원금)은 할인 청구서에서 스탭 합이 부풀었다(실측 +33%). ⚠️**네 집계 함수가 `ATTR_SUPPLY`를 함께 써야 한다** — 한 곳만 라인 원금으로 남으면 그 화면만 다시 부푼다. 항등식 `스탭 합 + 미귀속 = 라인 귀속 공급가 총합`을 `revenue.test.js`가 잠금. **외주 지급(worker_rate)은 배분 안 함**(고객 할인해도 외주 단가는 그대로).
  - 🔒 **미귀속 행**(2026-07-20): 스탭 축은 작업=`engineer_id`·세션=`engineer_name`으로 사람에 붙는데 **그 연결이 없으면 그 매출이 스탭별 화면에서 조용히 사라진다**(실측 2건·65만 — 총 매출·청구처 축엔 있으니 '금액이 틀린 게 아니라 누가 한 일인지만 비는' 상태). 목록 **맨 아래 고정**(순위에 섞으면 1위가 사람이 아니게 되고 개요 Top5가 오염된다), 0원이면 행 자체를 렌더 안 함. 세션 조건에 `engineer_name NOT IN (SELECT name FROM project_managers)`를 둬 **담당자 개명으로 이름 매칭이 끊긴 세션**도 잡힌다. 수동 청구서 라인(task·session 참조 없음)은 제외(업체·개인별 탭이 제자리).
  - 🔒 **청구처 상세의 행 단위는 청구서 그대로**(사용자 결정): 라인 단위로 바꾸면 **할인이 청구서 단위라 라인 합 ≠ 매출**이 된다(실측 도너츠컬처 18건 중 17건 할인, 라인 30만 청구서의 실제 매출은 20만 → 행 금액이 33% 부풀고 월 소계·총계와 어긋난다). 금액은 언제나 청구서 공급가(할인 반영)이고 종류·세부만 그 청구서 **첫 라인**에서 온다(`attachWorkSummary`, 라인 여럿이면 `N개 항목`).
  - ⚠️ **곡 제목 중복 판정은 '표시 이름'(아티스트) 기준** — 프로젝트명과 비교하면 화면에 없는 값과 같다는 이유로 필요한 곡 제목이 사라진다(이름을 아티스트로 바꾸며 실제로 생긴 결함, 회귀로 잠금).
  - 목록·상세는 **기간 없이 전체 누적**(개요만 기간 렌즈 — 언제 vs 누구를 한 화면에 섞으니 '이번 달 1위'와 '역대 1위'가 헷갈렸다). 구 드릴다운 `/revenue/staff|payer/:id`는 302 호환 유지.
  - 데이터=`src/data/revenue.js`(`revenueSummary/ByStaff/ForStaff/ByPayer/ForPayer/Unattributed/ByType/Tax/Years`·`issuedInPeriodSql` — 기간 없으면 `1=1` 전체 모드), 뷰=`src/views.revenue.js`. 무JS·의존성 0. 회귀 `revenue.test.js`·`revenue-views.test.js`(no-inline-style 포함).
  - **회계 내보내기(CSV, 2026-07-22)**: 공용 빌더 `src/lib/csv.js`(`toCsv`/`cell` — RFC4180 이스케이프 + **UTF-8 BOM**[Excel 한글]·의존성 0). 매출 CSV `GET /revenue/export.csv?year=&month=`(발행 청구서 한 줄씩: 발행일·청구번호·청구처·아티스트·공급가·VAT·할인·합계·세금상태·미수금, 순수 함수 `revenueCsv`), 정산 CSV `GET /workers/export.csv`(**지급 완료** 외주 전체 + `withholding33` 3.3% 분해, `payoutExportRows`+순수 `payoutCsv`). 둘 다 `requireInvoice`·`Cache-Control:no-store`(민감). 회귀 `revenue-export.test.js`.
- **거래명세서 PDF**: 발행/입금완료 인보이스 → A4 PDF(`GET /invoices/:id/statement.pdf`, **PDFKit 직접 그리기(벡터 텍스트)** — 2026-07-28 전환, `src/invoice-pdf.js`). 옛 파이프라인(SVG→resvg PNG→pdf-lib 이미지 삽입)은 ①글자가 150dpi 비트맵이라 확대 시 번지고 ②번들 폰트에 가나·한자 글리프가 없어 일본어가 통째로 사라졌다 — 벡터 텍스트 전환으로 번짐이 해소됐고, 폰트를 pan-CJK 전체 빌드(Noto Sans CJK KR, 아래 참조)로 교체해 CJK 커버리지도 넓혔다. 좌표계는 옛 1240×1754 SVG 좌표를 그대로 쓰고 페이지에 `scale(A4폭/1240)`만 걸어 축소(레이아웃 수치 불변). PDFKit이 실제 쓰인 글자만 서브셋 임베드하므로 **PDF는 보통 10~20KB 안팎**이고 텍스트 복사·검색이 된다(옛 이미지 PDF는 안 됐다). **다중 페이지 지원**: `drawPages`가 행을 페이지별로 채우고(1p=풀 헤더+표, 이후=축약 헤더 '번호 · 이어서'+표) 합계·납부금액은 마지막 페이지(공간 없으면 전용 페이지), 페이지 번호 'N / M' 푸터(다중일 때만).
  ⚠️**폰트에 없는 글리프는 오류 없이 조용히 사라진다**(2026-07-28 일본어 사고 — 처음 CJK 커버용으로 넣은 "Subset" 빌드에도 일부 일본어 상용한자[`録` 등]가 빠져 있어 같은 증상이 재발했다. 표본 문자 몇 개만 보는 자동 커버리지 검사로는 못 잡고 **육안 검증**에서 발견함). 지금은 서브셋이 아닌 pan-CJK 전체 빌드(`public/fonts/NotoSansKR-{Regular,Bold}.ttf`, 내용물은 Noto Sans CJK KR — 재생성 절차는 `docs/fonts-README.md`)로 해소했지만, **폰트를 교체할 때마다 실제 쓰일 법한 문자열로 PDF를 직접 만들어 확인할 것**(자동 테스트는 표본 코드포인트만 봄).
  레이아웃: 좌측 **제목**(거래명세서/내역서/견적서 — `?type=`로 선택, `DOC_TYPES`) + 공급자 헤더·**로고**(우측), 청구처 박스, **품목|금액** 표(수량·단가 생략 — 곡/세션 단위 고정), 소계/VAT/합계, **납부하실금액** 강조(견적서는 '견적 금액'·전용 푸터).
  공급자=스튜디오 세금정보·로고(환경설정), 공급받는자=클라이언트. `requireInvoice`·`no-store`·즉석 스트리밍(PII 최소화). CJK 폰트 `public/fonts`(pan-CJK 정적 인스턴스 TTF, `docs/fonts-README.md`) 번들.
- **청구처 정보 카드(`payerInfoCard`, 공용 — 청구 상세·프로젝트 청구 탭)**: 계산서/입금 배지를 **청구 제목 뒤 같은 줄**(flex·min-w-0 truncate 제목 + shrink-0 배지 — 함정 #17은 제목 '앞' 배치만 금지). 담당자 이메일도 표시(다를 때만), 그 아래 "담당자"(이름·전화·이메일) 행 유지(`listPersonsForOrg`/`affiliations` 재사용). **행 배치 = 홈택스 세금계산서 '공급받는자' 입력 순서**: `사업자등록번호`(등록증 보기 링크는 번호 앞) → `상호`(개인은 `성명`, 볼드, **클릭 복사** — 개인은 표시=`본명 (활동명)` 병기·복사=순수 본명[copyable display 옵션]) → `성명(대표자)`(**클릭 복사**) → `사업장 주소` → `세금계산서 발행 이메일` → 담당자(이름·전화·이메일) → 분류(부가 메타, 맨 아래). 개인 청구처는 등록번호 자리에 `현금영수증` 번호(발행 식별번호 동격). **개인인데 현금영수증 정보가 비면** 그 자리에 `⚠️ 미등록 — 발행 전 입력 필요` 경고(text-warning) — 발행 가드 `PAYER_CASH_RECEIPT_REQUIRED`(청구 생성 시 차단)의 사전 안내, 실시간 party 카드[프로젝트 청구 탭 `getParty`]에서 특히 유용(발행 후 정보를 지운 경우, 2026-07-18). **버그수정(2026-07-18)**: `getInvoiceForUser`(청구 상세용)가 `c.kind AS payer_kind`를 안 실어 상세 토글·`taxDocOf`가 개인 청구처도 '계산서'로 잘못 뜨던 것(목록 `listInvoices`는 payer_kind 있어 정상) → 상세 쿼리에 payer_kind 추가, party.test.js 회귀. **스냅샷 변경 감지 + 새로고침**: 청구 상세 카드 **우하단**에 발행 스냅샷과 현재 party 정보가 다를 때**만** `⚠️ 청구처 정보가 업데이트되었습니다` 경고(text-warning) + **[새로고침]** 버튼 — `payerSnapshotChanged(inv)`(src/data/invoices.js)가 의미 필드만 비교(상호·활동명·대표자·사업자번호·주소·이메일·현금영수증·담당자 이름/전화/이메일 — JSON 문자열 비교 금지: 옛 스냅샷은 이후 추가된 키가 없어 오탐), 스냅샷 없는 레거시·청구처 삭제는 false. `POST /invoices/:id/refresh-payer`(requireBilling)가 **payer_snapshot만 재저장**(금액·항목·청구번호·입금 상태 불변), return hidden으로 상세 복귀. payerInfoCard `footer` 옵션(청구 상세만 전달).

### 업체·그룹(사람/조직 축 정리 — 2026-07-17)
> **화면 표기 '클라이언트'는 폐기, '업체·그룹'으로 통일**(URL `/clients`·코드 식별자[`listClients`·`clientForm`·`client_id`·`client_files`]·DB 컬럼은 배포 안정성상 불변, 청구 맥락만 예외적으로 **'청구처'**[개인도 청구처가 되므로 '업체'로 바꾸면 틀림]). 설계: `docs/superpowers/specs/2026-07-17-people-orgs-split-design.md`.
> **배경**: 이전엔 이 메뉴가 업체/아티스트/그룹/**관계자** 4탭이었는데, 프로덕션 실측(2026-07-17)에서 관계자 151명·아티스트 73명이 **전원 이미 연락처에도 있었고**(연락처에 없는 사람 0명), 그중 22명은 관계자·아티스트 두 탭에 동시 노출됐다. 근본 원인은 **분류 축 혼재** — 연락처는 *무엇인가*(사람)로 자르는데 이 메뉴는 *무엇인가*(업체·그룹)와 *어떤 역할인가*(관계자·아티스트)를 한 탭바에 섞어 자르고, 역할은 겸업 가능(아티스트이면서 디렉터)이라 배타적일 수 없어 중복이 구조적으로 발생했다. → **이 메뉴는 조직 명부(업체·그룹)로만 남고, 사람(아티스트·관계자 포함)은 전부 연락처(`/contacts`) 한 곳으로 일원화**했다(데이터 이동 없음 — `parties` 한 테이블이라 순수 화면 정리).
- **탭 = 업체 / 그룹 2개뿐**(상호배타, `?group=company|group`, 기본=업체 — 관계자·아티스트 탭은 제거). 옛 링크는 연락처로 302: `/clients?group=associate`→`/contacts?tab=associate`, `?group=artist`→`/contacts?tab=artist`(검색어 `q` 보존). **사람 party id로 `/clients/:id`를 열면 `/contacts/:id`로 302**(아티스트 포함 — 이전엔 비아티스트만 리다이렉트라 아티스트는 클라이언트 상세[clientForm 인라인 편집]와 연락처 상세[읽기 뷰] 양쪽에서 열리는 '사람 상세가 두 벌'인 문제가 있었다. `from`·`return` 보존). 조직 id로 `/contacts/:id`를 열면 여전히 404(연락처는 사람 전용).
- **'+ 새 업체·그룹' 드롭다운 = 업체/그룹 2택뿐**(옛 관계자·아티스트 옵션 제거). `GET /clients/new?type=artist`는 `/contacts/new`로 리다이렉트(사람 생성은 연락처). **`clientForm`은 업체·그룹 전용**(아티스트 분기 완전 제거 — 활동명·현금영수증·소속 그룹·소속사는 연락처 폼으로 흡수됐고, **아티스트 폼의 '소속사' select는 연락처 폼의 '소속' 콤보 한 칸으로 통합**[같은 데이터=`affiliations` 현재 소속, 저장 경로도 연락처의 `syncCompanyAffiliation`로 통일 — 예전엔 `setPartyAgency`와 갈려 있었다]. **활동 형태(`activity_form`) select는 UI에서 폐기**[유일한 표시처였던 클라이언트 아티스트 탭 열이 사라짐 — 컬럼·config 상수·백필은 레거시로 보존]). **`POST /clients`의 `type=artist` 분기는 유지**(프로젝트 폼 '＋새 아티스트' 모달이 fetch로 거는 계약 — 지우면 프로젝트 작성 화면이 막힌다). **그룹 소속사(select)는 유지**(그룹은 사람이 아니라 클라이언트 상세에 남는다).
- **재발 방지 가드**: `test/guardrails-ui.test.js`가 서버 렌더 화면 문자열(`views.js`·`views.clients.js`·`views.contacts.js`·`views.invoices.js`·`views.settings.js`·주요 라우트)에 **'클라이언트' 리터럴이 없는지** 기계 검사(블록 주석·코드 식별자는 예외). 화면 용어 회귀를 리뷰가 아니라 테스트가 잡는다.
- **치프·스태프·대표 모두 열람·편집**(`requireEditor`, NAV `access:"editor"`) — **첨부 서류도 전 역할 개방**(여전히 인증 다운로드·공개 링크 없음).
  **목록 검색 = 실시간 필터(2026-07-15 사용자 요청)**: 검색 입력에 타이핑하면 **검색 버튼(서버 `?q=`)을 누르기 전에 이미 로드된 목록 행이 즉시 필터**(매칭 행만 남김, 매칭 0이면 '검색 결과가 없습니다'). `searchBox({liveFilter:true})`가 입력에 `data-live-filter`·`listGroup({filterList:true})`가 컨테이너에 `data-filter-list`(직접 자식 행)·빈 결과 `data-filter-empty` 렌더 → app.js가 `input` 시 행 `textContent`(이름·전화·이메일 등)로 매칭 필터. **숨김은 인라인 `style.display="none"`으로**(행이 `class="flex"`라 `[hidden]` 속성의 `display:none`[UA 스타일]이 Tailwind `.flex` 유틸에 밀린다 — 함정 #26). 검색 버튼/엔터는 여전히 서버 `?q=`(캡 넘는 전체 검색). 클라이언트 목록은 타이핑 제안 드롭다운(`/clients/suggest`) 대신 이 실시간 필터 사용. **상세**(`GET /clients/:id`)는 아래 '업체·그룹 상세 = 2단 읽기 뷰' 참조(탭 없이 섹션 나열: 프로젝트[이름 매칭 또는 청구처]·청구·결제[청구처 인보이스 전체 + 합계·입금·미수]).
  세금계산서 정보(`biz_no`·`owner_name`·`address`; **아티스트(개인)는 세금정보 대신 현금영수증 정보**[`cash_receipt_no` — 휴대폰 번호/현금영수증 카드번호] 입력. app.js `[data-client-kind]`가 분류=아티스트면 세금블록 숨기고 현금영수증블록 표시, 서버는 분류에 따라 반대 필드 null 강제). **청구처**(=실결제자, `client_id`)=클라이언트가 특정 인보이스에서 갖는 결제 역할(청구 생성 폼에서 결정).
- **업체·그룹 상세 = 2단 읽기 뷰 + [편집]**(연락처와 통일, 2026-07-18) — 패널 구성·칩 콤보 UX·드롭다운 배치는 **[SCREENS.md §업체·그룹 상세](SCREENS.md)**. 여기 남기는 계약·불변식:
  - `GET /clients/:id`=읽기 뷰(`clientReadView`, 빈 섹션은 헤딩까지 숨김) / `GET /clients/:id/edit`=편집(`clientEditPane`) / 저장·첨부는 edit에 머물고 성공 시 읽기 뷰로 복귀. 옛 3탭 구성은 폐기.
  - 🔒 **담당자는 재직과 분리된 역할**(`affiliations.is_contact`, 2026-07-10 사용자 결정): 담당자 칸에서 빠지면 **담당자 지정만 해제**하고 재직·연락처는 그대로다(담당자 해제 ≠ 퇴사). 프리필·청구처 담당자는 `listOrgContacts`(재직 + is_contact=1)로 좁힌다. 새로 지정한 사람이 이 회사 소속이 없으면 재직도 함께 등록(다른 소속은 안 끊음), 대표는 `ensureOwnerAffiliation`이 소속을 유지.
  - **제출 계약**: 칩마다 hidden `idField`(당사자 id·신규는 빈값) + `nameField`(**순수 본명**) 쌍(인덱스 페어링) → 저장은 `resolvePersonByName`(라벨 안전망) 후 `setOrgContacts(orgId, ids)` **통째 교체**. id로 연결되므로 '엄유미 실장님' 같은 표시 라벨이 새 사람으로 저장될 여지가 없다.
  - ⚠️ **칩 추가·삭제는 클릭이라 dirty 감시가 못 본다** — `form.dispatchEvent(new Event("change",{bubbles:true}))`로 수동 통지(함정 #23).
  - **후보 정렬은 공용 `comboRank`/`comboRankSort`**(우선순위 필드 안에서 정확 일치 > 앞부분 > 중간 포함, 동점은 원래 순서 유지). 전 콤보 통일이고 **가드레일 ⑩**이 정렬 없는 후보 목록의 재등장을 차단. 이미 적은 사람은 후보에서 제외하고, 같은 이름을 다시 치면 '＋새 등록' 대신 드롭다운을 닫는다(중복 생성 유도 방지).
  - 첨부(사업자등록증)는 **업체만** 노출. 목록 필터·스크롤 복원(`?from=`)과 청구·프로젝트 복귀(`?return=`) 병행.
- **첨부 서류**(사업자등록증만 — 통장사본 폐기): 상세 폼에서 **파일 찾기 버튼(`data-dropzone-pick`)·붙여넣기(Ctrl+V)·드래그앤드롭 업로드 + 열람**(드롭존 필드 `tabindex=0`+`paste` 리스너로 클립보드 이미지/파일 붙여넣기; 파일 선택·드롭·붙여넣기 모두 자동 제출). **드롭존 클릭 = 포커스만**(붙여넣기 준비만, 대화상자는 [파일 찾기] 버튼 전용 — 클라이언트·외주 공통)(`client_files`, 종류별 1개 교체식). 매직바이트 검증(PNG/JPEG/PDF, Content-Type 무시)·10MB·편집자 인증 다운로드(`inline`·`no-store`·**공개 링크 없음** — 민감 금융정보). `POST /clients/:id/files/:kind`·`GET .../raw`·`.../delete`, app.js `[data-dropzone]`. `bankbook` kind는 레거시(과거 업로드분 raw 열람만·업로드 UI 없음). **첨부는 업체(company)만 노출**(아티스트·그룹 제외, `clientFilesBlock`). **깨진 링크 감지**: 상세 렌더 시 `storage.exists`(Drive=files.get·휴지통/404면 없음, 로컬=존재)로 접근 가능 여부 확인 → 있을 때만 '보기', 없으면 '⚠️ 파일을 찾을 수 없습니다' + '기록 삭제'(dangling 기록 정리). 불확실(권한·네트워크)은 있음으로 간주. 상세 라우트 async.
- **조직 2분류(2026-07-17 이전 요구 — 업체/아티스트/그룹 + 관계자 4탭에서 축소)**: 목록 탭 = **업체 / 그룹** 상호배타(업체=kind='company', 그룹=kind='group'). 기본 진입=업체. 아티스트(사람)·관계자는 이제 이 메뉴에 없다(위 헤더 참조 — 전부 연락처). **'+ 새 업체·그룹'=드롭다운**(페이지 이동 없이 유형 선택, CSP-safe `<details>` 팝오버 — 업체/그룹 2택뿐; app.js `[data-menu]` 바깥 클릭 닫기). `GET /clients/new`(유형 없음)는 목록으로 리다이렉트. **목록 행 이름 뒤 메타**: 업체=대표, 그룹=**소속사**(text-xs muted). 빈 탭의 '+ 새 …' CTA는 그 탭 유형으로 바로 생성(그룹→새 그룹·업체→새 업체). **새 업체·그룹 폼의 back·취소는 목록(`/clients`)으로**(생성 폼 pageHeader back·`dirtyActionRow` cancelHref 모두 `/clients`). `clientForm(formType)`: **kind select 없음·유형 고정**(편집 시 유형 배지+'변경 불가', 유형은 party.kind라 불변). 업체=역할 체크·사업자·대표·주소·담당자 콤보·첨부, 그룹=그룹명·**소속사 select**·**담당자 personCombo**(멤버는 상세. 편집 시 기존값 보존). **소속사 select**(그룹, 업체 목록+'없음')는 `affiliations` 재사용(`setPartyAgency`: 회사 선택 시 이직 등록·'없음'이면 현재 소속 종료) → 소속사 상세 '소속 아티스트'에 자동 반영. **그룹 선택 시 그 그룹의 소속사로 연락처 폼의 '소속' 콤보를 자동 채움**(옵션에 `agency` 필드로 전달, `fillAgency` — 연락처 폼의 '소속 그룹' 필드에서 동작). **그룹↔멤버 소속사 연동**: 멤버를 그룹에 추가하면 그룹 소속사를 **기본 상속**(`setPartyGroup` — 새로 소속될 때만, 같은 그룹 재저장 시 재상속 안 함), 그룹 소속사 변경 시 그 소속사를 **'따르던' 멤버(현재 소속사=그룹의 이전 소속사)에게만 전파**하고 **개별 지정(오버라이드)한 멤버는 유지**(`setPartyAgency` 그룹 분기·멤버 분기는 전파 없음). `listGroupsForPicker`에 `agency_id`·`agency_name` 포함. `POST /`는 `type`(hidden)으로 `createCompany/createPerson[artist — 프로젝트 폼 모달 계약 유지]/createGroup` 디스패치. **업체 역할 표기 '제작사'→'제작사/운영사'**(`companyRoleLabel` — 저장값은 `제작사` 유지, 프로젝트 파생·마이그레이션 호환; 표시만 변경). `companyRolesFrom(b)`=체크된 roles CSV 또는 null.
- **그룹 ↔ 소속 멤버 연결**: `parties.group_id`(개인 아티스트의 소속 그룹, `kind='group'` 참조)로 밴드·아이돌 그룹에 멤버(개인 아티스트)를 연결. **연락처 상세**(아티스트명 옆, '소속 그룹' 필드)에서 지정(`setPartyGroup`), 소속 그룹 바로가기 링크. **그룹 상세(클라이언트 쪽)에 '멤버' 섹션**(상세 정보 **아래** 배치) — 소속 아티스트 목록 + 추가/제거(`POST /clients/:id/members`·`/:mid/remove`). **멤버 추가=personCombo**(검색+새 등록 콤보, `entityLabel="멤버"`→'새 멤버 등록', 옵션=`artistPersonOptions` 비멤버): 선택 id 우선·없으면 타이핑 이름 `resolvePersonByName`(재사용/생성)·추가 시 `is_artist=1` 승계+`setPartyGroup`(다른 그룹이면 이동). **personCombo `entityLabel` 파라미터**(모달 제목·'새 X 등록' 라벨 커스텀, `data-pc-entity`→app.js newRow). 헬퍼: `setPartyGroup`(group 파티만 유효)·`listGroupMembers`·`groupOfParty`·`listGroupsForPicker`·`artistPersonOptions`(parties.js). `deleteParty`가 그룹 삭제 시 멤버 `group_id` SET NULL. 인덱스 `idx_parties_group`. **그룹 담당자**: 그룹 폼(clientForm type=group)에 **담당자 personCombo**(`parties.contact_party_id`→사람 party) — **멤버 중 한 명이거나 관계자(비멤버)** 아무나 가능(옵션=`contactOptions()` 전 사람). 저장 시 `resolveContactPartyId`(hidden `contact_party_id` 우선·없으면 타이핑 이름 `resolvePersonByName` 재사용/생성·비면 null), `createGroup`/`updateParty`가 컬럼 갱신(person 폼은 미전송→기존 보존).

### 연락처 — 사람 전부(2026-07-17 사람/조직 축 정리로 사람 상세·편집이 이 메뉴로 일원화)
> 아티스트·관계자(대표·A&R·담당자·디렉터·작가 등)·외주 작업자·로그인 스태프까지 **사람은 전부 이 메뉴 하나**. 탭 = **전체(기본)/아티스트/관계자/외주/스태프** — 상호배타 아닌 **역할 필터**(겸업 가능해 겹침이 정상: 아티스트 73명 중 22명은 디렉터 등 관계자 역할도 겸해 아티스트·관계자 두 탭에 동시 노출). `listContacts({tab})`(src/data/parties.js) 단일 함수가 5분기(전체=`kind='person'` 전부·아티스트=`is_artist=1`·관계자=`listAssociates`와 동일 규칙[비스태프·비외주 + (is_artist=0 OR 관계자 역할 참조)]·외주=`user_id IS NULL`+외주 작업자 참조·스태프=`user_id IS NOT NULL`). `?tab=<모르는 값>`(옛 `external` 포함)은 '전체'로 폴백. 옛 클라이언트 쪽 '관계자' 탭·2단(`/clients?group=associate`)은 **여기로 흡수**(302 리다이렉트, 위 '업체·그룹' 섹션 참조) — 그쪽의 별도 2단 렌더는 **폐기**.
- **마스터-디테일 전환(2026-07-17 사용자 요청 — 애플 연락처식, 오늘 낮에 만든 '상세=2탭'은 이 전환으로 폐기)**: 연락처 목록의 근본 문제는 폭 계산이 아니라 **화면 성격 오인**이었다 — 6열 표(`contactTable`)는 '비교' 화면에 맞는 구조인데 연락처는 '찾기' 화면이라, 이름·소속·역할 열 폭 튜닝이 며칠 새 세 번 반복됐다(설계: `docs/superpowers/specs/2026-07-17-contacts-master-detail-design.md`). → **왼쪽 이름 목록(마스터) + 오른쪽 상세 패널(디테일)**로 바꿔 열 폭 경쟁 자체를 없앴다.
  - **키보드 이동(2026-07-17 사용자 요청 '연락처 선택 후 위아래 키로 옮겨 다니게')**: 선택된 사람이 있으면 로드 시 그 행에 자동 포커스(`aria-current` 행 — 사용자가 이미 다른 곳을 잡고 있으면 뺏지 않음) → **↑↓로 앞뒤 사람 이동**(애플 연락처식, 서버 렌더라 이동=그 사람 페이지 로드, 새 페이지에서 다시 포커스돼 연속 이동). **포커스가 목록 안일 때만 가로챈다** — 오른쪽 상세를 보는 중엔 ↑↓가 평소대로 스크롤. 처음/끝에서 순환 없음, IME 조합 중 무시(함정 #18), 실시간 필터로 숨은 행은 건너뜀(판정은 인라인 `style.display` — jsdom엔 레이아웃이 없어 `offsetParent`를 못 씀, 함정 #26 계열). app.js `[data-nav-list]` IIFE + ui-interactions 5(마커는 2026-07-19 매출 추가로 일반화 — 옛 이름 `data-contact-list`).
  - **왼쪽 목록은 lg에서 자체 스크롤 영역**(`lg:max-h-[calc(100vh-11rem)] lg:overflow-y-auto`): 전 명단(202명)이 페이지와 함께 흐르면 아래쪽 사람을 고를 때 페이지가 그 위치까지 내려가 오른쪽 상세 머리가 화면 밖으로 나간다(키보드 이동 도입 시 실측). 모바일은 한 단이라 그대로.
  - **이름 목록 폭 드래그 조절(2026-07-18 사용자 요청 — `/clients` 공유)**: 목록↔상세 사이 리사이저(`cl-resizer`, `data-cl-resizer`, lg 이상만)를 드래그하면 왼쪽 폭(CSS 변수 `--cl-list-w`, 기본 18rem)을 조절하고 **localStorage(`clListW`)에 저장·복원**(app.js pointer capture·MIN 180·MAX 560px 클램프, 키보드 ←→ ±16px a11y). **리사이저는 선 없음**(2026-07-18 사용자 요청 — hover 파란 선도 제거, `::before` 자체 삭제, 조절 가능함은 커서 col-resize로만 알림). **이름 목록도 카드 박스 제거**(`.cl-listwrap .card { border:0; box-shadow:none; border-radius:0 }` — 목록↔상세 하드 경계선 없이 깔끔) + **연락처 사이 구분선(divide-y)도 제거**(`.cl-listwrap .divide-y > * { border-top:0 !important }` — iCloud식으로 섹션 초성 헤더로만 구분). **목록 배경도 투명**(2026-07-18 사용자 요청 — `.cl-listwrap .card { background:transparent }`, 페이지 bg[아이보리] 위에 이름이 바로 놓임·별도 판 없음). **sticky 초성 헤더 배경도 `--color-bg`(surface→bg)로 맞춤**(투명 목록·페이지 bg와 일치, 스크롤 시 불투명 유지). 선택·hover 행은 배경색으로 구분(테두리 없이). **리사이저 더블클릭 = 기본 폭(18rem) 복귀**(CSS 변수·localStorage 제거). 폭은 `contactPanes` 컨테이너 변수 → `.cl-col-left { width: var(--cl-list-w) }`(lg 미디어쿼리 안 — 모바일은 전폭 불변, 함정 #27: 서버 인라인 style 대신 CSS 변수를 app.js가 세팅). contacts-views 계약 테스트(`data-cl-resizer`·`cl-col-left`).
  - `/contacts` = 왼쪽 5탭(**전체/아티스트/관계자/외주/스태프**, 각 라벨에 개수, **탭바는 2단 그리드 밖 전체 폭** — 18rem 열 안에 있으면 넓은 화면에서 라벨이 2줄로 접혀 f40a858로 이동)+실시간 필터 검색+**전 명단 이름만**(`personName`, 상한 없음 — 이름만 렌더라 200건대도 수십 KB) + 오른쪽 `emptyState("연락처를 선택하세요")`.
  - **초성 그룹 + 인덱스 레일(iCloud식, 2026-07-18 사용자 요청 — `/clients`도 같은 `contactNameList` 공유라 동일 적용)**: 이름순 정렬 목록에 **초성이 바뀌는 지점마다 sticky 섹션 헤더**(`cl-cho-head`, `data-cho-head`) 삽입 + 오른쪽 가장자리에 **초성 인덱스 레일**(`cl-rail`, `data-cho-rail`, lg 이상만). 초성=`src/lib/chosung.js` `chosungOf(personName)`(한글 음절→초성·쌍자음 병합 ㄲ→ㄱ·호환자모 단독 ㅌㅌㅌ→ㅌ·영문→대문자·기타→#). 레일 글자 **클릭·드래그 스크럽**하면 그 섹션 헤더로 스크롤(app.js — `[data-cho-head]` getBoundingClientRect로 `data-nav-list` scrollTop 이동, pointer capture로 손가락 따라 이동). 헤더는 `<div>`라 **키보드 이동(`<a>`만 순회)이 자연히 건너뜀**, **검색 중엔 실시간 필터가 헤더 텍스트도 안 맞아 함께 숨김**(iCloud와 동일). 헤더 divide-y 상단선은 `!important`로 제거(섹션 라벨은 선 없이). 레일은 초성 2개 이상일 때만·`justify-content:space-evenly`로 목록 높이에 펼침. chosung.test.js·데이터 계약(`data-cho-*`)은 가드레일 ⑧. **정렬 = 한글 우선(iCloud식, 2026-07-18 사용자 요청)**: `hangulFirstOrder(col)`(parties.js — `unicode(col)`이 한글 음절[44032~55203]·호환자모[12593~12686]면 rank 0, 아니면 1로 묶고 각 묶음 안은 이름순) → 한글(가나다) 먼저, 영문·숫자·기호 뒤. `listContacts`·`listClients` 적용(그룹 헤더·레일도 그 순서). 이전 `name COLLATE NOCASE`(영문 먼저)에서 전환. **업체 목록은 법인 표기를 뗀 상호로 정렬**(2026-07-20 사용자 요청 '주식회사나 (주) 같은 것들은 prefix 취급'): `coreCompanyName`(src/lib/company-name.js)이 `주식회사`·`(주)`·`㈜`·`유한회사`·`(유)`를 **앞뒤 어느 쪽이든** 떼고(실측 프로덕션 121곳 중 73곳이 법인 표기 — 앞 63·뒤 10, 그대로 두면 39곳이 'ㅈ' 한 덩어리), `byCompanyName` 비교자가 한글 우선 규칙 안에서 그 키로 정렬한다. **초성 그룹·레일도 같은 키**(`contactNameList({keyFn})` — 정렬과 초성이 다른 규칙이면 목록 순서와 헤더가 어긋나므로 양쪽이 반드시 같은 함수를 쓴다). **표시는 자리·글자 그대로 두되 법인 표기만 옅게**(`text-muted`, 2026-07-20 사용자 요청 '자리는 그대로 두고 옅은색으로 약화'): `splitCompanyName`이 `{pre, core, post}`로 갈라 pre/post만 muted로 칠한다 — 눈이 회색을 건너뛰고 실제 상호에 걸려 초성 헤더(정렬 기준)와의 인지 부조화가 줄어든다. **`pre+core+post`는 항상 원본과 정확히 같다**(회귀로 잠금) — 정식 상호가 목록에서도 보이고, 실시간 필터가 `textContent`를 매칭하므로 '주식회사'로도 계속 찾힌다. ⚠️투명도(`text-muted/60`)는 쓰지 않는다 — 다크에서 대비 3.8:1로 **WCAG AA(4.5:1) 미달**이었다(실측). `text-muted`는 다크 6.13:1·라이트 5.05:1로 통과하고 본문(18.7:1)과 3배 차이라 충분히 물러나 보인다. 남는 한계: 표기가 자리를 차지하므로 **상호 시작 위치는 행마다 어긋나고 잘림 폭도 그대로**(뒤로 빼면 정렬되지만 사용자가 자리 유지를 선택). ⚠️정렬은 **`capList`(100개 상한) 적용 전**에 끝나야 한다(상한이 JS 단이라 뒤에 정렬하면 잘린 뒤 순서가 된다). 표기를 떼면 빈 이름이 되는 경우(상호가 '주식회사'뿐)엔 원래 이름을 지킨다. 회귀=`test/company-name.test.js`(실데이터 6형태).
  - **상세 패널 읽기 폭 제한(2026-07-18 사용자 요청 '끝까지 안 써도 된다·시선 분산 방지')**: 오른쪽 상세 내용을 `max-w-content`(48rem/768px)로 감싸 좌측 정렬 — 정보 카드·프로젝트/세션 표가 화면 끝까지 늘어나지 않고 읽기 컬럼으로 모임(앱 전반 `max-w-content` 읽기 폭과 통일). `/clients` 상세도 공유(contactPanes). contacts-views 계약 테스트.
  - `/contacts/:id` = 같은 왼쪽 목록(선택 행 강조·`aria-current`) + 오른쪽 **읽기 뷰**(탭 없이 한 화면 스크롤): 헤더(이름+역할 배지+우상단 **[편집]**) → 연락 정보(전화·이메일·현금영수증, 각 `copyable`) → 소속(현재 회사·**소속 그룹**·직책·부서 + 소속 이력 타임라인, **읽기 전용**) → 메모 → 참여 내역(프로젝트 표+세션 표, 오늘 낮 2탭에서 만든 `dataTable` 렌더를 그대로 재사용) → **청구·결제**(이 사람이 청구처(payer)인 청구서 — 개인 현금영수증 결제를 여기서 확인, 업체 상세와 동일 `clientBillingSection`[미수 요약 + 발행일·청구번호·청구·상태·금액 표], 청구 0건이면 섹션 통째 숨김·`listInvoicesForParty(person.id)`, 2026-07-18) → 연동 정보(대표 클라이언트·아티스트로 보기·담당자 연동 배지). **읽기 뷰에서 연락처 밖으로 나가는 링크(회사·프로젝트·세션→프로젝트·아티스트로 보기·대표 클라이언트·담당자 연동)는 새 탭**(`target=_blank rel=noopener`, 라벨에 `↗`, 2026-07-17 사용자 요청 '연락처에서 프로젝트를 보다가 들어가면 연락처로 돌아오기 번거롭다' — 왼쪽 목록이 곧 작업 맥락인데 같은 탭에서 나가면 그 화면의 백링크가 연락처가 아니라 프로젝트/클라이언트 목록이라 보던 사람으로 못 돌아온다). 연락처 안에 머무는 링크([편집]·이름 목록·좁은 화면 뒤로가기)는 같은 탭. 규칙이 블록 안에서 갈리지 않게 **연동 정보의 링크도 전부** 새 탭(contacts-views 계약 테스트). **참여가 0인 섹션은 헤딩·빈 안내까지 통째로 숨긴다**(2026-07-17 사용자 요청 — 세션 → 프로젝트 순으로 확장. 관계자·연락처 대다수가 참여 0이라 '0 + 빈 안내'가 자리만 차지했다. 둘 다 0이면 참여 내역 영역 자체가 없다). **소속 그룹(개인 아티스트→그룹, `parties.group_id`)은 회사 바로 아래 줄**(값 있을 때만·부서와 같은 규칙, 그룹 상세 `/clients/:id`로 **새 탭**, 표시=활동명 우선·없으면 name): 편집 폼엔 `groupCombo`가 있고 그룹 상세(업체·그룹)엔 '멤버' 섹션이 있는데 **읽기 뷰에만 빠져 있어** 그룹에서 멤버는 보이는데 그 멤버의 연락처를 열면 그룹이 사라지던 비대칭(2026-07-20 사용자 리포트). 라우트 `readPaneFor`가 `group_id` 있을 때만 `groupOfParty` 조회(없으면 쿼리 자체 생략), contacts-views 회귀 3. **'연결 프로젝트'에는 세션 디렉터 참여도 포함**(`listProjectsForParty` — 2026-07-17 사용자 리포트 '세션에 참여했으면 프로젝트에도 연관된 것'. 이전엔 프로젝트 단 역할[아티스트·소속사·제작사·고객측 담당자]만 봐서 세션 디렉터로만 참여한 사람이 '세션 1 · 프로젝트 0'으로 보였다. party.test.js 회귀).
  - `/contacts/:id/edit` = 왼쪽 목록 유지 + 오른쪽만 편집 폼(현행 폼+소속 이력 인라인 편집·추가/이직·삭제, 옛 '상세 정보' 탭 내용 그대로). 저장·취소는 읽기 뷰로 복귀(`?return=` 있으면 `safePath` 검증 후 그 경로로).
  - **'상세=바로 편집'(2026-07-01 결정)을 연락처에 한해 '읽기 후 편집'으로 뒤집었다**(사용자 요청 — 목록에서 직접 복사하는 흐름은 거의 안 쓰고 다 열어서 확인한다는 확인 기반). **2026-07-18 업체·그룹(`/clients`)도 연락처에 이어 같은 방향으로 뒤집었다** — 아래 '업체·그룹' 섹션 참조(아티스트는 사람이라 이미 연락처 소속).
  - (구 클라이언트 쪽 '관계자' 탭의 별도 2단 렌더는 2026-07-17 사람/조직 축 정리로 **폐기** — `/clients?group=associate`는 이제 `/contacts?tab=associate`로 302, 편집도 이 연락처 2단 하나뿐이다.)
  - **좁은 화면(<1024)**: 목록 URL(선택 없음)이면 왼쪽만, 상세/`sel` URL(선택 있음)이면 오른쪽만 — 서버가 선택 여부로 반응형 클래스(`hidden lg:block`)를 정하므로 **JS 0**.
  - **목록 상한(100건) 제거**: 이름만 렌더라 부담이 없다(실측: 외부 연락처 202건·관계자 151건 — 옛 6열 표는 각 177KB·127KB였는데 이름만은 그 1/4 이하). 부담 임계는 명단 2,000건대로 늘 때(가상 스크롤·서버 페이징 검토, 지금은 안 함).
  - 5탭(전체/아티스트/관계자/외주/스태프)·실시간 필터(`liveFilter`)·`?q=` 서버 검색은 유지. `contactTable`(6열 표)·연락처 상세 2탭(`?tab=info|activity`)·`capList`(연락처·관계자)는 **제거**. 목록 행 링크의 `?return=`은 왼쪽에 목록이 항상 있어 불필요해져 제거(단 청구·프로젝트·업체·그룹에서 연락처로 들어오는 **유입 `?return=`**과 편집 화면 [편집]의 `?return=`은 유지 — 백링크 규약).
  - 렌더러는 **`src/views.contacts.js`**(신설, `views.js`가 이미 커서 독립 단위로 분리)에 모은다: `contactPanes({left,right,hasSelection})`(2단 골격)·`contactNameList({rows,selectedId,hrefFn})`(이름만 목록)·`contactReadView(party,{affs,projects,sessions,editHref,extras})`(읽기 뷰 6섹션 — `extras`는 esc 없이 그대로 삽입되는 신뢰 HTML, 호출부 esc 책임).
- **상세 정보 탭 = 인라인 편집**(2026-07-01): 연락처 이름 클릭 → 상세가 곧 편집 화면(읽기전용 카드+'정보 수정' 버튼 폐기, `contactForm` 인라인·dirty 저장, `embedded`로 폼 자체 pageHeader[연락처 수정/상세 back] 생략 — 상단 이름 헤더와 중복 제거). `/:id/edit`는 상세로 리다이렉트. **소속 이력도 각 행을 펼쳐(`<details>`) 회사·직함·시작/종료일·메모 인라인 수정**(`updateAffiliation`·`POST /:id/affiliations/:aid`·종료일 비우면 현재, dirty 저장·종료 처리·삭제 포함). 파생정보(성명·아티스트명 클라이언트·대표 클라이언트·담당자 연동 배지)+삭제는 폼 아래 카드.
- **'회사' 입력 = 공용 companyCombo**(2026-07-04 사용자 요청 '프로젝트 폼과 통일'): 연락처 폼 회사칸이 datalist → **검색 + '＋새 업체 등록' 모달 콤보**(프로젝트 소속사/제작사와 동일 UX). `companyCombo`는 projects.routes.js 로컬 함수에서 **views.js 공용 헬퍼로 이동**(views.js가 partyOptions 지연 require, `companyCombo(fieldName, value, roleKey, label)`) — 보이는 입력 name 없음·hidden `name=company` 제출(함정 #19), app.js `[data-company-combo]` 범용 초기화라 무변경. **소속사·소속 이력 회사도 companyCombo**(2026-07-05 사용자 요청·구식 필드 전수 정리 마무리): 클라이언트 폼 소속사(옛 `<select agency_id>`)와 연락처 소속 이력 추가/편집 회사(옛 `<select client_id>`)를 검색+새 업체 등록 콤보로 통일 — 제출은 이름(`agency_company`/`affiliation_company`), 저장 시 `ensureCompanyParty`(parties.js 공용 — 이름→업체 party 재사용/생성)로 해석해 `setPartyAgency`/`addAffiliation`/`updateAffiliation`. 그룹→소속사 자동연동(app.js)은 그룹 옵션 `data-agency`를 id→이름으로 바꿔 콤보 값 세팅. 남은 엔티티 select은 내부 목록(엔지니어·예약담당자·매니저·소속그룹)이라 유지. **업체 폼 대표자도 personCombo**(2026-07-04 구식 필드 전수 정리 — datalist → `personCombo({idField:"owner_id", nameField:"owner_name", initialName})`: 검색+새 연락처 등록 모달, 서버 resolveOwnerParty가 owner_id 우선; **`initialName` 파라미터 신설**[선택 id 없이 이름 텍스트만 있는 레거시 값 표시·보존]). **personCombo 검색이 소속 회사명도 매칭**(2026-07-04 사용자 요청 '회사가 먼저 생각날 때' — app.js 필터가 name·alt·company 3필드, 드롭다운 부제에 회사 표시). **'회사' 입력 → 소속 이력 자동 반영**(`syncCompanyAffiliation`): 연락처 저장 시 회사명으로 업체 클라이언트를 찾거나(없으면 소속사/레이블로 생성) 현재 소속이 그 업체가 아니면 이직(closeCurrent)으로 등록. 회사 비면 no-op(종료는 수동), 담당자 셸('기타'+source_contact_id)·아티스트에는 연결 안 함. 이전엔 `contacts.company` 텍스트만 저장돼 소속 이력에 안 잡히던 것(company↔affiliation 분리) 해소. **라벨 '회사'→'소속'(2026-07-17 사람/조직 축 정리)**: 이전엔 업체 폼(아티스트)의 '소속사' select(`setPartyAgency`)와 이 '회사' 콤보(`syncCompanyAffiliation`)가 같은 데이터(`affiliations` 현재 소속)를 서로 다른 저장 경로로 갱신하는 **두 칸**이었다 — 아티스트 폼의 artist 분기가 제거되며 이 한 칸(내부 필드명은 `company` 그대로, 표시만 '소속')으로 통합됐고, 저장은 이 콤보의 `syncCompanyAffiliation` 경로 하나로 일원화됐다.
- 클라이언트 회사와 **별개의 '사람' 마스터**(`contacts`): 레이블/제작사 직원·프리 매니저·아티스트 지인 등. 사이드바 `/contacts` 독립 메뉴(NAV `access:"editor"`, 라우트 `requireEditor` — 치프·스태프·**대표**(2026-07-07 개방) 편집). 목록·이름/전화 검색(`?q=`)·CRUD. 메뉴 아이콘=명함(클라이언트 '여러 사람'과 구분). **필드**: 성·이름·호칭·별명·회사·직책·부서·전화·이메일·**현금영수증 정보**(`cash_receipt_no`, 2026-07-04 사용자 요청 — **개인을 청구처로 지정하려면 필요**[청구 폼 payerDocMeta 가드와 자동 연동], 휴대폰/카드번호)·메모. **updateParty는 cash_receipt_no 미전송 시 기존값 보존**(contact_party_id와 동일 규칙 — 과거엔 무조건 덮어써 필드 없는 폼[Google 동기화 등] 저장 시 지워지던 데이터 손실 수정). **연락처 생성 폼의 '현재 소속' 블록(평면 select) 제거**(2026-07-04 구식 필드 정리 — 회사 콤보+직책이 syncCompanyAffiliation으로 첫 소속 등록, 기능 중복; 무소속=회사 비움). 표시명(`name`)은 미입력 시 호칭+성+이름(또는 별명)으로 자동 생성(`resolveContactName`).
- **Google 연락처 양방향 동기화**(People API, **fail-safe**): 앱에서 생성/수정/삭제 시 Google에 push(`people.js`·`google_resource_name`/`etag`, 자동). Google→앱 반영은 `POST /contacts/sync`(`syncFromGoogle` syncToken 증분·삭제 양방향·루프 방지=data 직접호출)가 담당하나 **연락처 페이지의 'Google 동기화' 버튼은 제거**(2026-07-04 사용자 요청 — 앱→Google push가 자동이라 사용자가 누를 일 없음; 라우트는 향후 자동화용으로 보존). scope `.../auth/contacts`(치프 재로그인 필요), 미연동 시 앱만 정상.
- **소속 이력(이직 히스토리)**: `contact_affiliations` 타임라인. `ended_on IS NULL`=현재 소속(가장 최근 1건), `client_id` NULL=무소속(프리·지인). **이직**=소속 추가 시 `closeCurrent`로 기존 현재 소속을 자동 종료(`ended_on`)하고 새 소속 INSERT. 상세에 현재(`badge-success`)/종료(`badge-neutral`) 타임라인 + 추가·이직/종료/삭제 + 연결 프로젝트.
- **생성 시 현재 소속 입력**: 새 연락처 폼에 소속 회사 select(무소속+`listClients`)+직함 → 생성과 동시에 첫 소속 등록(`addAffiliation`, `closeCurrent=false`) → 회사 상세 직원 목록에 즉시 반영.
- **프로젝트 연결**: 프로젝트 메타의 '고객측 담당자'(`contactCombo`, `projects.contact_id`) — 선택 시 전화·이메일·소속을 즉시 표시(`contactOptions`에 email 포함·datalist `data-*`·app.js `[data-contact-info]`), **목록에 없는 이름 입력 시 저장하면 새 연락처 자동 생성**(`resolveContactId`→`createContact`, 이름만; 전화·소속은 연락처 메뉴에서 보강). 내부 '담당 엔지니어'(`manager_id`)와 별개.
- **담당자(외주·하우스) 연동**: 전 담당자(`project_managers`)가 연락처에 **자동 노출**(`contact_id` 백필). 연락처에서 전화 수정→담당자 동기화(**양방향**), 이메일은 외주만 양방향·**하우스는 연락처에서도 잠금**(`users.email` 보호, 폼 readonly). 연락처 목록·상세에 외주/하우스 배지. **담당자→연락처 연동 시 이름을 성·이름 자동 분리**(`splitKoreanName`[`src/lib/korean-name.js`], 김준상→성 김·이름 준상). 담당자 **추가·수정 시 `ensureContactForManager` 런타임 호출**로 연락처+성이름 생성(settings/workers 4지점, 트랜잭션 원자화) + db.js 기동 백필도 family/given 채움(기존 연락처 1회 보강·수동 입력 보존). **대표(owner)는 작업 담당자가 아니므로 연동 제외**(`getManagerByContactId`가 owner 제외, `syncUserToManager`가 owner는 비활성). `ensureContactForManager`/`syncContactToManager`/`syncManagerToContact`.

### 장비 대장(/equipment) — 보유 자산 참조 목록(2026-07-23)
- **정체성**: 스튜디오가 **보유·운용하는 장비**(마이크·프리앰프·아웃보드·모니터·악기 등)의 참조용 대장. **판매 재고 아님**(위 정체성 경계 참조 — 입출고·재주문 없음). 연락처·단가표처럼 **삭제 중심 마스터데이터 CRUD**.
- **데이터**: `equipment` 테이블(`name` 필수·`category`·`serial_no`·`purchase_price` 정수·`purchased_on` 날짜·`location`·`memo`, 전부 name 외 선택 — 오래된 장비는 매입가·구매일 모를 수 있음). `src/data/equipment.js`(CRUD + `equipmentLocationSuggestions`[룸 이름 + 기존 장소값]·`equipmentCategorySuggestions`).
- **장소 = 단일 텍스트 필드**(사용자 결정 — FK 아님): 룸/자유를 한 칸에. 입력 시 **제안 칩**(등록 룸 + 이미 쓴 장소값, 클릭하면 채움 — `data-fill-value`/`data-fill-target`, app.js 위임 핸들러; datalist 안 씀[가드 ②]). ⚠️트레이드오프: 텍스트라 **룸 개명 시 저장된 "A룸"은 자동 미전파**(가벼운 대장·룸 개명 드묾으로 수용). 룸은 골라 넣어 표기가 일정 → "A룸 장비 모아보기" 텍스트 일치 집계 성립.
- **목록 `/equipment`**: **종류별 그룹**(`equipmentList` — 빈 종류=미분류 맨 뒤) + 실시간 검색(`liveFilter`, 장비명·종류·시리얼·장소). ⚠️**단일 `data-filter-list` 컨테이너**에 종류 헤더+행을 형제로(연락처 `contactNameList` 패턴) — 그룹마다 컨테이너를 나누면 app.js 실시간 필터가 첫 그룹만 훑는다(회귀로 잠금). 검색 중엔 헤더도 텍스트 불일치로 함께 숨어 평면 결과(iCloud식).
- **폼**: `equipmentForm`(추가/편집). 장비명 필드=`equipment_name`(가드 ① bare name= 회피), 매입가=`purchase_price`(app.js MONEY 정규식 등록·가드 ⑫). **삭제 폼은 편집 폼과 형제**(`dirtyActionRow` + `<button form="equip-del-N">` — 중첩 폼 금지: 중첩하면 브라우저가 내부 폼을 버려 삭제 버튼이 수정 폼을 제출한다[리뷰가 jsdom으로 실증]).
- **권한**: 전원(대표·치프·스태프) 열람·편집(`requireEditor`·NAV `access:"editor"` group `manage`·navKey `q`). 매입가도 내부 공유. **삭제 중심**(하드, 감사 `equipment.delete` — 장비는 청구·정산 비연결이라 참조 잠금 없음).
- 회귀: `test/equipment.test.js`(데이터 8)·`test/equipment-views.test.js`(뷰 계약 — 단일 컨테이너·삭제 sibling 포함)·`ui-interactions`(칩 핸들러)·스모크(`/equipment` 200+editor). 설계=`docs/superpowers/specs/2026-07-23-equipment-register-design.md`, 계획=`docs/superpowers/plans/2026-07-23-equipment-register.md`.

### 자료 전달
- 업로드(multer 디스크) → Drive/로컬 폴백 → 인증 다운로드(프록시) + 공개 만료 토큰 링크 `/d/:token`(다운로드 카운트·철회·만료).
- **권한**: 자료 전달은 **치프·스태프만** — 사이드바 메뉴·`/deliverables`·인증 다운로드·프로젝트 상세 '자료 전달' 탭 모두 **대표 제외**(NAV `access:"staff"`, 라우트 `requireStaff` — 2026-07-07 canEdit에 대표가 포함되면서 자료 전달만 `isStaffOrChief`/`requireStaff`로 분리 유지). 공개 토큰 링크 `/d/:token`만 인증 불필요(유지).

### 환경설정(/settings) — 좌측 세로 메뉴 + 화면당 한 주제(2026-07-28 재설계)
> **메뉴명 = '환경설정'**(사이드바 항목·페이지 제목·백링크). 사이드바 **그룹 헤딩 '관리'는 유지**. 라우트 `/settings`·내부 함수명은 표시명과 별개(무변경).
> 🏗 **골격 = `contactPanes` 재사용**(연락처·업체·매출·외주와 같은 2단 마스터-디테일): 왼쪽=설정 메뉴, 오른쪽=고른 설정 하나. 새 레이아웃을 만들지 않으므로 **모바일 대응·폭 드래그·목록 스크롤이 공짜**로 따라온다. ⚠️`hasSelection`은 **`?s=` 유무**로 판정한다(`cur`[무엇을 그릴지]와 분리) — `true` 고정으로 두면 `leftCls="hidden lg:flex"` 때문에 **1024px 미만에서 메뉴가 영영 안 보이고 다른 설정으로 갈 방법이 사라진다**(재설계 첫 구현에서 실제로 발생, 리뷰가 잡음). `widthKey`는 `setListW`(연락처 `clListW`와 분리).
> **주소 = `/settings?s=<key>`**(기본 `rooms`). 키 9개: `rooms`·`rates`·`tasks`·`booking`·`users`·`studio`·`google`·`alerts`·`system`. 옛 `?tab=` 4종은 **302 매핑**(`settings`→rooms·`content`→rates·`people`→users·`system`→system, 나머지 쿼리 보존). 저장·액션 리다이렉트는 **그 설정 화면으로** 돌아간다(섹션 앵커는 화면이 한 주제라 불필요해져 제거).
> **메뉴 = 손 가는 순서**(`SETTINGS_NAV`): 자주 쓰는 것(장소·요금표·작업 종류·예약 기본값) / 가끔 보는 것(스태프 계정·스튜디오 정보) / 한 번만 하는 것(구글 연동·알림) / 상태 보기(시스템 ⚠N). **각 항목에 한 줄 부제 필수** — 이름만으로 내용을 모르던 것이 재설계의 출발점이었다.
> **화면당 값-편집 저장 버튼은 1개 이하**(`test/settings-nav.test.js`가 기계로 잠금). ⚠️'저장'과 '액션'을 구분한다 — 추가·삭제·순서 이동·업로드·테스트 발송·백업 실행·동기화는 **누르는 순간 일어나는 일**이라 각자의 버튼이다(그것까지 통합 저장에 묶으면 '추가하려면 저장을 눌러야 하나'가 된다). 이를 위해 예약 기본값(`POST /settings/booking-defaults` — 기본 세션 시간·예약 담당자·기본 장소)과 알림(`POST /settings/alerts` — 웹훅+청구 메일)은 **폼을 합쳤다**(옛 단건 라우트는 존치). 알림은 이메일 검증이 저장보다 앞서 **형식 오류 시 웹훅도 저장되지 않는다**(원자성, 회귀로 잠금).
> 🔒 **통합 저장 폼은 dirty 패턴이 필수**(`data-dirty-form` + `data-dirty-save` + 힌트, 공용 `saveRow()`): 없으면 ①안 바꿔도 버튼이 활성으로 보이고 ②바꾼 채 다른 설정으로 옮길 때 **경고 없이 유실**된다. 재설계 직후 실측에서 예약 기본값·알림·스튜디오 정보 세 화면이 저장 버튼만 있고 마커가 없어 약속의 절반이 조용히 빠져 있었다(저장 버튼 **개수**만 세던 계약 테스트는 이걸 못 잡았다 — 지금은 마커까지 잠근다).
> ⚠️ **인라인 편집 표는 `scrollX()`로 감싼다**(2026-07-29): 행이 고정 rem 열이라 좁은 패널에서 줄지 않고 **잘린다** — 요금표 행(809px)이 패널(729px) 밖으로 나가 순서·삭제 버튼에 손이 닿지 않았고 1024px에선 세 표 모두 그랬다. **페이지 가로 오버플로우 검사로는 안 잡힌다**(바깥이 잘라 버려 `documentElement`는 멀쩡하다) — 그래서 눈에 안 띄었다. 폭 확보도 함께 했다: 설정 메뉴는 `narrowList`(14rem — 항목 9개·짧은 라벨이라 18rem은 남는다), 오른쪽은 **표가 있는 3화면만**(`TABLE_SCREENS`=장소·요금표·작업 종류) `rightWide`로 읽기 폭(`max-w-content`) 해제. ⚠️**전 화면을 열면 안 된다** — 폼·상태판(스튜디오 정보·예약 기본값·알림·스태프 계정·구글·시스템)까지 화면 끝까지 늘어나 시선이 흩어진다(사용자 지적으로 되돌림). 폭은 '목록 편집이냐 읽기냐'로 가른다. 실측 결과 **1400px부터 요금표 행이 스크롤 없이 들어온다**(그 아래는 스크롤되지만 모든 컨트롤에 도달 가능).
> **설명은 상시 노출**(`settingDesc`) — 옛 `explain()`은 `<details>`로 접혀 판단에 필요한 정보가 클릭 뒤에 숨었다. `explain`은 앱 전체 공용이라 **건드리지 않고** 설정 화면 전용 헬퍼를 따로 뒀다. 상시 노출이므로 문구는 **1~2문장**으로 유지할 것(길면 화면을 잡아먹는다).
> **접근 권한**: 스태프도 **관리 메뉴(환경설정·콘텐츠)**를 열람·사용(`requireStaff`=치프·스태프, 대표 차단·메뉴 숨김). **스태프가 못 보는 건 매출(`/revenue`)과 외주 작업자(`/workers`)** — 둘 다 `requireInvoice`(치프·대표. 정산=재무 성격 → 대표·치프; 스태프는 외주 지급단가를 작업 편집에서 입력만). **로그인 계정 관리(담당자 추가·역할 변경·삭제·정보수정 = `/settings/users*`)는 권한 상승 방지를 위해 치프 전용(`requireChief`)** — 스태프는 '스태프 계정' 화면을 열람만 하고 계정 관리 폼은 숨겨진다. **알림 웹훅(조직 보안 설정)만 치프 전용**(스태프는 상태 열람만) — 청구·PII 알림이 외부로 나가는 경로라 보호. **매출은 실제 청구분(is_invoiced=1·청구된 세션 invoice_items)만 집계**(미청구 초안 제외).

- **시스템 화면**: 전체 시스템 상태 — ①**⚠️ 확인 필요 카드**(`systemWarnings`: 백업 26시간+ 침묵[cron 실패 감지]·Drive 미연동·캘린더 꺼짐·**디스크 여유 500MB 미만**[`diskFreeBytes`=fs.statfsSync·측정 불가 플랫폼은 생략, 데이터 현황에도 여유 공간 표시] — 좌측 메뉴 '시스템' 항목에도 경고 개수 배지) ②연동 상태 배지(캘린더·Drive·연락처 푸시·웹훅) ③**DB 백업**(마지막 시각·크기·보관 개수 + **[지금 백업]** 치프 버튼 `POST /settings/backup-now` — cron과 동일 산출물+Drive 오프사이트 fail-safe) ④데이터 현황(DB·WAL 크기·테이블 카운트·로컬 잔존 첨부) ⑤앱 정보(버전·Node·가동시간) ⑥**감사 로그 = 카드 2개**(각 최근 50건). **감사 로그(`audit_log`·`lib/audit.js`)**: 파괴적·재무 액션 기록(청구 삭제/생성/계산서·입금 상태, 프로젝트·클라이언트·연락처·작업자 삭제, 지급/일괄 지급, 계정 역할 변경·삭제, 수동 백업) — logAudit은 fail-safe(본 흐름 비차단·**성공 여부를 boolean 반환**, 기존 호출부는 무시), target은 짧은 요약만(민감정보 금지).
  - **인증 기록(2026-07-20 사용자 요청 '감사로그에 로그인 기록도 — 브라우저 켜서 ERP 열 때')**: `auth.login`(구글 OAuth 성공)·`auth.deny`(거부 — **미등록/비활성 사유 구분**: `upsertUserFromGoogle`이 둘 다 null이라 콜백에서 `findUserByEmail` 재조회로 가른다)·`auth.access`(**접속 = 사람당 하루 첫 1건**, `attachUser`에서 `logAccessDaily`). ⚠️**'브라우저 켜서 여는 것'은 로그인이 아니다** — 쿠키가 30일이라 실제 로그인은 사람당 월 1회꼴이고 그건 `users.last_login`이 이미 보여준다. 사용자가 보고 싶어한 사용 흔적은 `auth.access`로만 남고, **정작 지금까지 아무 데도 안 남던 건 거부 시도**라 이 카드의 실질 가치는 거기 있다. **IP는 `audit_log.ip` 별도 컬럼에**(2026-07-20 사용자 요청 '아이피도 남기자' — 처음엔 안 남기기로 했다가 뒤집었다. `target`에 끼우면 200자 절단 대상이 되고 기존 요약과 섞이므로 컬럼을 나눴고, **인증 기록에만** 채워 파괴적·재무 액션의 '민감정보 금지' 성격은 유지. `clientIp(req)`가 `trust proxy` 뒤 실제 IP를 주고 IPv4-mapped IPv6[`::ffff:1.2.3.4`]은 IPv4로 펴서 저장 — 같은 접속이 표기만 달라 다르게 보이지 않게. 180일 보존 정책 그대로 적용), 함께 **기기 이름**(`lib/user-agent.js` `deviceLabel` → `크롬/맥`·`사파리/아이폰`)이 '내가 아닌 접속'을 가려내는 단서. ⚠️UA 매칭은 **좁은 것부터**(엣지 UA엔 Chrome·Safari가 다 있고 iPad는 UA에 Macintosh를 쓴다). **하루 경계 = KST 자정**(`kstDay()`) — UTC 자정은 한국 오전 9시라 8시 반 출근자만 두 줄이 찍혔다(리뷰 지적으로 교정). 중복 판정은 **메모리 캐시 + DB 확인 2단**(캐시만 쓰면 재시작마다 재기록·DB만 쓰면 요청마다 쿼리)이고 **기록 성공 시에만 캐시**(실패를 성공으로 착각하면 그날 접속이 영영 유실 — logAudit 반환값을 보는 이유). **화면은 카드 2개로 분리**(`listAudit(50, "work"|"auth"|"all")`, 기본 work=`auth.*` 제외): 한 목록에 섞으면 접속 기록이 삭제·청구 기록을 50건 창 밖으로 밀어낸다. 회귀=`test/audit-auth.test.js`(UA 매칭 함정·dedupe·KST 경계·실패 시 재시도·목록 분리·fail-safe). **계정 마지막 로그인**(`users.last_login`·`touchLastLogin`) — 담당자 행에 '로그인 오늘/N일 전'. **정렬 UI**: 단가표 분류(같은 kind 안 ↑↓, 잠긴 기본 분류도 순서는 이동 가능)·작업 종류 ↑↓(`moveRateCategory`/`moveTaskType` — 표시 순서 물질화 후 이웃 교환·sort_order 10 간격 재부여로 중복 값에도 결정적, 작업 종류는 캐시 무효화 포함). **2026-07-27부터 단가 항목·룸·작업 종류의 이 ↑↓는 인라인 통합저장 행 안에 배치**(아래 '콘텐츠' 참조 — 이동 함수 자체는 무변경, 감싸는 UI만 접이식에서 한 줄로 전환).
- **담당자**: 하우스 엔지니어(로그인, 작업 담당자 자동 연계; **각 행 '정보 수정'으로 이름·전화 편집** `POST /settings/users/:id/edit` — 이름은 users+작업 담당자 동기화, 전화는 작업 담당자 행). **외주 작업자는 `/workers` 메뉴로 일원화**('스태프 계정' 화면=로그인 계정 관리). **역할 변경**(`POST /users/:id/role`): 비본인은 역할 select(치프↔스태프↔대표) 노출 — **본인은 자기 역할 변경 불가**, **치프→비치프 강등 시 본인 제외 활성 치프 0이면 거부**(최소 1명, `flash=last_chief`). 삭제 잠금은 기본 치프·본인 유지(락아웃 방지).
- **`/workers` 권한 분리**: 목록·상세 열람 + **정산(지급 처리/취소)** = 대표·치프(`requireInvoice`), 작업자 **추가·삭제·정보수정**(마스터) = 치프(`requireChief`). 추가 폼·삭제·수정은 치프에게만 노출. 외주 **정보 수정**(이름·전화·이메일·정산 정보)=상세 `POST /workers/:id/edit`(이름 변경 시 `track_tasks.engineer_name` 동기화로 정산 매칭 유지). 외주 **지급단가**(`worker_rate`) 입력은 작업 편집(`requireEditor`=치프·스태프)이라 대표는 단가는 안 건드리고 **지급만** 실행. 흐름: 치프/스태프 외주단가 입력 → 완료 → 대표/치프 정산 탭 지급. **목록 = 마스터-디테일(2026-07-20 사용자 요청 '이름·전화만으로 폭을 다 쓰는 건 낭비 — 오른쪽을 활용하자')**: 왼쪽 이름 목록(`workerNameList` — 한 줄에 **이름·연락처·미지급 금액/건수 + 계좌 미등록 경고**) + 오른쪽 **정산 카드**(`workerPayoutCard` — 미지급 헤드라인·원천세 3.3%·실지급·[전부 지급 처리] / 계좌·서류 **등록 여부만**[번호는 상세 편집 폼에서만 복호화, 사용자 결정] / 누적 지급·최근 지급월·참여 건수) + 기존 **3탭 상세**. 골격은 연락처·업체·매출과 같은 `contactPanes`(`views.workers.js` 신설·`data/worker-summary.js` `workerPayoutSummary`가 작업+세션+첨부를 합산). ⚠️**폭을 넓히는 건 답이 아니었다** — 외주는 4명 남짓이라 넓힐수록 빈 공간이 늘어난다(채운 건 가로가 아니라 **행의 내용**). 카드의 [전부 지급 처리]=오늘 날짜 빠른 경로, 정산 탭 폼=지급일 소급(둘이 공존하는 이유). 옛 **목록 카드 밑 지급 요약 + 일괄 지급 처리**: 각 작업자 카드 바로 아래 미지급 건수·합계 한 줄(`listTasksForWorker` 재사용) + 오른쪽 끝 **[지급 처리]** 버튼(`POST /workers/:id/payout-all` — 그 작업자의 미지급 작업 전부를 한 번에 지급 처리, 대표·치프=`requireInvoice`). 미지급 없으면 요약 줄 자체가 안 보임. **미지급 항목 미리보기 한 줄**: 요약 줄 아래 `프로젝트명 · 작업종류/세션종류 날짜` 라벨을 콤마로 나열, 최대 3개+`외 N건`(작업+세션 통합 목록, `taskTypeLabel`/`formatYmdShort`, 라벨 맨 앞 `project_title` — 예 `루나 1집 - 타이틀곡 '월광' · 마스터링 7월 6일`). **정산 항목 식별**: `listTasksForWorker`가 트랙/프로젝트 조인에 `track_artist`(트랙 아티스트, 없으면 프로젝트 아티스트 폴백)를 조회, 목록 미리보기·상세 정산/참여 내역 작업 행 공용 `taskMeta`가 **아티스트 + 작업일(생성일)** 을 함께 표시(`보컬튠 · 곡아티스트 · 프로젝트/트랙 · 7월 1일`; 세션 항목은 `session_date`). **정산 정보(주민등록번호/사업자등록번호·계좌)**: 정보 수정 폼에 `id_number`(주민등록번호 또는 사업자등록번호 — 어느 쪽이든 한 칸에)·`bank_name`(은행)·`account_number`(계좌번호)·`account_holder`(입금자명/예금주). **`id_number`·`account_number`는 `db.encrypt`/`decrypt`로 암호화 저장**(민감정보/재무정보라 DB 파일 유출 시에도 평문 노출 방지, AES-256-GCM). 편집 폼은 치프가 볼 때 **복호화해 그대로 표시**(마스킹 없음 — 저장은 암호화, 표시는 인증된 치프에게만 평문). `bank_name`/`account_holder`는 평문 저장. **첨부 서류(주민등록증 사본·통장사본)**: `worker_files` 테이블(client_files와 동일 구조, kind별 1개 교체식) + 치프 전용 업로드·인증 다운로드·삭제(`POST/GET /workers/:id/files/:kind[...]`, 매직바이트 검증 PNG/JPEG/PDF, 공개 링크 없음). **담당자 목록 정렬 = 하우스 먼저·외주 나중**: `listProjectManagers`(전 앱 공용 — 세션·작업 엔지니어 select·예약담당자 select·`/workers`·`/settings` 스태프 계정 화면이 전부 이 함수 하나를 씀) `ORDER BY active DESC, (user_id IS NULL) ASC, name COLLATE NOCASE`(활성 우선 → 하우스[user_id 있음] 먼저 → 외주 나중 → 그룹 내 가나다순). 클라이언트 첨부는 사업자등록증만(통장사본 폐기)이지만 **외주는 스튜디오가 돈을 지급하는 방향이라 통장사본이 필요**(client_files와 다른 이유). `storage-migrate.js`의 로컬→Drive 이관 대상(`LOCAL_TABLES`)에도 포함. **상세 3탭**: 기본 정보(기본 탭 — 정보 수정 펼침 카드+필드 라벨+첨부 서류, 비치프는 읽기 카드) / 참여 내역 / 정산. **드롭존 붙여넣기 단축키 OS 구분**(맥=⌘V·그 외 Ctrl+V — 서버는 Ctrl+V 렌더, app.js가 맥에서 라벨·aria 치환). **뷰어 크기 규칙**(viewer.js — 원본 이상 업스케일 금지·상한 높이 85%×폭 60%·하한 360px·리사이즈 1회 재시도). **PDF 첨부는 `<iframe>`+`#view=FitH`로 감싼다**(`fileViewerPage({pdf:true})`, 2026-07-20 사용자 요청 '사이드탭 없이 PDF 자체로 하나만 — 작게 나와서 별로'): PDF를 **최상위로 열면**(옛 동작: `/view`가 raw로 302) A4 비율 팝업에서 본문이 45%로 쪼그라들었다 → 뷰어 페이지 안에 끼우고 **폭 맞춤**으로 시작. ⚠️**`<embed>`/`<object>`는 CSP `object-src 'none'`에 막혀 빈 화면**이 된다 — iframe만 `default-src 'self'`로 허용. clients·workers 양쪽 `/view` 라우트 동일(guardrails-ui 계약 2).
  - ⚠️**썸네일 사이드탭은 우리가 못 없앤다**(2026-07-20 사용자 확인으로 확정): iframe이면 크롬이 간이 뷰어로 그릴 거라 보고 바꿨는데 **사이드탭이 그대로였다**. 크롬 PDF 뷰어가 사이드탭 열림 상태를 **브라우저별로 기억**하기 때문이고, `#navpanes=0`·`#toolbar=0`은 크롬이 무시한다(Adobe 전용 파라미터). 사용자 결정으로 **현 상태 유지**(폭 맞춤만 취함). 다시 시도한다면 남은 선택지는 팝업 창을 넓히는 것뿐 — 뷰어 UI 자체는 브라우저 영역이다.
  - 📌 검증 한계 메모: **크롬 PDF 플러그인은 스크린샷 도구에 안 잡힌다**(까맣게 나옴). 이 변경도 로컬에서 눈으로 못 보고 배포 후 사용자가 확인해줬다 — PDF 뷰어 관련 작업은 처음부터 사용자 확인을 전제로 잡을 것. **정산 탭**: ①**이체 정보 카드**(정산 탭 상단 — 은행·계좌번호[복호화·클릭 복사]·예금주, 미입력 시 경고 배지) ②**지급월별 이력**(지급완료를 worker_paid_date 월로 그룹, 월 헤더에 지급액·원천세·실지급 — 원천세 신고[지급월 기준]에 그대로 사용, 최근 달만 펼침) ③**미지급 섹션 상단 분리**(최근 일자순) ④**상세 탭에도 [전부 지급 처리]**(지급일 date 입력 = 실제 이체일 **소급 기록**, `paid_on`→`setTaskPayout/setSessionEngineerPayout` 3번째 인자; `return=detail`이면 상세 복귀) ⑤**삭제 confirm에 미지급 경고** ⑥**단가 미입력 가시화**(배정됐는데 worker_rate 0이면 참여 내역·정산 목록에 배지; `listSessionsForWorker`에 my_assigned/my_rate) — **0원도 지급·정산 가능**: 개별 [지급 처리] 버튼 상시 제공·일괄 지급/미지급 집계의 rate>0 필터 제거(0원 포함), '단가 미입력' 배지는 누락 실수 감지용 정보 표시로 유지(의도한 무료면 그대로 지급 처리 안내) ⑦상세→연동 연락처 크로스링크(party_id) ⑧정보 수정 폼 dirty 저장 패턴. **원천징수 3.3% 표시**: 목록 카드 미지급 요약 아래 `원천세 3.3% −₩X → 실지급 ₩Y` 한 줄 + 정산 탭 합계 카드에 `소득세+지방소득세` 분해 줄(미지급 기준). 계산=`lib/tax.js withholding33`(소득세 3%·지방세는 소득세의 10%, **각 10원 미만 절사 — 홈택스 신고 방식**). **표시 참고용** — 소액부징수·사업자 외주(세금계산서 발행, 원천징수 없음) 예외 미반영(화면에 명시). 일괄 [지급 처리] confirm에도 실지급 병기. **세션 참여 표시**: 상세 탭 라벨 **'참여 내역'**(작업+세션 합계 개수), `listSessionsForWorker`(다대다 `session_engineers.manager_id` 매칭 + 레거시 `engineer_name` 폴백, DISTINCT로 중복 제거) — 탭 안에 '작업 N'/'세션 N' 두 섹션으로 나눠 표시(세션은 `worker_rate`/정산 개념이 없어 참고용, 배지만·금액 없음). '정산' 탭은 작업만(세션은 정산 대상 아님).
- **콘텐츠**: 단가표(녹음/촬영 종류)·**작업 종류 카탈로그**(곡·콘텐츠 후반작업 종류 + 기본단가·과금·**가격 유형**·빠른추가; **분류 개념 폐기** — 곡·콘텐츠 작업은 모두 후반작업이라 분류 선택 UI 없음, `task_group`은 레거시 컬럼으로만 보존). 단가 항목은 **↑↓ 순서 이동 + 활성/비활성 토글 + 삭제**(2026-07-26 — 이전엔 삭제-only에 이름 가나다순 강제였고, 바로 옆 `rate_categories`·`task_types`에는 이미 ↑↓가 있어 일관성이 깨져 있었다), 작업 종류는 ↑↓ + 삭제. 단가표 추가 폼(신규 항목)만 필드에 `<label>` 표기 — **단가표·작업 종류(그리고 룸, 아래 참조)는 접이식 '수정' 없이 인라인 한 줄 편집 + 섹션 통합 저장으로 전환됐다**(2026-07-27 — 각 행이 펼치지 않고도 이름·분류·금액 등 전 필드를 한 줄에 노출하고, 한 섹션에 보이는 행 전부를 `POST /settings/{rate-items,rooms,task-types}/bulk`로 한 번에 저장): 행 자체엔 `<label>` 없이 `aria-label`만(단가표·작업 종류는 섹션 상단에 데스크톱 전용 열 제목 행[`sm:grid`, 모바일 숨김]이 시각 라벨을 대신하고, 룸은 체크박스가 자기 라벨["예약 대상"·"외부"]을 이미 갖고 있어 열 제목 자체가 없음). 필드명은 `<이름>_<id>`(`rate_name_1`·`label_1`·`room_name_1` …), 데이터 계층 `bulkUpdate{RateItems,Rooms,TaskTypes}`가 DB의 전 id를 순회하며 **그 id의 필드가 요청 바디에 없으면 건너뛰고**(화면에 없던 행은 무변경) 있으면 기존 단건 `update*`를 재사용해 검증까지 그대로 태운다 — **검증은 2단 구조**: 이름 입력에 `required`가 걸려 있어 정상 브라우저에서는 한 행이라도 이름이 비면 제출 자체가 막히고(1차 저지), 서버의 행별 skip(이름·가격 누락)은 **JS-off·직접 제출처럼 `required`를 우회한 경우의 안전망**(2차)이라 그 행만 조용히 건너뛰고(silent-skip) 트랜잭션은 롤백하지 않아 나머지 정상 행은 저장된다. ⚠️**단가 항목의 분류(category)를 바꿔도 표시 순서(sort_order)는 재배치되지 않는다** — 새 분류 안에서 임의 위치에 놓일 수 있고 ↑↓로 조정해야 한다(기존 동작이지만 인라인 편집으로 분류 변경이 쉬워져 눈에 띈다). ↑↓·활성/비활성·삭제 같은 **행 액션은 통합 저장 폼과 별개인 `form=` 형제 hidden 폼**으로 즉시 제출(중첩 폼 금지 — 장비 대장과 같은 이유)되고, 성공 시 그 섹션 앵커(`#rates-section`/`#task-types-section`/`#rooms-section`)로 되돌아온다. ⚠️**`updateTaskType`은 `sort_order` 미전송 시 100으로 리셋하는 잠복 버그가 있어**(단건 수정 라우트도 원래 이 필드를 안 보내 같은 위험이 있었다) `bulkUpdateTaskTypes`가 각 행의 **현재 sort_order를 조회해 명시적으로 함께 전송**해 ↑↓ 순서를 보존한다(rate_items·rooms의 update 함수는 SET절에 sort_order가 없어 애초에 안전). **미저장 상태에서 행 액션(↑↓·비활성·삭제)을 누르면 그 액션은 즉시 반영되지만 같은 폼의 다른 행에 입력해 둔 미저장 변경은 유실된다**(사용자 결정으로 수용 — 행 액션은 '이 변경은 버리고 그 액션만 실행'하는 의도로 간주).
- **환경설정**: 스튜디오 캘린더(자동 연동 대상)·**기존 캘린더 일정 재동기화 버튼**(제목·설명 포맷 변경을 기존 일정에도 반영하는 1회성 관리 액션 — `POST /settings/resync-calendar`=`requireChief`, `gcal_event_id` 있고 취소 아닌 세션 전부를 순회해 `calendar.updateEvent`만 재호출[생성/삭제 없음, fail-safe·건별 실패해도 계속], 결과는 `notice`로 성공/실패 건수 안내; `eventInputForSession`을 `sessions.routes.js`가 `module.exports`에 달아 재사용)·예약 일정 기본 장소·**기본 세션 시간**(녹음 외 세션의 소요 슬라이더 기본값, `POST /settings/pro-minutes` — 옛 '운영시간' 폼은 2026-07-27 제거)·**룸 CRUD**(추가·이름/상위/플래그 수정·**인라인 통합저장**[`POST /settings/rooms`, `/:id`, `/bulk`, `/:id/delete`] — 통합저장 메커니즘은 위 '콘텐츠' 참조)·**공급자(스튜디오) 세금정보 + 로고**(거래명세서 PDF용; 로고는 PNG/JPG 업로드→base64, 매직바이트 검증)·알림 웹훅 URL.

### 배포 · 운영
- **배포는 수동(`autoDeploy: false`, 2026-07-20 사용자 결정)**: **디스크가 붙은 서비스는 무중단 배포가 불가능**하다 — 같은 디스크에 두 버전이 동시에 쓰면 데이터가 깨지므로 Render가 **기존 인스턴스를 정지한 뒤** 새 것을 띄운다(공식 문서 "your service is unavailable"). 즉 **푸시 1회 = 실서비스 1분 안팎 중단**. 2026-07-20에 main 커밋 35개가 대부분 업무 시간대에 자동 배포돼 사용자가 '접속이 잘 안 되고 느리다'고 리포트했고, 실측에서 푸시 직후 `/healthz`가 25회 연속 502였다(몇 분 뒤 전량 200·0.29초). → **커밋·푸시는 자유롭게, 배포는 Render 대시보드 Manual Deploy로 사람이 고른 시점에.** ⚠️render.yaml 변경은 Blueprint 동기화가 붙어 있어야 반영되므로 대시보드 Settings에서 Auto-Deploy Off를 눈으로 확인할 것.
- **성능 실측(2026-07-20)**: 서버 렌더는 **1~5ms**(프로덕션 데이터 기준 청구 93건 5ms·연락처 354명 5ms) — 앱 연산은 병목이 아니다. 비용은 **왕복**에 있다(원본이 싱가포르라 왕복 1회 ~65ms, 페이지 TTFB 300~400ms의 대부분). 그래서 최적화는 '코드를 빠르게'가 아니라 **왕복·바이트 줄이기**로 간다: ①정적 자산 `max-age=365d, immutable`(URL이 `?v=ASSET_VERSION`이라 안전 — 아래 ⚠️) ②구글 폰트에서 Noto Serif KR 제거(렌더 차단 CSS 305KB→19KB) ③**Cloudflare가 `/css/*`·`/js/*`를 엣지 캐시하지 않는다**(`cf-cache-status: DYNAMIC` — CF 대시보드 Cache Rules 설정 필요, 코드로는 못 고침·미해결). HTML 압축은 이미 Cloudflare가 처리하므로(원본 onrender.com도 CF 뒤) `compression` 미들웨어는 불필요하다. 청구 목록 HTML은 318KB지만 brotli로 **19.7:1** 압축돼 실제 전송은 16KB — 마크업 크기는 문제가 아니다(압축 후로 판단할 것).
- ⚠️ **`ASSET_VERSION`은 layout이 참조하는 정적 파일 전부**(`views.js ASSET_FILES` = app.css·app.js·theme-init.js·viewer.js)를 봐야 한다. 예전엔 app.css·app.js만 봐서 theme-init.js만 고치면 버전이 안 바뀌었는데, 캐시가 1시간일 땐 곧 만료돼 안 보이던 결함이 **1년 immutable로 올리는 순간 '그 파일이 영영 갱신 안 됨'**으로 바뀐다. 새 정적 파일을 참조하면 ASSET_FILES에도 반드시 추가(가드레일 ⑰가 기계 검사).
- Render Blueprint(web + cron) + Disk. 일일 백업(`VACUUM INTO`·14일 보존 + **첨부 uploads 폴더 날짜별 스냅샷** `backups/uploads-YYYY-MM-DD/`·14일)·**감사 로그 보존(`pruneAudit` — 180일·최대 2만 건, 2026-07-10 무한 증가 방지)** cron(`/internal/cron/daily`, `BACKUP_TOKEN`).
  **DB 백업 오프사이트**: Drive 연결 시 일일 DB 백업을 Drive `backups` 폴더로 사본 전송(`drive.backupToDrive`, 최신 14개 보존, fail-safe — 미연동/오류면 skip). Render 디스크 단일 장애점 완화.
  정적 자산 캐시 버스팅(`?v=` mtime+size).
- **알림(웹훅)**: 청구 발행·자료 공유 시 Slack/Discord 등 팀 알림(`src/notify.js`, fail-safe·비차단, 미설정 시 무음).
- **청구 발행 알림 = 이메일(2026-07-14 구현)**: 프로젝트 청구 탭에서 **청구 생성 시** 지정 주소로 알림 메일 발송. 발송=**지메일 API**(`src/mailer.js` — googleapis 재사용·새 의존성 0, 발신=스튜디오 구글 계정 `STUDIO_DRIVE_EMAIL`, 인증=Drive·캘린더·연락처와 **동일 refresh token**·새 스코프 `gmail.send`만 추가 → 스튜디오 계정 1회 재로그인 필요). 수신자=**관리>환경설정>알림의 주소 칸**(콤마로 여러 명·`admin_state.alert_email_to` 평문 — 비밀 아님·로그인 계정과 분리돼 외부 회계/세무사도 가능). **메일 본문 = 청구 상세 화면 그대로**(2026-07-14 사용자 요청 — '메일에서 업무를 한 번에'): 제목 `[청구 발행] {청구번호} · {아티스트|청구처}`, 본문은 **①청구처 정보**(홈택스 공급받는자 입력 순서 — 사업자등록번호[개인은 현금영수증]·상호[개인은 성명]·성명(대표자)·사업장 주소·세금계산서 발행 이메일·담당자[이름·전화·이메일]) **②청구 항목**(라인별 설명·금액) **③소계·할인·VAT·총액** + **[청구서 열기]** 버튼(`{BASE_URL}/invoices/:id`). 수신자(대표)가 메일만 보고 홈택스 계산서를 발행할 수 있고, 링크는 확인이 필요할 때만 쓰는 보조. **발행 시점 `payer_snapshot` 우선**(이후 클라이언트 정보가 바뀌어도 메일은 발행 당시 값 — 회계 정합). **첨부(거래명세서 PDF)는 없음**(유출 시 피해 확대 방지). 조회 실패해도 요약만으로 발송(fail-safe). `notify.dispatchEmail`이 **invoice_issued만** 라우팅하고 **웹훅과 독립**(웹훅 미설정·SSRF 차단·throw와 무관하게 발송) + fail-safe(청구 생성 절대 비차단)·SIGTERM 드레인. 수신 주소 없으면 **시스템 화면 ⚠️ 경고**(조용한 장애 방지)·연동 배지. 치프 전용 저장/테스트(`POST /settings/alert-email[/test]`), 형식 불량 주소는 저장 거부. 인보이스 조회에 `projects.artist` 파생 추가(메일 제목·본문). 테스트 22(mailer 11·notify-email 5·settings-email 1 + 기존). **이력**: 2026-07-13 카카오 '나에게 보내기' → 푸시 인지 약함으로 전량 폐기, 알림톡(SOLAPI)은 비즈앱·템플릿 심사 관문이 커서 착수 전 폐기(설계 2종 ⛔폐기 표시로 보존).
  URL은 환경설정에서 암호화 저장 또는 `ALERT_WEBHOOK`. 자료 공유는 공개 토큰 대신 내부 프로젝트 링크로 통지(PII 보호).

### 관리 항목 편집 = 삭제 중심 (활성/비활성 폐기)
- 하우스 엔지니어·외주 작업자·클라이언트·단가표·작업 종류 모두 토글 없이 **삭제(하드)**. 강제 삭제 시 참조 FK는 SET NULL(인보이스·프로젝트 등), 과거 작업의 종류 라벨은 key로 폴백 보존. 본인·부트스트랩 치프만 삭제 차단.

## 스택

| 영역 | 선택 |
|---|---|
| 런타임 | Node ≥20(`engines`), **표준 버전=22 고정**(`.nvmrc` — Render 프로덕션·nvm 로컬이 읽음, CI는 20+22 매트릭스 유지), Express 4 (CommonJS) |
| DB | SQLite — `better-sqlite3`(운영, prebuild) / `node:sqlite`(폴백) 어댑터(`src/sqlite.js`) |
| 인증 | 전원 Google OAuth + 화이트리스트(`users` 행) → httpOnly 서명 JWT 쿠키(30일). 비밀번호 로그인 폐기 |
| 저장소 | Google Drive(**고정 스튜디오 계정** `STUDIO_DRIVE_EMAIL`=studio@omgworks.kr의 토큰만 저장, `drive.file`) — 첨부·자료 전달용. 그 계정 로그인 시에만 토큰 갱신(치프 무관·고정). 미연동/Drive 업로드 실패 시 로컬 디스크 폴백(단 Drive 연결 상태에서 실패는 예외로 알림) |
| 캘린더 | Google Calendar(관리자 토큰, scope `calendar`) — 일정 자동 생성/수정/삭제(취소 포함). FreeBusy 하드차단은 비활성(다중 룸 도입으로 앱 DB 룸별 겹침이 정식 차단) |
| 보안 | helmet(CSP, 인라인 스크립트 0) + express-rate-limit + 토큰 AES-256-GCM 암호화 |
| 프론트 | 서버 렌더 HTML(`src/views.js`) + 클래식 폼 POST + 최소 JS(`public/js/app.js`), Tailwind CLI 빌드; **Pretendard** 한글폰트(jsdelivr CDN, CSP 허용) + 구글 폰트 Inter·Source Serif 4(**Noto Serif KR은 2026-07-20 제거** — 렌더 차단 CSS의 93%인데 세리프를 쓰는 건 Claude 팔레트뿐이고 기본은 Linear라 대부분 안 쓰이고 버려졌다); 크림·클레이 디자인 톤; 수동 라이트/다크 테마(`html[data-theme]`+localStorage) + **테마 선택(팔레트) 5종**(2026-07-18 — **Linear[기본·indigo·미니멀]**/Apple[HIG·SF·iOS블루·16px radius]/Spotify[그린 #1DB954·pill·다크 정체성]/Pinterest[레드 #E60023·넉넉한 pill·둥근 카드]/Claude[크림·클레이 원본 정체성=`:root` 기본값]. **미선택 사용자 기본 팔레트 = Linear**(theme-init.js가 저장 palette 없으면 `data-palette="linear"`, 기존 선택자는 유지). **라이트/다크는 OS 설정 추종**(2026-07-18 — 저장 theme 없으면 로드 시점 OS 설정을 `matchMedia("(prefers-color-scheme: dark)")`로 스냅샷해 `data-theme` 세팅. ⚠️2026-07-19 수정: 이전엔 저장 theme 없으면 `data-theme`를 아예 안 걸었는데, 팔레트 다크 변형이 전부 `[data-theme="dark"]` 속성 게이트라 base media 다크가 팔레트 라이트 블록[같은 특정도·소스 뒤]에 밀려 **OS 다크가 무시**됐다 — Claude 팔레트만 OS 추종되고 기본 Linear 포함 나머지 4팔레트는 OS 다크에서 라이트로 떴다. theme-init가 OS를 스냅샷해 세팅하게 고쳐 전 팔레트 OS 추종). Material은 폐기. 사이드바 특징색 스와치 아이콘 클릭 선택. `html[data-palette]`+localStorage, `data-theme`와 직교) |
| 배포 | Render Blueprint(`render.yaml`) + Disk — **라이브** |
| 테스트 | Node 내장 `node:test` — 743개(**스모크 1**: `test/smoke.test.js` — 실서버 기동+치프 로그인 후 주요 화면 22개 200 확인[People API 무음 사망류 '조용히 죽는' 회귀를 CI가 검출, 2026-07-10] **+ 역할별 권한 배선 매트릭스**[owner/staff 로그인 후 requireInvoice(/revenue·/workers)·requireStaff(/settings·/deliverables) 차단·허용을 기계 검증 — 라우트↔미들웨어 드리프트(실제 발생 이력)를 잠금, 2026-07-11] + 단위 172: safePath/청구 3탭(nav)· 채번·VAT·Pro단가·세션겹침·권한·party·입금이력·대관세션·프로젝트 금액 할인 차감·세션 담당 엔지니어 다대다·세션 외주 정산(worker_rate 보존)·0원 항목 확인청구·청구 항목 날짜순 정렬·작업 완료 토글(task-status)·단가표 분류(rate-categories)·청구 안 함 무료 처리(waive)·프로젝트 목록 3탭(project-list)·**지급 축 삭제 잠금(payout-lock)**·**겹침 경고≡차단(session-conflict-warn)**·**청구 레이더(billing-radar — 단가 미선택 세션 노출·미청구 집계·진행 중 배지 + 모바일 행 레이아웃 계약)**·**당사자 삭제 가드(party-delete-guard — 청구처 보존·세 문 공용 헬퍼)**·**확정 진행 청구서 삭제 권한(invoice-delete-guard — 스태프 403·치프 허용·감사 로그 금액)**·**웹훅 SSRF 대역·매핑우회(ssrf)**·**공개링크 게이트 존재/철회/만료(token-gate)**·**전화·사업자번호 정규화(format)**·**캘린더 종일/다일/야간 KST 시간조립(calendar-event-times)**·외주 작업자 세션참여·첨부·정산정보암호화(worker-detail)·세션 취소 '(취소)' 캘린더 prefix(session-cancel)·**연락처 마스터-디테일 렌더러(contacts-views)**·**연락처 2단 라우트(contacts-panes — 상한 없음·open-redirect 차단, 5탭 역할 필터·리다이렉트·`listContacts({tab})` 5분기 계약 포함, 2026-07-17 사람/조직 축 정리)**·**룸 계층·과금 개편 마이그레이션(pricing-rooms-migration — id 보존·멱등)**·**룸 관리(rooms — 이름 수정 id 보존·계층·예약 대상·순서)**·**단가 항목(rate-items — 가격 유형·순서·활성 토글)**·**요금 근거 라인(charge-lines — 1프로 블록 규칙 경계값·할증)**·**청구 근거 분리(invoice-charge-breakdown)**·**촬영 3구간(session-segments)**·**목록 N+1 금지 가드(session-list-batch — 행 10배에도 쿼리 수 동일·배치≡단건 금액)**·**청구 화면 가격 유형(invoice-price-type)** + **가드레일 22**[백엔드 12+UI 계약 10(신규 ⑯ '클라이언트' 화면 문구 금지)] + **jsdom 상호작용 81**[네비게이션 가드·모달 드래그-아웃·날짜 콤보·이중 제출 가드·청구 금액칸 Tab 이동·실시간 필터 서버 보강·**촬영 구간 토글·구간 span 겹침 판정** 포함]), `npm test`(격리 임시 DB 자가정리). 유일한 테스트 devDep=**jsdom**(2026-07-04 사용자 승인 — '의존성 0'의 명시적 예외): `test/helpers-dom.js`가 실제 views 렌더 위에서 실제 app.js를 window.eval로 실행(`test/ui-interactions.test.js` — 금액 캐럿 보존·콤보 검색(본명/활동명/회사)/선택/새등록 모달·IME 가드·세션 종류↔단가 옵션 스왑·dirty 폼). **`test/guardrails*.test.js` = 반복 실수 클래스의 기계 가드**(아래 함정 #21) |

## 아키텍처 핵심

- <a id="레이아웃-통일"></a>**레이아웃 통일 — 사이드바 고정 + 페이지별 콘텐츠 폭(2026-07-16 사용자 '구성안')**: 예전엔 `layout()`이 페이지마다 다른 `max-w`(기본 1024·청구 1760·캘린더 full)를 **중앙정렬(mx-auto)** 바깥 컨테이너에 걸어, 메뉴 이동 시 중앙정렬 블록의 좌측이 밀려 **사이드바가 튀었다**. → **바깥 컨테이너를 전 페이지 공통 `max-w-wide`(1760, 중앙정렬)로 고정** → 사이드바 위치 불변. **main 폭만 페이지별**: `layout({wide:true})`=넓은 목록·표·캘린더(프로젝트·청구·클라이언트·연락처 목록, 일정 캘린더, **매출 전 탭**[2026-07-19 마스터-디테일 전환으로 읽기 폭→wide 복귀 — 패널 오른쪽이 `max-w-content`라 읽기 폭은 보장], **환경설정**[2026-07-28 사용자 결정 — 단가표·룸·작업 종류 인라인 편집 표가 들어와 '폼 읽기'에서 '목록 편집'으로 성격이 바뀌어 읽기 폭에서 되돌림. `/settings/drive-check`는 단순 점검 페이지라 read 유지]) → main 전체 폭(적극 사용) / 기본(wide 없음)=폼·상세·**대시보드·외주작업자·자료전달**(2026-07-16 사용자 '폭이 넓어 시선 분산' → wide에서 읽기 폭으로 전환. 대시보드 카드는 2열 고정) → main 안 `max-w-content`(**768**, 2026-07-16 사용자 '폭이 넓어 부담·시선 분산' → 1024→768[48rem]로 좁혀 집중) 좌측 정렬 읽기 폭(입력칸이 화면 끝까지 늘어나 스캔 힘든 것 방지). 사이드바는 어느 쪽이든 고정. 로그인·공개 다운로드(무user)는 별도 `max-w-md` 브랜치(무관). 실측: 전 페이지 사이드바 left 동일·wide=main 채움·read=768·가로 오버플로우 0. **사이드바 스크롤 고정(2026-07-18)**: 데스크톱 사이드바(`aside#sidebar`)를 `sm:sticky sm:top-6 sm:self-start sm:max-h-[calc(100vh-3rem)] sm:overflow-y-auto`로 — 긴 상세(특히 연락처·업체·그룹 마스터-디테일 편집 폼)를 스크롤해도 사이드바가 화면 밖으로 밀려나지 않고 뷰포트에 고정(내용 길면 자체 스크롤). 이전엔 `sm:static`이라 페이지와 함께 스크롤됐다(사용자 리포트). 마스터-디테일 왼쪽 목록은 이미 `lg:sticky`라 함께 유지 → 우측 상세만 스크롤.
- **돌아가기(백링크) 규약 — 전 목록 공통(2026-07-14 사용자 요청 '모든 돌아가기를 이 방식으로')**: 목록 행 링크가 **현재 목록 주소**(탭·검색·필터·open 포함)를 `?return=`(encodeURIComponent)으로 실어 보내고, 상세의 `pageHeader.back`이 **`safePath(req.query.return)`**(내부 절대경로만 — open-redirect 차단)로 복귀한다. 없으면 기본 목록 폴백. 적용: **청구**(`retPath`·2026-07-08) · **프로젝트**(`listQuery`→`projectRowHref`) · **연락처**(2026-07-17 마스터-디테일 전환 후 목록 행 링크 자체는 return 불필요 — 청구·프로젝트·클라이언트에서 오는 유입 `?return=`만 유지) · **업체·그룹**(2026-07-18 같은 방식으로 마스터-디테일 전환 — 목록 행 링크는 불필요, 청구·프로젝트에서 오는 유입 `?return=`과 레거시 `?from=` 필터 병행만 유지) · **외주 작업자**. 매출은 목록에 탭·검색이 없어 유실될 상태가 없다(기본 폴백). 백링크 라벨은 복귀 경로 prefix로 판정(청구/프로젝트/클라이언트/연락처/돌아가기). **가드**: `test/nav.test.js`가 라우트 소스에서 ①행 링크의 return 전달 ②상세의 safePath 사용을 검사 — 새 목록을 추가할 때 이 계약을 빠뜨리면 테스트가 실패한다.

- **role 기반 게이트(3단계)**: `attachUser`(활성 + owner/chief/staff만 세션 인정) → 권한 술어
  `isOwner`/`isChief`/`isStaffRole`, 복합술어 `canEdit`(chief|staff)·`canInvoice`(chief|owner)·`canBill`(chief|owner|staff=청구서 발행)
  (모두 `auth.js`). 미들웨어: `requireAuth`(로그인=보기), `requireEditor`(canEdit=치프·스태프·**대표**[2026-07-07 개방] — 프로젝트·곡·작업·세션·**연락처·클라이언트(첨부 포함)** 편집), `requireStaff`(isStaffOrChief=치프·스태프 전용 — **자료 전달·관리(/settings)**, 대표 차단·메뉴 숨김, 2026-07-07 신설), `requireChief`(치프 전용=계정 관리·웹훅 등), `requireBilling`(canBill=**청구서 발행**=치프·대표·스태프), `requireInvoice`(canInvoice=
  매출·외주 정산=치프·대표). 내부 도구이므로 로그인 직원은 모든 프로젝트를 열람한다(클라이언트 범위 강제 폐기).
- **Google 화이트리스트(`auth.js upsertUserFromGoogle`)**: 로그인 Google 이메일이 `ADMIN_EMAIL`(부트스트랩
  **치프=chief**, 없으면 자동 생성)이거나 `users`에 등록된 활성 행이면 그 역할로 로그인, 아니면 거부. 치프는
  `/settings`에서 사용자(이메일+역할 owner/chief/staff) 추가·역할변경·활성/비활성으로 화이트리스트를
  운영한다(본인·부트스트랩 치프는 잠금 방지로 강등/비활성 불가). 대표 계정은 치프가 owner로 등록한다.
- **미들웨어 순서(플레이북 §3-1)**: helmet/ratelimit → cookie/body → `attachUser` → 라우트 →
  **`express.static`은 맨 뒤**(보호 HTML은 라우트, static은 css/js 자산만). 인증 우회 방지.
- **작업 옵션/상태값 = 코드 상수**(`config.js`)가 단일 진실원천. DB CHECK 제약 금지(§2.8 마이그레이션 지옥 회피).
- **돈=정수(원)**, 날짜=`"YYYY-MM-DD"` 문자열(`src/lib/date.js`).
- **시간대: 저장=UTC · 표시=KST**(2026-07-20 사용자 요청 '시스템 시간대가 UTC인데 우리나라 시간으로'). ⚠️**화면이 KST로 보여준 값을 그대로 저장하면 안 된다** — `utcStampFromKst(kstYmd, kstHms)`로 되돌린다(작성일 편집이 저장할 때마다 하루씩 밀던 회귀, 같은 날 메인터넌스에서 발견·재현). SQLite `datetime('now')`가 **UTC**라 `created_at`·`audit_log.at`·`users.last_login`이 UTC로 쌓인다. **저장 형식은 그대로 두고**(표준·이식성·기존 데이터와 안 섞이게) **표시에서만 +9h** 한다 — `kstYmd(v)`(날짜)·`kstDateTime(v)`(시각까지)·`kstDate(v)`(Date 객체, lib/date.js). ⚠️이걸 안 쓰고 `created_at.slice(0,10)`으로 자르면 **KST 00:00~08:59에 만든 것이 하루 이르게** 보인다(실측: 개발 DB 프로젝트 7건이 그랬다). ⚠️**날짜만 있는 값(`session_date`·`issued_date`·`worker_paid_date`)에는 쓰지 않는다** — 그건 사용자가 고른 KST 날짜라 +9h를 먹이면 하루 밀린다(헬퍼도 길이 10 문자열은 그대로 통과시킨다). 적용처: 감사 로그(옛 `UTC` 라벨 제거)·프로젝트 목록 작성일·업체/연락처 상세의 프로젝트 표·자료 전달·외주 작업일. `todayYmd()`는 원래부터 KST 기준이라 '오늘' 판정·D-day는 영향 없고, 백업 시각은 이미 `toLocaleString(ko-KR, Asia/Seoul)`. `lastLoginLabel`은 경과 시간이 아니라 **KST 달력 날짜 차이**로 센다('오늘/어제'는 시계가 아니라 달력의 말 — 20시간 전이 어제인데 '오늘'로 나오던 것). 회귀=`format.test.js`.
- **at-rest 암호화**(`db.encrypt/decrypt`, AES-256-GCM): Drive/Calendar refresh token 등 비밀.
- **CSRF 방어**: `sameOriginRequest`가 비안전 메서드(POST/PUT/PATCH/DELETE)에서 Sec-Fetch-Site 헤더 없으면 기본 거부. Authorization 헤더 요청과 `/internal` cron 경로만 예외.
- **OAuth 논스**: Google OAuth state에 랜덤 논스 포함 + httpOnly 쿠키 대조(로그인-CSRF 차단). `safeNext` 역슬래시(`\`) open-redirect 우회 차단.
- **모바일 UX(플레이북2 §5)**: 입력 16px(iOS 자동확대 방지, `src.css` `@media(max-width:639.98px)`), 반응형 카드, 콘텐츠 max-width 통일. 모바일 드로어 헤더(로고+X)·백드롭 탭 닫기, 포커스링, theme-color 다크 대응. **터치 타깃 44px(모바일 전용)**: `src.css` 언레이어드 `@media(max-width:639.98px){ .btn·.row-link·[role=listbox]>button { min-height:44px } }`(언레이어드라 `btn-xs` min-h 36px보다 우선, 데스크톱 ≥640px 밀도 현행 유지). **`active:` 눌림 피드백**(터치엔 hover 없음): `btn-primary/ghost`·`row-link`·`listRow`·콤보 행. **커스텀 콤보는 `mousedown`+preventDefault로 blur 방지**(터치도 mousedown 합성 → 표준 패턴; `pointerdown`+preventDefault는 click 억제 위험이라 미채택). **반응형 검증법**: 오프스크린 `<iframe width=390>`에 각 페이지를 로드해 `documentElement.scrollWidth-clientWidth`(가로 오버플로우)를 실측 — Chrome 창 최소폭(500px) 우회, 실제 미디어쿼리(sm=640 미적용) 적용된 상태로 320~430px 검증(2026-07-04, 15개 페이지 overflow=0).
- **UI 공통 헬퍼·테마/팔레트** — 헬퍼 목록·팔레트 5종·사이드바 구성 상세는 **[SCREENS.md §공통 뷰 헬퍼 · 테마/팔레트](SCREENS.md)**. 여기 남기는 계약·불변식:
  - `dataTable(cols, rows, {filterList})`·`listGroup`/`listRow`·`tabBar`·`filterChips`·`emptyState`·`explain`·badge 5변형이 목록·표의 공용 골격. **콜 폭은 CSS 클래스 리터럴로**(인라인 style은 CSP에 막힌다 — 함정 #27), `<col>`은 숨겨도 폭을 예약하므로 반응형 hide 시 `table-column`으로 함께 숨긴다.
  - **테마 아키텍처**: 색·폰트·radius·shadow를 `:root` CSS 변수로 두고 컴포넌트가 참조 → 팔레트(`html[data-palette]`)는 그 변수만 덮어쓴다(마크업·로직 불변, 순수 시각 레이어). Claude 팔레트 = `:root` 기본값. 기본 팔레트=Linear, 라이트/다크는 OS 추종. `data-theme`와 팔레트는 직교.
  - 🔒 **FOUC 방지 = 서버가 `<html data-theme data-palette>`를 첫 바이트에 렌더**(쿠키 → `AsyncLocalStorage` 요청 컨텍스트 → `currentThemeAttrs()`). theme-init.js만 쓰던 옛 방식은 그 파일을 받는 동안 서버 기본값이 먼저 그려져 매 이동 깜빡였다. 스와치 활성 링도 **CSS로**(app.js `aria-pressed`는 defer라 링이 깜빡였다). 회귀 `request-theme.test.js`·`theme.test.js`.
  - **모달 스크롤 잠금**: 모달 마커 중 하나라도 열리면 `<html>·<body>` overflow:hidden(+스크롤바 폭 보정). ⚠️캘린더 팝오버는 **일부러 `data-modal`을 안 붙인다**(배경 클릭 통과가 목적).
  - 검색 3방식: `suggestUrl`(서버 typeahead)·`liveFilter`(로드분 즉시 필터)·`remote`(상한 초과분 서버 보강 — 목록이 `capList`로 잘리면 실시간 필터가 100번째 뒤를 못 찾는다).

## 데이터 모델 (생성됨)

- `rooms(name, bookable, is_external, sort_order, active)` — **스튜디오 룸 마스터**. `bookable`=예약 대상(Lounge 등은 0). 치프가 `/settings` 환경설정 탭에서 CRUD(**이름 수정 포함** — id 보존이 곧 기존 예약 보존). 삭제 시 `sessions.room_id` SET NULL. `parent_id`=**레거시**(2026-07-28 계층 폐지, 읽는 코드 0). 명칭 체계는 위 '세션 일정' 섹션 참조.
- `session_segments(session_id→sessions CASCADE, kind[setup|shoot|teardown], start_time, end_time, sort_order)` — **촬영 구간**(반입·설치/촬영/철수, 2026-07-26). 🔒**요금 시간 = 구간 합산 / 룸 점유·캘린더 = 첫 구간 시작 ~ 마지막 구간 종료 span**(사용자 확정). 구간 사이에 빈 시간이 있어도 그 방은 계속 잡혀 있으므로 점유는 아우르는 한 덩어리이고, 점유를 `sessions.start_time~end_time`에 그대로 두면 **겹침 판정·캘린더 동기화(gcal_event_id 1개)·청구 후보 쿼리가 종전 구조 그대로** 동작한다. `sessionFields`가 구간이 오면 start/end를 span으로 파생(같은 시간을 두 번 입력받지 않는다), `sessionBillableMinutes`가 합산. 종일은 구간을 지우고, **구간 칸이 없는 요청은 기존 구간을 건드리지 않는다**(구간을 모르는 폼·API의 저장이 조용히 지우지 않게 — `segmentsFromInput`이 null 반환). 헬퍼=`listSessionSegments`/`setSessionSegments`(트랜잭션 안 염 — 호출부가 감쌈, setSessionDirectors와 같은 규약)/`segmentsSpan`.
- 🚫 **할증 개념 없음**(2026-07-27 사용자 결정 — 도입 이틀째 폐기): 미장센처럼 가산이 필요한 요금은 **단가 항목 자체로** 표현한다(운영은 `촬영 (미장센 및 다수 카메라)` 600분/150만). 같은 요금을 '할증'과 '단가 항목' 두 경로로 넣을 수 있으면 어느 쪽으로 잡았는지에 따라 금액이 갈리기 때문. `surcharges` 마스터 테이블은 `surcharges_drop_v1` 게이트로 제거됐고 `computeCharge`도 surcharge 인자가 없다. `sessions.surcharge_key`/`surcharge_memo`는 **레거시 컬럼으로만 잔존**(읽는 코드 0, 참조 세션 0 — sessions는 큰 테이블이라 DROP COLUMN은 이득 없이 위험만 있다: `director_contact_id`와 같은 취급). 회귀 `pricing-rooms-migration.test.js`가 '테이블 없음'을 잠금.
- `users(email, role[owner|chief|staff], name, google_sub?, active, client_id?[레거시], password_hash?[레거시])` —
  `active=0`이면 로그인 차단(화이트리스트 제거). 마이그레이션에서 기존 `admin`→`chief` 자동 승계.
  `password_hash`/`client_id`는 구 모델 잔재 컬럼(미사용). **`name`은 기존값 보존**(치프가 `/settings`에서 수정한 한글 이름이 Google 프로필 영문 이름에 매 로그인 덮어쓰이지 않게; `users.name` 빈 경우만 Google 이름). 이름 수정 시 `project_managers.name`·`track_tasks.engineer_name`(작업 스냅샷)도 동기화.
- 🏗 **당사자(Party) 모델 — 정체성 통합(완결·배포)**. 옛 clients/contacts 이중화를 **`parties` 한 테이블**로 통합: kind=person|company|group·is_artist 플래그, 역할(아티스트·청구처·담당자·디렉터·엔지니어)은 전부 `parties.id` 참조. 데이터 헬퍼=`src/data/parties.js`. 이관은 `migrateToPartyModel`(party_model_v1)로 parties·affiliations·역할 컬럼 생성 후 `dropLegacyIdentity`(legacy_drop_v1)가 레거시 테이블(clients·contacts·contact_affiliations)·FK 컬럼을 제거 — **둘 다 완료·배포됨**(신선 DB는 CREATE에서 레거시 없음, 이관 게이트는 `hasLegacy` 가드·멱등). 임시 어댑터 compat.js도 제거됨. **⚠️ `projects.artist/artist_company/production_company` TEXT는 표시용 denormalization으로 의도적 유지**(파생·표시 편의). 완결 이력 상세는 git.
- **`parties(id, kind[person|company|group], name, activity_name?[활동명], is_artist, phone?, email?, memo?, / person: family_name·given_name·honorific·department·job_title·user_id·google_resource_name·google_etag·cash_receipt_no / company: biz_no·owner_name·owner_party_id·address·roles[겸업 CSV] / person(아티스트): group_id[소속 그룹→parties.id kind='group']·activity_form[활동 형태 solo|group|both — 수동 3택, 2026-07-16, **2026-07-17부터 UI 레거시**(유일한 표시처였던 업체·그룹 아티스트 탭이 사람/조직 축 정리로 폐기). **컬럼·백필만 보존하고 config 상수(`ARTIST_ACTIVITY_FORMS`·`..._LABELS`)는 2026-07-27 제거** — 데이터 호환은 컬럼이 지키고, 읽는 코드가 없는 상수는 '아직 쓰는 화면이 있다'는 오해만 남긴다] / group: **name=activity_name 동기화**[그룹명=단일 정체성 — 생성 시 activity_name=name, 수정 라우트도 `activity_name=새 name`으로 동기화(안 그러면 옛 activity_name이 남아 '옛이름 (새이름)'으로 표시되던 버그, 2026-07-16 사용자 리포트·1회 백필 `group_activity_name_sync_v1`. **솔로 아티스트도 같은 규칙**: 수정 시 `(그룹 || 옛 name==activity_name) ? 새 name : 옛 activity_name` — 이름==활동명이던 흔한 케이스는 동기화, 본명≠활동명(모달 등록)은 activity_name 보존[이름 필드=본명 편집]. 솔로는 본명≠활동명이 정상이라 백필 없음)]·contact_party_id[그룹 담당자→parties.id 사람·멤버 또는 관계자])`** — **정체성 통합 마스터**(clients+contacts 대체). 사람(아티스트 포함)=연락처 뷰(`listContacts`·`/contacts`), 조직(업체·그룹)=업체·그룹 뷰(`listClients`·`/clients`, 2026-07-17부터 사람은 여기서 완전히 빠짐). `is_artist`=아티스트 역할(person solo·group). `user_id`=로그인 계정(스태프·owner) 연결(`ensurePartyForUser`). 활동명(솔로 아티스트)=`activity_name`=Google nickname 동기화. 청구처(payer)·프로젝트 아티스트/소속사/제작사/담당자·세션 디렉터·담당자 연동이 전부 `parties.id` 참조. 데이터 헬퍼=`src/data/parties.js`(CRUD·`classifyParty`·`resolvePersonByName`·`partyOptions`·`ensurePartyForManager/User` 등). **`updateParty`=부분 갱신 계약(2026-07-04)**: 호출부가 보낸 필드(`undefined` 아님)만 갱신, 미전송 필드는 보존, 빈 문자열=의도적 비움(null) — Google 동기화(`personToContactFields`가 Google에 있는 필드만 전송)·유형별 폼이 나머지 필드(메모·전화·현금영수증 등)를 지우던 데이터 손실 클래스 근본 차단(person·company·group 3분기 모두, 가드레일 ⑦로 보증). 삭제(`deleteParty`)는 역할 참조 SET NULL·첨부 CASCADE. 🔒 **발행 청구의 청구처(payer)는 삭제 거부 — 세 문 공용 가드**(2026-07-23 기능성 평가): `deleteParty`가 `invoices.payer_id`를 NULL로 만들어 그 사람 기준 미수 추적(`listInvoicesForParty`)이 끊기고 `payer_kind` 소실로 현금영수증이 '계산서'로 오표시되므로, **`partyHasIssuedInvoice(id)`**(`status='발행' OR tax_status IN ('계산서 발행','입금완료')`)면 삭제를 막는다. ⚠️예전엔 이 판정이 **`/clients` 삭제 라우트에만 인라인**으로 있었고 **`/contacts` 삭제와 구글→앱 pull(`people.js` meta.deleted)은 무방비**라, 같은 사람이 **어느 문으로 들어오느냐에 따라** 지워지거나 보존되는 비대칭이었다(연락처에서 개인 청구처를 지우면 재무 링크가 끊겼다). → 판정을 헬퍼로 올려 **세 곳이 공유**(clients·contacts=409 거부, 구글 pull=삭제 건너뛰고 `keptWithInvoice` 집계 — 구글 주소록 정리가 우리 매출 기록을 소급 삭제하지 못하게). 인라인 SQL 판정 재등장·헬퍼 호출 누락은 `test/party-delete-guard.test.js` 계약 테스트가 차단. **직책(job_title) 입력 시 호칭(honorific) 자동 파생**(2026-07-10 사용자 결정 — '대표 직책→대표님 호칭' 흐름을 전 직책으로 일반화: `honorificFromTitle`[lib/korean-name] '실장'→'실장님'·이미 '님'으로 끝나면 그대로, `createPerson`/`updateParty`가 **호칭이 비어 있을 때만** 파생[명시 호칭 존중], 1회 백필 `person_honorific_from_title_v1`. → 호칭 컬럼 하나만 보면 전 화면[연락처 목록·콤보 칩·청구처 카드]이 '엄유미 실장님'으로 일관 표시). **대표자(회사 owner) 등록 시 그 사람 party 호칭='대표님' 자동 세팅**(`resolveOwnerParty`, 2026-07-03 — 이름 기반 재사용/생성·호칭 비어 있을 때만·기존 호칭 존중·이름 유지로 중복 방지) **+ 그 대표자의 직장(소속)을 그 회사로 연결**(`ensureOwnerAffiliation` — 현재 소속이 이 회사가 아니면 이직 등록[직함 '대표'], 이미면 no-op·중복 없음; 업체 생성·수정 양쪽). **대표자 입력=칩 콤보(공동대표 여러 명, 2026-07-10)**: 담당자 연락처와 동일한 `personCombo(multi, selected=listCompanyOwners)` — 제출은 칩마다 `owner_id`+`owner_name` 쌍, 라우트 `resolveOwnerIds`가 인덱스 페어링으로 해석해 `setCompanyOwners`에 넘긴다(호칭·소속·레거시 컬럼 동기화는 그쪽 책임). 상세 크로스링크 '대표자 연락처'도 전원 나열. **업체 생성 모달(companyCombo '＋새 업체 등록') 안 대표자 미니콤보도 칩**(2026-07-10 — 신규 회사를 만들 때부터 공동대표 지정 가능): 저장 시 fetch body에 `owner_id`/`owner_name` 쌍을 칩 수만큼 append(서버 `resolveOwnerIds` 인덱스 페어링 재사용, 라우트 무변경). 옛 단일 hidden `data-cc-owner-id` 폐기.
- `company_owners(company_id→parties CASCADE, party_id→parties CASCADE, sort_order, PRIMARY KEY(company_id, party_id))` — **회사 대표자 다대다(공동대표, 2026-07-10 사용자 요청 '대표자가 2명인 경우도 있다')**. 진실원천은 이 테이블, `parties.owner_party_id`=첫 대표(레거시 참조)·`owner_name`=콤마 목록(청구처 카드 '성명(대표자)'·거래명세서 스냅샷 표시 텍스트)을 `syncCompanyOwnerColumns`가 동기화. `setCompanyOwners(companyId, ids)`=통째 교체(각 대표에 '대표님' 호칭[비었을 때만]·이 회사 소속[직함 '대표'] 부여, **빠진 대표는 대표 역할만 해제 — 연락처·재직 유지**), `listCompanyOwners`. `deleteParty`가 대표 삭제 시 남은 대표로 레거시 컬럼 재동기화(승계). `orgsWithOwnerParty`·관계자 탭 `ASSOCIATE_ROLE_SUBQUERY`도 company_owners 기준(둘째 대표 누락 방지). 1회 백필 `company_owners_backfill_v1`(기존 owner_party_id 복사).
- `affiliations(person_id→parties CASCADE, org_id→parties SET NULL, title?, started_on?, ended_on?, memo?, is_contact, created_at)` — **소속 이력(이직 히스토리)**. **`is_contact`=이 조직의 '담당자'로 지정됨**(2026-07-10 사용자 결정 — 재직과 별개 역할: 담당자 해제≠퇴사). 담당자 조회=`listOrgContacts`(재직 + is_contact=1), 재직 전원=`listPersonsForOrg`. 1회 백필 `affiliation_is_contact_v1`(기존 재직자 전원 담당자 승계 — 화면·청구서 표시 불변). `ended_on IS NULL`=현재 소속, `org_id` NULL=무소속. 이직 시 기존 현재 소속 종료 후 새 행(`addAffiliation(closeCurrent)`). 헬퍼: `currentAffiliation`·`listAffiliations`·`syncCompanyAffiliation`·`listPersonsForOrg`·`listArtistsForAgency`. 뷰 호환 별칭 `client_*`(=`org_*`) 제공.
- `client_files(client_id→parties CASCADE[조직 party], kind['biz_license'], storage_backend, file_id, file_name, mime_type, file_size, created_at)` — **조직 첨부 서류**(사업자등록증). 컬럼명은 `client_id`(레거시) 유지하되 **parties(조직) 참조**. party당 kind별 1개(UNIQUE, 교체식). 매직바이트(PNG/JPEG/PDF)·치프 전용 인증 다운로드(공개 링크 없음).
- `projects(title, project_type[레거시·미사용], artist?, artist_company?, production_company?,
  client_id?→clients ON DELETE SET NULL, manager_id?→project_managers ON DELETE SET NULL, contact_id?→contacts ON DELETE SET NULL, services JSON, due_date?, rate, memo)` —
  `project_type`은 **레거시**(유형 구분 폐기 — 신규=session, UI 미노출). `contact_id`=고객측 담당자 연락처(내부 `manager_id`와 별개). `services`는 레거시 배열(편집 UI 제거). `status`·`kind`·**`due_date`는 레거시**(프로젝트 마감일 UI 폐기 — 청구 입금마감일과 무관한 잔재 컬럼).
- `project_managers(name, email?, phone?, active, user_id?→users, contact_id?→contacts, id_number?, bank_name?, account_number?, account_holder?, created_at)` — 작업 담당자 마스터.
  `user_id` 있으면 **하우스 엔지니어**(로그인 사용자와 링크, `auth.syncUserToManager`가 자동 생성·동기화),
  null이면 **외주 작업자**(로그인 없이 관리에서 직접 추가). 둘 다 세션·작업 담당 드롭다운에 노출. **`contact_id`=연동 연락처**(서버 기동 시 백필로 전 담당자 자동 연결): 전화는 연락처↔담당자 **양방향**, 이메일은 외주만 양방향·**하우스는 잠금**(`users.email` 보호). `ensureContactForManager`/`syncContactToManager`/`syncManagerToContact`(상호 재호출 없음=루프 방지). **`id_number`(주민등록번호/사업자등록번호)·`account_number`(계좌번호)는 `db.encrypt`로 암호화 저장**(2026-07-06 — 외주 정산 정보, `/workers/:id/edit`에서만 치프가 입력·복호화 표시). `bank_name`·`account_holder`는 평문.
- `worker_files(worker_id→project_managers CASCADE, kind['id_card'|'bankbook'], storage_backend, file_id, file_name, mime_type, file_size, created_at)` — **외주 작업자 첨부 서류**(주민등록증 사본·통장사본, 2026-07-06 사용자 요청). `client_files`와 동일 구조(kind별 1개 UNIQUE 교체식). 매직바이트(PNG/JPEG/PDF)·치프 전용 인증 다운로드(공개 링크 없음). `storage-migrate.js` 로컬→Drive 이관 대상 포함.
- `task_types(key UNIQUE, label, task_group[레거시·분류 폐기], billing_type, unit_price, is_quick, sort_order, active)` — **작업 종류 카탈로그**
  (곡·콘텐츠 후반작업). config `TASK_TYPES`를 `task_types_seed_v1` 게이트로 1회 시드 후 DB가 단일 진실원천(기존 9 key 보존, 신규=`tt_<hex>`).
  `track_tasks.task_type`이 이 key를 문자열로 보관(FK 아님). 라벨 해석은 `data.js` 모듈 캐시(`taskTypeLabel`, 쓰기 시 무효화). **분류(`task_group`) 폐기**(2026-07-01 — 곡·콘텐츠 작업은 모두 후반작업이라 분류 무의미: 추가/수정 폼 select·행 배지·빠른추가 optgroup·진행요약 그룹묶음 제거[진행요약은 종류별로], 신규=`Post_Production` 고정, `taskTypeGroup` 헬퍼 제거. `TASK_GROUPS`/`TASK_GROUP_LABELS` config·컬럼만 레거시 보존).
  `is_quick`=곡·콘텐츠 빠른추가 버튼 노출, `unit_price`=빠른추가 기본 단가. 삭제-only(강제), 치프가 `/settings?s=tasks`에서 CRUD.
- `project_service_items(key UNIQUE, label, active, created_at)` — 레거시(구 services JSON 라벨 호환). **관리 UI·시드 폐기**(작업 종류 카탈로그가 대체), 테이블만 잔존.
- `rate_items(name, category[스튜디오 녹음|로케이션 녹음|스튜디오 촬영|공연], base_minutes, base_price, extra_minutes, extra_price, price_type, sort_order, active)` —
  🏷 **과금 체계**(2026-07-26 사용자 지시): **과금은 룸이 아니라 작업 항목 단위**다(어느 룸을 썼는지는 요금에 영향 없음 — 룸은 일정 관리용). 1프로=210분, 초과 요율은 전 서비스 공통 시간당 10만. 마이그레이션 시드(**신선 DB 한정**) = 녹음 `솔로 녹음`(210분/30만)·`드럼 · 합주 녹음`(210분/40만). **촬영 항목은 시드하지 않는다**(2026-07-27 — `기본 패키지`를 넣었다가 운영의 기존 `촬영`·`촬영 (미장센 및 다수 카메라)`와 역할이 겹쳐 폐기). ⚠️**운영(프로덕션)에는 녹음 2건이 없다**(2026-07-26 배포 후 사용자 결정): 운영의 실제 항목명은 `보컬녹음`·`보컬녹음 (그룹)`·`성우녹음`·`악기 녹음`·`악기 녹음 (드럼)`이라 마이그레이션의 rename 별칭(dev 시드 이름 `보컬 녹음`·`악기+보컬 녹음(그룹)`)과 정확 일치하지 않아 **신규 삽입 경로를 탔고**, 사용자가 중복을 원치 않아 그 2건(id 12·13)을 삭제했다. 기존 항목은 손대지 않았으므로 과거 세션·청구 근거는 그대로다. **운영 녹음 단가는 여전히 기존 이름으로 청구한다** — 문서의 시드 목록을 운영 현황으로 읽지 말 것. 운영 촬영 항목은 기존 `촬영`(240분/40만)·**`촬영 (미장센 및 다수 카메라)`(600분/150만)** 둘이다 — **가산은 단가 항목으로 표현하는 것이 정본**(할증 메커니즘은 폐기). **포스트(믹싱 100만 `base`·보컬튠 20만 `minimum`)는 rate_items가 아니라 `task_types`에 있다** — 이 ERP는 세션(대관)=rate_items / 곡·콘텐츠 후반작업=task_types로 청구 경로가 갈려 있어서, 포스트를 rate_items에 넣으면 세션 폼에서 도달 불가한 죽은 데이터가 되고 '믹싱'이 두 카탈로그에 동시 존재한다.
  **`price_type`**(fixed|base|minimum, `task_types`에도 동일 컬럼) = 청구 화면이 금액칸을 어떻게 다룰지: `fixed`=기본가 잠금(초과·할증 자동 가산) / `base`=기준가 자동 입력 후 위아래 수정 / `minimum`=최소가 자동 입력 후 상향만(`data-line-min` 하한). ⚠️**잠금은 산정치가 정해졌을 때만**(computed>0) — fixed라도 카탈로그 단가가 0인 '금액 미정' 항목을 잠그면 아예 청구할 수 없다. base·minimum 행에는 **조정 사유 메모**(`session_billing_memo_<id>`/`task_billing_memo_<id>` → 청구 라인 근거로 스냅샷 + 원본 행 `billing_memo`에도 저장). 트레이드오프: 녹음·촬영은 전부 fixed라 세션 금액 직접 수정(2026-07-14)이 잠긴다 — 협의 감액은 **청구서 할인(정액/정률)** 또는 그 항목의 가격 유형을 '기준가'로 바꿔서 한다.
  `sort_order` + ↑↓ 이동(`moveRateItem` — **분류 경계는 넘지 않는다**, 관리 화면이 분류별 접이식이라 넘으면 사라진 것처럼 보인다), 활성/비활성 토글(`setRateItemActive` — `active` 컬럼은 처음부터 있었는데 라우트·UI가 없어 한 번 비활성이 되면 되살릴 수 없었다).
  **단가표 · 녹음 종류**. `category`는 이제 **DB `rate_categories` 테이블이 진실원천**(2026-07-05 사용자 요청으로 config 하드코딩에서 전환 — `RECORDING_CATEGORIES`[스튜디오/로케이션 녹음]·`FILMING_CATEGORIES`[스튜디오 촬영]·`PERFORMANCE_CATEGORIES`[공연]는 db.js `seedDefaultCatalogs`의 **1회 시드 데이터**로만 남음, `rate_categories_seed_v1` 게이트). **녹음/촬영/공연 kind는 category로 구분**(별도 컬럼 없음): `rateCategoryKind(name)`(`src/data/rate-categories.js`)이 DB 조회로 kind 반환(등록 안 된 이름은 recording 폴백); `SESSION_TYPE_RATE_KIND`={녹음:recording,촬영:filming,공연:performance}는 여전히 config. **세션 폼 단가 항목은 세션 종류에 맞춰 녹음/촬영만 노출** — 서버가 현재 종류 kind로 렌더 + kind별 `<template data-rate-opts-{kind}>` 임베드(recording·filming·performance — SESSION_TYPE_RATE_KIND에서 파생, `views.sessions.js`가 `listRateCategories()`로 optgroup 순서 결정), app.js `swapRateOptions`가 세션 종류 변경 시 옵션 교체(`data-rate-kinds` 맵). 관리>콘텐츠 단가표 폼 category select는 kind별(녹음/촬영/공연) optgroup(`rateCategoryOptions`, DB 조회). **단가표 목록은 분류별 접이식(`<details>`, 기본 접힘)으로 묶임**(2026-07-05 사용자 요청 — `ratesGroupedByCategory`, 분류당 항목 수 배지). **기준시간(1Pro)마다 묶어서 과금** — 완전한 Pro 블록은 각각 `base_price`, 마지막 1Pro 미만
  자투리만 `extra_minutes` 단위 올림으로 `extra_price`(`base_minutes=0`이면 정액). 예) 1Pro=210분·30만/초과 60분·10만
  → 630분(3Pro)=90만·240분(1Pro+30분)=40만. `computeRatePrice(item, minutes)`가 산정.
  🔒 **초과 규칙 = 1프로 블록 반복(2026-07-26 사용자 재확정)** — 홈페이지 `pricing.ts computeCharge`는 '기본가 + 초과 시간당'이라 **7시간부터 갈린다**(630분: 블록 반복 90만 vs 시간당 100만, 420분: 60만 vs 70만. **2프로 미만은 두 방식이 완전히 동일**). 후자는 2026-07-01에 사용자가 '10.5시간 → 90만'으로 리포트해 고친 것을 되돌리는 셈이라 **블록 반복을 지킨다**. 홈페이지의 '초과 시간당 10만' 문구와는 7시간부터 어긋나며, 이는 **의도된 차이**다(공개 안내 ≠ 실청구, 두 앱은 동기화하지 않는다). 회귀 `test/charge-lines.test.js`가 경계값을 잠금.
  **`computeCharge(item, minutes, {surcharge})` → `{lines, total}`**(2026-07-26, 홈페이지에서 이식): 근거를 한 줄로 뭉치지 않고 **기본가 / 초과 N시간 / 할증**을 라인으로 쪼개 돌려준다(각 라인 `{label, amount, detail, quantity, unit_price}`). `computeRatePrice`는 `computeCharge().total` 래퍼로 남아 기존 호출부 6곳 무변경(동일성을 전 구간 파라미터화 테스트로 잠금). **정액(회당) = 기준 시간 비움(base_minutes=0)** — 시간 무관 1회=기준가; **가격까지 비우면 '금액 미정'**(2026-07-04 사용자 결정 — 플레이백 세션처럼 회당 가격이 매번 다른 항목): `RATE_PRICE_REQUIRED` 가드는 시간제만 강제, 산정 0원 세션도 청구 후보 유지(세션 행 '청구액 미정' 표시·`listBillableSessionsForProject`/`computeInvoiceDraft`의 amount>0 필터 제거). **0원 항목은 확인 후 청구 가능(2026-07-05 사용자 결정 — 이전 '발행 시 0원 라인 차단' 정책 완화, 무료로 해준 작업도 기록·청구서에 남길 수 있게)**: 체크한 항목 중 0원이 있으면 제출 시 `window.confirm("금액이 0원인 청구 항목이 있습니다. 0원으로 청구할까요?")` → 확인하면 hidden `confirm_zero_amount=1` 전송(app.js), 서버 `createInvoiceFromTasks`는 이 플래그 없이는 여전히 `TASK_AMOUNT_REQUIRED` throw(JS 미동작·경합 대비 안전망 — 세션 겹침 `override_conflict`와 동일 패턴). **버그수정**: 애초 확인창은 있었으나 서버에 알리는 수단이 없어 '확인'을 눌러도 서버가 그대로 되돌리던 잠복 결함이었음(사용자 리포트로 발견) — 이번에 hidden 플래그로 실제 연결. 미리보기 PDF는 참고용이라 원래도 0원 허용. 청구 폼에서 금액 입력=확정(2026-07-01 수정: 이전엔 1Pro
  초과분 전부를 시간당 추가요금으로 매겨 3Pro가 100만이 되던 버그). 관리 메뉴에서 치프가 CRUD.
- **`rate_categories(name UNIQUE, kind[recording|filming|performance], locked, sort_order)`**(2026-07-05 신설) — 단가표 분류 마스터. 🔓**기본 분류도 수정·삭제 가능**(2026-07-29 사용자 요청 — 옛 `locked=1` 잠금 해제): 잠금 근거였던 '세션 종류↔kind 매핑 등 코드가 이름에 의존'을 전수 확인하니 **이름에 의존하는 코드가 없었다** — kind는 이 테이블 조회로 얻고(`rateCategoryKind`), 이름을 바꾸면 참조 중인 `rate_items.category`가 함께 갱신되며, 시드는 마이그레이션 게이트(`rate_categories_seed_v1`)라 지워도 되살아나지 않는다(config 상수는 시드 값일 뿐). **`locked` 컬럼은 레거시**(읽는 코드 0)이고 `CATEGORY_LOCKED` 에러도 제거됐다. ⚠️**남은 안전망은 `CATEGORY_IN_USE` 하나** — 그 분류를 쓰는 rate_items가 있으면 기본이든 아니든 삭제 거부(먼저 항목을 옮기거나 지워야 한다). ⚠️분류를 다 지워 특정 kind가 0개가 되면 그 세션 종류의 단가 select이 빈다(막지 않는다 — 다시 추가하면 복구). `src/data/rate-categories.js`(listRateCategories·rateCategoryKind·createRateCategory·updateRateCategory·moveRateCategory·deleteRateCategory), 라우트=`POST /settings/rate-categories`(+`/:id`·`/:id/move`·`/:id/delete`). rate-categories.test.js(시드·kind 매핑 / 기본 분류 수정·삭제 + 사용 중 거부 / 커스텀 이름변경 cascade / 이름누락).
- `project_contacts(project_id→projects CASCADE, party_id→parties CASCADE, created_at, PRIMARY KEY(project_id, party_id))` — **프로젝트 고객측 담당자 다대다**(2026-07-11, 여러 명 칩·project_artists 패턴). `projects.contact_party_id`=첫(대표, 청구처 파생·관계자 탭·표시 호환), 전체=이 테이블. `setProjectContacts`/`listProjectContacts`(src/data/projects.js), 1회 백필 `project_contacts_backfill_v1`(기존 contact_party_id 복사). `listProjectsForParty`·`ASSOCIATE_ROLE_SUBQUERY`가 UNION해 각 담당자 상세·관계자 탭에 전원 반영. 인덱스 `idx_project_contacts_party`.
- `project_artists(project_id→projects CASCADE, party_id→parties CASCADE, PRIMARY KEY(project_id, party_id))` — **프로젝트 아티스트 다대다**(2026-07-05, 콤마로 여러 명). `projects.artist` TEXT=콤마 표시 목록, `projects.artist_id`=첫(대표) 아티스트(청구처 파생·호환), 전체 목록은 이 테이블(각 아티스트 상세 '연결 프로젝트' 매칭 `listProjectsForParty`). `setProjectArtists`/`listProjectArtists`(src/data/projects.js), 1회 백필 `project_artists_backfill_v1`(기존 artist_id 복사).
- `project_tracks(project_id→projects CASCADE, title, artist?, content_type[Music|Video_Post], created_at)` —
  프로젝트 하위 곡·콘텐츠. **`artist`=곡별 아티스트**(한 프로젝트에 여러 아티스트 가능 — 곡·콘텐츠 추가/수정 폼에서 입력, 미입력 시 프로젝트 아티스트; 트랙 헤더에 표시). **콤마로 여러 명 가능(2026-07-05)**: 콤보는 공용 artistCombo라 조각 검색·이어붙임 자동, 서버 `normalizeArtistList`가 표시 정규화("A,B "→"A, B") — 트랙 artist는 표시 TEXT라 party 해석 없음(프로젝트 단과 달리). `content_type` 상수·정규화(`config.js`)는 있으나 **현재 UI 미노출 → 전부 Music**, 영상 구분은 향후 확장용.
- `track_tasks(track_id→project_tracks CASCADE, task_type, billing_type[Time_Charge|Fixed_Per_Track],
  quantity, unit_price, total_price, engineer_name?, engineer_id?→project_managers SET NULL,
  worker_rate?, status[Pending|Completed], waived[0|1],
  is_invoiced, invoice_id?, session_id?→sessions SET NULL, worker_paid, worker_paid_date?)` — 실제 청구 가능한 모듈형 작업 단위.
  `engineer_id`: 담당 엔지니어 FK(저장 시 `engineer_name` 동기 기록). `worker_rate`: 외주 **지급단가**(고객청구 `total_price`와 별개 — 마진 산정용). `worker_paid`=외주 정산(지급) 여부(`/workers` 정산 탭). 정산 합계=Σ`worker_rate`.
  `session_id`는 녹음 세션에서 자동 생성된 작업 추적(부분 유니크: 세션당 1건). **`waived`=청구 안 함(무료 처리)**(2026-07-06 — `setTaskWaived` 순수 토글, `total_price` 미변경). `is_invoiced=1`이면 waive 불가(`TASK_LOCKED`).
- `deliverables(project_id→projects ON DELETE CASCADE, title, version, kind, storage_backend[drive|local],
  file_id, file_name, file_size, mime_type, access_token?, expires_at?, download_count, revoked, note)`
- `invoices(project_id?→projects SET NULL, payer_id?→parties SET NULL, payer_snapshot?[발행 시점 청구처 JSON], title, amount, paid_amount,
  invoice_number?, tax_amount, discount_amount, status[미발행|발행], tax_status[계산서 미발행|계산서 발행|입금완료], issued_date?, due_date?, memo)` —
  돈=정수(원), 부분납은 코드 파생(연체는 2026-07-27 폐지). `amount`는 VAT 포함 총액. **청구처(payer)=`parties.id`**(당사자 모델). **`payer_snapshot`=발행 시점 청구처 정보 스냅샷 JSON**(상호·사업자번호·대표·주소·이메일·담당자) — 이후 클라이언트 정보가 바뀌어도 과거 청구서 표시·거래명세서 PDF가 발행 당시 정보로 고정(회계 정확성). 표시·PDF는 스냅샷 우선(`payerView`/`payerName`), NULL(레거시)이면 실시간 party 폴백. 발행 시 `snapshotPayer(payerId)`로 저장(from-tasks·수동 양 경로). **`email_overridden`**(2026-07-27)=청구서 건별로 발행 이메일을 party의 실제 이메일과 다르게 지정했다는 플래그 — 참이면 `payerSnapshotChanged`가 이메일 필드를 비교 대상에서 빼고 `refreshedPayerSnapshot`(새로고침)도 이 이메일을 보존한다. **상태 2축 분리**: `status`=청구서 발행 축(미발행/발행), `tax_status`=계산서·입금 축(계산서 미발행/계산서 발행/입금완료). 두 축 독립(자유 선택). `입금완료` 선택=완납(입금액=총액). 채번은 어느 축이든 발행되면. `discount_amount`=청구서 할인(공급가 차감, `invoiceAmountsFromSupply`로 과세표준·VAT 재계산).
- `invoice_items(invoice_id→invoices CASCADE, task_id?→track_tasks SET NULL, session_id?→sessions SET NULL, track_title, task_type,
  description, quantity, unit_price, amount, item_date?)` — 청구서 라인아이템 스냅샷.
  🔒 **세션 라인은 근거별로 쪼갠다**(2026-07-26 사용자 지시 — '청구서에 근거를 항목별로 분리 표시'): 한 세션이 **기본가 / 초과 N시간 / 할증** 최대 3라인. 첫 라인만 기존 형식(`날짜 · 아티스트 · 항목명`)을 유지해 목록·PDF·매출 집계가 종전대로 읽히고, 뒤 라인은 `초과 시간 · 1시간 30분 초과 → 2시간 × ₩100,000`처럼 근거를 그대로 쓴다. **금액을 조정했으면(입력값 ≠ 산정치, 또는 확정 청구액) 쪼개지 않는다** — 조정된 총액을 기본가·초과로 되쪼개면 거짓 근거가 되므로 한 줄 + 조정 사유가 근거로 들어간다.
  ⚠️**라인이 늘어난 다운스트림 정합**(라인 수가 곧 항목 수가 아니게 됐다): 한 세션의 모든 라인이 **같은 `session_id`·`item_date`**를 가져야 청구 잠금(`isSessionInvoiced` EXISTS)·삭제 시 미청구 복원·날짜 정렬이 성립한다. 라인 합 = 청구서 공급가는 그대로. `revenue.attachWorkSummary`의 `item_count`는 **라인 수가 아니라 일(작업·세션) 수**로 센다(안 고치면 세션 하나가 매출 상세에서 '3개 항목'으로 접혀 아티스트·세부가 사라진다). 회귀 `test/invoice-charge-breakdown.test.js` 7. **`item_date`**(2026-07-05 사용자 요청 — '청구 항목도 세션 탭처럼 항상 날짜순')=정렬용 날짜 스냅샷: 세션 라인=`session_date`(실제 일정), 작업 라인=작업 생성일(작업 자체엔 일정 개념이 없어 `created_at` 앞 10자로 근사). `computeInvoiceDraft`가 라인 생성 시 채우고 `items.sort()`로 미리 정렬(초안·미리보기 PDF도 반영), `listInvoiceItemsForInvoice`가 `ORDER BY (item_date IS NULL), item_date ASC, id ASC`(날짜순, 값 없는 레거시/원본삭제 라인은 뒤로). 1회 백필 `invoice_item_date_backfill_v1`(기존 라인을 task_id/session_id로 역참조해 채움 — 참조가 SET NULL된[원본 삭제] 라인은 복원 불가해 NULL 유지).
- `sessions(project_id→projects CASCADE, session_type[녹음|믹싱|마스터링|기타], session_date,
  all_day[0|1·종일=하루 종일·시간 없음], start_time?, end_time? "HH:MM"(종일이면 둘 다 NULL), booker_name?, engineer_name?(**첫 엔지니어 이름 — 다대다는 `session_engineers`**), status[예정|완료|취소], waived[0|1], memo,
  rate_item_id?→rate_items SET NULL, billing_amount?[확정 청구액 — NULL이면 단가표 자동 산정], room_id?→rooms SET NULL, director_contact_id?(→contacts, 담당 디렉터 **레거시**·다대다는 `session_directors`), gcal_event_id?)` — 스튜디오 일정. **`waived`=청구 안 함(무료 처리)**(2026-07-06 — `setSessionWaived` 순수 토글). 청구된(`invoice_items` 참조) 세션은 waive 불가(`SESSION_INVOICED`).
- `session_directors(session_id→sessions CASCADE, contact_id→contacts CASCADE, PRIMARY KEY(session_id, contact_id))` — **세션 담당 디렉터 다대다**(한 세션에 고객측 디렉터 여러 명). `sessions.director_contact_id`(첫 디렉터)는 레거시 동기화 보존. `setSessionDirectors`/`listSessionDirectors`/`resolveDirectorIds`, 기존 단일 디렉터는 `session_directors_backfill_v1`로 1회 복사.
- `session_engineers(session_id→sessions CASCADE, manager_id→project_managers CASCADE, worker_rate, worker_paid, worker_paid_date?, PRIMARY KEY(session_id, manager_id))` — **세션 담당 엔지니어 다대다**(2026-07-05, 한 세션에 스튜디오측 엔지니어 여러 명). 디렉터와 달리 자유 텍스트 등록이 없어(담당자 마스터에서만 선택) 세션 폼은 select 반복 행 + `+ 담당 엔지니어 추가하기`(template 복제·✕ 제거, app.js 위임). `sessions.engineer_name`(첫 엔지니어)은 레거시 동기화 보존. `setSessionEngineers`/`listSessionEngineers`/`resolveEngineerAssignments`(존재하지 않는 id만 제외·비활성 담당자는 유지·dedup. 옛 id-only 래퍼 `resolveEngineerIds`는 소비처 0으로 2026-07-27 제거), 기존 단일 엔지니어는 `session_engineers_backfill_v1`로 1회 복사(이름이 담당자 마스터와 유일 일치할 때만). **세션 외주 정산(`worker_rate`/`worker_paid`/`worker_paid_date`, 2026-07-06 사용자 상담)**: track_tasks와 동일 구조로 세션도 외주 지급 관리 — 세션 폼의 엔지니어 행마다 **외주(data-external=1)일 때만 지급단가 칸 노출**(`engineerRow`, app.js 위임 토글, 작업 폼 `engineerSelect`와 동일 규칙), 제출은 `engineer_ids[]`/`engineer_rates[]` 인덱스 페어링(`resolveEngineerAssignments`). **`setSessionEngineers`는 더 이상 DELETE+재삽입이 아니라 diff 갱신**(2026-07-06 수정 — 이전 방식은 매 저장마다 `worker_paid`/`worker_paid_date`가 초기화되던 버그: 계속 배정된 엔지니어는 단가만 갱신하고 지급상태 보존, 제거된 엔지니어만 삭제, 새 엔지니어만 `worker_paid=0`으로 삽입). 정산 조회·처리는 `listSessionPayoutsForWorker`(session_engineers에 실제 배정된 것만 — 레거시 `engineer_name` 폴백 매칭은 지급단가가 없어 정산 대상 제외)·`setSessionEngineerPayout`, `/workers/:id?tab=payout`에 작업과 함께 노출(`POST /workers/:id/session-payout/:sessionId`), 목록 카드 미지급 요약·`payout-all` 일괄 처리도 세션 포함.
  `booker_name`(예약 담당자)·`engineer_name`(담당 엔지니어)은 둘 다 담당자 마스터에서 선택(별개 역할).
  `rate_item_id`는 녹음 세션 시간제 자동 산정용 단가표 연결. `room_id`는 룸별 겹침 검사 단위(IFNULL 0으로 가상룸 처리). `gcal_event_id`는 자동 생성한 구글 캘린더 일정 id(수정·삭제 추적).
  **`surcharge_key`/`surcharge_memo`**=레거시(2026-07-27 할증 폐기 — 읽는 코드 없음). **`billing_memo`**=금액을 산정치와 다르게 조정한 사유(청구 화면 입력 → 청구 라인 근거로 스냅샷). 촬영 3구간은 `session_segments`(위 참조).
- `admin_state(key, value)` — drive folder_id·refresh token(암호화)·테마 캐시·`studio_calendar_id`(스튜디오 캘린더)·`studio_location`(기본 장소)·~~`studio_hours`~~(운영시간 — **2026-07-27 설정 자체 제거**[사용자 결정]. 예약 시작 그리드가 날짜·시간 콤보로 바뀐 뒤 저장값을 읽는 화면·검증이 하나도 없어 **바꿔도 아무 일이 없는 칸**이었다. 세션 시간은 자유 입력이 맞고[야간·초과 세션이 실제로 있다] 검증을 붙이면 정상 예약을 막는다 → 필요해지면 그때 만든다. `admin_state`의 옛 행[`studio_hours_start/end`]은 지우지 않았다)·`studio_biz_*`(공급자 세금정보, 거래명세서 PDF용, 평문)·`studio_logo`(거래명세서 로고, base64 data URI)·`alert_webhook_url`(알림 웹훅, 암호화)·**`default_booker`**(기본 예약 담당자 이름, 세션 폼 기본 선택).
- `payments(invoice_id→invoices CASCADE, amount, paid_on 'YYYY-MM-DD', memo?, created_at)` — **입금 이력**(부분납 여러 건). `invoices.paid_amount`는 **`SUM(payments.amount)` 파생 캐시**(add/deletePayment·recomputePaid가 유지; 기존 조회부·잔금·납입상태 파생 무변경). 입금 처리=이력 **건별 추가**(누적), 완납=잔금 전액 1건, '입금완료' 선택=잔금 자동 1건. 이력 행 **삭제** 시 재계산 + 잔금 생기면 입금완료→계산서 발행 강등(삭제 중심 관리). 마이그레이션 `payments_backfill_v1`이 기존 `paid_amount`>0을 이력 1건으로 백필(이력 없는 인보이스만). 헬퍼=`listPayments`/`addPayment`/`deletePayment`/`recomputePaid`(`src/data/invoices.js`), UI=`paymentHistory`(views.invoices.js·전체화면 전용). **입금 이력에 날짜 개념 없음(2026-07-05 사용자 결정)**: 입금일 입력·이력 행 날짜 표시 제거 — 행 라벨=메모(없으면 '입금'), `paid_on` 컬럼은 레거시 잔존(서버 addPayment가 기본 오늘 기록·표시 안 함). **전체화면(`/invoices/:id`) 레이아웃(2026-07-05 사용자 요청, 2026-07-08 순서 재배치 — 사용자 '헷갈린다')**: **청구처 정보 최상단**(분류 행 삭제 — 세금계산서 기입에 안 쓰는 부가 메타) → **통합 카드 하나**(**청구번호 → 발행일 → 프로젝트 → 청구 항목 → 소계·할인·VAT·총액** 순 — 영수증식으로 합산이 항목 아래. '공급가'(항목 헤더 우측)는 삭제하고 항목 아래 **'소계'** 행으로 이동, 총액은 `text-base font-semibold` 강조, 금액 블록 래퍼는 항목 있을 때만 `border-t`[중복 선 방지]. **행 사이 줄 제거(2026-07-08 후속)**: row() 자체의 border-b 삭제 — 청구번호·발행일·프로젝트 사이, 소계·할인·VAT·총액 사이엔 줄 없음, **청구 항목 목록 사이 줄도 제거**(invoiceItemsSection 항목 행 border-b 삭제), 구분선은 그룹 경계(메타 그룹 border-b / 금액 그룹 border-t)만. **상세 헤더 정리 + 클릭 복사(2026-07-09 사용자 요청)**: pageHeader의 상태 배지(우상단 라벨)·제목 밑 클라이언트명(desc) 제거(상태는 목록 배지·처리 토글로 충분, 청구처는 아래 카드), **청구 항목 설명·항목 금액·소계·할인·VAT·총액 전부 클릭 복사** — 금액은 `copyable(String(원값), {display: formatKRW})`로 **숫자만 복사**(홈택스 붙여넣기용, ₩·콤마 없음)) **세션 청구 라인 형식 = "7월 25일 · 차가을 · 보컬녹음"(2026-07-08 사용자 요청, · 구분)**: 이전 `{session_type} 세션 {날짜} · {단가항목} ({소요})`에서 **'세션' 접두·소요시간 제거** → `날짜 · 프로젝트아티스트 · 단가항목명`(`computeInvoiceDraft` — 거래명세서 품목명 동일, 기존 발행분 스냅샷은 불변). 청구 생성 폼 세션 체크박스 라벨(`unbilledInvoiceForm`)도 동일 형식. (`invoiceItemsSection` — 카드 래퍼 없는 내부 섹션, border 없음[2026-07-06 사용자 리포트 '프로젝트랑 청구 항목 사이 줄이 2개' — 바로 위 항목의 `row()` border-b와 겹쳐 보이던 것, 자체 border-t 제거로 1선 통일] → **PDF 발행 + 계산서·입금 처리를 한 줄에**(2026-07-06 사용자 요청 — 이전엔 각각 별도 줄·border-t로 분리: 왼쪽=PDF 발행 3종 항상, 오른쪽=**계산서·입금 처리 토글 버튼 2개**(canInvoice — [(계산서|현금영수증) 발행 완료]·[입금완료], 청구 목록 카드와 동일 `taxToggleButtons` 공용 헬퍼·select 폐기, 2026-07-05)) → 메모 → 삭제.

## 자료 전달 아키텍처 (플레이북1 §2.3·§4.3)

- **스토리지 추상화(`src/storage.js`)**: Drive 연동 시 Drive, 미연동 시 로컬 디스크(`config.uploadsDir`).
  SQLite 어댑터와 같은 폴백 전략 → Google 자격증명 없이도 전체 흐름 로컬 검증 가능.
- **업로드**: multer **디스크 스토리지**(메모리 금지, OOM 방지) → `storage.put` → 임시파일 정리.
  multipart 파일명은 latin1 → UTF-8 복원(한글 보존).
- **비공개 프록시 스트리밍**: 파일은 공개 URL 없음. 인증 다운로드 `/deliverables/:id/raw`(범위 강제) +
  공개 링크 `/d/:token`(로그인 불필요). 둘 다 백엔드가 프록시.
- **만료 토큰 링크**: 난수 `access_token`, `expires_at` 만료, `revoked` 철회, `download_count` 추적.
  게이트(`tokenGate`)가 존재/철회/만료를 검사.

## 환경변수

| 키 | 용도 |
|---|---|
| `ADMIN_EMAIL` | 부트스트랩 치프(chief) Google 이메일. 최초 로그인 시 자동 생성·chief 보장. 대표·스태프는 치프가 `/settings`에서 등록 |
| `STUDIO_DRIVE_EMAIL` | **자료 저장 Drive 고정 계정**(기본 `studio@omgworks.kr`). **이 이메일로 로그인할 때만** Drive refresh token 저장 → 누가/어떤 역할로 로그인하든 자료는 항상 이 계정 Drive 한 곳에만. 치프가 바뀌어도 고정. UI로는 못 바꾸고 이 env로만 변경. `/auth/google?drive=1`(설정 연결 버튼)이 계정 선택기 강제+login_hint로 이 계정 선택 유도 |
| `SESSION_SECRET` | JWT 서명 |
| `TOKEN_ENC_KEY` | AES-256-GCM 키 파생(비밀 암호화) |
| `GOOGLE_CLIENT_ID`/`SECRET` | OAuth(로그인 + Drive + Calendar + **People/연락처**). scope: `openid·email·profile·drive.file·calendar·contacts`(연락처 동기화는 GCP People API 활성화 + 치프 재로그인 필요) |
| `BASE_URL` | 외부 URL. **우선순위: BASE_URL > RENDER_EXTERNAL_URL(Render 자동 주입) > 로컬**(2026-07-14 수정 — 이전엔 자동 주입이 우선이라 커스텀 도메인을 붙여도 모든 외부 링크[OAuth redirect·자료 전달 공개 링크·청구 알림·캘린더 링크]가 onrender.com으로 나갔다. `test/config-baseurl.test.js` 회귀 잠금·커스텀 도메인 절차=DEPLOY §4.6) |
| `PORT` / `DB_PATH` / `MAX_UPLOAD_MB` | 서버/DB/업로드 |
| `DEV_LOGIN` | =1 시 `/dev-login` 활성(로컬 검증용, **프로덕션 금지**) |
| `BACKUP_TOKEN` | cron이 `POST /internal/cron/daily`(백업·Drive 오프사이트·감사 로그 보존)를 트리거하는 인증 토큰. 미설정 시 라우트 비활성(404). web·cron 동일값 |
| `CRON_TRIGGER_URL` / `WEB_HOSTPORT` | (cron 서비스) 트리거 대상 web URL. `WEB_HOSTPORT`는 Render `fromService hostport` 자동 주입 |
| `GOOGLE_PLACES_API_KEY` | (선택) 장소 주소 자동완성 — Places API(New) 키. 세션 외부 장소 주소칸 타이핑 시 서버가 `/sessions/place-suggest`로 프록시 호출(CSP 무변경·키 미노출). 미설정 시 자동완성 비활성(자유입력만·캘린더가 지오코딩). **제안 목록이 열린 채 Enter는 폼 제출(세션 추가)로 새지 않는다** — 하이라이트된 제안을 선택하거나(없으면) 목록만 닫고 타이핑 텍스트 유지(app.js `[data-place-suggest]` keydown, IME 가드) |
| `ALERT_WEBHOOK` | (선택) 알림 웹훅 URL 운영 오버라이드. 미설정 시 `/settings` 환경설정에서 암호화 저장한 값 사용(Slack/Discord 등) |

프로덕션(`NODE_ENV=production`)에서는 `ADMIN_EMAIL`, 강한 `SESSION_SECRET`/`TOKEN_ENC_KEY`,
Google OAuth 자격증명이 없거나 `DEV_LOGIN`이 켜져 있으면 서버가 시작되지 않는다.

## 빠진 함정 (다음에 또 밟지 말 것)

> **가드레일 기준 분류(2026-07-04 재정리)** — 함정마다 상태 태그. **번호는 코드 주석·가드 테스트가 참조하므로 불변.**
> - 🔒 **기계 가드**: 재발하면 `npm test`가 실패(가드 ID는 #21 목록 — B=guardrails.test.js·U=guardrails-ui.test.js): **#14**(B⑤)·**#18**(U⑪+상호작용⑤)·**#19**(U⑨·B①)·**#25**(B⑤-b)
> - ✅ **구조 차단**: 코드가 원천 차단(재발하려면 그 코드를 지워야 함): #2·#4·#6·#7·#8·#11·#12·#15·#20·#24
> - 📏 **관례**: 기계화 불가·리뷰로 지킴: #1·#13·#17·#22·#23·#28
> - 🧪 **검증 요령**: 검증·CI 작업 방식: #3·#5·#9·#10·#16
> - 📜 **정책**: #21 — **같은 실수 2회 → 🔒로 승격**(가드 전체 목록·신설 기준). 새 함정 추가 시 태그를 붙이고, 🔒 승격되면 태그 갱신.

1. 【📏 관례】**인증 게이트 → static 순서**(§3-1). 보호 HTML을 정적 파일로 두지 말 것.
2. 【✅ 구조】**better-sqlite3 네이티브 빌드**: 최신 Node(예: 26)에서 컴파일 실패 가능 →
   `optionalDependencies` + `node:sqlite` 폴백(`src/sqlite.js`). Render(Node 20/22)는 prebuild 사용.
3. 【🧪 검증】**헤드리스 full-page 스크린샷은 폭을 잘못 렌더**(플레이북2 §6) → CDP `setDeviceMetricsOverride`로 측정.
4. 【✅ 구조】**OAuth state로 next 전달** 시 base64url + open-redirect 방지(`safeNext`, 내부 경로만, 역슬래시 우회 차단). **OAuth state에 랜덤 논스** 포함 + httpOnly 쿠키 대조로 로그인-CSRF 차단.
5. 【🧪 검증】**유휴 백그라운드 서버가 포트 점유** — 이전 검증 세션의 `node src/server.js`가 살아 있으면 새 서버가
   바인딩 실패하고 **옛 코드가 응답**. 검증 전 `pkill -f "src/server.js"`로 정리하고 단일 프로세스 확인할 것.
6. 【✅ 구조】**multipart 파일명 latin1** → `Buffer.from(name,'latin1').toString('utf8')`로 한글 복원.
7. 【✅ 구조】**동일 출처 POST 검사 + CSP upgrade-insecure-requests**: 로컬 http에서 브라우저가 폼 제출 Origin을
   `https://`로 올려 보내 `req.protocol`(http)과 불일치 → 403. `server.js sameOriginRequest`를
   **Sec-Fetch-Site 우선 + host(프로토콜 무시) 비교**로 변경(외부 host는 여전히 차단해 CSRF 방어 유지).
8. 【✅ 구조】**정적 자산 캐시 버스팅 필수**: `/css/app.css`에 버전을 안 붙이면 배포해도 브라우저가 옛 CSS 캐시를 재사용해
   레이아웃이 깨져 보인다. `views.js ASSET_VERSION`(mtime+size) → `?v=`로 해결.
9. 【🧪 검증】**한글 쿼리스트링 인코딩**: curl로 `?kind=아티스트` 같은 한글 쿼리를 **인코딩 없이** 보내면 서버가 다른
   문자열로 받아 필터가 빈 결과(코드 버그 아님). 검증 시 `--data-urlencode` 또는 `-G --data-urlencode` 사용.
10. 【🧪 검증】**메인터넌스는 브라우저 헤더로 E2E**: curl이 아니라 Sec-Fetch-Site·Origin·multipart 헤더로 폼 제출까지 검증할 것.
11. 【✅ 구조 + 🔒 가드(ssrf.test.js)】**웹훅 SSRF**: 알림 웹훅 URL을 그대로 fetch하면 내부망 공격 가능. `notify.js`에서 DNS 해석 후 사설IP 대역(`10.`, `172.16-31.`, `192.168.`, `127.`, `169.254.`, `::1`, fc00::/7[fc·fd], fe00::/8) 차단 후 전송. **IPv4-mapped IPv6(`::ffff:127.0.0.1`)는 `::ffff:` 제거 후 IPv4 패턴 재검사로 매핑 우회 차단**. **2026-07-11 감사 테스트로 ULA 결함 발견·수정**: fc00::/7 패턴이 `f[ce]`(fc·fe)라 실무에서 흔한 **fd 대역(fd00::/8)을 통째로 놓치던 SSRF 갭** → `f[cde]`로 교정(fc·fd·fe). `test/ssrf.test.js`가 대역 매트릭스·매핑 우회·경계값을 회귀 잠금. OAuth 콜백은 `verified_email=false` 계정 거부.
12. 【✅ 구조】**로고 업로드 매직바이트 검증**: Content-Type 헤더만 믿으면 안 됨. PNG(`\x89PNG`)·JPEG(`\xFF\xD8\xFF`) 매직바이트를 서버에서 확인 후 거부.
13. 【📏 관례】**Tailwind opacity.12 미등록**: `badge-*`(`bg-*/12` 같은 불투명도 변형 클래스)를 Tailwind 커스텀 색과 함께 쓸 때 opacity 스케일에 `12`가 없으면 CSS 빌드에서 제거된다. `tailwind.config.js`의 `theme.extend.opacity`에 `'12': '0.12'` 추가.
14. 【🔒 가드 B⑤】**AJAX 폼 전송은 `URLSearchParams`(urlencoded)로**: 서버 미들웨어가 `express.urlencoded`+`express.json`만 등록(multipart 파서 없음, multer는 파일 업로드 전용 라우트만). `fetch(url, { body: new FormData(form) })`는 **multipart/form-data**로 나가 파싱되지 않고 **req.body가 빈 객체** → 라우트가 기본값으로 저장(작업 자동저장이 작업 종류·담당을 기본값으로 덮어쓰던 근본 원인). 자동저장·AJAX POST는 `new URLSearchParams(); fd.forEach((v,k)=>p.append(k,v))`로 변환해 보낼 것.
15. 【✅ 구조】**CI `node --test`의 `**` 글롭은 Node 22+ 전용**: `node --test "test/**/*.test.js"`는 Node 20에서 글롭을 확장하지 못해 "Could not find …" 로 **테스트 0개 실패**(로컬 최신 Node에선 통과해 안 보임). 셸이 확장하는 `node --test test/*.test.js`로 고정(현 플랫 `test/` 구조 기준 결정적 40개). 향후 중첩 디렉터리 도입 시 재검토.
16. 【🧪 검증】**CI 잔여 deprecation 경고(DEP0040 punycode·DEP0169 url.parse)는 `actions/setup-node` 자체 발**: 로그상 `Use Node.js`/`Post Use Node.js` 단계에서만 나오고 `Install/Run tests/Build CSS`엔 없음 → 우리 앱 코드·의존성이 아니라 **액션 번들 코드(러너 Node 24)** 가 냄. `setup-node@v5`(최신)라 우리 쪽 업그레이드로 못 없앰 = **upstream 이슈**. 빌드는 success(무해 annotation). **의도적으로 그대로 둠**(2026-07-01 결정) — `NODE_NO_WARNINGS=1` 전역 억제는 우리 코드의 실제 경고까지 가리므로 채택 안 함. setup-node 상위 수정 시 자연 소멸.
17. 【📏 관례】**모바일 목록 행: 배지는 제목 *아래* 줄에, `.badge`는 `whitespace-nowrap`**: 배지(상태·분류)를 제목 *앞에* 인라인으로 두면 좁은 화면에서 배지가 가로폭을 먹어 제목이 과하게 잘리고, 공백 있는 라벨("계산서 미발행"·"소속사/레이블")은 배지 안에서 2줄로 쪼개진다. → (a) `.badge` 베이스에 `whitespace-nowrap shrink-0`(공백 라벨 줄바꿈·찌부 방지, `src.css`), (b) 목록 행은 **제목 전폭(첫 줄) → 배지 줄(`flex flex-wrap gap-1`) → 클라이언트/메타** 순으로 쌓기(청구 `views.invoices.js`·클라이언트 `clients.routes`·연락처 `contacts.routes`). 세션 행(`views.sessions.js`)의 `flex-wrap`+단일 배지가 모범. 375/390px 검증 완료(2026-07-02).
18. 【🔒 가드 U⑪+상호작용】**한글 IME 조합 중 keydown(엔터/방향키) 무시 필수**(2026-07-03): 검색 콤보·모달의 keydown 핸들러가 **한글 조합 중 엔터**(예: "한" 조합 확정용)를 선택·등록·제출로 오인하면, 한 번의 엔터가 [모달 열림 → 항목 등록 → 폼 제출]로 **연쇄 오작동**하고 마지막 글자가 중복("김조한한")된다. macOS 한글 특히 취약. → 모든 콤보/모달 keydown 맨 앞에 **`if (e.isComposing || e.keyCode === 229) return;`** 가드(app.js: `comboKbdNav`·artistCombo 인라인·아티스트/회사/담당자 모달 keydown 5곳). 조합 중 엔터는 그냥 통과시켜(preventDefault 안 함) 브라우저 IME가 조합만 확정하게 두고, 조합 끝난 뒤의 엔터에서만 콤보 동작.
19. 【🔒 가드 U⑨·B①】**검색 콤보 보이는 입력칸은 name 없이 + 숨김 필드로 제출**(2026-07-03): 콤보의 보이는 `<input>`에 `name`이 있으면(특히 `contact_name`·`*_company` 등 Chrome 자동완성 카테고리[name·organization]와 매칭) `autocomplete="off"`를 무시하고 **Chrome 네이티브 자동완성 팝업이 앱 드롭다운을 덮는다**. → 보이는 입력은 `data-*-input`만(name 제거), 값은 **숨김 `<input name=…>`** 로 제출하고 app.js가 입력 이벤트로 동기화(personCombo `data-pc-name-hidden`·payerCombo hidden·companyCombo `data-cc-hidden`). artistCombo(`name="artist"`)는 자동완성 카테고리 없어 그대로 둠. **주의: `fillAgency` 등 `input[name="artist_company"]`로 찾던 코드는 이제 숨김 필드를 잡으므로, 보이는 칸도 함께 세팅해야 표시됨**.
20. 【✅ 구조】**새 party는 `party-created` 전역 이벤트로 모든 콤보에 브로드캐스트**(2026-07-03): 어느 콤보/모달(아티스트·회사·담당자·소속사 미니콤보·대표자 미니콤보)에서 사람/회사를 새로 만들면, 그 party가 **다른 콤보의 로컬 옵션(opts)에는 없어** 재검색 시 '새로 등록'이 다시 떠 중복 생성되던 문제가 반복됨. → 생성 성공 시 `announceParty({kind,id,name,...})`가 `document`에 `party-created` CustomEvent를 dispatch, 각 콤보가 init 때 리스너를 달아 자기 관련 kind면 옵션에 push(dedup). 개별 `opts.push` 땜질을 **한 메커니즘으로 통일** — 어느 곳에서 만들든 페이지의 모든 콤보가 즉시 인식(app.js `announceParty`·콤보별 party-created 리스너 6곳). fillAgency의 `__ccOpts` 노출도 이 흐름과 함께 동작.
21. 【📜 정책】**반복 실수는 가드레일 테스트로 승격**(`test/guardrails.test.js`, 2026-07-04 — 사용자 '아예 무결하게' 요청): 같은 실수 클래스가 **두 번** 나오면 조심하기가 아니라 **기계 검사**를 추가한다. 현재 가드 8종: ①**보이는 `<input>`에 bare `name="name|company|address"` 금지**(Chrome이 autocomplete=off를 무시하는 자동완성 카테고리 — 단가 항목명·콤보에서 재발. 전 폼 개명 완료: `party_name`/`worker_name`/`user_name`/`room_name`/`rate_name`/`biz_address`, 핸들러는 구명 폴백) ②**datalist 허용목록**(`contact-artist-clients`만 — 콤보 통일 후 구식 필드 잔존 방지) ③**'녹음' 하드코딩 비교/IN 금지**(config `RENTAL_SESSION_TYPES`/`RENTAL_IN`만) ④**에러코드 메시지 맵 ↔ 실제 구현 교차검증**(TASK_AMOUNT_REQUIRED가 문구만 있고 throw 없던 잠복 결함 재발 방지) ⑤**app.js fetch body FormData 금지**(함정 #14) ⑥**personCombo 기본 companyOptions 임베드**(미전달 호출도 모달 회사 검색 동작 — 디렉터·대표자에서 ×2 재발했던 클래스) ⑦**updateParty 부분 갱신 계약**(미전송=보존·빈 문자열=비움 — 아래 참조) ⑧**모달 배경 클릭 닫기의 mousedown 확인**(함정 #25 — 그룹·아티스트·업체·담당자 4개 모달에서 동시 재발했던 클래스) ⑨**외부 API 호출 메서드 실존**(people.js의 `people.people.<name>(` 호출을 googleapis People v1 클라이언트 표면과 대조 — 2026-07-09 감사: party 리네임 스윕이 Google API 호출부까지 createPerson/updateParty/deleteParty로 바꿔 TypeError가 fail-safe catch에 무음 흡수, 연락처 푸시가 통째로 죽은 채 은폐됐던 사고). **UI(프론트) 가드 9종**(`test/guardrails-ui.test.js` — 서버 렌더 HTML↔app.js는 data-* 마커로만 연결돼 어긋나도 조용히 죽는 구조라, 브라우저 없이 양쪽 소스의 **계약**을 검사): ⑧**data-* 마커 계약**(app.js가 찾는 모든 마커[셀렉터·getAttribute·동적 접두 `data-rate-opts-${k}` 포함]는 서버 템플릿/app.js HTML 어딘가에서 렌더돼야 — 양방향 리네임 드리프트를 한 검사로. 도입 첫 실행에서 죽은 참조 5건[레거시 직접입력 토글·구 클라이언트 유형 토글 IIFE] 검출·제거) ⑨**콤보 보이는 입력(data-pc/cc/pk-input) name 금지**(함정 #19 기계화) ⑩**CSP 계약**(인라인 핸들러·javascript:·인라인 script 금지 — CSP에선 배포에서만 조용히 죽는 최악 드리프트) ⑪**IME 가드 강제**(Enter/방향키 keydown 핸들러는 isComposing 필수 — 함정 #18 기계화) ⑫**금액칸↔MONEY 정규식 계약**(금액성 name은 콤마 포맷터에 매칭돼야) ⑬**personCombo 옵션 JSON 키↔app.js 소비 정합**(워터멜론 alt·회사검색 company·호칭 회귀 잠금) ⑭**companyCombo 사람 옵션 키(kind/alt/honorific)↔app.js dispOf·자동채움(__pcSetById) 계약**(제작/운영 개인 병기 — ⑬과 동일 드리프트 클래스, 2026-07-05) ⑮**서버 렌더 인라인 style 치수·레이아웃 금지**(함정 #27 — CSP style-src에 막혀 조용히 무시, `dataTable`의 `<col style="width">`가 안 먹어 6열 균등 분배로 렌더되던 사고. 이메일 HTML·PDF SVG는 예외, 2026-07-16) ⑯**사용자 노출 문자열에 '클라이언트' 금지**(2026-07-17 사람/조직 축 정리 — 화면 용어를 '업체·그룹'/'청구처'로 통일하기로 결정한 뒤 회귀를 리뷰가 아니라 기계로 잡기 위해 신설. 대상 파일의 코드를 블록 주석 제거 후 줄 단위로 훑어 문자열 리터럴[`"`/`'`/`` ` ``] 안의 '클라이언트'만 위반 처리, 코드 식별자·주석은 예외). **상호작용 계층(jsdom, `test/ui-interactions.test.js`)**: 정적 계약이 '연결'을 보증한다면 이건 '동작'을 검증 — 실제 app.js를 jsdom에서 실행해 금액 캐럿·콤보 픽/모달·IME·옵션 스왑·dirty를 상호작용으로 확인(테스트 작성 팁: 드롭다운 하이라이트는 MutationObserver라 타이핑→Enter 사이 `await tick()` 필요).
22. 【📏 관례】**폼의 첫 submit 버튼 = 엔터(암묵 제출) 기본 버튼**(2026-07-05): 미리보기·보조 액션 버튼(`formaction`/`formmethod`)이 주 버튼보다 DOM에서 앞서면 텍스트 필드 엔터가 그 버튼을 누른다(청구 생성 폼에서 필드 엔터가 견적서 PDF를 열던 사례). 보조 submit이 앞서는 폼은 맨 앞에 `<button type="submit" disabled hidden aria-hidden="true"></button>` sentinel — 표준상 기본 버튼이 disabled면 암묵 제출 무동작(엔터=필드 확정만, JS·IME 무관). `requestSubmit()`·명시 클릭은 영향 없음.
23. 【📏 관례】**dirty-form 감시는 input/change만 본다 — 행 추가·제거(클릭)는 합성 이벤트로 알려야**(2026-07-05, 세션 담당 엔지니어 다대다 구현 중 발견): `[data-dirty-form]` 감시(`public/js/app.js`)는 `document`에 `input`/`change`만 위임 등록 — 반복 입력 행(디렉터·엔지니어 등)을 버튼 클릭으로 **제거**하면 폼 상태가 실제로 바뀌는데도 dirty로 안 잡혀 저장 가드가 조용히 안 뜬다. 행 추가는 빈 선택(placeholder)이라 무해하지만, **행 제거처럼 클릭으로 실데이터를 바꾸는 조작은 `form.dispatchEvent(new Event("change", {bubbles:true}))`로 dirty 감시에 수동 통지**(세션 엔지니어 remove 핸들러 참조). 클릭으로 폼 데이터를 바꾸는 새 UI(행 삭제·토글 등)를 추가할 때마다 이 통지를 빼먹지 않을 것.
24. 【✅ 구조】**`data-no-guard`는 커스텀 모달만 우회할 뿐, 브라우저 네이티브 `beforeunload` 프롬프트는 별개 안전망이라 안 막는다**(2026-07-05, 프로젝트 '취소' 버튼 구현 중 실브라우저 E2E로 발견): 네비게이션 가드는 두 층 — ①클릭 가로채기(커스텀 저장/저장하지 않음 모달)와 ②`beforeunload`(브라우저 기본 "나가시겠습니까?")가 각각 독립적으로 `bypass` 플래그를 본다. `data-no-guard`만 걸고 `bypass`를 안 세우면, 클릭 핸들러는 통과시켜도 뒤이어 실제 이동이 일어날 때 `beforeunload`가 그대로 걸려 **의도한 '조용히 나가기'가 여전히 경고를 띄운다**(취소=이미 저장 안 함을 택한 동작이라 이중 확인이 사용자 의도와 모순). → `data-no-guard` 링크 클릭 시 클릭 핸들러가 **`bypass=true`(폼 제출과 동일하게 1초 타이머로 자동 해제)도 함께 세팅**해야 두 층 모두 통과한다(app.js 네비게이션 가드 클릭 핸들러 참조). 새로 '조용히 나가기' 성격의 링크(취소·건너뛰기 등)를 추가할 때 이 두 층을 다 확인할 것 — jsdom은 클릭 가로채기(①)만 검증 가능하고 ②는 실브라우저에서만 확인 가능하다(`test/ui-interactions.test.js` 네비게이션 가드 테스트 주석 참조).
25. 【🔒 가드 B⑤-b】**모달 '배경 클릭 닫기'는 click 이벤트의 target만 보면 안 된다 — mousedown도 배경에서 시작했는지 함께 확인**(2026-07-06 사용자 리포트 '이름 드래그로 전체선택 하는데 모달이 닫힘' — 그룹·아티스트·업체·담당자 4개 간이 등록 모달에서 동시 재발): `click` 이벤트의 target은 mousedown·mouseup의 공통 조상이라, 입력칸(안쪽)에서 텍스트 드래그를 시작해 마우스를 모달 배경 위에서 떼면 `e.target === modal`이 참이 돼 버려(mouseup이 배경이었을 뿐인데) '배경 클릭 닫기'가 오작동한다. → `mousedown` 리스너로 `mdOnBackdrop = e.target === modal`을 별도 추적하고, `click` 핸들러는 `e.target === modal && mdOnBackdrop`일 때만 닫는다(4개 모달 전부 동일 패턴 적용). 가드 테스트(`test/guardrails.test.js`)가 mousedown 확인 없는 옛 불안전 패턴 재등장을 차단, jsdom 상호작용 테스트(`ui-interactions.test.js`)가 드래그-아웃 시나리오(안 닫힘)와 진짜 배경 클릭(닫힘) 둘 다 잠금.
26. 【📏 관례】**Tailwind display 유틸(`flex`/`grid`/`inline-block` 등)이 붙은 요소는 `hidden` 속성·`el.hidden=true`로 안 숨겨진다**(2026-07-15 사용자 리포트 '클라이언트 실시간 필터가 안 먹고 전체 목록 그대로' — 목록 실시간 필터 첫 배포에서 재발): `[hidden]{display:none}`은 UA/preflight 스타일이라 **author 스타일인 display 유틸(같은 특정도, 소스 순서상 뒤)에 밀린다** → `.flex` 행에 `hidden`을 걸어도 `display:flex`가 이겨 안 사라진다. JS로 확실히 숨기려면 **인라인 `el.style.display="none"`**(인라인=특정도 1,0,0,0으로 클래스 규칙을 항상 이김; 복원은 `el.style.display=""`). display 유틸이 없는 요소(예: 빈 결과 안내 `block`)만 `hidden`으로 충분. ⚠️**jsdom은 CSS를 적용하지 않아** `el.hidden=true`가 '통과'하지만(속성만 반영·실제 숨김 미검증) 실브라우저에선 실패 — jsdom 테스트는 `.hidden`이 아니라 `el.style.display`로 검증할 것(이 버그가 jsdom 테스트를 통과해 배포됐다).
27. 【📏 관례】**서버 렌더 인라인 `style="…"`는 CSP `style-src`에 막혀 조용히 무시된다 — 폭·레이아웃은 CSS 클래스로**(2026-07-16 사용자 리포트 '표 오른쪽 여백·이메일 잘림' — `dataTable`의 `<col style="width:…">`가 안 먹어 6열 균등 분배로 렌더되던 근본 원인): CSP가 `style-src 'self' …`(unsafe-inline 없음)라 **서버가 HTML에 박은 `style=` 속성은 브라우저가 파싱조차 안 한다**(`el.style.width`가 빈 값). 반면 app.js의 `el.style.x=…`(JS 프로퍼티 세팅)는 style-src와 무관하게 동작 → '앱은 되는데 서버 렌더만 안 되는' 혼란. 그래서 `inv-table`·`proj-thead`는 처음부터 외부 CSS(`.inv-w-*`·grid-template-columns)로 폭을 준다. **새 표·레이아웃의 치수는 반드시 CSS 클래스로**(Tailwind 임의값 `w-[16rem]` 등 — 콜러가 **리터럴**로 넘겨야 Tailwind가 스캔·생성, 동적 `w-${x}`는 안 잡힘). `<col>`은 폭만 주면 숨겨도 폭을 예약하므로, 반응형 hide 시 col도 `hidden xl:table-column`으로 함께 숨겨 예약을 없앤다(셀은 `table-cell`, 콜은 `table-column`). 참고: JS로 토글하는 `style="display:none"`(inv-bulkbar)은 로드 시 app.js가 다시 세팅해 기능은 유지되나, **초기 상태를 인라인 style에 의존하지 말 것**.
28. 【📏 관례 + 🔒 계약(billing-radar)】**한 줄 flex에 고정폭 컨트롤을 여럿 넣으면 유연 라벨이 짜부라지고, 한글은 음절 단위로 쪼개져 세로로 폭발한다**(2026-07-23 사용자 리포트 — 청구 생성 폼 항목 행): 행이 `flex items-center`에 [체크박스 16 · 라벨(flex-1) · '청구 안 함' 버튼 ~66 · 금액칸 `w-28`(112)]을 다 담고 있었는데, 390px 화면에서 고정 폭들이 자리를 다 먹어 **라벨에 42px만 남았다**(실측). 한글은 CSS 기본값(`word-break: normal`)이 **CJK 음절 사이 어디서나 줄바꿈**이라 그 폭에서 글자별로 쪼개져 **행 높이가 221px**(정상 ~50px)로 부풀었다("7월/20일/·/프로미/스나인/·/플레/이백…"). → ①**컨트롤을 한 컨테이너로 묶어** 모바일은 둘째 줄 전폭(`w-full`)·`sm:w-auto`로 인라인 복귀, 행은 `flex-wrap items-start`(체크박스는 `mt-0.5`로 첫 줄 텍스트에 정렬) ②라벨에 **`break-keep`**(word-break:keep-all — 공백에서만 줄바꿈) ③컨트롤 줄은 `justify-between`(둘 다 오른쪽에 몰면 왼쪽이 텅 빈다). **같은 계열**: 전폭 버튼 스택(`grid-cols-1 sm:grid-cols-3`)은 짧은 라벨 3개(견적서·내역서·거래명세서)에 148px를 먹었다 → `grid-cols-3`으로 44px. 긴 버튼 라벨은 줄이고 `whitespace-nowrap`(안 그러면 '임시저\n장'). **결과: 폼 높이 1321→981px, 항목 박스 536→310px**(390px 실측). ⚠️**jsdom으로는 못 잡는다**(레이아웃 없음 — 함정 #26 계열) → 폭·높이는 **오프스크린 iframe 실측**(아래 검증법)으로 확인하고, 마크업 계약만 테스트로 잠근다(`test/billing-radar.test.js`).


## 검증 상태 (현재 운영 전제)

- **구글 캘린더 자동 연동**: 실제 동작은 사용자 사전작업 필요 — GCP Calendar API 활성화 + 치프 재로그인(scope `calendar`) + `/settings`에서 캘린더 선택 + 기본 장소 입력.
- **Drive 저장**: 최소 권한 `drive.file`로 앱 전용 폴더에만 저장(연동 시 storage.put=drive, 미연동=local). **폴더 구조**: 루트 `omg-studios-manager` 아래 `사업자등록증`·`통장사본`(클라이언트 첨부 kind별)·`deliverables`(자료 전달) — `drive.ensureSubfolder`/`storage.put({folder})`. `/settings`에 연동 상태 + 로컬→Drive 이관 버튼(`/settings/migrate-drive`·`storage-migrate.js`).
- 기능 검증은 `node:test` 579개·브라우저 E2E·CI(Node 20/22)가 상시 담당(각 세션 검증 이력=git·`HISTORY.md`).

## 다음 단계 TODO

- **자료 전달 본격 다듬기(2026-07-19 사용자 보류)**: 조만간 실사용 시작하며 UX·기능 본격 정비 예정. 지금은 v1(업로드→Drive/로컬→인증 다운로드 + 공개 만료 토큰 링크) 유지. 실제 사용 흐름 확인 후 착수.
- (보류) content_type/billing_type UI 노출 — 영상 구분·과금 유형 선택, 현재 미노출/강제(향후 확장 시).
- **다중 현재 소속(겸직) 지원**: 현재 소속 모델은 `ended_on IS NULL` 1건 가정(`closeCurrent` 이직 자동 종료)이나, 실무에선 동시 2개 소속·대표 겸직이 있음(데이터 감사에서 이중 현재 소속 1건 발견 — 오류 아니라 실제 겸직). 개선 시 `currentAffiliation`(단수) 소비처를 복수 허용으로 재설계 + UI(소속 타임라인·프로젝트 파생·청구처 담당자) 다중 표시. 그 전까지 이중 현재 소속은 유효 취급(감사 오탐 주의). 완료 항목은 git·`HISTORY.md` 참조.
### 세션 완료 이력
> 세션별 완료 이력 상세는 **git 커밋 메시지**와 **[`HISTORY.md`](HISTORY.md)**에 있다(2026-07-15 CLAUDE.md 150k자 한도 관리로 이관 — 이력이 계속 늘어 CLAUDE.md는 상시 참조 지침만 유지). 큰 줄기(주요 변경 이력 요약·🏁 v1.0 마일스톤)도 `HISTORY.md`로 이관됨.
