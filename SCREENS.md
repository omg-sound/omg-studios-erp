# OMG Studios ERP — 화면 구현 상세 (SCREENS.md)

> **CLAUDE.md에서 분리한 화면 단위 구현 상세**(2026-07-27, 150k자 한도 관리).
> CLAUDE.md는 매 세션 로드되는 통치 문서라 커질수록 매 세션 토큰을 먹는다. 그래서 **다음 세션의 판단을 바꾸는 것**
> (불변식·함정·권한·데이터 계약·경계)만 CLAUDE.md에 남기고, **화면 배치·컴포넌트 열거·반응형 단계·도입 이력**은 여기로 옮겼다.
>
> ⚠️ **이 파일은 옮겨온 원문이다**(요약 아님). CLAUDE.md 해당 항목에 `→ SCREENS.md §…` 포인터가 있다.
> 화면을 손볼 때는 여기를 먼저 읽고, **여기서 발견한 새 함정·불변식은 CLAUDE.md에 올린다**(그게 그 파일의 값어치).

## 프로젝트 상세 — 정보(메타) 탭 폼

- 메타('프로젝트' 탭): **폼 배치**: 1줄 `프로젝트명 | 프로젝트 매니저` · 전폭 `아티스트`(여러 명 칩) · 전폭 `제작/운영`(단일) · 전폭 `고객측 담당자`(여러 명 칩) · 전폭 `메모`. **고객측 담당자 = 여러 명**(personCombo multi 칩): `project_contacts` 다대다 + `contact_party_id`=첫(대표, 청구처 파생·관계자 탭·표시 호환). 제출=칩마다 `contact_id`+`contact_name` 쌍(`resolveContactIds` 인덱스 페어링·linkClientContact 패턴), `setProjectContacts` 통째 교체. 각 담당자 상세 '연결 프로젝트'·관계자 탭에 전원 반영(`listProjectsForParty`·`ASSOCIATE_ROLE_SUBQUERY`에 project_contacts UNION). **제작/운영은 단일**. **소속/레이블 입력 필드 없음**: 저장 시 `resolveProjectParties`가 **첫(대표) 아티스트의 현재 소속(`currentAffiliation(artistId).org_id`)에서 `agency_id`·표시 `artist_company`를 파생**(다중 아티스트=첫 기준·소속 미설정=null·기존 프로젝트는 편집-저장 시 재파생). 소속 지정은 아티스트/연락처 쪽에서(간이 등록 모달 '소속사' 칸 유지). app.js `fillAgency`/`currentAgencyValue`는 소속사 칸이 없어도 no-op 가드. **제작/운영은 프로젝트 속성이라 명시 입력 유지**. 담당자는 특정 회사에 안 묶인 고객 측 PM으로 전폭 독립(고객측 PM은 소속/레이블/제작/운영 어디서든 옴 — [[two-pm-model]]). 세션의 '담당 엔지니어'(`session_engineers`)와 겹치지 않게 프로젝트 단 `manager_id`는 **'프로젝트 매니저(PM)'** 라벨. **고객측 담당자**(연락처 콤보 `contactCombo` — 선택 시 전화·이메일·소속 즉시 표시, 목록에 없는 이름 입력 시 저장하면 새 연락처 자동 생성·연결) / **담당 엔지니어**(스튜디오 내부, `manager_id`) / 메모. **담당자 콤보(personCombo)는 본명·활동명 둘 다 검색**: `contactOptions`에 `p.activity_name`, `personCombo`/`personComboOptionsScript`가 옵션에 `alt`(=활동명) 임베드, app.js 검색이 `name`(본명)·`alt`(활동명) 둘 다 매칭. **라벨에 호칭·활동명 병기**(`본명 호칭 (활동명)`, 예 `박수한 대표님 (워터멜론)`·`김보종 대표님`): `contactOptions`에 `p.honorific`, 옵션 JSON에 `honorific` 임베드, app.js 라벨이 `name`+호칭+`(활동명)` 표시. `party-created` 브로드캐스트도 본명 우선 표시+활동명 검색 보조로 정규화(pcPrimary=realName||name·pcAlt). **담당자 이름 전면 병기**(`본명 (활동명)`): ①personCombo **선택 표시**(보이는 입력, 서버 렌더 `shown`)=`본명 호칭 (활동명)`(app.js `labelOf`·서버 `shown` 동일 형식), **제출 숨김 이름(`pureName`)은 순수 본명**(app.js input 핸들러가 본명·라벨 정확 일치 시 id 유지+hidName=순수 본명, pick·모달 저장도 `labelOf` 라벨 세팅). **호칭 중복 방지**: `name`이 호칭으로 끝나면 호칭을 또 붙이지 않음(`n.slice(-h.length) !== h`일 때만). 표시는 `honorific` 컬럼 기준으로 일원화 + name은 순수 본명. 공용 헬퍼 **`personName(person)`**(views.js — `본명 + 호칭(컬럼) + (활동명)`, name이 호칭으로 끝나면 중복 안 붙임): 연락처 목록·상세·suggest·관계자 탭·personCombo `shown` 공통. `resolveDisplayName`이 name에 호칭 안 넣음(성+이름만, 호칭 컬럼 전용). **1회 마이그레이션 `person_name_honorific_split_v1`**(db.js): person name이 ` `+honorific으로 정확히 끝나면 name에서 호칭 제거. ②**필드 밑 주석**: 선택 담당자의 전화·이메일·소속 회사(`data-pc-info`)를 서버에서도 렌더(무JS, app.js setInfo와 동일 구조·data-copy 복사 버튼). ③세션 디렉터 dirRow가 `optionsRef`에 더해 `options`(sel 라벨 조회용)도 전달. ④`/sessions` 목록 디렉터 줄·구글 캘린더 설명 디렉터=`personLabel(name, activity_name)`. ⑤그룹 멤버 목록=`활동명 (본명)`·업체 상세 소속 아티스트(`listArtistsForAgency`에 `real_name`)·연락처/클라이언트 suggest 라벨 병기·그룹 멤버 콤보 본명 검색(`artistPersonOptions`에 `alt`=본명). **청구처 표시 = 본명 (활동명)**: 화면 표시(청구처 콤보 `payerCombo`·청구 목록/상세 `client_name`·청구처 정보 카드 상호·상세 제목). `clientOptions`에 `real_name`·`activity_name`, `listInvoices`/`getInvoiceForUser`/`listInvoicesForProject`의 `client_name` SQL은 `PAYER_DISPLAY_SQL`(CASE로 `본명 (활동명)`), `snapshotPayer`에 `activity_name`(name=본명 유지), `payerName`/`payerInfoCard`/상세 제목이 `personLabel(name, activity_name)` 병기. **거래명세서 PDF 상호(`invoice-pdf.js` `client.name`)는 본명만 유지**(현금영수증 법적 명의). **마감일 없음**(`due_date`는 레거시 컬럼·청구 입금마감일과 별개). **청구처(실결제자)는 청구 섹션에서 결정**. `projects.client_id`는 저장 시 제작사›소속사›아티스트 우선순위로 자동 파생(표시·대시보드·클라이언트 연결용). **필드명**: `소속/레이블`·`제작/운영`(표시 라벨 — 저장 role 값 `소속사/레이블`·`제작사` 보존, `companyRoleLabel`이 표시 변환·프로젝트 파생·db 마이그레이션 호환). 폼 라벨·목록 메타(`소속`·`제작/운영`)·업체 역할 배지·간이등록 모달 체크박스·클라이언트 desc 갱신. **제작/운영에 개인(관계자) 선택 가능**: `production_company` companyCombo에 `{partyIdField:"production_party_id", partyIdValue:production_id}` opt-in → 콤보가 회사+사람(관계자·개인) 함께 검색(사람 옵션 부제=소속회사/'관계자'/'아티스트'), 선택 시 hidden `production_party_id`에 party id 제출. `resolveProjectParties`가 `production_party_id` 있으면 그 party를 `production_id`에 직접 사용(사람이면 사람 party·회사면 회사 party), 없으면 `ensureCompanyParty`로 회사 찾/생성. **소속/레이블·제작/운영에 회사가 지정되면 그 회사 클라이언트 역할에도 반영**: `addCompanyRole(agencyId, "소속사/레이블")`·`addCompanyRole(productionId, "제작사")`(parties.js — 회사면 roles CSV에 추가·멱등·사람이면 no-op) → 한 회사가 겸하면 클라이언트 목록·상세에 두 배지. 사람(개인 제작자)은 roles 미사용·`classifyParty`가 `production_id` 참조로 '제작/운영' 배지 파생. **다운스트림 안전**: `client_id`/payer 파생이 `COALESCE(production_id, agency_id, artist_id) JOIN parties`라 party kind 무관(사람이 production_id여도 정상·사람 상세 '연결 프로젝트'에도 노출). app.js companyCombo: 사람/회사 pick 시 `data-cc-party-id` 세팅(모달 신규 회사 id도) — 순수 이름/병기 라벨과 정확 일치(유일)하면 타이핑에도 id 유지(부분 문자열만 해제, personCombo와 대칭), **party-created 사람 브로드캐스트 수신**(사람 허용 콤보만 — 함정 #20 일관). **서버 안전망**: id 없이 텍스트만 오면 `resolvePartyByDisplay`(회사 상호 정확 → 사람 본명/`본명 호칭`/`본명 (활동명)`/활동명 유일 매칭 — 생성 없음)로 기존 party 해석 후, 그래도 없을 때만 새 회사 생성 → 사람 라벨 텍스트가 유령 회사로 저장되는 것 차단. `resolvePersonByName`도 동일 라벨 안전망(라벨 형식 신규는 본명+활동명 분해 저장, 유일 매칭만 재사용·동명이인 보수). **소속/레이블은 회사 전용**(partyIdField 미전달 시 party-id 필드 없음). **아티스트 병기 + 담당자 자동채움**: ①제작/운영 사람 옵션·선택값을 `본명 호칭 (활동명)` 병기(app.js companyCombo `dispOf`, 회사는 name 그대로; `partyOptions`에 `honorific`, 사람 옵션에 `kind:'person'·alt:활동명·honorific` 임베드; 활동명 검색; production_company TEXT엔 병기 라벨 저장). ②제작/운영에 개인 선택 시 고객측 담당자 자동채움(비어있을 때만): app.js companyCombo pick이 `o.kind==='person'`이면 같은 폼 `[data-person-combo]`의 `__pcSetById(id)` 호출(personCombo IIFE가 `root.__pcSetById`/`__pcHasValue` 노출). **아티스트는 커스텀 콤보**(`artistCombo`, Gmail식 칩): 선택된 아티스트가 배지(칩)로 담기고 ✕/빈 칸 백스페이스로 통째 삭제. **서버 계약**: hidden `artist`=활동명 콤마 목록·`artist_contact_id`=단일 선택일 때만 명시 id(다중이면 비워 서버가 이름별 해석). 칩 라벨='활동명 (본명)' 병기. 소속사 자동 채움은 첫 아티스트 기준. 타이핑=기존 아티스트·사람 검색, 빈 입력 시 **[🔍검색]/[＋새 아티스트] 팝업**(app.js `[data-artist-combo]`, 옵션은 `<script type="application/json">` 정적 임베드·CSP-safe). 기존 사람을 고르면 hidden `artist_contact_id`로 연결. **콤마로 여러 명 등록**("아이유, 태연"): ①콤보는 마지막 콤마 뒤 조각으로만 검색(app.js `seg()`), 선택·간이등록은 이어붙임, 다중이면 `artist_contact_id`·본명 표시·소속사 덮어쓰기 비활성(소속사는 비어있을 때만 첫 아티스트로) ②서버 `resolveProjectParties`가 이름별 해석(기존 그룹 정확 일치 재사용 → `resolvePersonByName` 유일/라벨/활동명 안전망 → 신규 생성+아티스트 마킹, dedup) ③**`project_artists(project_id, party_id)` 다대다**(`setProjectArtists` 통째 교체·CASCADE·`project_artists_backfill_v1` 1회 백필)에 전원 기록 → 모든 아티스트의 상세 '연결 프로젝트'에 매칭(`listProjectsForParty` 확장·deleteParty 정리) ④`projects.artist` TEXT=정규화된 콤마 목록(표시), `artist_id`=첫(대표) 아티스트(레거시 호환·표시용. 청구처는 청구 폼에서 항상 직접 지정, 추천 칩만 제공). 단일 입력은 기존 경로(그룹 체크·명시 id·본명칸) 그대로. **새 아티스트 등록 모달 = 유형 먼저**: 등록 유형 select(`개인 아티스트 (솔로·멤버)` / `그룹 (밴드·팀 자체)`, `data-am-type`·name 없음=폼 미제출). **개인**: 라벨 '활동명' + 본명 + 소속 그룹 미니콤보(`data-am-group-*` — 검색 + `＋ 새 그룹 등록`[fetch POST /clients type=group], 선택 시 hidden `group_id` → 서버 `setPartyGroup`으로 멤버 연결) + 소속사 + 전화·이메일 / **그룹**: 라벨 '그룹명' + 소속사만(본명·소속 그룹·전화·이메일 숨김). 모달 소속사는 이름으로 전송(`agency_company`, `ensureCompanyParty` 재사용/생성). **저장 시 아티스트↔사람 자동 연결로 중복 방지**(`applyArtistLink`→`linkArtistToContact`): 개인 아티스트가 명시 선택(`artist_contact_id`) 또는 같은 이름 기존 연락처와 매칭되면 그 연락처의 아티스트로 통합(`source_contact_id` 세팅, `ensureClient`가 만든 orphan 아티스트 클라이언트 흡수·연락처 `nickname`=활동명). 그룹 체크 시 `ensureGroupArtist`로 `is_group=1`(연락처 미연결). 개인 아티스트는 매칭 연락처가 없어도 항상 새 연락처 생성·연결(`resolveContactForArtist`). **동명이인 오연결 방지**: 명시 선택 없을 때 이름으로 연락처를 찾되 정확 일치가 유일할 때만 재사용, 2+ 동명이인이면 새 연락처 생성(`resolveContactForArtist`, 일반 `resolveContactByName`[첫 매칭]과 달리 병합 경로에서만 보수적). **기존 중복 병합**: 고아 아티스트 클라이언트(`source_contact_id` NULL·개인)를 같은 이름 연락처에 서버 기동 1회 백필로 연결(`artist_contact_link_backfill_v1` in db.js — name/nickname 유일 매칭만, 그룹·이미 연결된 연락처 제외). **본명/활동명 규칙**: 콤보에 '본명' 입력칸(`artist_real_name`, 그룹 체크 시 숨김·app.js `[data-artist-real]`) (간이 등록 모달 필드=활동명·본명·소속사·전화·이메일[`data-am-email`→`POST /clients` `email` → `createPerson`]) — 저장 시 연락처 `name`=본명(있으면)·`nickname`=활동명, 아티스트 클라이언트 `name`=활동명. **표시**(`personLabel` in views.js, 본명≠활동명일 때만 괄호): 연락처=본명(활동명), 아티스트 클라이언트=활동명(본명)(`listClients`/`getClient`가 `source_contact_name` LEFT JOIN). `classifyContact`에 '아티스트' 배지(source_contact_id로 아티스트 클라이언트와 연동된 연락처).

## 프로젝트 목록 — 컬럼 표·정렬·반응형·펼침

- 목록=한 줄 요약 카드 + 검색(`?q=`). **'내 프로젝트만' 필터**: 전 프로젝트 열람 유지 + opt-in 토글(`?mine=1`, URL 쿼리만·기억 안 함). 내 것 = 로그인 담당자(`getManagerByUserId(user.id)`) 관여 전부 = PM(`projects.manager_id`) OR 담당 세션(`session_engineers`) OR 담당 작업(`track_tasks.engineer_id`) — 순수 함수 `listProjectIdsForManager(mid)`(UNION 한 쿼리→Set), 라우트가 `listProjects` 결과 필터. 토글 pill은 담당자 행 있는 계정만(없는 대표 등은 숨김·`?mine=1` 강제해도 무효 안전망). `keepQ`가 탭·검색·완료토글 복귀(`?open=`) 링크에 mine 보존. **정렬=탭별**: 진행 중=다가오는 세션 임박순(`splitProjectTabs`가 `next_session_date` ASC·없으면 뒤, SQL `created_at DESC` 폴백), 청구 필요·완료=작성일 최신순. **작성일 컬럼**(전 탭, `created_at` 앞 10자). 편집=상세 '정보' 탭 치프만(`projectMetaCard(p, err, {chief})`, 날짜 콤보 + **명시 '저장' 버튼**, `POST /projects/:id/created-at`=`requireChief`·`safePath(req.body.return)` 복귀·시각 HH:MM:SS 보존). **폼 제출 후 스크롤 보존(전 폼 공통)**: `public/js/app.js` 최상단 IIFE가 모든 `<form>` 제출(capture `submit`·`formtarget="_blank"` 제외)에서 `sessionStorage["scrollPos:"+pathname]`에 `window.scrollY` 저장 → 같은 경로 복귀 시 1회 복원(경로만 비교, `flash=`·`open=` 무시). **3탭 분리**: `splitProjectTabs(rows)`(순수 함수) = 진행 중(`active`·`!is_completed`·임박순)/청구 필요(`billing`·`is_completed && unbilled_cnt>0`)/완료(`done`·`is_completed && unbilled_cnt===0`) 상호 배타. `?tab=active`(기본)/`billing`/`done`, 활성 탭만 렌더(라벨 개수·빈 탭 문구 탭별). 청구 필요 탭 = 대시보드 '청구 필요' 카드와 같은 정의. `listProjects`에 `unbilled_cnt` 파생(미청구 작업 + `listBillableSessionsForProject` **동일 조건** 세션 — 두 곳이 갈리면 '청구 필요 N'과 실제 후보 목록이 어긋난다). ⚠️**단가 항목 미선택(`rate_item_id` NULL)도 센다**(2026-07-23) — 예전엔 양쪽 다 `rate_item_id IS NOT NULL`로 걸러서, 완료된 대관 세션이 **청구 후보와 집계에서 동시에 사라져** 프로젝트가 조용히 '완료' 탭으로 넘어갔다(매출 누락). 산정이 안 될 뿐 청구는 여전히 '필요'하므로 센다. 🔒 **미청구 신호는 완료 여부와 무관**(2026-07-23): 목록 행 '청구 필요 N' 배지는 **어느 탭에서든** `unbilled_cnt>0`이면 뜬다(예전엔 `tab === "billing"` 한정이라 진행 중 프로젝트의 미청구가 안 보였다). 탭 분류는 불변. ⚠️🔒 **후반작업(믹싱·마스터링) 세션도 '청구 미착수' 신호로 센다**(2026-07-24) — 후반작업은 세션이 아니라 곡·콘텐츠 작업으로 청구하는데(설계), **세션만 마치고 작업/금액을 아직 안 만든** 프로젝트는 청구할 게 없어 보여 조용히 '완료'로 넘어갔다(허영생·해로이키 믹스 사례, 07-23 대관 세션 버그와 **동일 클래스**). → `unbilled_cnt`에 **완료(또는 지난)·미취소·미waived 믹싱/마스터링 세션이 있고 곡·콘텐츠 작업 0 AND 청구서 0**이면 **+1**(EXISTS 플래그·라인 아님·`POSTPROD_SESSION_TYPES`/`POSTPROD_IN` config, 가드 ③). 작업/청구서가 생기면 **자동 해제**(작업 기반 분류가 이어받음)·무료면 작업 만들어 '청구 안 함'. **명시적 완료 세션만**(`status='완료'` — 2026-07-25 세션 완료 판정 버튼 기준화로 지난 예정 세션이 진행 중에 머물게 되며 날짜 게이트 제거. 완료도 안 누른 세션에 청구 배지가 뜨는 모순 제거). 후반작업 세션은 **청구 후보 목록·청구 폼엔 직접 라인으로 안 뜬다**(설계 유지) — 대신 청구 탭에 후보 비면 `hasPostprodSessionNeedingBilling`(unbilled_cnt 항과 **술어 정합**·회귀 잠금)로 '곡·콘텐츠 탭에서 작업을 만들어 청구' 안내. 회귀 `billing-radar.test.js` 18. **완료 판정** `is_completed`(`listProjects` 파생) = **예정 세션 없음**(`sess_scheduled=0` — 완료 안 누른 세션[`status='예정'`]이 하나라도 있으면 **미래든 지난이든** 진행 중, **작업 `open_tasks`와 대칭**. ⚠️2026-07-25: 이전엔 `upcoming_cnt=0`[미래 예정만]이라 **지난 예정 세션이 날짜만 지나면 완료/청구필요로 샜다** — 세션도 버튼 기준으로 통일해 교정[세션 상태는 자동 안 바뀜·완료 안 누르면 계속 예정]. 취소·완료 세션은 예정이 아니라 진행 안 막음. `upcoming_cnt`·`next_session_date`는 진행 중 정렬·다음 세션 표시 전용으로 유지[게이트만 `sess_scheduled`로 이동]) AND 미완료 작업 없음(`open_tasks=0`·`status<>'Completed'`) AND 실제 활동 있었음(`content_cnt>0` — 빈 새 프로젝트는 진행 중)(`projects.routes` GET `/`). **상세 백링크 = 보던 탭 복귀**: 카드 링크 `?return=<목록 경로(tab·q·mine)>`(encodeURIComponent), 상세 pageHeader back=`safePath(req.query.return)`(내부 절대경로만·`/projects` 폴백). `projectRowHref(p, tab, listQuery)`·`projectListRow({listQuery})`. **카드 클릭 = 탭 맥락 목적지 직행**: `projectRowHref(p, tab)`(views.projects.js 순수 함수) — 청구 필요→청구 탭(`?tab=invoice`) / 진행 중→세션 일정(`?tab=sessions`), 세션 없고 곡만 있으면 곡·콘텐츠(`?tab=tracks`) / 완료·둘 다 없음→정보 탭. `listProjects`에 `sess_cnt` 파생. 청구 탭은 `canBill`(전원)이라 권한 안 막힘. **행 = 청구식 컬럼 표**: 헤더 `projectTableHead` 항목명 = `작성일·제작사·아티스트·프로젝트·PM·다음 세션·금액`(**작성일 맨 앞**). ⚠️좁아지면 우측부터 열 숨김(작성일 <1280 → 금액 <1024 → PM <820), 모바일 카드(<640)에선 `.pt-created { order: 1 }`로 작성일을 맨 뒤로(첫 줄이 날짜면 프로젝트 식별이 밀림 — DOM 유지·flex order만). **항목명 클릭 = 정렬**(첫 클릭 오름·다시 내림·`aria-sort` 화살표·키보드 Enter/Space + IME 가드·리로드 시 서버 기본 정렬). **청구 목록과 공용 코어** app.js 파일 스코프 `wireSortHeaders`+`sortCellValue`(정렬 규칙[빈 값 뒤로·타입별 비교·방향 토글·aria-sort]을 한 벌로, **구조 차이만 `io`로 메운다** — 청구=표라 `row.cells[헤더 인덱스]`, 프로젝트=grid라 `[data-sort-key]`로 셀 찾음[열이 CSS로 숨어도 안전]). 마크업 계약: 컨테이너 `data-sort-rows`·행 `data-sort-row`·짝 `data-sort-key`·헤더 `data-sort-type`(num|date|text). ⚠️**셀 `data-sort-value` 항상 명시** — 금액 `₩1,200,000`은 문자열 정렬 불가(정수 필요), 다음 세션은 화면이 `D-3` pill이라 ISO 날짜 별도, **빈 칸의 `—`는 빈 값으로 안 쳐 이름들 사이에 끼므로** 값 없으면 `""`(방향 무관 뒤로). 회귀=`project-list.test.js`(마크업)+`ui-interactions.test.js`(app.js 동작). ⚠️**열 순서를 바꿀 땐 `grid-template-columns`의 fr·rem 값도 함께 옮긴다**(값이 자리에 붙어, 순서만 바꾸면 긴 상호가 좁은 칸에 들어가 더 잘리고 고정 폭도 어긋난다·반응형 4단계 전부). 각 행과 헤더가 **같은 CSS grid 열**(`.proj-thead`/`.proj-summary`)이라 칸 정렬. 각 행 `<details class="proj-row">` — **행 전체가 이동 링크**: 모든 데이터 셀이 `projectRowHref`로 가는 `<a class="proj-link">`(어느 셀이든 탭 맥락 목적지로, `<summary>` 안 `<a>`는 이동만·토글 안 함=브라우저 기본), **오른쪽 끝 `.proj-toggle`(비인터랙티브 span·chevron)만 클릭 시 그 자리 펼침**(세션·곡 요약 + 완료 토글, 네이티브 `<details>`·무JS). 셀 밑줄·색강조 없음(`.proj-link:hover`=inherit·no underline), 어포던스는 행 배경 호버(`.proj-summary:hover`)만. 컬럼값: 제작사=`client_name`, 아티스트=`projectArtistOnly`(다중 `외 N`·폴백 없음), 프로젝트=`title`, PM=`manager_name`, 다음 세션=**진행 중 탭만** `nextSessionLine`(디데이 pill 3색: 3일내 빨강·2주내 주황·그 외 회색), 금액=`projectAmount`(청구 필요 탭은 '청구 필요 N' 배지 병기), 작성일=`created_at` 앞 10자. ⚠️**셀 잘림 = `.pt-cell`**(CSS를 `.proj-cell`로 잘못 쓰면 마크업과 어긋나 nowrap 안 먹어 셀이 글자 단위로 세로로 쪼개진다 — grid 아이템은 `min-width:0`도 필요[기본 auto라 내용보다 못 줄어듦]). 아티스트·제작사·프로젝트=유연 열(넘치면 `…`), 나머지 고정 폭. **반응형 <640px = 2열 카드**: thead 숨김, **왼쪽=무엇**(작성일[작게]→제작사[약화]→아티스트[굵게]→프로젝트명[muted], 데스크톱 열 순서 그대로) / **오른쪽=얼마·언제**(금액[굵게·우측정렬]→다음 세션[우측·D-day pill]). 금액을 한 열에 우측 정렬하면 자릿수가 맞아 세로 비교 가능(청구 모바일 카드와 동일). **배치는 `grid-row`/`grid-column` 명시**라 DOM 순서 무관. 라벨 prefix 없음(자리가 이미 무엇인지 말함). **`⌄`는 첫 줄 오른쪽 제 열**(`grid-column:2; grid-row:1; justify-self:end`) — 금액·다음 세션은 그 아래로(⌄에 제 열을 안 주면 오른쪽 내용을 밀거나 겹친다). `row-gap:0` + `.pt-cell { line-height: 1.35 }` + 첫 줄 날짜 `align-self:center`(첫 줄 높이는 ⌄ 1.75rem가 정함). 헬퍼 `projectArtistLabel`/`projectSubLabel`/`projectIdentity`는 다른 소비처 호환으로 잔존. **펼침 본문**(`.proj-expand`) = 인라인 상세(다가오는 세션 우선 재정렬[`projectSummaryHtml`가 `todayYmd()` 기준 upcoming-우선 후 8건, 지난 세션 회색·뒤] + 곡별 아티스트·제목·작업자 + 작업별 행). **펼침 세션·작업 완료 토글**(`canEdit`): 각 옆 `− 완료`/`✓ 완료`(세션=`POST /sessions/:id/status`·작업=`POST /projects/tasks/:id/status`, `return=/projects?tab=&open=projectId`, `listProjectSummaries`에 `id`·`status` 추가) → 복귀하며 그 카드 재펼침(`?open=`→`<details open>`)·스크롤 보존. 요약=`listProjectSummaries(ids)` 배치 2쿼리(N+1 회피). **금액 = 전체 버짓 = 작업액 + 세션액**(작업액=Σ`COALESCE(NULLIF(total_price,0), task_types.unit_price)` — 확정 없으면 종류 기본단가 폴백; 세션액=`sessionAmountsByProject`·폴백 `p.rate`). **청구된 항목도 포함**(청구 여부 무관 전체 버짓). **청구 확정 시 확정액 우선**: 청구된 작업=`total_price`, 청구된 세션=`invoice_items` 확정액(`sessionAmountsByProject`가 청구 세션은 invoice_items 합계 우선), 미청구만 예상액. **청구서 할인 차감**: `listProjects`·`getProjectForUser`가 `invoice_discount_total`(from-tasks 청구서 할인 합·수동 청구서 제외) 파생, `projectAmount`가 라인 합에서 차감(0 하한·라인 없으면 `p.rate`). 목록·상세 동일.

## 세션 예약 폼 — 시간 블록·구간·세부정보·사람

- **폼 레이아웃(추가·편집 완전 통일) = 구글 캘린더식**: 추가·편집 모두 `sessionBookingFields(s, managers, rateItems, rooms, defaultBooker, pmName)` 3구역 — ①**최상단 전폭 시간 블록 = 구글식**: `[날짜][시작]–[종료][종료날짜]` 한 줄 + **종일** + 소요 슬라이더. **날짜 2개 = 커스텀 날짜 콤보(`data-date-combo`)**: 보이는 칸은 **자유 타이핑**(`317`·`3/17`·`12/31`·`2026-03-17`·`3월 17일`·`오늘`/`내일` — app.js `parseDateText`, 롤오버 날짜[2월 31일]는 거부하고 원래 값 유지), 표시 형식은 **`2026. 3. 17. (화)`**(서버 `formatYmdCombo`[lib/date]와 app.js `fmtDateCombo`가 **같은 형식** — ⚠️다르면 로드 직후 값이 튄다. 요일은 UTC 파싱이라 서버 TZ 무관). **값은 hidden**(`name=session_date`/`end_date`, `YYYY-MM-DD`) + 마커 `data-session-date`/`data-end-date`도 hidden에(소요 역산·겹침 조회·서버 계약이 사용), 보이는 입력은 name 없음(함정 #19). **팝오버 = 월 그리드**(‹ › 월 이동·날짜 클릭·오늘/선택 강조), **키보드**(←→ ±1일, ↑↓ ±7일 — 이동 시 칸에도 미리보기, Enter 확정, Esc 취소), IME 조합 중 Enter 무시(함정 #18). **팝오버 버튼(월 이동·날짜)·시간 옵션 버튼은 `tabindex="-1"`**(tab 순서 밖 — 날짜칸 타이핑 후 Tab이 팝오버가 아니라 다음 필드[시작 시간]로 이어지게, 시간 콤보도 동일). 종료 날짜 자동 계산(자정 넘김 +1일)은 hidden에 쓰고 `__dcSync()`로 표시 갱신, '사용자가 편집 중인 칸은 덮지 않음' 판정은 hidden이 포커스를 못 받으므로 `__dcInput`(보이는 칸)으로 한다. **시간 박스=콤보(`data-time-combo`)**: 포커스 시 **전체선택**(타이핑만으로 교체)+**00:00~23:30 30분 단위 드롭다운**(현재 값 근처 스크롤·클릭 선택·Escape/Enter 닫기[IME 가드]). **종료 날짜도 편집 가능** — 일수 차+종료 시각으로 소요 역산(<24h 표현 범위 밖이면 canonical 복원). **종일=구글/애플과 동일 개념(하루 종일·시간 없음, 운영시간 무관)**: `sessions.all_day` 컬럼(마이그레이션 `addColumn all_day`) + start/end NULL. 체크박스=`name="all_day" value="1"`(제출 소스·서버 권위: `all_day=1`이면 sessionFields가 시간값 무시하고 start/end NULL 저장), 체크 시 app.js `applyAllDay`가 시간 박스·종료 날짜·소요 블록 **숨김만**(시간값 안 건드림 → 해제 시 그대로 보존), 편집 진입 시 서버가 `checked` 렌더하면 UI 반영. **표시=`timeLabel`/billLine이 '종일'**(시간범위·소요 대신). **겹침 없음**(시간 NULL이라 findSessionConflict가 null 반환 — Google처럼 시간 점유 안 함, 청구 후보 쿼리도 `all_day=1 OR 시간 IS NOT NULL`). **캘린더=date-only 종일 이벤트**(`eventTimes`가 NULL 시간→`{start:{date},end:{date+1}}`). **청구=`sessionRateAmount`가 종일이면 1 기준 블록**(정액 항목=base_price·시간제 항목=1Pro, `allDay` 플래그·거래명세서 '종일' 스냅샷). **다일(multi-day) 종일**: 종일은 시간만 없애고 **종료 날짜(`sessions.end_date`)는 남겨 여러 날 일정**(예: 2/5~2/9). `end_date>session_date`일 때만 저장(단일일=NULL·시간세션 무시). 폼=종일 체크 시 시간콤보·소요만 숨기고 날짜 2개(시작·`name=end_date`) 유지, `updatePreview`가 종일이면 종료날짜 자동동기 스킵(사용자 지정 다일 보존). 표시=`종일 · ~2월 9일`. 캘린더=`eventTimes(endDate)` 배타적 종료(end_date+1). 16h 초과 편집-저장 잘림은 durationButtons hours>960 빈값 가드로 방지. **1회 백필 `session_all_day_backfill_v1`**: 00:00~23:59 세션을 종일(all_day=1·시간 NULL)로 승격. **소요 단일 진실원천=app.js `curDur`**(custom_hours는 **hidden 제출값**, ≤960일 때만 미러). 종일 체크=종료 날짜 옆 인라인, 소요 라벨(총 소요 '7시간')=**'소요 시간' 헤딩 옆**(`fmtDurationKo` 초기 렌더+app.js 동기), 시간 콤보 목록=현재 값 최상단 스크롤, 새 작성 프리필(시작=지금 다음 30분 슬롯·종료=+1Pro — `applyTypeDefault`에 스튜디오 기본 블록 폴백), 시간 입력 자동완성 원천 차단(보이는 입력 nameless+`data-time-hidden` 제출·함정 #19 패턴). 시작/종료 박스=콜론 자동(1400→14:00), **종료↔슬라이더 양방향**(종료 입력→소요 역산[자정 넘김 +24h], 소요 변경→종료 자동), 종료 날짜는 저장 필드 아님(자정 넘김 +1일 표시 — 스키마는 session_date 하나·야간=end<start). JS-on=start_time+custom_hours, **JS-off 폴백=start_time+end_time**(resolveEndTime이 hours 없으면 end_time 사용). ⚠️날짜 +1일 계산은 로컬 파트 조립 — `toISOString()`은 UTC라 KST에서 하루 밀림. ①-b **촬영 구간 블록**(2026-07-26, `data-segments`): 세션 종류가 촬영이면 시간 블록의 `[시작]–[종료]`·소요 슬라이더가 **숨고** 반입·설치/촬영/철수 3줄(각 시작·종료 시간 콤보, `name=seg_<kind>_start|end`)이 나온다 — 같은 시간을 두 번 입력하게 두면 어느 쪽이 진짜인지 알 수 없으므로 점유 시간은 서버가 구간 span에서 파생한다. 노출 조건은 화면에 '촬영'을 박지 않고 **단가 kind에서 파생**(`SESSION_TYPE_RATE_KIND`가 진실원천), 토글은 app.js `data-when-type` + `data-show-when-type`/`data-hide-when-type`. ⚠️**구간 입력에도 `attachTimeFormat`을 붙여야 한다** — 보이는 입력이 nameless(함정 #19)라 `syncTimeHidden`이 없으면 제출용 hidden이 비어 **구간이 조용히 저장되지 않는다**(콜론 자동 포맷도 함께 얻는다). 🔒**겹침 경고도 구간 span으로 판정한다**(app.js `segSpanMin`) — 숨겨진 시작·소요 칸의 옛 값을 보면 '경고 조건 ≡ 차단 조건' 불변식이 깨지고, 거짓 경고에 길들여진 승인이 진짜 이중 예약을 통과시킨다(2026-07-23 결함과 동일 클래스). ②**좌측 '세션 세부정보' 상자**(세션 종류·단가 항목[대관 종류일 때만]·룸 3열 + 담당 디렉터 + 메모, 구글의 일정 세부정보 카드. 🚫**할증 입력은 없다** — 아래 `surcharges` 참조) ③**우측 사이드 = 사람**(md:grid-cols-[minmax(0,1fr)_220px]; **PM=프로젝트 담당 엔지니어 읽기 전용 표기**['프로젝트 담당 — 표기만', sessionsSection이 project.manager_id로 계산해 전달] + 예약 담당자·담당 엔지니어 select, 구글의 참석자 패널). md 미만은 시간→세부정보→사람 세로 스택. **상태 필드 없음**(대기/완료는 완료 토글로 정함). **상태는 hidden `input[name=status]`로 현재값 보존**(편집 저장 시 초기화 방지 — 완료 세션 편집해도 완료 유지). 세션 취소는 **행/캘린더 팝오버의 '일정 취소' 토글 버튼**으로 처리(편집 폼이 아니라 토글 — `status=취소`, 캘린더는 삭제 없이 '(취소)' 제목으로 유지). 편집 폼은 기존 시간으로 슬라이더 초기화(`minutesBetween`), 저장 시 시작+소요로 종료 산정. `app.js`가 `[data-session-form]`을 **폼별로 초기화(멀티폼)**. **세션 종류(녹음/믹싱/마스터링/기타)는 항상 선택 가능**. **'+ 세션 추가' 버튼 우측 정렬**(`btn-sm`+`flex justify-end`).

## 공통 뷰 헬퍼 · 테마/팔레트

- **UI 공통 헬퍼**(views.js): `tabBar`(aria-current 포함)·`filterChips`·`projectTypeBadge`·`listGroup`/`listRow`/`listRowLinked`(목록 행 통일)·**`dataTable(cols, rows, {filterList})`**(범용 정보 표 — 청구·프로젝트식 컬럼 표를 연락처·클라이언트에 재사용, 2026-07-16: `<table class="dt">` table-layout:fixed·**콜 폭=CSS 클래스**(`w`에 Tailwind 폭 클래스명 리터럴 `w-[16rem]` — 인라인 style은 CSP style-src에 막힘[함정 #27], w 생략=유동 폭으로 남은 공간 흡수)·`hide:'sm'|'md'|'lg'|'xl'`로 좁을 때 열 숨김(col도 `table-column`으로 함께 숨겨 폭 예약 제거)·`mobileHide`로 <640 카드에서 셀 숨김·<640 카드[thead 숨김·라벨줄·primary 셀=카드 헤더·**갭/개별 테두리 없이 구분선 하나로 이어지는 목록**=`border-bottom`만, 흰 바탕·바깥 테두리는 래퍼가 제공, 2026-07-16 청구 목록과 통일]·`filterList`면 tbody `data-filter-list`로 실시간 필터). **`src/views.contacts.js`**(2026-07-17 마스터-디테일 전환으로 신설, `contactTable`[6열 표] 대체·제거 — 위 '연락처' 섹션 참조): `contactPanes`(2단 골격)·`contactNameList`(이름만 마스터 목록)·`contactReadView`(읽기 뷰). `/contacts` 목록 전용(사람은 전부 이 렌더러 하나 — 옛 클라이언트 '관계자' 탭 공용은 2026-07-17 사람/조직 축 정리로 그 탭 자체가 없어져 해소). **업체·그룹(`/clients`)도 2026-07-18 마스터-디테일로 전환**(연락처와 동일 구조 — 위 dataTable 탭별 컬럼 org 표는 폐기): `src/views.clients.js`의 `clientReadView`(읽기 뷰)·`clientEditPane`(편집 뷰)가 `contactPanes`/`contactNameList`(views.contacts.js, 재사용)와 조합돼 왼쪽 업체/그룹 이름 목록(상한 없음·`liveFilter`) + 오른쪽 패널을 이룬다. `GET /clients`=목록(빈 패널)·`GET /clients/:id`=읽기 뷰(우상단 **[편집]**)·`GET /clients/:id/edit`=편집 폼 — 3분기는 아래 '상세=바로 편집' 참조. `dataTable` cols에 `hide:'sm'|'md'|'lg'|'xl'`·`mobileHide`·`mCard` 지원(`.dt-m-hide`/`.dt-mcard` 특정도가 모바일 `.dt tbody td{display:flex}`를 이김)·`emptyState`(아이콘·CTA)·**`explain(content)`**(긴 안내/항목 설명을 기본 접힘 '설명' 토글로 — 네이티브 `<details>`·무JS·CSP 안전; 상태·오류·모달 경고엔 쓰지 않음). badge 변형 5종(`badge-neutral`·`badge-success`·`badge-warning`·`badge-error`·`badge-info`); 쿨톤 `--color-info`(상태색/브랜드 클레이 분리). 사이드바 그룹화(운영/청구/관리 섹션, 좌측 레일 활성표시. **청구 그룹 순서 = 돈의 흐름 `청구 → 매출 → 외주 작업자`** — 외주 작업자는 '정산'이라 매출 뒤, 2026-07-20 사용자 요청). 수동 테마 토글(라이트/다크, 크림 기본, `html[data-theme]`+localStorage). **테마 선택 아키텍처(2026-07-17)**: 색·폰트·radius·shadow를 전부 `:root` CSS 변수로 추출(`--color-*`·`--font-sans/serif/display`·`--radius-btn/input/card/badge`·`--shadow-card`) → 컴포넌트(.card/.btn/.input/.badge)가 변수 참조. **Claude**=`:root` 기본값(크림·클레이 원본 정체성 그대로 보존, 옛 'Original'을 2026-07-18 개명 — 스와치 선택 시 `data-palette` **속성 제거**=`:root` 렌더). **팔레트**(`html[data-palette=apple|linear|spotify|pinterest]` + `[data-theme=dark]` 변형)는 이 변수만 덮어씀(마크업·로직 불변, 순수 시각 레이어). Spotify/Pinterest는 브랜드 액센트+pill 버튼(`--radius-btn` 624rem)+system/Pretendard 폰트, 각각 라이트·다크 변형 제공(Spotify 다크=near-black #121212·그린 버튼 검정 텍스트). **미선택 사용자 기본 팔레트 = Linear**(2026-07-18 사용자 요청 — theme-init.js가 저장 palette 없으면 `data-palette="linear"`. 이미 고른 사용자는 그대로). **라이트/다크 테마는 OS 추종**(저장 theme 없으면 미설정 = prefers-color-scheme, 강제 안 함). 사이드바 **특징색 스와치 아이콘**(`[data-theme-swatch]` 5개 = Linear indigo/iOS블루/Spotify 그린/Pinterest 레드/클레이 원형 버튼, 색은 CSP상 `.theme-swatch-*` CSS 클래스, 활성=fg 링. `currentPalette()`는 속성 없으면 `"claude"`. 클릭 선택, 2026-07-17 사용자 요청 '드롭다운 대신 아이콘'. app.js가 `localStorage["palette"]` 저장·`aria-pressed` 동기화). **팔레트는 라이트/다크를 강제하지 않음**(Linear도 라이트 가능 — 별도 다크 토글을 따름, 2026-07-17). **FOUC 방지 = 서버가 `<html>`에 첫 페인트로 렌더(2026-07-21 사용자 리포트 '페이지 이동 시 깜빡'·3종 수정)**: 옛 방식은 `theme-init.js`(head 동기 로드)만이 렌더 전 `data-theme`/`data-palette`를 세팅했는데, **dev는 그 파일을 매 이동 재요청(max-age=0)·프로덕션도 첫 방문엔 캐시 없음**이라 그 지연 동안 서버 기본(라이트·Claude)이 먼저 그려졌다 바뀌는 깜빡임이 있었다. → **`theme`·`palette`를 쿠키에 저장하고 서버가 `<html data-theme data-palette>`를 첫 바이트에 렌더**한다: `src/lib/request-theme.js`가 `AsyncLocalStorage`로 쿠키를 요청 컨텍스트에 흘려(layout 호출부 30곳 무변경) `currentThemeAttrs()`가 속성 계산(쿠키 없으면 기본 팔레트 linear·테마 미설정 = 기존 렌더), server.js 미들웨어(cookieParser 뒤)가 `req.cookies.theme/palette`를 담고, `layout()`이 렌더. **app.js**가 테마 토글·팔레트 선택 시 쿠키 기록(1년·lax), **theme-init.js**는 해석값을 쿠키에 기록(기존 localStorage 사용자도 다음 이동부터 서버 렌더)+첫 방문·OS 변경 폴백으로 존속(claude면 속성 제거·미선택은 linear 확정). **스와치 활성 링도 CSS로**(`html[data-palette="X"] .theme-swatch-X`·claude=`:not([data-palette])`) — 옛날엔 `aria-pressed`(app.js defer)로만 그려 매 이동 '링 없이 페인트→app.js가 링 추가'로 링이 깜빡였다. 회귀 `test/request-theme.test.js` 7(컨텍스트·기본값·claude·layout 렌더). `test/theme.test.js`가 Claude 보존·변수 추출·2팔레트(Material 제거)·기본 Linear 라이트·배선·FOUC 순서를 잠금. muted 대비 AA 충족(#6E6A5F 5.15:1). **검색 typeahead(2026-07-04, 사용자 요청)**: 공용 `searchBox({action,q,placeholder,suggestUrl,hidden,liveFilter,noButton,remote})` 헬퍼(views.js). **`suggestUrl` 방식(타이핑 제안 드롭다운)**: `[data-search-suggest]`/`[data-suggest-pop]` 렌더 → app.js IIFE가 타이핑(200ms 디바운스)마다 `suggestUrl?q=`에서 JSON `[{label,sub?,href}]`을 받아 드롭다운 제안(↑↓ 이동·엔터 선택 이동·엔터 무선택 시 폼 제출=전체 검색·한글 IME 가드·AbortController 취소) — **프로젝트·세션**(`/projects/suggest`·`/sessions/suggest`, 반드시 `/:id` 앞 등록). **`liveFilter` 방식(실시간 목록 필터)**: 입력 `data-live-filter` + `listGroup({filterList:true})`의 `data-filter-list`/`data-filter-empty` → app.js가 타이핑 시 로드된 목록 행을 `textContent` 매칭으로 즉시 필터(서버 왕복 없음) — **업체·그룹·연락처 목록·청구 목록**(연락처는 2026-07-16 typeahead 드롭다운→실시간 필터로 전환). **`remote` 방식(상한 초과분 서버 보강, 2026-07-17 사용자 리포트 '업체 100개 넘어가면 검색이 못 찾음')**: 목록이 상한(`capList` 기본 100건)으로 잘리면 로드된 행만 훑는 실시간 필터가 100번째 뒤 항목을 못 찾는다 → 라우트가 `capped.more` 있을 때 `searchBox({remote:true})`로 입력에 `data-live-remote` 부여 → app.js가 타이핑 시 즉시 필터(로드분·빠른 피드백) **+ 220ms 디바운스 후 서버 전체 검색 결과(`?q=&limit=10000`)로 `[data-filter-list]` innerHTML 교체**(최신 요청만 반영·경합 토큰, 매칭 0이면 서버가 emptyState라 `data-filter-list` 없음 → 목록 비우고 안내, 검색어 비우면 원본 로드분 복원). **업체·그룹(전 탭)·연락처**에 적용(링크 행이라 innerHTML 교체 안전). **청구 목록(`/invoices`)은 remote 미적용**(행에 벌크 체크박스·롱프레스 바인딩 — Enter 서버검색 유지). `test/ui-interactions.test.js` jsdom(fetch 목)으로 교체·복원 잠금. **`noButton`=검색 버튼 없음**(2026-07-16 사용자 요청 — 업체·그룹·연락처 목록: 실시간 필터가 주 경험이라 버튼 제거, 폼은 단일 텍스트 입력이라 Enter로 서버 전체 검색은 유지). 서버 `?q=`(엔터)는 캡 넘는 전체 검색용. **모달 스크롤 잠금(전 모달 공통, 2026-07-04)**: app.js IIFE가 MutationObserver로 모달 마커(`[data-pc-modal]`·`[data-cc-modal]`·`[data-artist-modal]`·`[data-nav-guard]`·`[data-modal]`) 중 하나라도 열리면(=`hidden` 없음/DOM 존재) `<html>·<body>` overflow:hidden(+스크롤바 폭 padding 보정), 전부 닫히면 복원 — 모달 열린 채 배경이 스크롤되던 것 차단(사용자 요청).

