# OMG Studios ERP — 화면 구현 상세 (SCREENS.md)

> **CLAUDE.md에서 분리한 화면 단위 구현 상세**(2026-07-27, 150k자 한도 관리).
> CLAUDE.md는 매 세션 로드되는 통치 문서라 커질수록 매 세션 토큰을 먹는다. 그래서 **다음 세션의 판단을 바꾸는 것**
> (불변식·함정·권한·데이터 계약·경계)만 CLAUDE.md에 남기고, **화면 배치·컴포넌트 열거·반응형 단계·도입 이력**은 여기로 옮겼다.
>
> ⚠️ **이 파일은 옮겨온 원문이다**(요약 아님). CLAUDE.md 해당 항목에 `→ SCREENS.md §…` 포인터가 있다.
> 화면을 손볼 때는 여기를 먼저 읽고, **여기서 발견한 새 함정·불변식은 CLAUDE.md에 올린다**(그게 그 파일의 값어치).

## 프로젝트 상세 — 정보(메타) 탭 폼

- 메타('프로젝트' 탭): **폼 배치**: 1줄 `프로젝트명 | 프로젝트 매니저` · 전폭 `아티스트`(여러 명 칩) · 전폭 `제작/운영`(단일) · 전폭 `고객측 담당자`(여러 명 칩) · 전폭 `메모`. **고객측 담당자 = 여러 명**(personCombo multi 칩): `project_contacts` 다대다 + `contact_party_id`=첫(대표, 청구처 파생·관계자 탭·표시 호환). 제출=칩마다 `contact_id`+`contact_name` 쌍(`resolveContactIds` 인덱스 페어링·linkClientContact 패턴), `setProjectContacts` 통째 교체. 각 담당자 상세 '연결 프로젝트'·관계자 탭에 전원 반영(`listProjectsForParty`·`ASSOCIATE_ROLE_SUBQUERY`에 project_contacts UNION). **제작/운영은 단일**. **소속/레이블 입력 필드 없음**: 저장 시 `resolveProjectParties`가 **첫(대표) 아티스트의 현재 소속(`currentAffiliation(artistId).org_id`)에서 `agency_id`·표시 `artist_company`를 파생**(다중 아티스트=첫 기준·소속 미설정=null·기존 프로젝트는 편집-저장 시 재파생). 소속 지정은 아티스트/연락처 쪽에서(간이 등록 모달 '소속사' 칸 유지). app.js `fillAgency`/`currentAgencyValue`는 소속사 칸이 없어도 no-op 가드. **제작/운영은 프로젝트 속성이라 명시 입력 유지**. 담당자는 특정 회사에 안 묶인 고객 측 PM으로 전폭 독립(고객측 PM은 소속/레이블/제작/운영 어디서든 옴 — [[two-pm-model]]). 세션의 '담당 엔지니어'(`session_engineers`)와 겹치지 않게 프로젝트 단 `manager_id`는 **'프로젝트 매니저(PM)'** 라벨. **고객측 담당자**(연락처 콤보 `contactCombo` — 선택 시 전화·이메일·소속 즉시 표시, 목록에 없는 이름 입력 시 저장하면 새 연락처 자동 생성·연결) / **담당 엔지니어**(스튜디오 내부, `manager_id`) / 메모. **담당자 콤보(personCombo)는 본명·활동명 둘 다 검색**: `contactOptions`에 `p.activity_name`, `personCombo`/`personComboOptionsScript`가 옵션에 `alt`(=활동명) 임베드, app.js 검색이 `name`(본명)·`alt`(활동명) 둘 다 매칭. **라벨에 호칭·활동명 병기**(`본명 호칭 (활동명)`, 예 `박수한 대표님 (워터멜론)`·`김보종 대표님`): `contactOptions`에 `p.honorific`, 옵션 JSON에 `honorific` 임베드, app.js 라벨이 `name`+호칭+`(활동명)` 표시. `party-created` 브로드캐스트도 본명 우선 표시+활동명 검색 보조로 정규화(pcPrimary=realName||name·pcAlt). **담당자 이름 전면 병기**(`본명 (활동명)`): ①personCombo **선택 표시**(보이는 입력, 서버 렌더 `shown`)=`본명 호칭 (활동명)`(app.js `labelOf`·서버 `shown` 동일 형식), **제출 숨김 이름(`pureName`)은 순수 본명**(app.js input 핸들러가 본명·라벨 정확 일치 시 id 유지+hidName=순수 본명, pick·모달 저장도 `labelOf` 라벨 세팅). **호칭 중복 방지**: `name`이 호칭으로 끝나면 호칭을 또 붙이지 않음(`n.slice(-h.length) !== h`일 때만). 표시는 `honorific` 컬럼 기준으로 일원화 + name은 순수 본명. 공용 헬퍼 **`personName(person)`**(views.js — `본명 + 호칭(컬럼) + (활동명)`, name이 호칭으로 끝나면 중복 안 붙임): 연락처 목록·상세·suggest·관계자 탭·personCombo `shown` 공통. `resolveDisplayName`이 name에 호칭 안 넣음(성+이름만, 호칭 컬럼 전용). **1회 마이그레이션 `person_name_honorific_split_v1`**(db.js): person name이 ` `+honorific으로 정확히 끝나면 name에서 호칭 제거. ②**필드 밑 주석**: 선택 담당자의 전화·이메일·소속 회사(`data-pc-info`)를 서버에서도 렌더(무JS, app.js setInfo와 동일 구조·data-copy 복사 버튼). ③세션 디렉터 dirRow가 `optionsRef`에 더해 `options`(sel 라벨 조회용)도 전달. ④`/sessions` 목록 디렉터 줄·구글 캘린더 설명 디렉터=`personLabel(name, activity_name)`. ⑤그룹 멤버 목록=`활동명 (본명)`·업체 상세 소속 아티스트(`listArtistsForAgency`에 `real_name`)·연락처/클라이언트 suggest 라벨 병기·그룹 멤버 콤보 본명 검색(`artistPersonOptions`에 `alt`=본명). **청구처 표시 = 본명 (활동명)**: 화면 표시(청구처 콤보 `payerCombo`·청구 목록/상세 `client_name`·청구처 정보 카드 상호·상세 제목). `clientOptions`에 `real_name`·`activity_name`, `listInvoices`/`getInvoiceForUser`/`listInvoicesForProject`의 `client_name` SQL은 `PAYER_DISPLAY_SQL`(CASE로 `본명 (활동명)`), `snapshotPayer`에 `activity_name`(name=본명 유지), `payerName`/`payerInfoCard`/상세 제목이 `personLabel(name, activity_name)` 병기. **거래명세서 PDF 상호(`invoice-pdf.js` `client.name`)는 본명만 유지**(현금영수증 법적 명의). **마감일 없음**(`due_date`는 레거시 컬럼·청구 입금마감일과 별개). **청구처(실결제자)는 청구 섹션에서 결정**. `projects.client_id`는 저장 시 제작사›소속사›아티스트 우선순위로 자동 파생(표시·대시보드·클라이언트 연결용). **필드명**: `소속/레이블`·`제작/운영`(표시 라벨 — 저장 role 값 `소속사/레이블`·`제작사` 보존, `companyRoleLabel`이 표시 변환·프로젝트 파생·db 마이그레이션 호환). 폼 라벨·목록 메타(`소속`·`제작/운영`)·업체 역할 배지·간이등록 모달 체크박스·클라이언트 desc 갱신. **제작/운영에 개인(관계자) 선택 가능**: `production_company` companyCombo에 `{partyIdField:"production_party_id", partyIdValue:production_id}` opt-in → 콤보가 회사+사람(관계자·개인) 함께 검색(사람 옵션 부제=소속회사/'관계자'/'아티스트'), 선택 시 hidden `production_party_id`에 party id 제출. `resolveProjectParties`가 `production_party_id` 있으면 그 party를 `production_id`에 직접 사용(사람이면 사람 party·회사면 회사 party), 없으면 `ensureCompanyParty`로 회사 찾/생성. **소속/레이블·제작/운영에 회사가 지정되면 그 회사 클라이언트 역할에도 반영**: `addCompanyRole(agencyId, "소속사/레이블")`·`addCompanyRole(productionId, "제작사")`(parties.js — 회사면 roles CSV에 추가·멱등·사람이면 no-op) → 한 회사가 겸하면 클라이언트 목록·상세에 두 배지. 사람(개인 제작자)은 roles 미사용·`classifyParty`가 `production_id` 참조로 '제작/운영' 배지 파생. **다운스트림 안전**: `client_id`/payer 파생이 `COALESCE(production_id, agency_id, artist_id) JOIN parties`라 party kind 무관(사람이 production_id여도 정상·사람 상세 '연결 프로젝트'에도 노출). app.js companyCombo: 사람/회사 pick 시 `data-cc-party-id` 세팅(모달 신규 회사 id도) — 순수 이름/병기 라벨과 정확 일치(유일)하면 타이핑에도 id 유지(부분 문자열만 해제, personCombo와 대칭), **party-created 사람 브로드캐스트 수신**(사람 허용 콤보만 — 함정 #20 일관). **서버 안전망**: id 없이 텍스트만 오면 `resolvePartyByDisplay`(회사 상호 정확 → 사람 본명/`본명 호칭`/`본명 (활동명)`/활동명 유일 매칭 — 생성 없음)로 기존 party 해석 후, 그래도 없을 때만 새 회사 생성 → 사람 라벨 텍스트가 유령 회사로 저장되는 것 차단. `resolvePersonByName`도 동일 라벨 안전망(라벨 형식 신규는 본명+활동명 분해 저장, 유일 매칭만 재사용·동명이인 보수). **소속/레이블은 회사 전용**(partyIdField 미전달 시 party-id 필드 없음). **아티스트 병기 + 담당자 자동채움**: ①제작/운영 사람 옵션·선택값을 `본명 호칭 (활동명)` 병기(app.js companyCombo `dispOf`, 회사는 name 그대로; `partyOptions`에 `honorific`, 사람 옵션에 `kind:'person'·alt:활동명·honorific` 임베드; 활동명 검색; production_company TEXT엔 병기 라벨 저장). ②제작/운영에 개인 선택 시 고객측 담당자 자동채움(비어있을 때만): app.js companyCombo pick이 `o.kind==='person'`이면 같은 폼 `[data-person-combo]`의 `__pcSetById(id)` 호출(personCombo IIFE가 `root.__pcSetById`/`__pcHasValue` 노출). **아티스트는 커스텀 콤보**(`artistCombo`, Gmail식 칩): 선택된 아티스트가 배지(칩)로 담기고 ✕/빈 칸 백스페이스로 통째 삭제. **서버 계약**: hidden `artist`=활동명 콤마 목록·`artist_contact_id`=단일 선택일 때만 명시 id(다중이면 비워 서버가 이름별 해석). 칩 라벨='활동명 (본명)' 병기. 소속사 자동 채움은 첫 아티스트 기준. 타이핑=기존 아티스트·사람 검색, 빈 입력 시 **[🔍검색]/[＋새 아티스트] 팝업**(app.js `[data-artist-combo]`, 옵션은 `<script type="application/json">` 정적 임베드·CSP-safe). 기존 사람을 고르면 hidden `artist_contact_id`로 연결. **콤마로 여러 명 등록**("아이유, 태연"): ①콤보는 마지막 콤마 뒤 조각으로만 검색(app.js `seg()`), 선택·간이등록은 이어붙임, 다중이면 `artist_contact_id`·본명 표시·소속사 덮어쓰기 비활성(소속사는 비어있을 때만 첫 아티스트로) ②서버 `resolveProjectParties`가 이름별 해석(기존 그룹 정확 일치 재사용 → `resolvePersonByName` 유일/라벨/활동명 안전망 → 신규 생성+아티스트 마킹, dedup) ③**`project_artists(project_id, party_id)` 다대다**(`setProjectArtists` 통째 교체·CASCADE·`project_artists_backfill_v1` 1회 백필)에 전원 기록 → 모든 아티스트의 상세 '연결 프로젝트'에 매칭(`listProjectsForParty` 확장·deleteParty 정리) ④`projects.artist` TEXT=정규화된 콤마 목록(표시), `artist_id`=첫(대표) 아티스트(레거시 호환·표시용. 청구처는 청구 폼에서 항상 직접 지정, 추천 칩만 제공). 단일 입력은 기존 경로(그룹 체크·명시 id·본명칸) 그대로. **새 아티스트 등록 모달 = 유형 먼저**: 등록 유형 select(`개인 아티스트 (솔로·멤버)` / `그룹 (밴드·팀 자체)`, `data-am-type`·name 없음=폼 미제출). **개인**: 라벨 '활동명' + 본명 + 소속 그룹 미니콤보(`data-am-group-*` — 검색 + `＋ 새 그룹 등록`[fetch POST /clients type=group], 선택 시 hidden `group_id` → 서버 `setPartyGroup`으로 멤버 연결) + 소속사 + 전화·이메일 / **그룹**: 라벨 '그룹명' + 소속사만(본명·소속 그룹·전화·이메일 숨김). 모달 소속사는 이름으로 전송(`agency_company`, `ensureCompanyParty` 재사용/생성). **저장 시 아티스트↔사람 자동 연결로 중복 방지**(`applyArtistLink`→`linkArtistToContact`): 개인 아티스트가 명시 선택(`artist_contact_id`) 또는 같은 이름 기존 연락처와 매칭되면 그 연락처의 아티스트로 통합(`source_contact_id` 세팅, `ensureClient`가 만든 orphan 아티스트 클라이언트 흡수·연락처 `nickname`=활동명). 그룹 체크 시 `ensureGroupArtist`로 `is_group=1`(연락처 미연결). 개인 아티스트는 매칭 연락처가 없어도 항상 새 연락처 생성·연결(`resolveContactForArtist`). **동명이인 오연결 방지**: 명시 선택 없을 때 이름으로 연락처를 찾되 정확 일치가 유일할 때만 재사용, 2+ 동명이인이면 새 연락처 생성(`resolveContactForArtist`, 일반 `resolveContactByName`[첫 매칭]과 달리 병합 경로에서만 보수적). **기존 중복 병합**: 고아 아티스트 클라이언트(`source_contact_id` NULL·개인)를 같은 이름 연락처에 서버 기동 1회 백필로 연결(`artist_contact_link_backfill_v1` in db.js — name/nickname 유일 매칭만, 그룹·이미 연결된 연락처 제외). **본명/활동명 규칙**: 콤보에 '본명' 입력칸(`artist_real_name`, 그룹 체크 시 숨김·app.js `[data-artist-real]`) (간이 등록 모달 필드=활동명·본명·소속사·전화·이메일[`data-am-email`→`POST /clients` `email` → `createPerson`]) — 저장 시 연락처 `name`=본명(있으면)·`nickname`=활동명, 아티스트 클라이언트 `name`=활동명. **표시**(`personLabel` in views.js, 본명≠활동명일 때만 괄호): 연락처=본명(활동명), 아티스트 클라이언트=활동명(본명)(`listClients`/`getClient`가 `source_contact_name` LEFT JOIN). `classifyContact`에 '아티스트' 배지(source_contact_id로 아티스트 클라이언트와 연동된 연락처).

## 프로젝트 목록 — 컬럼 표·정렬·반응형·펼침

- 목록=한 줄 요약 카드 + 검색(`?q=`). **'내 프로젝트만' 필터**: 전 프로젝트 열람 유지 + opt-in 토글(`?mine=1`, URL 쿼리만·기억 안 함). 내 것 = 로그인 담당자(`getManagerByUserId(user.id)`) 관여 전부 = PM(`projects.manager_id`) OR 담당 세션(`session_engineers`) OR 담당 작업(`track_tasks.engineer_id`) — 순수 함수 `listProjectIdsForManager(mid)`(UNION 한 쿼리→Set), 라우트가 `listProjects` 결과 필터. 토글 pill은 담당자 행 있는 계정만(없는 대표 등은 숨김·`?mine=1` 강제해도 무효 안전망). `keepQ`가 탭·검색·완료토글 복귀(`?open=`) 링크에 mine 보존. **정렬=탭별**: 진행 중=다가오는 세션 임박순(`splitProjectTabs`가 `next_session_date` ASC·없으면 뒤, SQL `created_at DESC` 폴백), 청구 필요·완료=작성일 최신순. **작성일 컬럼**(전 탭, `created_at` 앞 10자). 편집=상세 '정보' 탭 치프만(`projectMetaCard(p, err, {chief})`, 날짜 콤보 + **명시 '저장' 버튼**, `POST /projects/:id/created-at`=`requireChief`·`safePath(req.body.return)` 복귀·시각 HH:MM:SS 보존). **폼 제출 후 스크롤 보존(전 폼 공통)**: `public/js/app.js` 최상단 IIFE가 모든 `<form>` 제출(capture `submit`·`formtarget="_blank"` 제외)에서 `sessionStorage["scrollPos:"+pathname]`에 `window.scrollY` 저장 → 같은 경로 복귀 시 1회 복원(경로만 비교, `flash=`·`open=` 무시). **3탭 분리**: `splitProjectTabs(rows)`(순수 함수) = 진행 중(`active`·`!is_completed`·임박순)/청구 필요(`billing`·`is_completed && unbilled_cnt>0`)/완료(`done`·`is_completed && unbilled_cnt===0`) 상호 배타. `?tab=active`(기본)/`billing`/`done`, 활성 탭만 렌더(라벨 개수·빈 탭 문구 탭별). 청구 필요 탭 = 대시보드 '청구 필요' 카드와 같은 정의. `listProjects`에 `unbilled_cnt` 파생(미청구 작업 + `listBillableSessionsForProject` **동일 조건** 세션 — 두 곳이 갈리면 '청구 필요 N'과 실제 후보 목록이 어긋난다). ⚠️**단가 항목 미선택(`rate_item_id` NULL)도 센다**(2026-07-23) — 예전엔 양쪽 다 `rate_item_id IS NOT NULL`로 걸러서, 완료된 대관 세션이 **청구 후보와 집계에서 동시에 사라져** 프로젝트가 조용히 '완료' 탭으로 넘어갔다(매출 누락). 산정이 안 될 뿐 청구는 여전히 '필요'하므로 센다. 🔒 **미청구 신호는 완료 여부와 무관**(2026-07-23): 목록 행 '청구 필요 N' 배지는 **어느 탭에서든** `unbilled_cnt>0`이면 뜬다(예전엔 `tab === "billing"` 한정이라 진행 중 프로젝트의 미청구가 안 보였다). 탭 분류는 불변. ⚠️🔒 **후반작업(믹싱·마스터링) 세션도 '청구 미착수' 신호로 센다**(2026-07-24) — 후반작업은 세션이 아니라 곡·콘텐츠 작업으로 청구하는데(설계), **세션만 마치고 작업/금액을 아직 안 만든** 프로젝트는 청구할 게 없어 보여 조용히 '완료'로 넘어갔다(허영생·해로이키 믹스 사례, 07-23 대관 세션 버그와 **동일 클래스**). → `unbilled_cnt`에 **완료(또는 지난)·미취소·미waived 믹싱/마스터링 세션이 있고 곡·콘텐츠 작업 0 AND 청구서 0**이면 **+1**(EXISTS 플래그·라인 아님·`POSTPROD_SESSION_TYPES`/`POSTPROD_IN` config, 가드 ③). 작업/청구서가 생기면 **자동 해제**(작업 기반 분류가 이어받음)·무료면 작업 만들어 '청구 안 함'. **명시적 완료 세션만**(`status='완료'` — 2026-07-25 세션 완료 판정 버튼 기준화로 지난 예정 세션이 진행 중에 머물게 되며 날짜 게이트 제거. 완료도 안 누른 세션에 청구 배지가 뜨는 모순 제거). 후반작업 세션은 **청구 후보 목록·청구 폼엔 직접 라인으로 안 뜬다**(설계 유지) — 대신 청구 탭에 후보 비면 `hasPostprodSessionNeedingBilling`(unbilled_cnt 항과 **술어 정합**·회귀 잠금)로 '곡·콘텐츠 탭에서 작업을 만들어 청구' 안내. 회귀 `billing-radar.test.js` 18. **완료 판정** `is_completed`(`listProjects` 파생) = **예정 세션 없음**(`sess_scheduled=0` — 완료 안 누른 세션[`status='예정'`]이 하나라도 있으면 **미래든 지난이든** 진행 중, **작업 `open_tasks`와 대칭**. ⚠️2026-07-25: 이전엔 `upcoming_cnt=0`[미래 예정만]이라 **지난 예정 세션이 날짜만 지나면 완료/청구필요로 샜다** — 세션도 버튼 기준으로 통일해 교정[세션 상태는 자동 안 바뀜·완료 안 누르면 계속 예정]. 취소·완료 세션은 예정이 아니라 진행 안 막음. `upcoming_cnt`·`next_session_date`는 진행 중 정렬·다음 세션 표시 전용으로 유지[게이트만 `sess_scheduled`로 이동]) AND 미완료 작업 없음(`open_tasks=0`·`status<>'Completed'`) AND 실제 활동 있었음(`content_cnt>0` — 빈 새 프로젝트는 진행 중)(`projects.routes` GET `/`). **상세 백링크 = 보던 탭 복귀**: 카드 링크 `?return=<목록 경로(tab·q·mine)>`(encodeURIComponent), 상세 pageHeader back=`safePath(req.query.return)`(내부 절대경로만·`/projects` 폴백). `projectRowHref(p, tab, listQuery)`·`projectListRow({listQuery})`. **카드 클릭 = 탭 맥락 목적지 직행**: `projectRowHref(p, tab)`(views.projects.js 순수 함수) — 청구 필요→청구 탭(`?tab=invoice`) / 진행 중→세션 일정(`?tab=sessions`), 세션 없고 곡만 있으면 곡·콘텐츠(`?tab=tracks`) / 완료·둘 다 없음→정보 탭. `listProjects`에 `sess_cnt` 파생. 청구 탭은 `canBill`(전원)이라 권한 안 막힘. **행 = 청구식 컬럼 표**: 헤더 `projectTableHead` 항목명 = `작성일·제작사·아티스트·프로젝트·PM·다음 세션·금액`(**작성일 맨 앞**). ⚠️좁아지면 우측부터 열 숨김(작성일 <1280 → 금액 <1024 → PM <820), 모바일 카드(<640)에선 `.pt-created { order: 1 }`로 작성일을 맨 뒤로(첫 줄이 날짜면 프로젝트 식별이 밀림 — DOM 유지·flex order만). **항목명 클릭 = 정렬**(첫 클릭 오름·다시 내림·`aria-sort` 화살표·키보드 Enter/Space + IME 가드·리로드 시 서버 기본 정렬). **청구 목록과 공용 코어** app.js 파일 스코프 `wireSortHeaders`+`sortCellValue`(정렬 규칙[빈 값 뒤로·타입별 비교·방향 토글·aria-sort]을 한 벌로, **구조 차이만 `io`로 메운다** — 청구=표라 `row.cells[헤더 인덱스]`, 프로젝트=grid라 `[data-sort-key]`로 셀 찾음[열이 CSS로 숨어도 안전]). 마크업 계약: 컨테이너 `data-sort-rows`·행 `data-sort-row`·짝 `data-sort-key`·헤더 `data-sort-type`(num|date|text). ⚠️**셀 `data-sort-value` 항상 명시** — 금액 `₩1,200,000`은 문자열 정렬 불가(정수 필요), 다음 세션은 화면이 `D-3` pill이라 ISO 날짜 별도, **빈 칸의 `—`는 빈 값으로 안 쳐 이름들 사이에 끼므로** 값 없으면 `""`(방향 무관 뒤로). 회귀=`project-list.test.js`(마크업)+`ui-interactions.test.js`(app.js 동작). ⚠️**열 순서를 바꿀 땐 `grid-template-columns`의 fr·rem 값도 함께 옮긴다**(값이 자리에 붙어, 순서만 바꾸면 긴 상호가 좁은 칸에 들어가 더 잘리고 고정 폭도 어긋난다·반응형 4단계 전부). 각 행과 헤더가 **같은 CSS grid 열**(`.proj-thead`/`.proj-summary`)이라 칸 정렬. 각 행 `<details class="proj-row">` — **행 전체가 이동 링크**: 모든 데이터 셀이 `projectRowHref`로 가는 `<a class="proj-link">`(어느 셀이든 탭 맥락 목적지로, `<summary>` 안 `<a>`는 이동만·토글 안 함=브라우저 기본), **오른쪽 끝 `.proj-toggle`(비인터랙티브 span·chevron)만 클릭 시 그 자리 펼침**(세션·곡 요약 + 완료 토글, 네이티브 `<details>`·무JS). 셀 밑줄·색강조 없음(`.proj-link:hover`=inherit·no underline), 어포던스는 행 배경 호버(`.proj-summary:hover`)만. 컬럼값: 제작사=`client_name`, 아티스트=`projectArtistOnly`(다중 `외 N`·폴백 없음), 프로젝트=`title`, PM=`manager_name`, 다음 세션=**진행 중 탭만** `nextSessionLine`(디데이 pill 3색: 3일내 빨강·2주내 주황·그 외 회색), 금액=`projectAmount`(청구 필요 탭은 '청구 필요 N' 배지 병기), 작성일=`created_at` 앞 10자. ⚠️**셀 잘림 = `.pt-cell`**(CSS를 `.proj-cell`로 잘못 쓰면 마크업과 어긋나 nowrap 안 먹어 셀이 글자 단위로 세로로 쪼개진다 — grid 아이템은 `min-width:0`도 필요[기본 auto라 내용보다 못 줄어듦]). 아티스트·제작사·프로젝트=유연 열(넘치면 `…`), 나머지 고정 폭. **반응형 <640px = 2열 카드**: thead 숨김, **왼쪽=무엇**(작성일[작게]→제작사[약화]→아티스트[굵게]→프로젝트명[muted], 데스크톱 열 순서 그대로) / **오른쪽=얼마·언제**(금액[굵게·우측정렬]→다음 세션[우측·D-day pill]). 금액을 한 열에 우측 정렬하면 자릿수가 맞아 세로 비교 가능(청구 모바일 카드와 동일). **배치는 `grid-row`/`grid-column` 명시**라 DOM 순서 무관. 라벨 prefix 없음(자리가 이미 무엇인지 말함). **`⌄`는 첫 줄 오른쪽 제 열**(`grid-column:2; grid-row:1; justify-self:end`) — 금액·다음 세션은 그 아래로(⌄에 제 열을 안 주면 오른쪽 내용을 밀거나 겹친다). `row-gap:0` + `.pt-cell { line-height: 1.35 }` + 첫 줄 날짜 `align-self:center`(첫 줄 높이는 ⌄ 1.75rem가 정함). 헬퍼 `projectArtistLabel`/`projectSubLabel`/`projectIdentity`는 다른 소비처 호환으로 잔존. **펼침 본문**(`.proj-expand`) = 인라인 상세(다가오는 세션 우선 재정렬[`projectSummaryHtml`가 `todayYmd()` 기준 upcoming-우선 후 8건, 지난 세션 회색·뒤] + 곡별 아티스트·제목·작업자 + 작업별 행). **펼침 세션·작업 완료 토글**(`canEdit`): 각 옆 `− 완료`/`✓ 완료`(세션=`POST /sessions/:id/status`·작업=`POST /projects/tasks/:id/status`, `return=/projects?tab=&open=projectId`, `listProjectSummaries`에 `id`·`status` 추가) → 복귀하며 그 카드 재펼침(`?open=`→`<details open>`)·스크롤 보존. 요약=`listProjectSummaries(ids)` 배치 2쿼리(N+1 회피). **금액 = 전체 버짓 = 작업액 + 세션액**(작업액=Σ`COALESCE(NULLIF(total_price,0), task_types.unit_price)` — 확정 없으면 종류 기본단가 폴백; 세션액=`sessionAmountsByProject`·폴백 `p.rate`). **청구된 항목도 포함**(청구 여부 무관 전체 버짓). **청구 확정 시 확정액 우선**: 청구된 작업=`total_price`, 청구된 세션=`invoice_items` 확정액(`sessionAmountsByProject`가 청구 세션은 invoice_items 합계 우선), 미청구만 예상액. **청구서 할인 차감**: `listProjects`·`getProjectForUser`가 `invoice_discount_total`(from-tasks 청구서 할인 합·수동 청구서 제외) 파생, `projectAmount`가 라인 합에서 차감(0 하한·라인 없으면 `p.rate`). 목록·상세 동일.

## 세션 예약 폼 — 시간 블록·구간·세부정보·사람

> ⚠️ 아래 원문에는 **할증 체크박스** 서술이 남아 있다 — 그 기능은 2026-07-27에 **폐기**됐다(가산은 단가 항목으로 표현).
> 배치·시간 콤보·구간 부분만 유효하다.

- **폼 레이아웃(추가·편집 완전 통일) = 구글 캘린더식**: 추가·편집 모두 `sessionBookingFields(s, managers, rateItems, rooms, defaultBooker, pmName)` 3구역 — ①**최상단 전폭 시간 블록 = 구글식**: `[날짜][시작]–[종료][종료날짜]` 한 줄 + **종일** + 소요 슬라이더. **날짜 2개 = 커스텀 날짜 콤보(`data-date-combo`)**: 보이는 칸은 **자유 타이핑**(`317`·`3/17`·`12/31`·`2026-03-17`·`3월 17일`·`오늘`/`내일` — app.js `parseDateText`, 롤오버 날짜[2월 31일]는 거부하고 원래 값 유지), 표시 형식은 **`2026. 3. 17. (화)`**(서버 `formatYmdCombo`[lib/date]와 app.js `fmtDateCombo`가 **같은 형식** — ⚠️다르면 로드 직후 값이 튄다. 요일은 UTC 파싱이라 서버 TZ 무관). **값은 hidden**(`name=session_date`/`end_date`, `YYYY-MM-DD`) + 마커 `data-session-date`/`data-end-date`도 hidden에(소요 역산·겹침 조회·서버 계약이 사용), 보이는 입력은 name 없음(함정 #19). **팝오버 = 월 그리드**(‹ › 월 이동·날짜 클릭·오늘/선택 강조), **키보드**(←→ ±1일, ↑↓ ±7일 — 이동 시 칸에도 미리보기, Enter 확정, Esc 취소), IME 조합 중 Enter 무시(함정 #18). **팝오버 버튼(월 이동·날짜)·시간 옵션 버튼은 `tabindex="-1"`**(tab 순서 밖 — 날짜칸 타이핑 후 Tab이 팝오버가 아니라 다음 필드[시작 시간]로 이어지게, 시간 콤보도 동일). 종료 날짜 자동 계산(자정 넘김 +1일)은 hidden에 쓰고 `__dcSync()`로 표시 갱신, '사용자가 편집 중인 칸은 덮지 않음' 판정은 hidden이 포커스를 못 받으므로 `__dcInput`(보이는 칸)으로 한다. **시간 박스=콤보(`data-time-combo`)**: 포커스 시 **전체선택**(타이핑만으로 교체)+**00:00~23:30 30분 단위 드롭다운**(현재 값 근처 스크롤·클릭 선택·Escape/Enter 닫기[IME 가드]). **종료 날짜도 편집 가능** — 일수 차+종료 시각으로 소요 역산(<24h 표현 범위 밖이면 canonical 복원). **종일=구글/애플과 동일 개념(하루 종일·시간 없음, 운영시간 무관)**: `sessions.all_day` 컬럼(마이그레이션 `addColumn all_day`) + start/end NULL. 체크박스=`name="all_day" value="1"`(제출 소스·서버 권위: `all_day=1`이면 sessionFields가 시간값 무시하고 start/end NULL 저장), 체크 시 app.js `applyAllDay`가 시간 박스·종료 날짜·소요 블록 **숨김만**(시간값 안 건드림 → 해제 시 그대로 보존), 편집 진입 시 서버가 `checked` 렌더하면 UI 반영. **표시=`timeLabel`/billLine이 '종일'**(시간범위·소요 대신). **겹침 없음**(시간 NULL이라 findSessionConflict가 null 반환 — Google처럼 시간 점유 안 함, 청구 후보 쿼리도 `all_day=1 OR 시간 IS NOT NULL`). **캘린더=date-only 종일 이벤트**(`eventTimes`가 NULL 시간→`{start:{date},end:{date+1}}`). **청구=`sessionRateAmount`가 종일이면 1 기준 블록**(정액 항목=base_price·시간제 항목=1Pro, `allDay` 플래그·거래명세서 '종일' 스냅샷). **다일(multi-day) 종일**: 종일은 시간만 없애고 **종료 날짜(`sessions.end_date`)는 남겨 여러 날 일정**(예: 2/5~2/9). `end_date>session_date`일 때만 저장(단일일=NULL·시간세션 무시). 폼=종일 체크 시 시간콤보·소요만 숨기고 날짜 2개(시작·`name=end_date`) 유지, `updatePreview`가 종일이면 종료날짜 자동동기 스킵(사용자 지정 다일 보존). 표시=`종일 · ~2월 9일`. 캘린더=`eventTimes(endDate)` 배타적 종료(end_date+1). 16h 초과 편집-저장 잘림은 durationButtons hours>960 빈값 가드로 방지. **1회 백필 `session_all_day_backfill_v1`**: 00:00~23:59 세션을 종일(all_day=1·시간 NULL)로 승격. **소요 단일 진실원천=app.js `curDur`**(custom_hours는 **hidden 제출값**, ≤960일 때만 미러). 종일 체크=종료 날짜 옆 인라인, 소요 라벨(총 소요 '7시간')=**'소요 시간' 헤딩 옆**(`fmtDurationKo` 초기 렌더+app.js 동기), 시간 콤보 목록=현재 값 최상단 스크롤, 새 작성 프리필(시작=지금 다음 30분 슬롯·종료=+1Pro — `applyTypeDefault`에 스튜디오 기본 블록 폴백), 시간 입력 자동완성 원천 차단(보이는 입력 nameless+`data-time-hidden` 제출·함정 #19 패턴). 시작/종료 박스=콜론 자동(1400→14:00), **종료↔슬라이더 양방향**(종료 입력→소요 역산[자정 넘김 +24h], 소요 변경→종료 자동), 종료 날짜는 저장 필드 아님(자정 넘김 +1일 표시 — 스키마는 session_date 하나·야간=end<start). JS-on=start_time+custom_hours, **JS-off 폴백=start_time+end_time**(resolveEndTime이 hours 없으면 end_time 사용). ⚠️날짜 +1일 계산은 로컬 파트 조립 — `toISOString()`은 UTC라 KST에서 하루 밀림. ①-b **촬영 구간 블록**(2026-07-26, `data-segments`): 세션 종류가 촬영이면 시간 블록의 `[시작]–[종료]`·소요 슬라이더가 **숨고** 반입·설치/촬영/철수 3줄(각 시작·종료 시간 콤보, `name=seg_<kind>_start|end`)이 나온다 — 같은 시간을 두 번 입력하게 두면 어느 쪽이 진짜인지 알 수 없으므로 점유 시간은 서버가 구간 span에서 파생한다. 노출 조건은 화면에 '촬영'을 박지 않고 **단가 kind에서 파생**(`SESSION_TYPE_RATE_KIND`가 진실원천), 토글은 app.js `data-when-type` + `data-show-when-type`/`data-hide-when-type`. ⚠️**구간 입력에도 `attachTimeFormat`을 붙여야 한다** — 보이는 입력이 nameless(함정 #19)라 `syncTimeHidden`이 없으면 제출용 hidden이 비어 **구간이 조용히 저장되지 않는다**(콜론 자동 포맷도 함께 얻는다). 🔒**겹침 경고도 구간 span으로 판정한다**(app.js `segSpanMin`) — 숨겨진 시작·소요 칸의 옛 값을 보면 '경고 조건 ≡ 차단 조건' 불변식이 깨지고, 거짓 경고에 길들여진 승인이 진짜 이중 예약을 통과시킨다(2026-07-23 결함과 동일 클래스). ②**좌측 '세션 세부정보' 상자**(세션 종류·단가 항목[대관 종류일 때만]·룸 3열 + 담당 디렉터 + 메모, 구글의 일정 세부정보 카드. 🚫**할증 입력은 없다** — 아래 `surcharges` 참조) ③**우측 사이드 = 사람**(md:grid-cols-[minmax(0,1fr)_220px]; **PM=프로젝트 담당 엔지니어 읽기 전용 표기**['프로젝트 담당 — 표기만', sessionsSection이 project.manager_id로 계산해 전달] + 예약 담당자·담당 엔지니어 select, 구글의 참석자 패널). md 미만은 시간→세부정보→사람 세로 스택. **상태 필드 없음**(대기/완료는 완료 토글로 정함). **상태는 hidden `input[name=status]`로 현재값 보존**(편집 저장 시 초기화 방지 — 완료 세션 편집해도 완료 유지). 세션 취소는 **행/캘린더 팝오버의 '일정 취소' 토글 버튼**으로 처리(편집 폼이 아니라 토글 — `status=취소`, 캘린더는 삭제 없이 '(취소)' 제목으로 유지). 편집 폼은 기존 시간으로 슬라이더 초기화(`minutesBetween`), 저장 시 시작+소요로 종료 산정. `app.js`가 `[data-session-form]`을 **폼별로 초기화(멀티폼)**. **세션 종류(녹음/믹싱/마스터링/기타)는 항상 선택 가능**. **'+ 세션 추가' 버튼 우측 정렬**(`btn-sm`+`flex justify-end`).

## 공통 뷰 헬퍼 · 테마

- **UI 공통 헬퍼**(views.js): `tabBar`(aria-current 포함)·`filterChips`·`projectTypeBadge`·`listGroup`/`listRow`/`listRowLinked`(목록 행 통일)·**`dataTable(cols, rows, {filterList})`**(범용 정보 표 — 청구·프로젝트식 컬럼 표를 연락처·클라이언트에 재사용, 2026-07-16: `<table class="dt">` table-layout:fixed·**콜 폭=CSS 클래스**(`w`에 Tailwind 폭 클래스명 리터럴 `w-[16rem]` — 인라인 style은 CSP style-src에 막힘[함정 #27], w 생략=유동 폭으로 남은 공간 흡수)·`hide:'sm'|'md'|'lg'|'xl'`로 좁을 때 열 숨김(col도 `table-column`으로 함께 숨겨 폭 예약 제거)·`mobileHide`로 <640 카드에서 셀 숨김·<640 카드[thead 숨김·라벨줄·primary 셀=카드 헤더·**갭/개별 테두리 없이 구분선 하나로 이어지는 목록**=`border-bottom`만, 흰 바탕·바깥 테두리는 래퍼가 제공, 2026-07-16 청구 목록과 통일]·`filterList`면 tbody `data-filter-list`로 실시간 필터). **`src/views.contacts.js`**(2026-07-17 마스터-디테일 전환으로 신설, `contactTable`[6열 표] 대체·제거 — 위 '연락처' 섹션 참조): `contactPanes`(2단 골격)·`contactNameList`(이름만 마스터 목록)·`contactReadView`(읽기 뷰). `/contacts` 목록 전용(사람은 전부 이 렌더러 하나 — 옛 클라이언트 '관계자' 탭 공용은 2026-07-17 사람/조직 축 정리로 그 탭 자체가 없어져 해소). **업체·그룹(`/clients`)도 2026-07-18 마스터-디테일로 전환**(연락처와 동일 구조 — 위 dataTable 탭별 컬럼 org 표는 폐기): `src/views.clients.js`의 `clientReadView`(읽기 뷰)·`clientEditPane`(편집 뷰)가 `contactPanes`/`contactNameList`(views.contacts.js, 재사용)와 조합돼 왼쪽 업체/그룹 이름 목록(상한 없음·`liveFilter`) + 오른쪽 패널을 이룬다. `GET /clients`=목록(빈 패널)·`GET /clients/:id`=읽기 뷰(우상단 **[편집]**)·`GET /clients/:id/edit`=편집 폼 — 3분기는 아래 '상세=바로 편집' 참조. `dataTable` cols에 `hide:'sm'|'md'|'lg'|'xl'`·`mobileHide`·`mCard` 지원(`.dt-m-hide`/`.dt-mcard` 특정도가 모바일 `.dt tbody td{display:flex}`를 이김)·`emptyState`(아이콘·CTA)·**`explain(content)`**(긴 안내/항목 설명을 기본 접힘 '설명' 토글로 — 네이티브 `<details>`·무JS·CSP 안전; 상태·오류·모달 경고엔 쓰지 않음). badge 변형 5종(`badge-neutral`·`badge-success`·`badge-warning`·`badge-error`·`badge-info`); 쿨톤 `--color-info`(상태색/브랜드 액센트 분리). 사이드바 그룹화(운영/청구/관리 섹션, 좌측 레일 활성표시. **청구 그룹 순서 = 돈의 흐름 `청구 → 매출 → 외주 작업자`** — 외주 작업자는 '정산'이라 매출 뒤, 2026-07-20 사용자 요청). **테마 = 라이트/다크 하나뿐**(2026-07-30 팔레트 폐지). 색·폰트·radius·shadow를 `:root` CSS 변수로 추출(`--color-*`·`--font-sans/serif/display`·`--radius-btn/input/card/badge`·`--shadow-card`) → 컴포넌트(.card/.btn/.input/.badge)가 변수 참조하는 구조는 그대로고, 그 변수를 덮어쓰던 **팔레트 레이어(`html[data-palette=apple|linear|spotify|pinterest]`)와 사이드바 색 스와치 5개는 제거**됐다. 현재 `:root` = 옛 Pinterest 라이트를 접은 값(Pretendard/Helvetica·`.font-display` 700)에 **모서리를 각지게**(버튼 4px·카드 6px·배지 3px, 옛 pill·16px 폐기)하고, 같은 날 **전수 대비 점검으로 색을 AA 통과선까지 조정**한 것이다 — 액센트 **#0066CC**(다크 #3D9BFF), `danger`는 빨강 **#C62828**(다크 #F27272), `muted` #6B6B6B, 폼 경계 전용 `--color-border-strong`, 차트 전용 `--chart-*`. 새 색은 **배지(12% 틴트 위 같은 색 글자)에서 4.5를 넘는가**로 고르고 `test/color-contrast.test.js`가 계산으로 잠근다. **바탕은 흰색**이고 페이지·카드가 같은 값이라(회색 캔버스는 같은 날 시도 후 사용자 판단으로 되돌림 — 카드 안쪽 `bg-bg` 채움과 겹쳐 얼룩덜룩해졌다) 깊이는 **보더 + `.row-link`의 전경색 6% hover 오버레이**가 만든다(`hover:bg-surface`는 무효). 회색 톤(`elevated`)은 **한 겹만** — 펼침 바닥·모바일 드로어·설정 분류 헤더. 사이드바 하단에는 **라이트/다크 토글([data-theme-toggle]) 하나만** 남는다(라벨은 '누르면 갈 방향'). **라이트/다크는 OS 추종**(저장 theme 없으면 theme-init.js가 OS를 스냅샷해 `data-theme` 세팅). **FOUC 방지 = 서버가 `<html data-theme>`를 첫 페인트로 렌더(2026-07-21 사용자 리포트 '페이지 이동 시 깜빡')**: 옛 방식은 `theme-init.js`(head 동기 로드)만이 렌더 전 속성을 세팅했는데, **dev는 그 파일을 매 이동 재요청(max-age=0)·프로덕션도 첫 방문엔 캐시 없음**이라 그 지연 동안 서버 기본(라이트)이 먼저 그려졌다 바뀌는 깜빡임이 있었다. → **`theme`를 쿠키에 저장하고 서버가 첫 바이트에 렌더**한다: `src/lib/request-theme.js`가 `AsyncLocalStorage`로 쿠키를 요청 컨텍스트에 흘려(layout 호출부 30곳 무변경) `currentThemeAttrs()`가 속성 계산, server.js 미들웨어(cookieParser 뒤)가 `req.cookies.theme`를 담고, `layout()`이 렌더. **app.js**가 토글 시 localStorage+쿠키를 함께 기록, **theme-init.js**는 첫 방문·OS 변경 폴백으로 존속. 회귀 `test/request-theme.test.js`(컨텍스트·기본값·옛 palette 쿠키 무시·layout 렌더)·`test/theme.test.js`(단일 테마 토큰·각진 radius·팔레트 재등장 차단·FOUC 순서·구글 폰트 제거). muted 대비 AA 충족(#767676 4.54:1). **검색 typeahead(2026-07-04, 사용자 요청)**: 공용 `searchBox({action,q,placeholder,suggestUrl,hidden,liveFilter,noButton,remote})` 헬퍼(views.js). **`suggestUrl` 방식(타이핑 제안 드롭다운)**: `[data-search-suggest]`/`[data-suggest-pop]` 렌더 → app.js IIFE가 타이핑(200ms 디바운스)마다 `suggestUrl?q=`에서 JSON `[{label,sub?,href}]`을 받아 드롭다운 제안(↑↓ 이동·엔터 선택 이동·엔터 무선택 시 폼 제출=전체 검색·한글 IME 가드·AbortController 취소) — **프로젝트·세션**(`/projects/suggest`·`/sessions/suggest`, 반드시 `/:id` 앞 등록). **`liveFilter` 방식(실시간 목록 필터)**: 입력 `data-live-filter` + `listGroup({filterList:true})`의 `data-filter-list`/`data-filter-empty` → app.js가 타이핑 시 로드된 목록 행을 `textContent` 매칭으로 즉시 필터(서버 왕복 없음) — **업체·그룹·연락처 목록·청구 목록**(연락처는 2026-07-16 typeahead 드롭다운→실시간 필터로 전환). **`remote` 방식(상한 초과분 서버 보강, 2026-07-17 사용자 리포트 '업체 100개 넘어가면 검색이 못 찾음')**: 목록이 상한(`capList` 기본 100건)으로 잘리면 로드된 행만 훑는 실시간 필터가 100번째 뒤 항목을 못 찾는다 → 라우트가 `capped.more` 있을 때 `searchBox({remote:true})`로 입력에 `data-live-remote` 부여 → app.js가 타이핑 시 즉시 필터(로드분·빠른 피드백) **+ 220ms 디바운스 후 서버 전체 검색 결과(`?q=&limit=10000`)로 `[data-filter-list]` innerHTML 교체**(최신 요청만 반영·경합 토큰, 매칭 0이면 서버가 emptyState라 `data-filter-list` 없음 → 목록 비우고 안내, 검색어 비우면 원본 로드분 복원). **업체·그룹(전 탭)·연락처**에 적용(링크 행이라 innerHTML 교체 안전). **청구 목록(`/invoices`)은 remote 미적용**(행에 벌크 체크박스·롱프레스 바인딩 — Enter 서버검색 유지). `test/ui-interactions.test.js` jsdom(fetch 목)으로 교체·복원 잠금. **`noButton`=검색 버튼 없음**(2026-07-16 사용자 요청 — 업체·그룹·연락처 목록: 실시간 필터가 주 경험이라 버튼 제거, 폼은 단일 텍스트 입력이라 Enter로 서버 전체 검색은 유지). 서버 `?q=`(엔터)는 캡 넘는 전체 검색용. **모달 스크롤 잠금(전 모달 공통, 2026-07-04)**: app.js IIFE가 MutationObserver로 모달 마커(`[data-pc-modal]`·`[data-cc-modal]`·`[data-artist-modal]`·`[data-nav-guard]`·`[data-modal]`) 중 하나라도 열리면(=`hidden` 없음/DOM 존재) `<html>·<body>` overflow:hidden(+스크롤바 폭 padding 보정), 전부 닫히면 복원 — 모달 열린 채 배경이 스크롤되던 것 차단(사용자 요청).

## 매출 현황 — 탭·마스터-디테일·차트·항목 행

- **매출 현황**(`/revenue`, 사이드바 청구 그룹, 대표·치프 `requireInvoice`, 2026-07-19 고도화 — 스펙 `docs/superpowers/specs/2026-07-19-revenue-analytics-design.md`): 기간(년·월)·축(스탭 / 업체·개인)·순이익 현황 도구. **매출=공급가(VAT 제외 `amount−tax_amount`)·발생(발행일 `issued_date`, `status<>'미발행'`) 기준** — 청구 화면 '발행액'(VAT 포함)과 의도적 분리('매출'이라는 단어는 이 화면 전용, 청구는 발행액·입금·미수=거래액). **순이익=매출−외주지급**(`track_tasks.worker_rate`+`session_engineers.worker_rate`, 같은 청구서 issued_date 기간). **상단 기간 컨트롤**(년 ▾·월 ▾, 월에 '전체(연간)'; GET 폼·기본 이번 년·월. **셀렉트 변경 즉시 조회**[2026-07-19 사용자 요청 '보기 안 눌러도 바로 변경'] — 폼에 `data-auto-submit`, app.js가 그 폼 안 select의 change에서 제출. '보기' 버튼은 `<noscript>`에만 둬 JS 있으면 안 보인다[항상 두면 죽은 컨트롤]. **기간 컨트롤·기간 쿼리는 개요 탭 전용**[2026-07-19 기간 렌즈 분리 이후 — 아래 참조. 스탭별·업체개인별 탭은 기간이 없다]) + **탭 3개**(`?tab=overview|staff|payer`): **개요**=선택 기간·선택 년 누적 매출/순이익 KPI 4장 + **월별 매출 인라인 SVG 바 차트**(CSP상 색=CSS `.rev-bar` fill·기하는 SVG 속성, 인라인 style 금지) + Top 5 스탭·Top 5 업체·개인 / **스탭별**=엔지니어 **전체 누적** 순위 목록(매출·순이익·작업/세션 건수·하우스/외주 배지, 작업=`engineer_id`·세션=`engineer_name` 라인 귀속) + **월별 그룹** 상세(작업·세션 내역+외주지급 차감) / **업체·개인별**=결제자 **전체 누적** 순위 목록(공급가 기여·건수·업체/개인 배지) + **월별 그룹** 상세(발행 청구서 내역·`payer_kind` 현금영수증/계산서 배지·청구서로 새 탭). **다인 세션 귀속 = 모델 A(리드 귀속, 사용자 확정)**: 세션 매출·전체 외주지급을 리드(`engineer_name`=첫 엔지니어)가 흡수, 공동 엔지니어 별도 행 없음, **총 순이익은 정확**(세션 외주 한 번만 합산). 🔒 **스탭·종류·미귀속 축은 청구서 할인을 라인에 금액 비례로 배분**(2026-07-23 기능성 평가 — 옛 `SUM(ii.amount)` 라인 원금 기준을 대체). `ATTR_SUPPLY = ROUND(ii.amount × (i.amount−i.tax_amount) ÷ 라인총합)`(`LINE_TOTAL_JOIN`으로 청구서별 라인총합 조인) — `i.amount−i.tax_amount = 라인총합−할인`이라 배분 후 **스탭 합 = 청구처 축 = 개요 = 종류별 축**(네 축 일치). 옛 라인 원금 기준은 할인 청구서에서 스탭 합이 부풀었다(실측 사례 +33%, 도너츠컬처 18건 중 17건 할인). **단일 라인·무할인이면 = ii.amount**(대부분 — 배분이 실제 작동하는 건 다라인 할인뿐), `ROUND(라인별)` 후 SUM이라 상세 행 합 = 소계 = 총계 정수 정합. **외주 지급(worker_rate)은 배분 안 함**(고객 할인해도 외주 단가는 그대로). ⚠️네 집계 함수가 `ATTR_SUPPLY`를 함께 써야 하고 한 곳이라도 라인 원금으로 남으면 그 화면만 다시 부푼다 — 항등식 `스탭 합 + 미귀속 = 일 라인 귀속 공급가 총합`을 revenue.test가 잠금. 옛 개요 각주('스탭별 매출 합은 할인 시 다를 수 있음')는 제거(더는 사실 아님). 데이터=`src/data/revenue.js`(`revenueSummary/revenueByStaff/revenueForStaff/revenueByPayer/revenueForPayer/revenueYears({year,month})`·`issuedInPeriodSql`), 뷰=`src/views.revenue.js`(`revBarChart` SVG 등). 무JS·의존성 0. 회귀: `test/revenue.test.js`(집계·기간·순이익·모델 A 다인 세션)·`test/revenue-views.test.js`(렌더 계약·no-inline-style).
  - **미귀속 행(2026-07-20 사용자 요청 — 실DB 점검 발견 후속)**: 스탭 축은 작업=`engineer_id`·세션=`engineer_name`으로 사람에 붙는데, **그 연결이 없으면 그 매출은 스탭별 화면 어디에도 안 뜨고 조용히 사라진다**(프로덕션 점검에서 담당 없이 청구된 세션 2건·65만원 발견 — 총 매출·업체개인별엔 이미 들어 있어 **금액이 틀린 게 아니라 '누가 한 일인지'만 비는** 상태). → **스탭별 목록 맨 아래에 `미귀속` 행**(`revenueUnattributed(period)` in src/data/revenue.js, 링크 `?tab=staff&staff=none`) + 클릭 시 **같은 상세 렌더러**(`revStaffDetail`)로 해당 작업·세션을 월별로 나열해 **어느 세션인지 바로 찾아 담당을 지정**할 수 있다. **순위에 안 섞고 맨 아래 고정** — 사람이 아니라서 매출순으로 끼우면 '1위'가 사람이 아니게 되고, 개요 Top 5도 오염된다(개요는 `revenueByStaff`만 쓰므로 영향 없음). 금액이 0이면 행 자체를 렌더하지 않는다(평소엔 없는 줄). 세션 조건에 `engineer_name NOT IN (SELECT name FROM project_managers)`를 함께 둬 **담당자 개명으로 이름 매칭이 끊긴 세션**도 잡힌다(2026-07-20에 고친 결함의 안전망 — 동기화가 다시 깨져도 여기서 드러난다). **수동 청구서 라인**(task·session 참조 없음)은 제외 — 일 기록이 없어 사람에 붙을 대상이 아니고 업체·개인별 탭이 제자리다. 항등식 `스탭 합 + 미귀속 = 일 라인 총합`을 회귀로 잠금(revenue.test.js), 목록 계약 4건은 revenue-views.test.js.
  - **스탭별·업체개인별 = 마스터-디테일(2026-07-19 사용자 요청 — 스펙 `docs/superpowers/specs/2026-07-19-revenue-master-detail-design.md`)**: 왼쪽 순위 목록(`revStaffList`/`revPayerList`) + 오른쪽 상세 패널(`revStaffDetail`/`revPayerDetail`). 골격은 연락처·업체그룹이 쓰는 **`contactPanes` 재사용**(src/views.contacts.js). **선택은 URL 쿼리로만**(`?tab=staff&staff=<id>`·`?tab=payer&payer=<id>`) — 서버 렌더만으로 동작하고 신규 JS 0, 데이터 레이어 무변경. **배경**: 직전(698c596)에 이 두 탭을 읽기 폭으로 좁혔더니 오른쪽이 비었다 → 그 폭을 없애는 대신 상세로 채우자는 사용자 제안. → **세 탭 모두 `layout({wide:true})`**(패널 오른쪽 안쪽이 `max-w-content` 768이라 읽기 폭은 그대로 보장 — '읽기 폭으로 좁힌다'던 이전 서술을 대체). **패널 높이 = `REV_PANE_H`(`lg:h-[calc(100vh-15.5rem)]`, revenue.routes.js 상수)**: 연락처(11rem)보다 기간 컨트롤 줄만큼 낮다. 값은 브라우저 실측으로 확정(1710px에서 패널 상단 222px + 하단 여백 24px → 669px 이하 필요). ⚠️Tailwind는 소스 **리터럴만** 스캔하므로 동적 조립 금지(함정 #27). **목록 폭 저장키는 `revListW`로 분리**(연락처·업체그룹의 `clListW`와 독립 — 이름만 있는 좁은 목록과 이름+금액+건수 목록은 적정 폭이 다르다). `contactPanes({widthKey, heightClass, wideList})` 파라미터로 받고 기본값은 연락처 기준이라 기존 두 화면 무변경, app.js 리사이저는 `data-cl-width-key` 속성에서 키를 읽는다. **`wideList`=목록 기본 폭 26rem**(2026-07-19 사용자 스샷 기준 — 좁은 창[<sm] 전체 폭에서 긴 상호[`(주) 미스틱스토리(MYSTIC STORY Inc.)` 등]가 안 잘리던 416px를 데스크톱 기본값으로)(`.cl-list-wide`가 CSS var 폴백만 교체 — 저장키를 나눠도 CSS 기본값 18rem은 `.cl-col-left` 한 규칙이라 화면 간 공유되므로 별도 훅이 필요했다. 드래그 저장값이 있으면 항상 그쪽이 이긴다). **행 배치(2026-07-19 사용자 요청)**: 왼쪽=이름 / 부가(청구처=업체·개인·그룹 배지, 스탭=외주 배지 + 작업·세션 건수), 오른쪽=금액 / 부가(청구처=청구 N건, 스탭=순이익). 배지를 이름 옆에 인라인으로 두면 이름 폭을 먹고 숫자가 좌우로 흩어져 스캔이 어려웠다. **왼쪽 목록은 `lg:overflow-y-auto` 래퍼 필수**(`listGroup`의 `.card`가 `overflow-hidden`이라 고정 높이 패널에서 넘친 행이 **잘려 보이지도 클릭되지도 않는다** — 2026-07-19 최종 리뷰가 잡은 결함, 회귀 테스트로 잠금. 연락처 `contactNameList`와 같은 패턴). **키보드 이동 마커 `data-nav-list` 부착**(2026-07-19 사용자 요청 '업체 선택하면 목록이 맨 위로 간다·화살표로 다니고 싶다'): 로드 시 선택 행에 자동 포커스하고 ↑↓로 앞뒤 이동한다. **선택해도 목록이 보던 위치를 유지**한다 — 서버 렌더라 선택=페이지 재로드고 목록이 0에서 시작하므로, 행 클릭·↑↓ 이동 시 `sessionStorage["navScroll:<서버가 찍은 키>"]`에 `scrollTop`을 저장하고 로드 시 복원한다. **키는 서버가 `data-nav-list` 값으로 찍는다**(`contacts:all`·`clients:company`·`revenue:payer` — app.js는 그 값을 그대로 쓰고, 값이 없으면 옛 URL 방식으로 폴백). ⚠️**URL로 키를 만들면 안 된다**(2026-07-20 사용자 리포트 '연락처에서 **처음 선택할 때만** 스크롤이 바뀐다'): 목록은 `/contacts`(탭 쿼리 없음)인데 행 링크는 `/contacts/24?tab=all`이라 **첫 이동에서만 키가 어긋나** 복원이 실패했다(둘째부터는 양쪽 다 `?tab=all`이라 정상 — 그래서 '처음만'). 활성 탭의 **기본값**을 아는 건 서버뿐이다. 키가 화면+탭 단위여야 하는 이유는 그대로다(연락처·업체그룹은 상세로 가면 pathname이 바뀌고, 매출은 pathname이 같아 스탭별·업체개인별을 갈라야 한다). **복원 후에도 선택 행이 안 보일 때만** `scrollIntoView({block:"nearest"})`로 최소 이동(↑↓로 가장자리를 넘었을 때·URL 직접 진입). ⚠️2026-07-20 수정: 복원 없이 `scrollIntoView`만 하면 **선택 행이 목록 맨 아래에 딱 붙는다**(nearest가 '보이게 만드는 최소한'만 움직이므로) — 사용자 리포트로 발견, 실측 재현(클릭 전 위에서 236px → 클릭 후 하단 간격 0px) 후 수정. 초기 검증이 '보이는가'만 보고 '어디에 놓이는가'를 안 봐서 놓쳤다. 마커 이름은 옛 `data-contact-list`에서 **일반화**(연락처·업체그룹·매출 3화면 공용), app.js·CSS 셀렉터도 `aria-current` 값 무관으로 넓혔다(연락처는 `"true"`·매출은 `"page"`)). **목록·상세는 기간 없이 전체 누적**(2026-07-19 후속 — 아래 '기간 렌즈 분리' 참조): 활동이 없는 대상은 순위 목록에 아예 안 뜨지만(`supply>0` 필터), id로 직접 접근하면 상세는 열리고 "내역이 없습니다"/"발행 청구서가 없습니다"가 뜬다(`revenueForStaff/ForPayer`는 **대상 부재일 때만** null). **유효하지 않은 id는 404가 아니라 미선택 화면**(패널 안이라 왼쪽 목록은 살아 있어야 한다). **상세로 가는 길은 하나** — 구 드릴다운 `/revenue/staff/:id`·`/revenue/payer/:id`는 **302 리다이렉트**(호환용으로 유지되며 기간 쿼리도 그대로 실어 보내지만, 후속 '기간 렌즈 분리'로 목적지인 목록 탭 자체가 기간을 무시하게 됐다 — 아래 참조)로만 남고 개요 Top5도 패널 URL을 가리킨다. 순위 표 뷰(`revStaffTable`/`revPayerTable`)는 소비처가 없어져 삭제(연락처에서 겪은 '상세가 두 벌' 재발 방지).
  - **기간 렌즈 분리(2026-07-19 같은 날 오후, 마스터-디테일 직후 — 스펙 `docs/superpowers/specs/2026-07-19-revenue-lens-split-design.md`)**: 마스터-디테일 도입 직후 넣었던 위 '기간 변경 시 선택 유지'(`revPeriodControl({sel})`가 `staff`/`payer`를 hidden으로 실어 보내 같은 대상을 달별로 비교하던 것)는 **몇 시간 만에 제거**됐다 — 기간 컨트롤 자체를 **개요 탭 전용**으로 옮기면서 유지할 선택이 없어졌기 때문(스탭별·업체·개인별 탭에는 이제 기간 컨트롤이 없다). **배경**: 기간(언제)과 순위(누구)를 한 화면에 섞으니 '이번 달 1위'와 '역대 1위'가 같은 목록에서 헷갈렸다 → **개요=언제·목록 탭=누구**로 역할을 쪼갰다. **변경**: ①`revenueByStaff`/`revenueByPayer`가 **period 인자 없이 호출**(전체 누적 순위) ②목록 행에 **최근 거래월**(`lastSeenLabel`, `최근 2026.7` — 작업·세션 중 최신 발행일 `last_issued`, 값 없으면 생략), 링크는 **기간 쿼리 없는 URL**(`/revenue?tab=payer&payer=<id>`) ③상세(`revStaffDetail`/`revPayerDetail`)는 **월별 그룹 + 월 소계**(`groupByMonth`/`monthHeader`, 최신 월이 위. ⚠️**월 헤더에 `border-b`를 두지 않는다** — 바로 아래 항목 목록이 `.card`(border 1px)라 **간격 0으로 두 선이 맞붙어** 2px처럼 보였다[2026-07-20 사용자 리포트 '월 아래 밑줄? 항목 위 뚜껑?']. 구분은 카드 경계 하나로 충분하고 월 헤더는 글자 굵기·여백으로 이미 구분된다. revenue-views 회귀) — **스탭 상세는 한 월 안에서 작업·세션을 종류로 가르지 않고 날짜순으로 섞는다**(사용자 확정: 월별 리듬이 목적인데 종류로 먼저 가르면 리듬이 두 번 쪼개진다). **항목 행 = 4칸 그리드**(`revItemRow`+`.rev-item` CSS — **스탭·청구처 공용**, 청구처는 2026-07-20 적용, 2026-07-19 사용자 요청 '항목별로 나눠·날짜 크지 않게·종류 알려주고'): `발행일(일만) | 종류 | **아티스트** · 세부 ↗ | 금액`. **날짜는 '일'만**(월 헤더에 년·월이 이미 있다)·우측 정렬·작게. **종류 = 실제 일의 종류**(작업=`taskTypeLabel` 믹싱·보컬튠·스템제작 / 세션=`session_type` 녹음·공연·촬영) — **`작업`/`세션` 배지는 없앴다**(실데이터상 종류만으로 구분되고 배지는 자리만 먹는다. 단 설정상 '믹싱 세션'도 가능해 이론적 중복은 있다). **이름 = 아티스트**(2026-07-20 사용자 요청 '프로젝트명 말고 아티스트명으로 — 프로젝트명은 크게 중요치 않다'): 프로젝트명이 `회사명 + 아티스트`를 반복하는 경우가 많아(`더블유엠코퍼레이션 차가을`) 아티스트가 짧고 정확하다. 작업=`COALESCE(NULLIF(tr.artist,''), p.artist)`(트랙 아티스트 우선)·세션·청구서=`p.artist`, 다중은 `projectArtistOnly` 재사용으로 `추화정 외 2`, **비면 프로젝트명 폴백**(실측 프로덕션 92/92가 아티스트 보유). ⚠️**곡 제목 중복 판정은 '표시 이름'(아티스트) 기준**이어야 한다 — 프로젝트명과 비교하면 화면에 없는 값과 같다는 이유로 필요한 곡 제목이 사라진다(이름을 아티스트로 바꾸며 실제로 생긴 결함, 회귀 테스트로 잠금). **세부 = "여럿 중 어느 것"**: 후반작업은 곡 제목(**프로젝트명과 같으면 생략** — `Inferno / Inferno` 중복 방지), 세션은 **실제 세션 날짜**(`1월 31일`, 발행월과 다를 수 있어 월까지). 세션 날짜를 여기 두는 이유: 주 날짜가 발행일이라 **앵콜 공연 이틀치가 한 청구서에 묶이면 두 행이 완전히 같아져 몇 건인지도 구분이 안 된다**(실데이터 확인 — 26건 중 10건이 세션월≠발행월). 표 헤더는 두지 않는다(월 그룹마다 반복되면 시끄럽고 내용만으로 읽힌다). 좁은 화면(<640)은 3칸으로 접힌다. 종류·이름 칸은 잘려도 `title`로 읽힌다.
    **청구처 상세도 같은 4칸·같은 내용**(2026-07-20 사용자 요청 — 처음엔 `상태 | 청구번호`로 냈다가 '스탭별과 내용이 달라 일관성이 떨어진다'는 지적으로 재작업): `발행일 | 일의 종류 | 프로젝트 · 곡제목|세션날짜 ↗ | 금액`. 스탭 상세와 칸도 내용도 동일하다(상태 배지·청구번호는 뺐다 — 미수는 청구 메뉴가 본업이고 번호는 눌러 들어가면 있다).
    ⚠️**행 단위는 청구서 그대로 두는 것이 핵심**(사용자 결정): 스탭 상세는 `invoice_items` 라인 한 줄씩인데 청구처를 그렇게 바꾸면 **할인이 청구서 단위라 라인 합 ≠ 매출**이 된다(실측 도너츠컬처 18건 중 **17건이 할인**, 라인 30만 청구서의 실제 매출은 20만 — 행 금액이 33% 부풀고 월 소계·총계와 어긋난다). 그래서 **금액은 언제나 청구서 공급가(할인 반영)**이고 종류·세부만 그 청구서의 **첫 라인**에서 온다. 라인이 1개인 청구서가 대부분(17/18)이라 결과적으로 라인 단위 화면과 거의 같다. **라인이 여럿이면 세부를 `N개 항목`으로 접는다.** 데이터=`attachWorkSummary`(src/data/revenue.js — 청구서별 `work_kind`·`work_detail`·`item_count` 파생, 원본 작업·세션이 삭제돼 참조가 끊긴 라인·수동 청구는 빈 값이라 뷰가 이름만 렌더), 뷰=`payerItemDetail`(곡 제목이 프로젝트명과 같으면 생략·세션 날짜는 `7월 15일`로 포맷). ⚠️폭은 CSS 클래스로 — 서버 렌더 인라인 style은 CSP에 막혀 조용히 무시된다(함정 #27. 종류는 배지로만 구분), 스탭 월 소계=매출+순이익, 청구처 월 소계=매출만(청구처엔 외주지급 개념 없음) ④`revenueForStaff`의 세션 행에 **`payout` 파생**(그 세션에 배정된 전 엔지니어 지급단가 합 — 모델 A 리드 귀속 유지, 월 소계 순이익 산정용) ⑤`issuedInPeriodSql`에 **전체 기간 모드**(`period` 없음·`year` 없음·`'all'` → 조건 없이 `1=1`, `ISSUED`가 이미 `issued_date IS NOT NULL`을 포함해 발행일 없는 행은 안 섞인다) ⑥개요 Top 5 아래는 **'전체 N명/N곳 보기' `<details>` 펼침**(무JS)으로 바뀌고 옛 '전체 보기' 링크는 제거(목록 탭이 누적 전용이 되며 그 링크가 다른 기간으로 데려가는 거짓말이 됐다 — 7월을 보다 눌렀는데 전체 누적이 열리면 안 되므로) — 개요에서 항목을 누르면 그 대상의 **누적** 상세로 간다.
  - **개요 = 넓은 경영 대시보드(v2, 2026-07-19 발전 — 스펙 `docs/superpowers/specs/2026-07-19-revenue-dashboard-v2-design.md`)**: 개요를 **대시보드 그리드**로 재배치 — KPI 4장(선택 기간 매출·순이익 + 올해 누적) → **월별 차트 + 세무 참고 카드** 2열 → **종류별 매출 구성 + [스탭별 매출·업체·개인별 매출]** 2열. **①비교 증감(B1)**: 선택 기간 KPI 2장(매출·순이익)에만 **전월·전년 동월 대비 ▲/▼%**(연간 선택 시 전년 전체, 직전 0이면 '—'), `revenueSummary`가 `cmp{isYear,prevPeriod*,prevYear*}` 파생·`revDeltaBadge`(상승 success·하락 danger). **②월별 차트 2색(B3)**: 매출(`.rev-bar`)+순이익(`.rev-bar-profit`) 월당 2막대+범례, `monthly[{month,supply,profit}]`, 음수 순이익은 높이 0 클램프. **차트 색은 전용 토큰 `--chart-revenue`/`--chart-profit`**(2026-07-30 — 라이트 `#0066CC`/`#0F7034`·다크 `#4DA3FF`/`#4CB782`): 원래는 팔레트마다 두 막대가 같은 색으로 수렴하는 것을 막으려 고정값(`#5e6ad2`/`#22c55e`)이었는데, 팔레트가 폐지돼 고정할 이유가 사라졌고 `#22c55e`는 흰 배경 대비 2.14로 **막대가 배경에 묻혔다**. 모드별 토큰으로 바꿔 비텍스트 3:1을 넘기면서 파랑↔그린 구분은 유지한다. ⚠️여전히 `primary`/`success`를 직접 참조하지는 않는다 — 액센트를 조정할 때 두 막대가 겹치는 위험은 팔레트가 없어도 남는다. 순이익 텍스트 색은 부호별(`profitCls` 음수=danger). **③종류별 매출 구성(B4)**: 작업(`taskTypeLabel`)+세션(`session_type`) 매출을 **같은 라벨 합산**(`revenueByType`), 가로 막대(비중% SVG rect width) 목록. **④세무 참고(B5)**: 기간 **VAT 합계**(Σtax_amount)·외주 **원천징수 3.3% 예상**(`lib/tax.js withholding33`·실지급 병기, 참고용)=`revenueTax`. 개요 상위 목록 명칭 **'스탭별 매출'·'업체·개인별 매출'**(각 상위 5 + **'전체 N명/N곳 보기' `<details>` 펼침** — 2026-07-19 후속 '기간 렌즈 분리'로 옛 '전체 보기' 링크 대체). 추가 뷰 `revDeltaBadge/revTypeBreakdown/revTaxCard`·CSS `.rev-bar-profit`. 순이익률(마진%)·입금 기준은 범위 밖.
  - **회계 내보내기(CSV, 2026-07-22)**: 세무사 전달·부가세/원천세 신고용. 공용 빌더 `src/lib/csv.js`(`toCsv`/`cell` — RFC4180 이스케이프 + **UTF-8 BOM**[Excel 한글]·의존성 0). **매출 CSV** `GET /revenue/export.csv?year=&month=`(`requireInvoice`, 개요 pageHeader `매출 CSV` 버튼): 선택 기간 발행 청구서 한 줄씩(발행일·청구번호·청구처·아티스트·공급가·VAT·할인·합계·세금상태·미수금), 순수 함수 `revenueCsv(listInvoices(...), period)`. **정산 CSV** `GET /workers/export.csv`(`requireInvoice`, 외주 목록 `정산 CSV` 버튼): **지급 완료** 외주 지급 전체(지급일·작업자·프로젝트·항목·지급액·소득세·지방소득세·실지급 — `withholding33` 3.3% 분해), `payoutExportRows()`(db) + 순수 `payoutCsv(rows)`. 둘 다 `Cache-Control:no-store`(민감). 회귀 `test/revenue-export.test.js`(csv 이스케이프/BOM·매출 필터/컬럼·정산 포맷).

## 일정 메뉴 — 목록/캘린더 뷰·칩 팝오버·세션 행

- 프로젝트 하위 세션 CRUD + 사이드바 `/sessions`(**목록/캘린더 뷰 전환** `?view=`; 목록=`일정(다가오는)`/`지난 세션` 탭바(`tabBar`, `?stab=upcoming`(기본)/`past`·`view=list`·`q` 유지), 캘린더=월 그리드 `?month=YYYY-MM`, `monthCalendar`/`sessionsForMonth`). **일정 메뉴 = 캘린더 기본(서베이) + 칩 클릭 팝오버**: 기본 뷰=캘린더(`view === "list" ? "list" : "calendar"`). 칩 클릭 시 **팝오버**(구글 캘린더식): 칩 `data-session-card="/sessions/:id/card"` → app.js가 fetch(무JS 폴백=프로젝트 세션 탭 링크). 조각=`sessionCardModal`(views.sessions.js): 종류·아티스트·회사 / 날짜·시간 / 예약·엔지니어·디렉터 / 예상 청구액·소요 / 메모 + 프로젝트 ↗ · **완료 토글**. **완료 토글은 네 표면(캘린더 팝오버·목록 펼침·세션 탭·작업) 전부 `− 완료`/`✓ 완료` glyph로 통일**. **위치 = 클릭한 칩 옆**(구글 캘린더식·딤 없음): 팝오버는 캘린더를 덧붙이는 것이라 **뒤 일정이 보여야 앞뒤와 견준다**. 바깥 레이어(`fixed inset-0`·투명)는 바깥 클릭 닫기만. 좌표는 **app.js `place()`가 칩 `getBoundingClientRect()`를 재서 넣는다**(⚠️서버 인라인 style은 CSP에 막힘 — 함정 #27, 서버는 `data-session-pop` 훅 + `absolute`만, 좌표는 JS). 배치=오른쪽 우선→왼쪽→화면 안 클램프, 세로는 칩 중앙+상하 클램프. 좁은 화면(<640)은 가로 여백 없어 가운데 폴백. 재는 동안 `visibility:hidden`(안 그러면 (0,0)에 한 프레임 튄다). **칩을 연달아 눌러 훑기**: 바깥 레이어 `pointer-events-none`이라 **클릭이 캘린더로 통과**(팝오버만 `pointer-events-auto`), 칩 핸들러가 이전 팝오버 먼저 닫고 새로 연다(항상 1개·응답 역전 대비 append 직전 close). ⚠️**`data-modal`을 일부러 안 붙인다** — 붙으면 공용 스크롤 잠금 옵저버가 배경을 얼려 통과가 무의미(✕도 app.js가 직접 배선·`scroll`/`resize`에 재배치). 닫기 = ✕·바깥 클릭·Escape. 바깥 클릭 판정은 document 리스너 + **mousedown이 팝오버 안에서 시작했으면 무시**(함정 #25 — 안에서 드래그해 밖에서 놓으면 오인). session-cancel.test.js가 '딤 없음 + data-session-pop 훅' 잠금. 완료=폼 POST(`/sessions/:id/status` + hidden `return=/sessions?view=calendar&month=YYYY-MM`) → 캘린더 복귀(`safePath` 내부경로만). 라우트 `GET /sessions/:id/card`(requireAuth)·`getSessionCard`. 역할: **일정=서베이(캘린더)·프로젝트 세션 탭=관리 타임라인**. 앱→구글 캘린더 별개 push. **캘린더 = 구글 캘린더식(2026-07-21)**: ①**세로 꽉 채움** — 요일 헤더/날짜 그리드 **분리**(한 그리드에 `auto-rows-fr` 걸면 요일 줄까지 늘어남), 날짜 그리드만 lg에서 `flex-1`+`auto-rows-[minmax(104px,1fr)]` 균등 분배(`lg:h-[calc(100vh-11rem)]`, 6주 달은 104px 하한). 모바일(<lg)은 자연 흐름(`min-h-[104px]`). ②**칩=색 점+글자·배경 없음**(`calendarDotColor` 예정=primary·완료=success·취소=muted, `hover:bg-muted/10` 회색 하이라이트). ③**격자 선 진하게**(`border-border`). ④**헤더 왼쪽 묶음** `[오늘] ‹ › 년월`(오늘=현재 달·화살표 아이콘만+aria-label) + **날짜 숫자 가운데**(`text-center`). ⑤**앞뒤 달 넘침** — `calendarMonthCells(ym)`(lib/date.js, 완전한 주 7칸 배수·`inMonth` 플래그)를 뷰(`monthCalendar`)와 데이터(`sessionsForCalendar` — `LIKE` 대신 격자 범위 `BETWEEN`)가 **공유**. 이웃 달 칸=흐린 배경(`bg-muted/5`)+흐린 숫자(`text-muted/50`)+그 날 세션도 표시. 회귀 `calendar-cells.test.js`. **칩 라벨**=아티스트›제작사›소속사›프로젝트명, 시간은 데스크톱만. **캘린더=넓게**(`layout({wide:true})`): `.card` 래퍼 없이 그리드 라인(셀 `border-b border-r`·컨테이너 `border-t border-l`), `-mx-4 sm:-mx-6`로 main 폭 끝까지. 오늘=`bg-primary/5`. **목록은 프로젝트별 카드**: 같은 프로젝트 세션 한 카드. 헤더=제목+아티스트·회사(**링크 아님**)+세션 수, 본문=세션 행(`showProject=false`). `sessionProjectCard(sessions, opts)`, 라우트가 `activeSess`를 날짜순 유지하며 `project_id`로 그룹. **세션 행 = '완료' 버튼 토글이 상태 표시**(배지 없음): 완료 버튼 `completeToggle` — 예정=비활성 `− 완료`(ghost·초록·누르면→완료), 완료=활성 `✓ 완료`(`border-success/40 bg-success/10 text-success`·`aria-pressed`·**다시 누르면→예정**). glyph는 고정폭 `<span class="inline-block w-3.5 text-center">`로 **버튼 크기 불변**(69px). **일정 취소 토글**(완료 버튼 옆·`sessionRow` isAdmin·`sessionCardModal` canEdit): 예정·완료→**'일정 취소'**(확인 후 `status=취소`)·취소→**'일정 취소 해제'**. ⚠️라벨을 그냥 '취소'로 두면 **캘린더 팝오버 안에서 닫기(X)와 헷갈린다**(모달 '취소'=하던 걸 무르고 닫기로 읽힘) — 목적어를 붙였고 session-cancel.test.js가 라벨 잠금. 취소 세션은 삭제 안 되고 제목 '(취소)' prefix + **목록·캘린더 칩 흐리게(`opacity-60`)**. ⚠️`sessionsForMonth`는 취소 세션 **포함**(`status<>'취소'`로 걸러 캘린더에서 안 보여 흐림 코드가 죽었던 것 수정)·`pastSessions`·`listSessionsForProject`도 포함이나 `upcomingSessions`는 제외(다가오는 목록·대시보드 노이즈 방지). **캘린더 동기화 가드**: `gcal_event_id` 없는 세션을 취소하면 `updateEvent(null)→createEvent` 폴백으로 없던 '(취소)' 일정이 생기던 것 차단(`syncSessionEvent`가 `취소 && !gcal_event_id`면 no-op). **청구 라인 3항목 flex-wrap**(예상 청구액 / 소요·종류 / 청구상태). ⚠️**`break-keep`(word-break:keep-all) 필수**(단어는 공백에서만 줄바꿈): 금액·상태 청크는 `whitespace-nowrap`, 소요·종류 청크는 `break-keep`. `POST /sessions/:id/status`가 반대 상태(`isDone?예정:완료`) 전송·청구된 세션 되돌리기 거부(`SESSION_INVOICED`). 예약 담당자·담당 엔지니어 별개(담당자 마스터 select; **예약 담당자 '미지정' 없음 — 기본 예약담당자 자동**, `admin_state.default_booker`). **담당 엔지니어 다대다**: select 행 반복 + '+ 담당 엔지니어 추가하기'(template 복제·✕ 제거, 담당자 마스터에서만·자유 텍스트 없음). `session_engineers(session_id, manager_id)`(`resolveEngineerAssignments`가 유효 id 필터[삭제 제외·비활성 배정 보존·dedup]·`setSessionEngineers` 통째 교체) + `sessions.engineer_name`은 **첫 엔지니어로 동기화**(매출·검색·캘린더 레거시 호환·매출은 첫에게만 귀속). 백필 `session_engineers_backfill_v1`. 캘린더 설명·참석자(`sessionAttendeeEmails`)는 배정 전원. **담당 디렉터 다대다 = Gmail식 칩 콤보**: `personCombo(multi, selected)`(칩·✕/백스페이스), 프리필=`listSessionDirectors`, 제출=칩마다 `director_contact_id`+`director_name`(순수 본명). `resolveDirectorIds`가 이름별 해석 — **`resolvePersonByName` 라벨 안전망**이 '박수한 대표님 (워터멜론)' 라벨도 그 사람으로 매칭(유일)·새 이름만 생성·dedup. `session_directors` 조인·`sessions.director_party_id`(첫) 동기화. **personCombo '새 담당자 등록' 모달 회사칸 = `companyCombo`**(타이핑=업체 검색 `data-pc-company-pop`·목록 외=`＋새 업체 등록`), 옵션 `partyOptions({role:"company"})`를 `data-pc-company-options` JSON 임베드·`initOne`(동적 행은 `__initPersonCombos`), `data-pc-company`→`POST /contacts` `company=`. **모달 '활동명' 필드**: `data-pc-activity`→`nickname=`, `createPerson`이 `activity_name` 저장 + **`is_artist=1` 자동**. 레거시 `sessions.director_contact_id`는 첫 디렉터로 동기화. 연락처 상세 '참여 세션'=조인+레거시(`listSessionsForContact`)·`classifyContact` 디렉터 배지 동일. **`/sessions` 목록 행 하위정보=줄 스택**(`sessionRow` `infoLines`): 회사(제작사›소속사·프로젝트 링크·액센트색) / 아티스트 / `예약 담당자 · 담당 엔지니어` / 디렉터 / 메모. 회사·아티스트는 `upcomingSessions`/`pastSessions`가 `p.artist`·`p.artist_company`·`p.production_company` 조인(없으면 프로젝트 제목 폴백)·검색(`matchesQ`) 포함. `showProject=false`(세션 탭)면 회사·아티스트 줄 생략.


## 청구 목록(/invoices) — 넓은 표·반응형 3단계·일괄 처리

  미완료 작업은 흐리게·체크 해제(세션 완료 강제와 동일 규칙). 청구 목록 **검색**(`?q=` 제목·채번·클라이언트 — **타이핑 즉시 실시간 필터**[`invoiceTable {filterList}`→tbody `data-live-filter`/`data-filter-list`, app.js가 로드된 `<tr>`을 textContent로 즉시 필터, 2026-07-16 사용자 요청]. 검색 버튼=캡 넘는 서버 전체 검색). **청구 목록(`/invoices`) = 데스크톱 넓은 표(2026-07-16 사용자 요청, bookipi 참고 — 지메일식 한 줄 행 대체)**: `invoiceTable`(views.invoices.js). 컬럼 = `[체크박스] 상태·청구번호·클라이언트·아티스트·프로젝트·금액·발행·[처리]`. **행(데이터 셀)을 누르면 상세 화면으로 이동**(셀마다 `<a class="inv-cell-link">` — `<tr>`을 `<a>`로 못 감싸므로; 체크박스·처리 열은 링크 아님). 청구권자(`isInvoicer`=대표·치프)만 **체크박스·처리 열·일괄 처리** 대상, 스태프는 상태 배지만(읽기). **헤더 클릭 정렬(Finder식·client-side, 2026-07-18 사용자 요청)**: 정렬 가능 `<th>`(상태·청구번호·청구처·아티스트·프로젝트·금액·발행)에 `data-sort-key`·`data-sort-type`(num|text|date)·`aria-sort`, 각 `<td>`에 `data-sort-value`(정렬 원값 — 상태=순위[미발행0<발행1<부분납2<입금완료3<연체4, `statusRank`]·금액=정수·발행=ISO[표시 '7월 14일'은 문자열 정렬 불가라 원값 필요]·나머지=텍스트). app.js IIFE(`th[data-sort-key]`)가 클릭 시 로드된 `<tr>`을 `data-sort-value`(없으면 textContent)로 즉시 재정렬 — **첫 클릭=오름차순, 같은 열 다시=내림차순**(방향 토글), `aria-sort`로 화살표(▲/▼, CSS `::after`). 빈 값은 방향 무관 뒤로. **서버 기본 정렬(발행 최신순)은 로드 시 그대로** — 정렬은 클릭할 때만·리로드 시 초기화. 실시간 필터로 숨은 행은 순서만 바뀌고 `display` 유지(정렬은 캡 안 로드분 대상, 필터와 동일). 키보드(Enter/Space, IME 가드 #18)·`th tabindex=0`. **넓게 = `layout({wide:true})`**: main 전체 폭 사용(바깥 컨테이너는 전 페이지 공통 `max-w-wide` 1760 — [레이아웃 통일](#레이아웃-통일) 참조. wide 미지정=폼·상세는 main 안 `max-w-content` 768 읽기 폭). **상태 컬럼 = 짧은 배지**(`taxBadgeShort` — 미발행/발행/입금, 2026-07-16 사용자 요청 '입금·발행으로 줄이자'·열 폭 절약). **청구번호 작은 폰트**(`.inv-c-num { font-size:0.75rem }`). **반응형 3단계**(찌부·잘림 방지 — 넓은 표는 금액/발행이 오른쪽 밖으로 잘리므로 좁아지면 단계적으로 축소): ①**≥1280(xl)**=9열(아티스트·프로젝트 포함, `hidden xl:table-cell`) ②**1024~1280**=7열(아티스트·프로젝트 숨김) ③**<1024**=`.inv-table @media`가 표를 **2열 카드 목록**으로 접음(bookipi 참고 — thead 숨김·각 `<tr>`=CSS grid 2열: 왼쪽 열=청구번호/상태/발행일, 오른쪽 열=클라이언트/금액[굵게], 아티스트·프로젝트·라벨 없음). **카드 사이 갭·개별 테두리 없이 구분선 하나로 이어지는 목록**(`border-bottom`만·last-child 없음, 2026-07-16 사용자 '갭 없애자'). **표 래퍼 흰 바탕(surface 판)은 전 폭 공통**(`overflow-hidden rounded-lg border bg-surface` — 2026-07-16 사용자 리포트 '<640에서 흰 바탕이 사라진다', 이전 `sm:` 한정자를 떼어 모바일에서도 유지. dataTable 래퍼도 동일). td 반응형 클래스=`inv-c-*`(status·num·client·amt·issued) + `inv-check`/`inv-act-col`. **찌부·잘림 방지 = `table-layout: fixed`**(2026-07-16 — auto 레이아웃은 셀 min-content가 컨테이너를 넘겨도 안 줄어 오른쪽 처리 열이 클립됐다): fixed면 표가 컨테이너 폭(width:100%)에 정확히 맞아 **어떤 열도 밖으로 안 넘침**. 고정 열은 rem 폭 지정(`.inv-w-status` 4rem·`.inv-w-num` 7.5rem·`.inv-amt-col` 7.5rem·`.inv-w-issued` 5rem·`.inv-check` 2.5rem·`.inv-act-col` 6rem[아이콘 2개 항상 확보]), **이름 열(클라이언트·아티스트·프로젝트)은 폭 미지정 → 남은 폭 균등 분할**, 넘치면 `text-overflow:ellipsis`로 `…`(창 넓을수록 이름 열이 넓어져 잘림 덜함). 모바일 카드 클라이언트는 그리드 열(`minmax(0,1.2fr)`)이 폭 제어. **모바일 선택 = long-press(꾹 누르기)**: **터치에서만** 평소 체크박스 숨김(`@media (hover:none) and (pointer:coarse)`), **카드를 꾹 누르면**(app.js touchstart 450ms·touchmove=스크롤이면 취소) `:root.inv-selecting` → 체크박스 노출 + 그 카드 선택 + **하단 고정 바**(`.inv-bulkbar` 모바일 `position:fixed;bottom:0`·**불투명 배경 `--color-elevated`**[bg-primary/5 반투명이면 뒤 카드 비침, 2026-07-16])로 계산서 발행·입금완료 일괄. **long-press가 iOS 링크 메뉴(열기·새 탭…)를 안 띄우게** 터치 블록에서 `-webkit-touch-callout:none`+`user-select:none`(카드·셀 링크), 안드로이드는 app.js `contextmenu` preventDefault(long-press 중일 때만·데스크톱 우클릭 무영향). 선택 모드 중 셀 링크 탭=상세 대신 선택 토글(캡처 preventDefault·long-press 직후 합성 click 삼킴), 선택 0이면 모드 자동 종료. **좁은 데스크톱 창(마우스, <1024 카드)은 체크박스 상시 노출**(롱프레스 불가·hover 미디어 분기). ≥1024 표는 체크박스·처리 상시·**하단 중앙 플로팅 바**(2026-07-18 — 이전 상단 sticky는 선택 시 흐름 공간을 먹어 목록이 밀렸다). 처리 열(모바일 카드)은 숨기고 하단 바로 통일. 가로 오버플로우 0·처리 열 미클립 실측(1280~1950 표·<1024 카드·390 모바일). **처리 열 = 아이콘 토글**(`taxToggleButtons(inv, ret, {iconOnly:true})` — 밀도 무관 항상 아이콘·hover 툴팁·aria-label). **일괄 처리(대표·치프)**: 행 체크 → **하단 중앙 플로팅 바**(`invoiceBulkBar`, `data-inv-bulk-*` — `position:fixed;bottom:1.5rem;left:50%` 알약, `.card` 불투명 배경·그림자; `@media min-width:1024`. `inv-table-wrap` margin-bottom으로 스크롤 맨 아래 마지막 행 클리어런스. fixed라 흐름 공간 0 → 목록 안 밀림. 모바일<1024는 전폭 하단 고정) '`N`건 선택됨' + **[계산서 발행 완료][입금완료 처리][선택 해제]**. app.js가 선택 id를 hidden `ids`에 모아 `POST /invoices/bulk-tax-status`(requireInvoice·`ids` 콤마 + `tax_status`, 버튼 name/value로 액션 결정) 제출(제출 전 `N건을 …할까요?` 확인). 서버는 `applyTaxStatusTx`(단건 `/tax-status`와 공용)를 id마다 한 트랜잭션으로 적용(입금완료=완납 자동 입금·되돌리기=자동 완납 입금만 제거·채번 보장). 선택 상태·바 표시는 app.js `sync()`(전체선택 체크박스·개수·행 하이라이트·바 표시는 `style.display`[함정 #26]). **행별 처리(`POST /invoices/:id/tax-status`)**: 계산서·입금 토글은 `return=ret`(현재 목록)로 복귀. **상태 반영(불) + 되돌리기**: 켜짐=success tint·꺼짐=ghost, 둘 다 토글. **입금완료 되돌릴 때 서버가 자동 완납 입금(memo='입금완료 처리')만 제거**해 잔금 복원(사용자 입금 이력은 memo가 달라 보존). **계산서/현금영수증 구분**: `taxDocOf(inv)`=청구처 `payer_kind==="person"`이면 현금영수증, 그 외 계산서. **상태 필터칩(탭 폐지)**: `?filter=all`(기본)/`todo`(계산서 미발행)/`done`(계산서 발행·입금 전)/`paid`(입금완료), `filterChips`(개수 라벨). 상태는 표의 **상태 컬럼**(배지)로 표시. 미수금 합계는 필터 무관 전체 기준. 행 복귀 경로(ret)에 **limit(더 보기 상태) 보존**. **상세 백링크 = 보던 필터로 복귀**: 셀 링크가 `/invoices/:id?return=<현재 필터·검색>`(encodeURIComponent)를 실어 보내고, 상세 라우트가 `safePath(req.query.return)`(내부 절대경로만)을 pageHeader back에 사용(없으면 `/invoices`). 상세의 계산서·입금 토글도 return 보존한 자기 경로(`selfRet`)로 복귀. **프로젝트 청구 탭·클라이언트 상세는 `invoiceRow`(compact 펼침형) 유지**(넓은 표는 `/invoices` 목록 전용).


## 업체·그룹 상세 — 읽기 뷰/편집 패널·담당자 칩 콤보

- **업체·그룹 상세 = 2단 읽기 뷰 + [편집]**(연락처와 통일, 2026-07-18 마스터-디테일 전환 — 옛 '인라인 편집'을 대체): `GET /clients/:id`가 `clientReadView`(`src/views.clients.js` — 탭 없이 한 화면, 빈 섹션은 헤딩까지 통째로 숨김)를 읽기 전용 렌더, 우상단 **[편집]**을 눌러야 `GET /clients/:id/edit`의 `clientEditPane`(`clientForm` dirty 저장 + 첨부 + 크로스링크 + 그룹 멤버·소속 아티스트)로 들어간다. 저장·첨부 업로드/삭제는 `/clients/:id/edit`에 머물고, 저장 성공 시 읽기 뷰(`/clients/:id?flash=saved`)로 복귀(옛 3탭[상세 정보/프로젝트/청구·결제] 구성은 읽기 뷰의 섹션 나열로 대체 — 탭 자체가 없다). **담당자 연락처는 편집 폼의 콤보에서 관리**(현재 담당자 전화 `☎`+소속을 서버에서 미리 표시). **Gmail식 칩**: `clientContactCombo`가 `personCombo(multi, selected)` — 선택된 사람은 **한 덩어리 배지(칩)**로 보이고 ✕ 또는 **빈 입력에서 백스페이스**로 통째 삭제(텍스트 단위 편집 아님). 프리필=`selected` 칩 목록. **제출 = 칩마다 hidden `idField`(당사자 id·신규는 빈값) + `nameField`(순수 본명) 쌍**(인덱스 페어링) — id로 정확히 연결되어 '엄유미 실장님' 같은 표시 라벨이 새 사람으로 저장될 여지가 없다. 드롭다운=**Gmail식 2줄 컴팩트 행**(이름·호칭 굵게 / 이메일·소속 작게, 내용 폭에 맞춘 좁은 팝업). **후보 정렬=매칭 강도순(공용 `comboRank`/`comboRankSort`, app.js 파일 스코프)**: 우선순위 필드(앞이 중요) 안에서 정확 일치 > 앞부분 일치 > 중간 포함, 동점은 원래 순서 유지(안정 정렬). **전 콤보 통일**: personCombo(본명>활동명>라벨>소속회사)·payerCombo(이름>소속·분류)·companyCombo(이름>라벨>활동명)·artistCombo(활동명>본명)·groupCombo/미니콤보(이름). **가드레일 ⑩**이 정렬 없는 후보 목록(`view = …filter(…)` 중 comboRankSort 미사용) 재등장을 차단. **활동명=본명이면 괄호 병기 생략**(`labelOf`·서버 `personName`/`personLabel`·드롭다운 행 렌더 모두 `a !== n` 조건). 칩 추가·삭제는 클릭이라 dirty 감시에 `change` 수동 통지(함정 #23). **검색 입력 `min-w-[3rem]`+`size="1"`**(2열 그리드 좁은 칸에서 칩과 함께 한 줄 유지, 칩이 더 늘면 자연 래핑). **칩이 하나라도 있으면 placeholder 없음**(빈 상태 안내 문구만 유지). 서버 `personChip`(views.js)과 app.js `chipEl`이 마크업 쌍. **세션 담당 디렉터도 동일**(같은 컴포넌트). 저장(`linkClientContact`)은 조각별 `resolvePersonByName`(라벨 안전망 — '박수한 대표님 (워터멜론)'도 그 사람으로 매칭·새 이름만 생성·dedup) 후 **`setOrgContacts(orgId, ids)`로 통째 교체**. **담당자 = 재직과 분리된 역할(`affiliations.is_contact`)**: 프리필·청구처 담당자를 **`listOrgContacts`(is_contact=1)** 로 좁히고, 칸에서 빠진 사람은 `is_contact=0`으로 **담당자 지정만 해제**(재직·연락처 그대로 — 담당자 해제≠퇴사). 새로 지정한 사람은 이 회사 소속이 없으면 재직도 함께 등록(다른 소속 안 끊음). 대표를 담당자로 지정·해제 가능하고 소속은 `ensureOwnerAffiliation`이 유지. 담당자 아닌 직원은 연락처의 회사·소속 이력에서 관리. `snapshotPayer`(청구처 스냅샷 담당자)·`payerInfoCard` 담당자(청구 상세·프로젝트 청구 탭)도 지정된 담당자 기준. **이미 적은 사람은 후보에서 제외**: app.js `isChosen(o)`(head를 콤마 split해 라벨·본명·활동명 중 하나와 일치)로 후보에서 제외 + **이미 적은 이름을 그대로 다시 치면 '＋새 등록' 대신 드롭다운을 닫음**(중복 생성 유도 방지). 세션 디렉터·multi 콤보 전체 공통. **목록 필터·스크롤 복원**: 목록 행→상세 링크에 `?from=<필터쿼리>` 전달→'← 업체·그룹' 백링크가 같은 필터로 복귀, 스크롤은 sessionStorage(app.js). **청구·프로젝트 복귀(`?return=`)**: 청구처 카드 '업체·그룹 ↗'가 `?return=<보던 화면 경로>`(청구 상세=selfRet[백링크 체인 보존]·프로젝트 청구 탭=`?tab=invoice&open=`)를 실어 보내고, 업체·그룹 상세 백링크가 그 경로로 복귀(라벨 청구/프로젝트/돌아가기, 내부 절대경로만 허용). **사람(아티스트 포함 전원) 리다이렉트(`/clients/:id`→`/contacts/:id`, 2026-07-17 사람/조직 축 정리로 비아티스트뿐 아니라 아티스트까지 확장)도 return 보존** + 연락처 상세 백링크 동일 처리.
